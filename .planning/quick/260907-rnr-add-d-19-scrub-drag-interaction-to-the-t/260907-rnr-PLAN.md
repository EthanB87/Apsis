---
phase: 260907-rnr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/lib/trendStats.ts
  - apps/mobile/lib/__tests__/trendStats.test.ts
  - apps/mobile/app/trends.tsx
autonomous: true
requirements: [D-19, D-20]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Dragging a finger across the /trends detail chart shows a 1px ash hairline that tracks the finger with zero lag plus a floating tooltip carrying that day's date, HSS, ATL, CTL and TSB — the same interaction the home chart already has (D-19/D-20)."
    - "The detail tooltip's copy format is identical to the home chart's because the date text and the signed-TSB text both come from the already-tested formatters in lib/trendStats.ts — the detail screen re-implements neither, and the copy string is built in exactly one place."
    - "Only opacity eases (≤150ms); the hairline's x-position and the tooltip's horizontal placement are read straight from the press state every frame with no timing or spring."
    - "The tooltip never renders clipped: it centres on the finger where it fits and clamps inside the chart card at both edges."
    - "The scrub does not fight the page: no touchable wraps the chart card, the 28D/90D/1Y switcher stays a sibling above it, and vertical ScrollView scrolling still works with a finger starting on the chart."
    - "packages/engine, apps/mobile/components/home/TrendChart.tsx and 04-UI-SPEC.md show zero changes attributable to this task."
    - "pnpm run typecheck is clean and all four suites pass with no reduction: mobile ≥107 (103 baseline + the new tooltip-copy tests), db 64, engine 95, shared 21."
  artifacts:
    - apps/mobile/lib/trendStats.ts
    - apps/mobile/lib/__tests__/trendStats.test.ts
    - apps/mobile/app/trends.tsx
  key_links:
    - "lib/trendStats.ts is the single tooltip-copy source of truth: formatScrubTooltip composes the existing formatTrendDateLabel and formatSignedDelta, and app/(tabs)/index.tsx already routes the home chart's dateLabel through formatTrendDateLabel — so home and detail cannot drift on date or sign formatting."
    - "victory-native's chartPressState prop is typed Record<YK, number> over the chart's yKeys union; because the detail chart's yKeys are atl/ctl/tsb, the press state MUST be initialised with all three keys or tsc fails. The typecheck gate is doing real work here, not ceremony."
    - "The chart card already sets overflow:'hidden', which is why an unclamped tooltip would be silently cut off on the right half of the card — the clamp is what makes the feature readable across the whole width."
    - "app/(tabs)/index.tsx already runs the identical default press-pan inside a ScrollView and the user has confirmed that feels right on device, so gesture parity is achieved by matching the home configuration exactly, not by adding pan tuning the home chart does not have."
---

<objective>
Port the home chart's D-19 scrub / D-20 tooltip interaction to the `/trends` detail chart.

The user has the trends screen running on device, likes it, and asked for one addition,
verbatim: *"trends page looks great but I think I just want you to be able to drag and see the
day etc like on the home page."* Nothing here is broken — quick task 260907-qe6 deliberately
declared "no scrub on the detail chart" to hold scope, and the user is now reversing that cut.

Purpose: the detail chart is where per-day interrogation actually belongs — it is the widest
chart in the app, it draws TSB as a real line, and it is the one screen where reading an exact
day's numbers is the point.

Output: a scrub hairline + tooltip on `/trends` whose copy is generated by one shared, unit-
tested formatter, so the home and detail tooltips can never drift apart.

**Deliberate non-goals** (stated so the executor does not sprawl):

- **`apps/mobile/components/home/TrendChart.tsx` is NOT edited.** It is concurrently owned by
  quick task 260907-qe6b and is read-only here. Read its scrub mechanism (which qe6b is not
  changing) and its exact tooltip copy string as reference; change nothing in it. This is also
  why the shared scrub logic is NOT extracted into a hook in this task — extraction would
  require editing that file. If extraction is still wanted later, it is a follow-up once qe6b
  has landed.
- **`04-UI-SPEC.md` is NOT edited** (same concurrent owner). This task adds no divergence from
  section 5 anyway: it *implements* the D-19/D-20 contract already written there, on a second
  chart.
- No engine change; no new npm dependencies; no new chart series, range, or stat; no change to
  the home screen; no `git push`; the running Metro dev server (port 8081) is never killed or
  restarted at any point.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/mobile/app/trends.tsx
@apps/mobile/lib/trendStats.ts
@apps/mobile/components/home/TrendChart.tsx
</context>

<interfaces>
Verified against the installed packages at planning time — do not re-derive from memory, and do
not cast around any of it:

- `victory-native@41.26.0` › `useChartPressState(init)` returns `{ state, isActive: boolean }`.
  `state` carries `matchedIndex: SharedValue<number>`, `x: { value, position: SharedValue<number> }`,
  and `y: Record<keyof Init["y"], { value: SharedValue<number>, position: SharedValue<number> }>`.
- `CartesianChart` accepts `chartPressState`, `onChartBoundsChange(bounds: ChartBounds)`, and
  (unused here, see Task 2) `chartPressConfig?: { pan?: ChartPressPanConfig }` where
  `ChartPressPanConfig` exposes `activateAfterLongPress`, `activeOffsetX`, `activeOffsetY`,
  `failOffsetX`, `failOffsetY`, `simultaneousWithExternalGesture`.
- `chartPressState` is typed `ChartPressState<{ x: …; y: Record<YK, number> }>` where `YK` is the
  union of the chart's `yKeys`. The detail chart's `yKeys` are `['atl','ctl','tsb']`, so the press
  state must be initialised with all three y keys.
- `react-native-reanimated@4.5.0` exports `clamp(value, min, max)` and
  `Animated.addWhitelistedNativeProps(props)`. Both are used below.
- `@shopify/react-native-skia@2.6.9` › `Line` (imported in `trends.tsx` as `SkiaLine`) accepts
  animated `p1`/`p2`/`opacity`. Known asymmetry from prior tasks: `Area.opacity` is a plain
  `number` and needs a `<Group opacity>` wrapper — irrelevant here, no `Area` is added.
- `apps/mobile/vitest.config.mts` includes `lib/**/__tests__/*.{test,spec}.ts` only. There is no
  screen/component test harness (screens import native modules), so `app/trends.tsx` itself is
  verified by typecheck + structural gates + an on-device human check, and the *copy contract* is
  verified by real unit tests in `lib/`.

Green baseline confirmed by running all four suites at planning time: mobile 103, db 64,
engine 95, shared 21; `pnpm run typecheck` clean.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One tested tooltip-copy formatter in lib/trendStats.ts</name>
  <files>apps/mobile/lib/trendStats.ts, apps/mobile/lib/__tests__/trendStats.test.ts</files>
  <behavior>
    Tests first (RED), in the existing `lib/__tests__/trendStats.test.ts`, under a new
    `describe('formatScrubTooltip')`. At least these cases:
    - A typical day renders the full D-20 line, asserted as one exact expected string.
    - A rest day (`dayHss: 0`) still renders ATL/CTL/TSB (D-20 explicitly requires this — EWMA
      always has values on a zero-input day).
    - Negative TSB renders with the U+2212 minus sign and contains no ASCII hyphen.
    - Zero TSB renders as the signed-zero form `formatSignedDelta(0)` already returns.
    - Fractional `atl`/`ctl`/`dayHss` round to integers (e.g. 42.6 → 43).
    - Parity: the line starts with `formatTrendDateLabel(row.localDate, { withDay: true })`,
      proving the date segment is the shared formatter's output and not a second copy.
  </behavior>
  <read_first>
    `apps/mobile/components/home/TrendChart.tsx` — its `tooltipText` derived value holds the
    authoritative D-20 copy string (separator, label order, spacing). Read it and reproduce that
    format exactly. **Do not edit that file** (D-19/D-20 reference only; owned by qe6b).
    `apps/mobile/lib/trendStats.ts` — `formatTrendDateLabel` and `formatSignedDelta` already
    exist and are already tested; `app/(tabs)/index.tsx`'s `formatShortDate` already delegates
    the home chart's `dateLabel` to `formatTrendDateLabel(…, { withDay: true })`, which is why
    composing these two helpers reproduces the home tooltip byte-for-byte.
  </read_first>
  <action>
    Add one exported function `formatScrubTooltip(row: TrendStatRow): string` to
    `apps/mobile/lib/trendStats.ts`, implementing D-20's copy contract by COMPOSING the existing
    helpers — `formatTrendDateLabel(row.localDate, { withDay: true })` for the date, `Math.round`
    on `row.dayHss` / `row.atl` / `row.ctl`, and `formatSignedDelta(row.tsb)` for the signed TSB.
    Do not write a second date formatter and do not write a second sign formatter: the whole
    reason this lives in `trendStats.ts` rather than in the screen is that both tooltips must
    resolve to one implementation. Keep the module pure — it still imports nothing from
    `@apsis/db` or `react-native`, so it stays inside the vitest `lib/**` include.
    Document in a short header comment that this is the single source of the D-19 tooltip line
    and that the home chart's equivalent string is the format of record (per D-20 /
    04-UI-SPEC.md section 5) until the two can be unified after qe6b lands.
    Write the tests first, watch them fail, then implement.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" || exit 1
strip() { sed -E '/^[[:space:]]*(\/\/|\*|\/\*|\{\/\*|\*\/)/d' "$1"; }

# G1a — mobile suite passes and GREW by at least 4 tests (103 baseline verified at plan time)
OUT=$(cd apps/mobile && npx vitest run 2>&1); printf '%s\n' "$OUT" | tail -6
N=$(printf '%s\n' "$OUT" | sed -nE 's/.*Tests +([0-9]+) passed.*/\1/p' | head -1)
{ [ -n "$N" ] && [ "$N" -ge 107 ] && ! printf '%s\n' "$OUT" | grep -qE 'Tests +[0-9]+ failed'; } \
  && echo "G1a GREEN ($N tests)" || { echo "G1a RED (N=$N)"; exit 1; }

# G1b — the formatter is exported from the shared module and exercised by its own describe block
strip apps/mobile/lib/trendStats.ts | grep -q 'export function formatScrubTooltip' \
  && grep -q "describe('formatScrubTooltip'" apps/mobile/lib/__tests__/trendStats.test.ts \
  && echo "G1b GREEN" || { echo "G1b RED"; exit 1; }

# G1c — ANTI-DUPLICATION: exactly ONE U+2212 minus sign in trendStats.ts code (the one already
# inside formatSignedDelta). A re-implemented sign formatter adds a second and turns this RED.
# Baseline verified at plan time = 1. Comment lines are stripped so a rationale comment that
# names the character cannot self-invalidate the gate.
M=$(strip apps/mobile/lib/trendStats.ts | grep -c $'−')
[ "$M" -eq 1 ] && echo "G1c GREEN" || { echo "G1c RED (found $M)"; exit 1; }
    </automated>
  </verify>
  <done>
    `formatScrubTooltip` is exported from `lib/trendStats.ts`, delegates date and sign formatting
    to the two existing tested helpers, and is covered by at least 4 new passing tests (mobile
    suite ≥107, zero failures). G1a/G1b/G1c all GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the D-19 scrub hairline + D-20 tooltip into app/trends.tsx</name>
  <files>apps/mobile/app/trends.tsx</files>
  <precondition>
    Record the pre-edit commit before touching anything — `BASE_SHA=$(git rev-parse HEAD)` — and
    keep it for this task's gates. Take it at task start, i.e. after any 260907-qe6b commits have
    already landed, so the forbidden-path gate measures only this task's own changes.
  </precondition>
  <read_first>
    `apps/mobile/components/home/TrendChart.tsx` — the reference implementation of every mechanic
    below (press state, `chartBoundsSV` + `onChartBoundsChange`, `cursorP1`/`cursorP2` derived
    vectors, the `CURSOR_FADE_MS` opacity fade, the whitelisted-`text` AnimatedTextInput tooltip).
    Read it; **do not edit it** — it is concurrently owned by quick task 260907-qe6b, and by
    execution time it may carry extra gridlines, area fills and point dots that are none of this
    task's business. Its scrub mechanism is the part qe6b is not touching.
  </read_first>
  <action>
    Add the scrub to the existing chart in `apps/mobile/app/trends.tsx`. Do not restructure the
    screen: the `<ScrollView>`, the range switcher, the three series, the dashed zero reference,
    the legend, the band strip and the stats grid all stay exactly as they are.

    **Imports.** Extend the existing lines rather than adding parallel ones: `useEffect` from
    react and `type ComponentProps` from react; `TextInput` from react-native;
    `useChartPressState` and `type ChartBounds` from victory-native; `formatScrubTooltip` from
    `../lib/trendStats`. Add one `react-native-reanimated` import: default `Animated` plus
    `clamp`, `useAnimatedProps`, `useAnimatedStyle`, `useDerivedValue`, `useSharedValue`,
    `withTiming`.

    **Module scope.** Call `Animated.addWhitelistedNativeProps({ text: true })` and create
    `AnimatedTextInput = Animated.createAnimatedComponent(TextInput)`, exactly as the home chart
    does. Add named constants `CURSOR_FADE_MS = 150` (the D-19 ceiling — same value as the home
    chart's so the two screens' fade cannot drift), `TOOLTIP_TOP_OFFSET = 8` and
    `TOOLTIP_EDGE_INSET = 4`. No magic numbers inline, matching this file's existing convention.

    **Press state.** `const { state, isActive } = useChartPressState({ x: 0, y: { atl: 0, ctl: 0,
    tsb: 0 } })`. All three y keys are mandatory: the `chartPressState` prop is typed
    `Record<YK, number>` over the chart's `yKeys` union, so a two-key state is a compile error.
    Pass `chartPressState={state}` and `onChartBoundsChange={(bounds) => { chartBoundsSV.value =
    bounds; }}` to the existing `<CartesianChart>`, with
    `chartBoundsSV = useSharedValue<ChartBounds>({ left: 0, right: 0, top: 0, bottom: 0 })`.

    **Hairline.** Derive `cursorP1` / `cursorP2` with `useDerivedValue` returning
    `vec(state.x.position.value, chartBoundsSV.value.top)` and `…bottom` respectively (`vec` is
    already imported). Render one more `<SkiaLine>` as the LAST child inside the chart's render
    function so it paints above the three series and above the dashed zero reference — 1px,
    `Colors.dark.mutedText` (ash, no volt), `opacity={cursorOpacity}`.

    **Fade.** `cursorOpacity = useSharedValue(0)`, driven by a `useEffect` on `isActive` with
    `withTiming(isActive ? 1 : 0, { duration: CURSOR_FADE_MS })`. Only opacity may ease — the
    hairline x and the tooltip x must be read straight from `state.x.position.value` each frame
    with no timing, spring or interpolation wrapper, so the cursor tracks the finger with zero
    lag (D-19).

    **Tooltip text.** Precompute the whole line per day on the JS thread:
    `tooltipLines = useMemo(() => windowRows.map(formatScrubTooltip), [windowRows])`, then
    `tooltipText = useDerivedValue(() => tooltipLines[state.matchedIndex.value] ?? '',
    [tooltipLines])` and `tooltipAnimatedProps = useAnimatedProps(() => ({ text:
    tooltipText.value }))`. Two reasons this shape is required rather than formatting inside the
    worklet: `formatScrubTooltip` is a plain JS function and calling it on the UI thread would
    need it workletised, and precomputing keeps the D-20 copy in exactly one tested place. The
    `?? ''` is load-bearing — `matchedIndex` is -1 before the first press and can point past the
    end for one frame after a range switch shortens `windowRows`. `windowRows` (not `chartData`)
    is the source because it carries `dayHss` and `localDate`, and its indices are 1:1 with the
    chart's `day` values by construction.

    **Tooltip view.** Inside the existing `styles.card` `<View>`, as a SIBLING after
    `</CartesianChart>`, render an `Animated.View` with `pointerEvents="none"` carrying the
    animated style below, containing an `AnimatedTextInput` configured exactly as the home
    chart's (`editable={false}`, `pointerEvents="none"`, `caretHidden`,
    `underlineColorAndroid="transparent"`, `defaultValue=""`, `multiline={false}`,
    `animatedProps` cast to `Partial<ComponentProps<typeof TextInput>>`). Add `tooltip` and
    `tooltipText` entries to the existing `StyleSheet` mirroring the home chart's (carbon
    surface, hairline border, `Radius.md`, `Spacing.sm` horizontal padding, mono 11px bone,
    absolutely positioned at `top: TOOLTIP_TOP_OFFSET`, `left: 0`).

    **Clamped placement (the one intentional improvement over the home chart).** The card sets
    `overflow: 'hidden'`, and this tooltip line is wide, so a left-anchored tooltip would be cut
    off across the right half of the card. Measure both widths with `onLayout` into shared values
    — `tooltipWidthSV` from the `Animated.View`, `cardWidthSV` from the existing `styles.card`
    `<View>` (add `onLayout` to it; it stays a plain View) — and in `useAnimatedStyle` set
    `opacity: cursorOpacity.value` and a single `translateX` of
    `clamp(state.x.position.value - tooltipWidthSV.value / 2, TOOLTIP_EDGE_INSET,
    Math.max(cardWidthSV.value - tooltipWidthSV.value - TOOLTIP_EDGE_INSET, TOOLTIP_EDGE_INSET))`.
    The `Math.max` guard is required: before first layout both widths are 0 and an inverted
    min/max would make `clamp` meaningless. Centring-then-clamping is still untweened, so D-19's
    zero-lag rule holds.

    **Gesture contention — resolve it by matching, not by tuning.** Pass NO `chartPressConfig`
    and no `gestureHandlerConfig`: `app/(tabs)/index.tsx` already renders the home chart with the
    identical default press-pan inside a `ScrollView`, and the user has confirmed on device that
    it feels right — adding pan offsets here would make the detail chart behave differently from
    the screen the user asked it to match. Two hard rules follow: (1) the chart card must not be
    wrapped in — or converted into — any touchable or press responder; the 28D/90D/1Y switcher
    stays a sibling ABOVE the card, exactly as the home chart's detail affordance is a sibling
    below its card (D-02's rationale, same hazard); (2) do not add `scrollEnabled` toggling to
    the ScrollView. Leave a comment above the card recording that
    `chartPressConfig.pan.activeOffsetX` / `failOffsetY` (present on `ChartPressPanConfig` in
    victory-native 41.26.0) is the documented escape hatch if on-device testing ever shows the
    page's vertical scroll fighting the horizontal scrub — so the next person does not have to
    rediscover it.

    Do not build the copy string in this file, do not add a local sign or date formatter, and do
    not touch `components/home/TrendChart.tsx`, `04-UI-SPEC.md`, or `packages/engine`.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" || exit 1
strip() { sed -E '/^[[:space:]]*(\/\/|\*|\/\*|\{\/\*|\*\/)/d' "$1"; }
F=apps/mobile/app/trends.tsx
FAIL=0

# G2a — typecheck. This is the load-bearing gate: it proves the press state's y record matches
# the chart's atl/ctl/tsb yKeys union, and that animatedProps/ChartBounds are correctly typed.
pnpm run typecheck && echo "G2a GREEN" || { echo "G2a RED"; FAIL=1; }

# G2b — POSITIVE wiring (comments stripped, so a comment alone can never satisfy it)
for TOK in 'useChartPressState' 'chartPressState={state}' 'onChartBoundsChange' \
           'p1={cursorP1}' 'p2={cursorP2}' 'opacity={cursorOpacity}' \
           'AnimatedTextInput' 'addWhitelistedNativeProps' 'animatedProps=' \
           'CURSOR_FADE_MS' 'clamp('; do
  strip "$F" | grep -qF "$TOK" || { echo "G2b RED — missing: $TOK"; FAIL=1; }
done
# the tested formatter must be CALLED, not merely imported
strip "$F" | grep -v '^import' | grep -qF 'formatScrubTooltip(' \
  || { echo "G2b RED — formatScrubTooltip never called"; FAIL=1; }
[ "$FAIL" -eq 0 ] && echo "G2b GREEN"

# G2c — NEGATIVE, gesture contention. Baselines verified at plan time: 1 touchable open tag and
# 1 press handler (both the range switcher), 0 responder props. Wrapping the card in a touchable
# pushes these to 2 and turns the gate RED. Dry-run at plan time against a fixture that wrapped
# the card => RED, and against a fixture that only NAMES these tokens in a comment => GREEN.
T=$(strip "$F" | grep -cE '<(Pressable|Touchable[A-Za-z]*)\b')
P=$(strip "$F" | grep -cE 'onPress[A-Za-z]*=|onLongPress=')
R=$(strip "$F" | grep -cE 'onStartShouldSetResponder|onMoveShouldSetResponder')
S=$(strip "$F" | grep -c 'scrollEnabled')
{ [ "$T" -eq 1 ] && [ "$P" -eq 1 ] && [ "$R" -eq 0 ] && [ "$S" -eq 0 ]; } \
  && echo "G2c GREEN" || { echo "G2c RED (touchables=$T handlers=$P responders=$R scrollEnabled=$S)"; FAIL=1; }

# G2d — NEGATIVE, copy locality. The D-20 line must exist in the tested module and NOWHERE in
# the screen. Baselines verified at plan time: trends.tsx 0, trendStats.ts 0 (1 after Task 1).
A=$(strip "$F" | grep -c '· HSS')
B=$(strip apps/mobile/lib/trendStats.ts | grep -c '· HSS')
C=$(strip "$F" | grep -c $'−')
{ [ "$A" -eq 0 ] && [ "$B" -ge 1 ] && [ "$C" -eq 0 ]; } \
  && echo "G2d GREEN" || { echo "G2d RED (screen=$A module=$B minus-in-screen=$C)"; FAIL=1; }

# G2e — NEGATIVE, ownership. Nothing this task committed may touch the concurrently-owned files
# or the engine. BASE_SHA is the pre-edit SHA captured in this task's precondition.
OWN=$(git diff --name-only "$BASE_SHA" HEAD -- \
      apps/mobile/components/home/TrendChart.tsx \
      .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md \
      packages/engine)
[ -z "$OWN" ] && echo "G2e GREEN" || { echo "G2e RED — touched: $OWN"; FAIL=1; }

# G2f — all four suites, no reduction (plan-time baseline: 103 / 64 / 95 / 21)
check_suite() { # $1=dir $2=min
  O=$(cd "$1" && npx vitest run 2>&1)
  V=$(printf '%s\n' "$O" | sed -nE 's/.*Tests +([0-9]+) passed.*/\1/p' | head -1)
  { [ -n "$V" ] && [ "$V" -ge "$2" ] && ! printf '%s\n' "$O" | grep -qE 'Tests +[0-9]+ failed'; } \
    && echo "  $1 GREEN ($V)" || { echo "  $1 RED (V=$V, min=$2)"; return 1; }
}
check_suite apps/mobile 107 && check_suite packages/db 64 \
  && check_suite packages/engine 95 && check_suite packages/shared 21 \
  && echo "G2f GREEN" || { echo "G2f RED"; FAIL=1; }

[ "$FAIL" -eq 0 ] && echo "TASK 2 ALL GATES GREEN" || { echo "TASK 2 FAILED"; exit 1; }
    </automated>
    <human-check>
      On the running Metro instance (never restart it — the user is testing live), open
      /trends and confirm, reporting each in the SUMMARY:
      1. Dragging across the chart shows the ash hairline and the tooltip, and the tooltip reads
         the same shape of line the home chart shows.
      2. The tooltip stays fully visible at BOTH the far-left and far-right edges of the chart.
      3. The hairline keeps up with the finger (no rubber-banding); only the fade in/out eases.
      4. A vertical drag starting ON the chart still scrolls the page, and the 28D/90D/1Y buttons
         still switch ranges on a single tap.
      5. Switching range mid-scrub does not leave a stale tooltip line on screen.
    </human-check>
  </verify>
  <done>
    `/trends` has a working scrub hairline + tooltip whose copy comes from `formatScrubTooltip`;
    G2a–G2f all GREEN; the chart card is wrapped by no touchable; `TrendChart.tsx`,
    `04-UI-SPEC.md` and `packages/engine` are untouched by this task's commits.
  </done>
  <reversibility rating="reversible">
    Additive change to one screen behind no migration — reverting the single commit restores the
    approved trends screen exactly.
  </reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| touch input → chart press state | User gesture coordinates cross into a Skia/worklet render path |
| already-persisted `load_daily` rows → tooltip string | Locally-produced numbers rendered as display text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-rnr-01 | Denial of Service | `tooltipText` derived worklet in `app/trends.tsx` | low | mitigate | `matchedIndex` is -1 before first press and may exceed `windowRows.length` for one frame after a range switch; the `?? ''` fallback on the precomputed array makes an out-of-range index render empty instead of throwing on the UI thread |
| T-rnr-02 | Information Disclosure | scrub tooltip copy | low | accept | The tooltip renders only the user's own already-persisted local training numbers on their own device; no new data class is surfaced and nothing leaves the device |
| T-rnr-03 | Tampering | npm/pip/cargo installs | n/a | mitigate | No packages are installed by this plan — every API used is already present in the lockfile and was verified against the installed `.d.ts` files at planning time, so no package-legitimacy gate is triggered |
</threat_model>

<verification>
1. `pnpm run typecheck` clean from the repo root.
2. All four suites green with no reduction: mobile ≥107 (103 + new tooltip tests), db 64,
   engine 95, shared 21.
3. G1a–G1c and G2a–G2f all report GREEN.
4. `git diff --name-only $BASE_SHA HEAD` lists only `apps/mobile/lib/trendStats.ts`,
   `apps/mobile/lib/__tests__/trendStats.test.ts` and `apps/mobile/app/trends.tsx`.
5. Human on-device pass on the five `<human-check>` items, reported in the SUMMARY.
</verification>

<success_criteria>
- Dragging on the `/trends` chart shows a hairline + tooltip with that day's date, HSS, ATL, CTL
  and TSB, in the home chart's D-20 format (D-19/D-20).
- The tooltip copy is produced by one tested function shared with the formatters the home chart
  already uses — no second date formatter, no second sign formatter, no second copy string.
- The scrub coexists with the page scroll and the range switcher with no gesture tuning, because
  it matches the home chart's proven configuration exactly.
- `TrendChart.tsx`, `04-UI-SPEC.md` and `packages/engine` are untouched.
</success_criteria>

<output>
Create `.planning/quick/260907-rnr-add-d-19-scrub-drag-interaction-to-the-t/260907-rnr-SUMMARY.md`
when done. Commit with a `feat(trends):` message. No `git push`.
</output>
