---
phase: 260907-qe6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/queries.ts
  - packages/db/src/index.ts
  - packages/db/src/__tests__/history-queries.test.ts
  - apps/mobile/app/trends.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/components/home/TrendChart.tsx
  - apps/mobile/components/home/ReadinessLight.tsx
  - apps/mobile/constants/readinessBand.ts
  - apps/mobile/lib/trendStats.ts
  - apps/mobile/lib/__tests__/trendStats.test.ts
  - apps/mobile/lib/devSeedPlan.ts
  - apps/mobile/lib/devSeed.ts
  - apps/mobile/lib/__tests__/devSeedPlan.test.ts
  - apps/mobile/app/(tabs)/settings/index.tsx
autonomous: true
requirements: [D-01, D-02, D-04]

estimate:
  tokens: 63000
  raw_tokens: 31500
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Tapping the VIEW FULL TREND row beneath the home trend chart opens a pushed /trends screen with a native back button (D-04)."
    - "The /trends range switcher swaps chart, stats block and readiness strip between 28D / 90D / 1Y with no second database read (D-01)."
    - "The /trends chart draws three real lines — ATL bone, CTL ash, TSB volt — plus a dashed steel zero reference for TSB (D-01, overrides D-18's no-TSB-line rule for this screen only)."
    - "The stats block shows current ATL/CTL/TSB each with a 7-day delta, peak day load with its date, rest-day count and average HSS — every value read from persisted load_daily rows, never a re-derived EWMA (D-01)."
    - "When fewer stored days exist than the selected range, the screen renders only the days that exist and says so in a mono caption — it never pads or fabricates points (D-01)."
    - "The VIEW FULL TREND affordance is a sibling row beneath the chart card, not a wrapper around it, so it never contends with the pan gesture the card itself owns for the D-19 scrub (D-02)."
    - "pnpm run typecheck is clean and all four existing vitest suites still pass with no reduction in test count."
  artifacts:
    - apps/mobile/app/trends.tsx
    - apps/mobile/lib/trendStats.ts
    - apps/mobile/lib/__tests__/trendStats.test.ts
    - apps/mobile/constants/readinessBand.ts
    - packages/db/src/queries.ts
  key_links:
    - "app/(tabs)/index.tsx supplies TrendChart's onPressDetail, which is the ONLY navigation entry point to /trends — TrendChart itself stays router-free and presentational."
    - "trends.tsx calls recentTrend(db, 365) exactly once per focus, then slices client-side per range — one read, no refetch on range change (matches History's read-everything-fold-in-memory precedent)."
    - "constants/readinessBand.ts is the single band→color mapping consumed by BOTH ReadinessLight and the trends readiness strip — the two can never drift (mirrors the calibratingCaption shared-constant precedent)."
    - "expo-router typegen must emit /trends into apps/mobile/.expo/types/router.d.ts before pnpm run typecheck can pass — that file is inside the mobile tsconfig include list."
    - "trendStats.ts imports nothing from @apsis/db or react-native, so it lands inside apps/mobile/vitest.config.mts's lib/** include (the runEntryLogic.ts / devSeedPlan.ts pure-vs-native split precedent)."
---

<objective>
Add a pushed full-page trends detail route, reached from a new tap affordance beneath the
home trend chart.

Four locked user decisions govern this quick task and are cited by ID throughout. This plan
implements D-01, D-04 and the tap-affordance half of D-02; the sibling plan
`260907-qe6b-PLAN.md` implements the rest of D-02 and all of D-03.

- **D-01 — Detail page content.** A range switcher (28D / 90D / 1Y), all three series on
  one large chart including **TSB as a real line**, a stats block (current ATL/CTL/TSB with
  7-day deltas, peak load, rest-day count, average HSS), and a readiness-band history strip.
- **D-02 — Home chart: "rich, but keep volt for the ring."** Only the **visible
  tap-for-detail affordance** half of this decision lands in this plan (Task 1). The
  gridline / area-fill / point-dot enrichment and the palette-containment gate that keeps
  the HssRing as the Today screen sole focal accent live in the sibling plan
  `260907-qe6b-PLAN.md`, along with all of D-03 (the spec amendment).
- **D-04 — Navigation shape.** The detail view is a **pushed route at
  `apps/mobile/app/trends.tsx`** (sibling of `modal.tsx`, outside `(tabs)/`), not a fifth
  tab. DESIGN-SYSTEM.md section 5's unbuilt 5-tab layout with a TRENDS tab is deliberately
  NOT resurrected; the sibling plan records that in the spec amendment.

**Plan split (checker-directed).** This plan is Tasks 1-3. The former Task 4 — the home
chart's visual enrichment plus the `04-UI-SPEC.md` section 5 amendment — is now
`260907-qe6b-PLAN.md`, a standalone sibling with `depends_on: []` that touches only
`TrendChart.tsx` and the spec document. The two run SEQUENTIALLY, this plan first, on the
same working tree by separate executors. There is no cross-plan coordination to perform and
nothing here to wait on; qe6b simply must not start until this plan is committed, because it
edits a file Task 1 also edits.

Purpose: the trend chart is the Today screen's second hero and currently under-reads at hero
scale, and there is nowhere in the app to interrogate load history beyond 28 days.

Output: a `/trends` route, a pure `trendStats` module with vitest coverage, a wider
`load_daily` query builder, a tap affordance on the home chart, and a longer dev seeder so
the 1Y range is demonstrable.

**Deliberate non-goals (stated so the executor does not sprawl):** no scrub/tooltip on the
detail chart (the stats block replaces per-point interrogation; the home chart keeps its
D-19 scrub); no fifth tab; no engine change; no new npm dependencies; no `git push`; no
Phase 06-07 or App Store artifact edits; whichever Metro dev server is currently serving the
app is NOT killed or restarted at any point — it is the expo-router typegen watcher Task 1
depends on.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@apps/mobile/AGENTS.md
@DESIGN-SYSTEM.md
@.planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md
@apps/mobile/components/home/TrendChart.tsx
@apps/mobile/components/home/ReadinessLight.tsx
@apps/mobile/app/(tabs)/index.tsx
@apps/mobile/app/session/detail.tsx
@apps/mobile/constants/Colors.ts
@apps/mobile/constants/theme.ts
@apps/mobile/lib/devSeedPlan.ts
@apps/mobile/lib/devSeed.ts
@packages/db/src/queries.ts
@packages/db/src/loadDaily.ts
</context>

<interface_context>
Verified live against installed files at planning time (mutable-scope authority, #3786 —
these were read from `node_modules`, not recalled):

**victory-native 41.26.0** (`node_modules/victory-native/dist/`):
- `Line` props: `SkiaDefaultProps<Pick<PathProps, "color"|"strokeWidth"|…|"opacity"|"start"|"end">, "start"|"end">`
  → `opacity` and `end` are both `AnimatedProp`. Safe to drive from a Reanimated shared value.
- `Area` props: `Partial<Pick<PathProps, "color"|"blendMode"|"opacity"|"antiAlias">>` — plain
  `PathProps`, NOT run through `SkiaDefaultProps`. `Area.opacity` is a plain `number`. This is
  the la6 asymmetry; the existing wrapping Skia `<Group opacity={…}>` is the fix. **Never cast.**
- `Scatter` props: `SkiaDefaultProps<Pick<PathProps, "style"|"color"|…|"opacity"|"start"|"end">, "start"|"end">`
  plus `radius?: number | ((pt) => number)` and `shape?: "circle"|"square"|"star"`.
  `Scatter.opacity` **is** animated-capable — dots can ride `drawProgress` directly, no Group needed.
- `xAxis` accepts `lineColor?: Color` and `lineWidth?: number` (vertical gridlines at tick
  values — currently zeroed out in TrendChart.tsx) alongside `font`, `labelColor`, `tickValues`,
  `tickCount`, `formatXLabel`.
- `yAxis[]` accepts the same keys plus `domain?: YAxisDomain`.
- `CartesianChartRenderArg` supplies `{ xScale, xTicks, yScale, yTicks, chartBounds, canvasSize, points }`.
  `yScale` is a d3 `ScaleLinear<number, number>` → **`yScale(0)` gives the pixel y for TSB = 0**,
  which is how the dashed zero reference line is placed.
- `Scatter` must be added to the existing `from 'victory-native'` import.

**expo-router 56.x:** typed routes are ON (`app.json` → `experiments.typedRoutes: true`) and
generated into `apps/mobile/.expo/types/router.d.ts`, which `apps/mobile/tsconfig.json`
includes. A brand-new route file is not typecheckable until the running Metro watcher
regenerates that file. Import navigation from `expo-router` only — never `@react-navigation/*`.

**Existing shapes reused, not redesigned:**
- `TrendChartPoint { day, hss, atl, ctl, tsb, dateLabel, [key: string]: number | string }` and
  `TrendChartProps { data, calibratingDayN? }` — the index signature is load-bearing for
  victory-native's generic inference. Do not remove it.
- `last28DaysTrend(db)` = `select().from(loadDaily).orderBy(desc(localDate)).limit(28)`.
- `load_daily` columns: `localDate` (PK, `YYYY-MM-DD`), `dayHss`, `atl`, `ctl`, `tsb` (all
  `real` → `number | null` in the drizzle row type), `readinessBand`
  (`'green'|'amber'|'red'|'calibrating'`, nullable), `updatedAt`.
- `computeLoadDailyUpsertRows` gap-fills a **calendar-contiguous** range, so `load_daily` has
  exactly one row per day since the first-ever session and rest days appear as explicit
  `dayHss = 0`. Row count == day count. Every stat in D-01 is derivable from these rows.
- `session/detail.tsx`'s nested `<Stack.Screen options={{ headerShown: true, title: '',
  headerBackTitle: '', headerStyle: { backgroundColor: Colors.dark.background },
  headerShadowVisible: false, headerTintColor: Colors.dark.text }} />` is the established
  pushed-screen header idiom (the root layout defaults `headerShown` to false).
- `ReadinessLight`'s local `BAND_COLOR` maps green→accent, amber→warning, red→destructive.
</interface_context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "tap the home chart, land on a real /trends screen" — one path only</name>
  <files>packages/db/src/queries.ts, packages/db/src/index.ts, packages/db/src/__tests__/history-queries.test.ts, apps/mobile/components/home/TrendChart.tsx, apps/mobile/app/(tabs)/index.tsx, apps/mobile/app/trends.tsx</files>
  <action>
Wire ONE path through every layer this task touches — db query builder, presentational
component, screen, expo-router navigation — end to end, production quality. No range
switcher, no stats block, no readiness strip yet; those are Tasks 2 and 3 expanding out from
this proven slice.

**Layer 1 — db.** In `packages/db/src/queries.ts`, add `recentTrend(db: QueryableDB, days:
number)` returning `db.select().from(loadDaily).orderBy(desc(loadDaily.localDate)).limit(days)`,
with a doc comment matching the file's existing voice: most-recent-first, caller reverses to
chronological, no soft-delete filter needed because `load_daily` is recompute output over
already-filtered workouts. Refactor `last28DaysTrend` to delegate to it (`recentTrend(db, 28)`)
so there is one query shape, and keep `last28DaysTrend` exported unchanged — the home screen
and its existing test both still call it. Re-export `recentTrend` from `packages/db/src/index.ts`
in the same query-builder export block. `days` is bound through drizzle's parameterized
`.limit()`; never build the limit with an interpolated `sql` template (T-qe6-01).

Add a `describe('recentTrend')` case to `packages/db/src/__tests__/history-queries.test.ts`
following the existing `last28DaysTrend` sqlite-proxy `.toSQL()` shape-test idiom: assert the
generated SQL orders by `local_date` descending and carries a bound limit placeholder. Do not
modify the existing `last28DaysTrend` case.

**Layer 2 — presentational component.** In `apps/mobile/components/home/TrendChart.tsx`, add
an optional `onPressDetail?: () => void` to `TrendChartProps` and, when it is supplied, render
a tap affordance row **beneath** the chart card inside the existing `styles.wrapper` (per D-02's
"visible tap for detail affordance"). It is a separate `Pressable` row, deliberately NOT a
wrapper around the card: the card already owns a pan gesture via `useChartPressState`, and
wrapping it would put a tap recognizer in contention with the D-19 scrub. The row is a ghost
control — transparent fill, 1px `Colors.dark.border` top hairline, `HIT_TARGET_MIN` minimum
height, mono ash label reading `VIEW FULL TREND` with a trailing `▸`, `accessibilityRole`
"button" and an explicit `accessibilityLabel`. TrendChart imports nothing from expo-router and
never navigates itself — it only calls the callback. Every other existing behavior in this file
(scrub, draw-on, calibrating caption, curve type, the public `TrendChartPoint` shape) is
untouched by this task; visual enrichment belongs to the sibling plan 260907-qe6b-PLAN.md.

**Layer 3 — screen + navigation.** In `apps/mobile/app/(tabs)/index.tsx`, pass
`onPressDetail={() => router.push('/trends')}` to the `<TrendChart>` render (the `router` from
`useRouter()` is already in scope). This is the ONLY entry point to the new route (D-04).

**Layer 4 — the new route.** Create `apps/mobile/app/trends.tsx` — a top-level sibling of
`modal.tsx`, outside `(tabs)/`, so it pushes over the tab shell rather than becoming a tab
(D-04). It needs no `<Stack.Screen name>` registration in `app/_layout.tsx`: `session/detail.tsx`
and `session/finish.tsx` are already reached the same way. Re-enable the native header with the
exact nested `<Stack.Screen options={…} />` block `session/detail.tsx` uses. Wrap in
`SafeAreaView` + `ScrollView` matching that screen's shape. Load data with `useFocusEffect`
+ `useCallback` calling `recentTrend(db, 365)` once, reverse to chronological, and coalesce
nulls exactly the way `index.tsx` already does (`row.atl ?? 0`, `row.readinessBand ??
'calibrating'`). Wrap the load in try/catch; on failure `console.error` a generic
`'[Apsis] TRENDS load failed:'` prefix plus the caught error only — never log row payloads or
health-derived values (T-qe6-02).

For this tracer, render a fixed 90-day slice (the last 90 chronological rows) as one large
`CartesianChart` (height ~260) with `yKeys={['atl','ctl','tsb']}` and three `Line`s: ATL in
`Colors.dark.text`, CTL in `Colors.dark.mutedText`, TSB in `Colors.dark.accent`, all at
strokeWidth 2 with the same `monotoneX` curve the home chart uses. Volt is used **deliberately
and only** for the TSB line here: this screen has no HssRing, TSB is the readiness quantity the
screen exists to show, and it is the one line the eye should follow — the achromatic palette
only offers two clearly-distinguishable values on carbon, so a third series needs the accent.
Add a dashed steel horizontal reference line at `yScale(0)` (Skia `Line` + `DashPathEffect`)
so a negative TSB reads correctly against a shared y-scale, plus horizontal and vertical steel
gridlines via `yAxis`/`xAxis` `lineColor`/`lineWidth`, and mono 9px ash axis labels reusing the
same guarded `matchFont` pattern TrendChart.tsx uses. Add a mono legend row —
`ATL` bone, `CTL` ash, `TSB` volt.

Guard honestly (this is the D-22 discipline generalized): zero rows renders a mono
`NO TREND DATA YET` caption and nothing else; fewer than two rows renders the caption
`NOT ENOUGH HISTORY TO CHART YET` instead of a chart, because a one-point line path draws
nothing. Never pad the series to reach a target length.

**Typegen step — do this before claiming typecheck.** Adding `app/trends.tsx` makes the route
literal `/trends` appear in `apps/mobile/.expo/types/router.d.ts`, regenerated by whichever
Metro dev server is currently serving the app (port 8081 at planning time, but do not rely on
that and do not rely on any particular process id). Do NOT kill or restart the running Metro
process, whatever its pid turns out to be. If the literal has not appeared within about a
minute, nudge the watcher by re-saving any file under `apps/mobile/app/`; the verify gate
below polls for it.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='apps/mobile/.expo/types/router.d.ts';const t0=Date.now();(function poll(){if(fs.existsSync(p)&&fs.readFileSync(p,'utf8').includes('/trends'))return console.log('typed route /trends present');if(Date.now()-t0>90000)throw new Error('expo-router typegen has not emitted /trends into '+p+' after 90s -- re-save any file under apps/mobile/app/ to nudge the running Metro watcher; do NOT kill whatever Metro process is currently serving the app');setTimeout(poll,2000);})()"</automated>
    <automated>pnpm run typecheck</automated>
    <automated>pnpm --filter @apsis/db test</automated>
    <automated>node -e "const fs=require('fs');const strip=t=>t.split('\n').map(l=>/^\s*(\*|\/\/|\/\*)/.test(l)?'':l).join('\n');const q=strip(fs.readFileSync('packages/db/src/queries.ts','utf8'));const idx=strip(fs.readFileSync('packages/db/src/index.ts','utf8'));if(!/export\s+function\s+recentTrend\s*\(/.test(q))throw new Error('recentTrend not exported from queries.ts');if(!/export\s+function\s+last28DaysTrend\s*\(/.test(q))throw new Error('last28DaysTrend export was removed -- index.tsx and its test still call it');if(!idx.includes('recentTrend'))throw new Error('recentTrend not re-exported from @apsis/db barrel');if(/sql\`[^\`]*limit/i.test(q))throw new Error('limit must be bound through drizzle .limit(), never an interpolated sql template (T-qe6-01)');console.log('db layer OK');"</automated>
    <automated>node -e "const fs=require('fs');const strip=t=>t.split('\n').map(l=>/^\s*(\*|\/\/|\/\*)/.test(l)?'':l).join('\n');const t=strip(fs.readFileSync('apps/mobile/app/trends.tsx','utf8'));const home=strip(fs.readFileSync('apps/mobile/app/(tabs)/index.tsx','utf8'));const chart=strip(fs.readFileSync('apps/mobile/components/home/TrendChart.tsx','utf8'));for(const m of ['CartesianChart','recentTrend','points.tsb','yScale','useFocusEffect','Stack.Screen'])if(!t.includes(m))throw new Error('trends.tsx missing: '+m);if(/@react-navigation/.test(t))throw new Error('trends.tsx must import navigation from expo-router only (CLAUDE.md, SDK 56 fork)');if(!/onPressDetail/.test(chart))throw new Error('TrendChart is missing the onPressDetail prop');if(/expo-router/.test(chart))throw new Error('TrendChart must stay router-free -- navigation belongs to index.tsx');if(!home.includes('onPressDetail')||!home.includes(\"'/trends'\"))throw new Error('home screen does not wire onPressDetail to a /trends push');console.log('tracer wiring OK');"</automated>
  </verify>
  <done>`pnpm run typecheck` is clean, `@apsis/db`'s suite passes with one new `recentTrend` case, and tapping VIEW FULL TREND beneath the home chart pushes a `/trends` screen that renders three real lines (ATL bone, CTL ash, TSB volt) over the last 90 persisted `load_daily` rows with a back button, honest empty/short-history captions, and no `@react-navigation` import anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure trendStats module + stats block + readiness-band strip on /trends</name>
  <files>apps/mobile/lib/trendStats.ts, apps/mobile/lib/__tests__/trendStats.test.ts, apps/mobile/constants/readinessBand.ts, apps/mobile/components/home/ReadinessLight.tsx, apps/mobile/app/trends.tsx, apps/mobile/app/(tabs)/index.tsx</files>
  <behavior>
Tests to write FIRST against `computeTrendStats` / `sliceRange` / `formatSignedDelta` /
`formatTrendDateLabel`, all pure and all fed chronological oldest-first rows:

- Empty input returns `null` (the screen branches on that, no sentinel zeros).
- Current `atl`/`ctl`/`tsb` equal the LAST row's values, not the first.
- `atlDelta7` equals `last.atl - rows[len - 8].atl`; same for ctl/tsb. With fewer than 8 rows
  every delta is `null` (never a fabricated 0).
- `peakHss` is the maximum `dayHss` and `peakDate` is the `localDate` of its FIRST occurrence
  on a tie.
- `restDays` counts rows whose `dayHss` is exactly 0 — `load_daily` is calendar-contiguous, so
  rest days are real explicit rows, not gaps.
- `avgHss` is the mean `dayHss` across ALL rows in the window including rest days (the honest
  per-calendar-day training-load average), and `days` equals `rows.length`.
- `sliceRange(rows, n)` returns the LAST n rows in chronological order, and returns every row
  unchanged when `rows.length <= n` — it never pads.
- `formatSignedDelta` renders `+4`, `−3` using U+2212 (matching TrendChart's `formatSignedTsb`
  convention), `+0` for zero, and `—` for `null`.
- `formatTrendDateLabel('2026-07-08', { withDay: true })` is `JUL 8`; without `withDay` it is
  `JUL`.
  </behavior>
  <action>
**Module placement rationale, stated because engine purity is a hard project constraint:**
these are presentation-layer aggregates over already-persisted `load_daily` values — max,
mean, count, and a subtraction of two stored numbers. No new training-science math, no EWMA,
nothing the engine should own. They go in `apps/mobile/lib/trendStats.ts`, which imports
nothing from `@apsis/db` or `react-native` so it lands inside `apps/mobile/vitest.config.mts`'s
`lib/**` include — the same pure-vs-native split `runEntryLogic.ts` and `devSeedPlan.ts`
already established. `packages/engine` is not modified by this plan.

Create `apps/mobile/lib/trendStats.ts` exporting: a `TrendStatRow` interface (`localDate`,
`dayHss`, `atl`, `ctl`, `tsb`, `readinessBand` — all non-nullable; the screen coalesces
drizzle's nullable columns before calling); a `TrendStats` interface; `computeTrendStats(rows):
TrendStats | null`; `sliceRange(rows, days)`; `formatSignedDelta(value: number | null): string`;
`formatTrendDateLabel(localDate, opts?)`; a module-level `MONTH_ABBR`; and a
`TREND_RANGES` const array of `{ key: '28D' | '90D' | '1Y', days: 28 | 90 | 365 }` typed
`as const` so Task 3's switcher and the 365-row read both derive their numbers from one place.
Declare the four helpers as `export function` and the range table as `export const` (the gate
below greps for those exact declaration forms — do not use arrow-const or a re-export block).
Write the tests in `apps/mobile/lib/__tests__/trendStats.test.ts` first and watch them fail
before implementing (RED → GREEN), following `devSeedPlan.test.ts`'s file-header and
describe/it voice.

Create `apps/mobile/constants/readinessBand.ts` exporting the band→color mapping currently
private to `ReadinessLight.tsx` (green → `Colors.dark.accent`, amber → `Colors.dark.warning`,
red → `Colors.dark.destructive`) plus a `bandColor(band)` helper that maps `'calibrating'` to
`Colors.dark.steel`. Edit `ReadinessLight.tsx` to import that mapping instead of declaring its
own local copy — one source of truth so the status light and the new strip can never drift,
exactly like the shared `calibratingCaption` precedent. Do not change any other ReadinessLight
behavior (the pulse, the copy strings, the calibrating null return).

In `apps/mobile/app/(tabs)/index.tsx`, replace the body of the local `formatShortDate` helper
with a delegation to `formatTrendDateLabel(localDate, { withDay: true })` so the home chart's
`dateLabel` strings and the trends screen's axis labels come from one formatter. This is the
only change to that file in this task; leave its `MONTH_ABBR` in place if
`formatGreetingTimestamp` still needs it.

In `apps/mobile/app/trends.tsx`, add two sections beneath the chart (D-01):

1. **Readiness-band history strip** — one flex segment per row in the visible window, 6px
   tall, `Radius.pill`, background from `bandColor(row.readinessBand)`, laid out left-to-right
   oldest-to-newest so it reads in the same direction as the chart above it. Beneath it, a mono
   ash caption and a compact legend (`READY` / `CAUTION` / `OVERREACHING` / `CALIBRATING`) with
   matching swatches. Volt appears here only for green segments, which is the same
   semantic/state-driven usage 04-UI-SPEC.md's One-Volt Discipline already grants
   `ReadinessLight` as its locked exception — not a decorative second accent.
2. **Stats block** — a two-column grid of six cells on carbon surfaces with hairline borders
   and `Radius.md`: ATL, CTL and TSB (each showing the current value plus its 7-day delta via
   `formatSignedDelta`), PEAK LOAD (value plus `formatTrendDateLabel` of `peakDate`), REST DAYS
   (`{restDays} / {days}`) and AVG HSS. Numbers use `Typography.heading` in `Colors.dark.text`
   with `tabularNums`; captions use the `Kicker` mono role in `Colors.dark.mutedText`. Numbers
   here are bone, never volt — the screen's accent budget is already spent on the TSB line and
   the semantic band strip. When `computeTrendStats` returns `null`, render the existing empty
   caption instead of a grid of zeros.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile test</automated>
    <automated>pnpm run typecheck</automated>
    <automated>node -e "const fs=require('fs');const strip=t=>t.split('\n').map(l=>/^\s*(\*|\/\/|\/\*)/.test(l)?'':l).join('\n');const s=strip(fs.readFileSync('apps/mobile/lib/trendStats.ts','utf8'));if(/@apsis\/db|react-native|expo-/.test(s))throw new Error('trendStats.ts must stay pure -- no native or db imports (it runs under plain node in vitest)');for(const m of ['export function computeTrendStats','export function sliceRange','export function formatSignedDelta','export function formatTrendDateLabel','export const TREND_RANGES'])if(!s.includes(m))throw new Error('trendStats.ts missing export: '+m);if(!s.includes('−'))throw new Error('formatSignedDelta must use the U+2212 minus sign, matching TrendChart formatSignedTsb');const rl=strip(fs.readFileSync('apps/mobile/components/home/ReadinessLight.tsx','utf8'));if(!rl.includes('readinessBand'))throw new Error('ReadinessLight still owns a private band-color copy -- it must import the shared mapping');const t=strip(fs.readFileSync('apps/mobile/app/trends.tsx','utf8'));for(const m of ['computeTrendStats','bandColor','restDays','avgHss','peakHss'])if(!t.includes(m))throw new Error('trends.tsx missing: '+m);console.log('stats layer OK');"</automated>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('apps/mobile/lib/__tests__/trendStats.test.ts','utf8');const n=(c.match(/\bit\(/g)||[]).length;if(n<9)throw new Error('expected at least 9 it() cases covering the behavior block, found '+n);console.log('trendStats test coverage OK: '+n+' cases');"</automated>
  </verify>
  <done>`trendStats.ts` is pure and fully covered by at least nine passing vitest cases; `constants/readinessBand.ts` is the single band→color source consumed by both `ReadinessLight` and the trends strip; `/trends` renders a readiness-band history strip and a six-cell stats block (ATL/CTL/TSB with 7-day deltas, peak load with date, rest days, average HSS) with bone numbers and no zero-filled placeholders on empty data; typecheck clean and all mobile tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Range switcher (28D / 90D / 1Y) with honest sparse history, and a ~400-day dev seeder</name>
  <files>apps/mobile/app/trends.tsx, apps/mobile/lib/devSeedPlan.ts, apps/mobile/lib/devSeed.ts, apps/mobile/lib/__tests__/devSeedPlan.test.ts, apps/mobile/app/(tabs)/settings/index.tsx</files>
  <action>
**Range switcher (D-01).** In `apps/mobile/app/trends.tsx`, replace Task 1's fixed 90-day
slice with a `useState` range selector driven by `TREND_RANGES`. The single `recentTrend(db,
365)` read stays as-is and every range is served by slicing that already-loaded array client
side via `sliceRange` — no refetch on range change, so switching is instant. This matches
History's established read-everything-fold-in-memory discipline for this local-first dataset
size, and 365 rows of six columns is a trivial read against the `load_daily_date_idx` index.

The switcher itself is a segmented control matching the run form's convention: steel track,
`Radius.sm`, `HIT_TARGET_MIN` minimum height, active segment filled **bone** with void text,
inactive segments transparent with ash text, mono uppercase labels `28D` / `90D` / `1Y`,
default `28D`. Bone, not volt — the screen's accent is already committed to the TSB line and
the semantic band strip, and the "active segment goes bone whenever the screen already has an
accent element" rule is the same one Phase 03 applied across every other segmented control in
this app. Each segment needs `accessibilityRole` "button" and an `accessibilityState` selected
flag.

Changing range must re-drive the chart, the readiness strip and the stats block together from
the same sliced array — one derived value feeding all three, never three independent slices
that could disagree.

**Honest sparse history (D-01).** When the sliced array is shorter than the requested range
(a three-month-old install asking for 1Y), render every row that exists and, directly beneath
the chart, a mono ash caption of the form `SHOWING {n} OF {requested} DAYS · SINCE {MON D}`
using `formatTrendDateLabel` on the first row's date. Never pad the series, never extend the
domain to the requested length, never draw a flat leading segment for days with no data — the
same rule D-22 already imposes on the home chart's calibrating state. Axis label density
scales with the range: use `formatTrendDateLabel` with `withDay` for 28D and 90D and without
it (month abbreviation only) for 1Y, and thin the tick values so labels never overlap — one
named constant per range, not a magic number inline. Point dots are drawn only for the 28D
range; at 90 or 365 points they become noise, so at wider ranges the lines carry alone.

**Dev seeder extension.** The seeder currently generates ~90 days, so the 1Y range would look
near-empty in a dev build and could not be evaluated on-device. Extend it to ~400 days,
preserving every la6 guarantee exactly: `devSeedPlan.ts` stays pure (no `__DEV__`, no
`@apsis/db`, no `react-native`, no `Math.random`, `today` always caller-supplied), and
`devSeed.ts` keeps its positional first-statement dev guard, its `Promise<void>` return, its
`devseed-` id prefix, its absence of any HealthKit path, its refusal to write `load_daily`
directly, and its single terminal `recomputeLoadDaily` call.

In `devSeedPlan.ts`, introduce a `MACROCYCLE_DAYS` constant of 90 and restructure
`phaseMultiplier` so a window longer than one macrocycle reads as a sequence of repeated
build→taper blocks of near-equal length rather than one implausible 400-day linear ramp, with
a mild across-block progression so the athlete still trends upward year over year. The existing
single-block shape must be preserved bit-for-bit when the window is 90 days or shorter, so
every current test in `devSeedPlan.test.ts` keeps passing untouched. In `devSeed.ts`, add a
named `DEV_SEED_DAYS` constant of 400 and pass it to `buildDevSeedSessions`. Update the
Settings copy in `app/(tabs)/settings/index.tsx` — the row label, its `accessibilityLabel`, the
success alert body and the failure `console.error` prefix — to state the new day count; make no
structural change to the dev-gated JSX block, whose la6 gate is re-run below unchanged.

Add a new `describe` block to `devSeedPlan.test.ts` covering the long window: the returned
dates are a strictly-increasing duplicate-free subset of the 400-day window ending on the
supplied date; the rest-day fraction still lands inside 15–25%; output is deterministic across
two calls and differs for a different date; more than one activity type appears; and each
macrocycle block ends in a taper (its final seven calendar days average strictly less load
than the fourteen days preceding them). Do not modify the existing 90-day cases.

Note the runtime honestly in the SUMMARY: 400 days at ~80% training density is roughly 320
workout rows plus 320 segment rows, so the dev-only seed button now takes noticeably longer to
complete; the existing `devSeedBusy` double-tap guard already covers that window.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile test</automated>
    <automated>pnpm run typecheck</automated>
    <automated>node -e "const fs=require('fs');const strip=p=>{if(!fs.existsSync(p))throw new Error('missing '+p);return fs.readFileSync(p,'utf8').split('\n').map(l=>/^\s*(\*|\/\/|\/\*)/.test(l)?'':l.replace(/(^|[^:])\/\/.*/,(x,a)=>a)).join('\n');};const seed=strip('apps/mobile/lib/devSeed.ts');const lineOf=i=>seed.slice(0,i).split('\n').length;const re=/export\s+async\s+function\s+([A-Za-z0-9_]+)\s*\(/g;const found=[];let m;while((m=re.exec(seed))){let d=0,close=-1;for(let i=re.lastIndex-1;i<seed.length;i++){const c=seed[i];if(c==='(')d++;else if(c===')'){d--;if(d===0){close=i;break;}}}if(close<0)throw new Error('unbalanced parameter list for '+m[1]);const brace=seed.indexOf('{',close);if(brace<0)throw new Error('no body found for '+m[1]);if(!/^\s*if\s*\(\s*!__DEV__\s*\)\s*return;/.test(seed.slice(brace+1)))throw new Error('release-build gate FAILED: export '+m[1]+' (line '+lineOf(m.index)+') does not open with the dev guard as its FIRST body statement');found.push(m[1]);}if(found.length===0)throw new Error('no exported entry points found in devSeed.ts');if(/writeBackRun|getSyncState|from '\.\/healthkit/.test(seed))throw new Error('seeder must never invoke a HealthKit path');const plan=strip('apps/mobile/lib/devSeedPlan.ts');if(plan.includes('__DEV__'))throw new Error('pure devSeedPlan.ts must not reference the RN dev flag');if(/@apsis\/db|react-native/.test(plan))throw new Error('pure devSeedPlan.ts must not import native modules');if(/Math\.random/.test(plan))throw new Error('seeder must be deterministic -- no Math.random');console.log('devSeed guard adjacency + purity OK: '+found.join(', '));"</automated>
    <automated>node -e "const fs=require('fs');const p='apps/mobile/app/(tabs)/settings/index.tsx';const strip=t=>t.split('\n').map(l=>/^\s*(\*|\/\/|\/\*)/.test(l)?'':l.replace(/(^|[^:])\/\/.*/,(x,a)=>a)).join('\n');const src=strip(fs.readFileSync(p,'utf8'));const lineOf=i=>src.slice(0,i).split('\n').length;const regions=[];const op=/\{\s*__DEV__\s*(\?|&&)/g;let m;while((m=op.exec(src))){let d=0,end=-1;for(let i=m.index;i<src.length;i++){const c=src[i];if(c==='{')d++;else if(c==='}'){d--;if(d===0){end=i;break;}}}if(end<0)throw new Error('unbalanced dev-gated JSX block opened at line '+lineOf(m.index));regions.push([m.index,end]);}if(regions.length===0)throw new Error('release-build gate FAILED: no structural dev-gated JSX block in '+p);for(const name of ['seedDevTrendData','clearDevSeedData']){const at=[];let from=0,i;while((i=src.indexOf(name,from))>=0){from=i+name.length;if(/^\s*\(/.test(src.slice(from,from+40)))at.push(i);}if(at.length===0)throw new Error('release-build gate FAILED: no call site for '+name+'() in '+p);for(const j of at){const rg=regions.find(r=>j>r[0]&&j<r[1]);if(!rg)throw new Error('release-build gate FAILED: '+name+'() called at line '+lineOf(j)+' is OUTSIDE every dev-gated JSX block');if(!src.slice(rg[0],rg[1]).includes('onPress'))throw new Error('release-build gate FAILED: the dev-gated block enclosing '+name+'() contains no onPress row');}}console.log('settings entry-point structural gate OK: '+regions.length+' gated block(s)');"</automated>
    <automated>node -e "const fs=require('fs'),path=require('path');const hits=[];(function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(e.name==='node_modules'||e.name==='.expo'||e.name==='dist')continue;const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(/\.tsx?$/.test(e.name)&&fs.readFileSync(p,'utf8').includes('devSeed'))hits.push(p.split(path.sep).join('/'));}})('apps/mobile');const got=hits.sort();const expected=['apps/mobile/app/(tabs)/settings/index.tsx','apps/mobile/lib/__tests__/devSeedPlan.test.ts','apps/mobile/lib/devSeed.ts','apps/mobile/lib/devSeedPlan.ts'].sort();if(JSON.stringify(got)!==JSON.stringify(expected))throw new Error('devSeed referenced from unexpected modules.\n  got: '+got.join(', ')+'\n  expected: '+expected.join(', '));console.log('seeder reachable only from Settings');"</automated>
    <automated>node -e "const fs=require('fs');const strip=t=>t.split('\n').map(l=>/^\s*(\*|\/\/|\/\*)/.test(l)?'':l).join('\n');const plan=strip(fs.readFileSync('apps/mobile/lib/devSeedPlan.ts','utf8'));const seed=strip(fs.readFileSync('apps/mobile/lib/devSeed.ts','utf8'));if(!/MACROCYCLE_DAYS\s*=\s*90/.test(plan))throw new Error('MACROCYCLE_DAYS constant missing or not 90');const d=seed.match(/DEV_SEED_DAYS\s*=\s*(\d+)/);if(!d)throw new Error('DEV_SEED_DAYS constant missing from devSeed.ts');if(Number(d[1])<365)throw new Error('DEV_SEED_DAYS must cover at least a full year so the 1Y range is demonstrable, got '+d[1]);if(!/buildDevSeedSessions\([^)]*DEV_SEED_DAYS/.test(seed))throw new Error('devSeed.ts still seeds the default window -- DEV_SEED_DAYS is not passed through');const t=strip(fs.readFileSync('apps/mobile/app/trends.tsx','utf8'));for(const m of ['TREND_RANGES','sliceRange'])if(!t.includes(m))throw new Error('trends.tsx missing: '+m);for(const lit of ['28D','90D','1Y'])if(!t.includes(lit))throw new Error('range switcher missing label: '+lit);console.log('range switcher + seeder window OK');"</automated>
    <automated>pnpm --filter @apsis/db test</automated>
    <automated>pnpm --filter @apsis/engine test</automated>
    <automated>pnpm --filter @apsis/shared test</automated>
  </verify>
  <done>The `/trends` screen switches between 28D / 90D / 1Y from one 365-row read with no refetch, drives chart + strip + stats from the same slice, and captions short history honestly instead of padding; the dev seeder covers at least a full year through a named constant while every la6 structural gate (positional `__DEV__` guard, Settings gating, no-HealthKit, purity, determinism, reachability) still passes unchanged and every pre-existing `devSeedPlan` test still passes untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app code → SQLite (`load_daily`) | The new `recentTrend` builder takes a numeric window size and reaches the on-device database. |
| rendered UI → crash reporter / console | The trends screen displays health-derived values (HSS, ATL, CTL, TSB, readiness band). |
| dev-only seeder → `workout` / `endurance_segment` | The extended seeder writes ~320 fabricated sessions into the developer's real local database. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-qe6-01 | Tampering | `recentTrend(db, days)` in `packages/db/src/queries.ts` | low | mitigate | `days` originates from the `TREND_RANGES` const, never user input, and is bound through drizzle's parameterized `.limit()`. Task 1's gate fails the build if a `sql` template is used to construct the limit. |
| T-qe6-02 | Information disclosure | `apps/mobile/app/trends.tsx` error handling | medium | mitigate | The load path's `catch` logs a fixed `'[Apsis] TRENDS load failed:'` prefix and the caught error only — never row payloads, HSS values or dates. `sanitizeSentryEvent`'s existing allowlist (only `exception` + `contexts.device/app` survive) already blocks health values from reaching Sentry; no new field is added to any reported context. |
| T-qe6-03 | Tampering | `devSeed.ts` / `devSeedPlan.ts` extended to ~400 days | high | mitigate | Every la6 control is preserved and re-verified by unchanged gates in Task 3: positional first-statement `__DEV__` guard on both exports (so Metro dead-code-eliminates the whole body in a release bundle), `devseed-` id prefix with a parameterized `like` delete, no bare `delete(workout)`, no HealthKit import, no direct `load_daily` write, deterministic PRNG with no `Math.random`, and reachability limited to the dev-gated Settings block. |
| T-qe6-04 | Denial of service | 400-day seed + 365-row trend read | low | accept | The seed is a developer-only button already guarded by `devSeedBusy` against double-taps; the trend read is a single indexed `load_daily` scan of at most 365 six-column rows, well inside the read-everything-fold-in-memory budget History already operates under. |
| T-qe6-SC | Tampering | npm/pip/cargo installs | high | accept | **No packages are installed by this plan.** Every symbol used (`Scatter`, `DashPathEffect`, `yScale`, expo-router's `Stack`/`useRouter`) comes from already-installed victory-native 41.26.0, @shopify/react-native-skia and expo-router, verified against their installed `.d.ts` files at planning time. The package-legitimacy gate therefore does not apply; if any task discovers it needs a new dependency, stop and escalate rather than installing. |
</threat_model>

<verification>
Automated (every task's gates plus this final sweep):
- `pnpm run typecheck` clean at the repo root (`tsc --build`, the authoritative cross-package entry point).
- `pnpm --filter @apsis/mobile test`, `@apsis/db`, `@apsis/engine`, `@apsis/shared` all pass.
  Floors — mobile ≥ 87 + the new `trendStats` and 400-day `devSeedPlan` cases, db ≥ 63 + the
  new `recentTrend` case, engine ≥ 95 unchanged, shared ≥ 21 unchanged. No suite may lose a test.
- `/trends` is present in `apps/mobile/.expo/types/router.d.ts`.
- The la6 structural gates owned by this plan re-run green: positional `__DEV__` guard
  adjacency, `devSeedPlan` purity/determinism, Settings dev-block enclosure, seeder
  reachability. The TrendChart palette/public-contract gates and the 04-UI-SPEC
  collateral-damage gate belong to `260907-qe6b-PLAN.md` and are re-run there.
- `packages/engine` is untouched (`git status` shows no change under `packages/engine/`).

On-device (developer, via the running Metro dev server — never killed, never restarted):
- The VIEW FULL TREND row is obviously tappable and does not fight the scrub gesture.
- `/trends` pushes with a working back button; 28D / 90D / 1Y each redraw chart, strip and
  stats together; TSB reads correctly against the dashed zero line including when negative.
- With the 400-day seed applied, 1Y is genuinely populated; with the seed cleared, short
  history shows the honest "SHOWING n OF N DAYS" caption rather than a padded line.
</verification>

<success_criteria>
- Every `must_haves.truths` entry is observably true on device.
- Every artifact in `must_haves.artifacts` exists and is committed.
- Every automated gate in all four tasks passes.
- No `git push`, no Phase 06-07 or App Store artifact touched, no dependency added, the
  running Metro dev server never killed or restarted.
</success_criteria>

<output>
Create `.planning/quick/260907-qe6-enrich-the-home-trendchart-and-add-a-tap/260907-qe6-SUMMARY.md` when done.

The SUMMARY must record, at minimum: which victory-native/Skia symbols the `/trends` screen
confirmed present in the installed typings during execution (and any that differed from this
plan's `<interface_context>`); the exact final value of `DEV_SEED_DAYS` and the per-range tick
thinning constants; the observed wall-clock duration of the 400-day seed on device; and the
resulting per-suite test counts. `X_TICK_EVERY_N_DAYS` and `CTL_AREA_FILL_TOP_ALPHA` belong to
`260907-qe6b-PLAN.md` and are recorded in that plan SUMMARY.
</output>
</content>
</invoke>
