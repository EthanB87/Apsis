---
phase: 260907-qe6b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/components/home/TrendChart.tsx
  - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md
autonomous: true
requirements: [D-02, D-03]

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "The home chart shows horizontal AND vertical steel gridlines, gradient area fills under BOTH ATL and CTL with the CTL alpha strictly below ATL's, point dots on both series, and denser mono axis labels on both axes (D-02)."
    - "No volt reaches the home chart in any form — not as a palette token name, a raw hex literal in any casing, an rgb channel triple, an hsl() literal, a computed palette lookup, a destructured binding, or an import from a module that can itself reach volt (D-02)."
    - "Every la6 gate and the public TrendChart contract survive unchanged: the D-19 scrub mechanics, the animate-once draw-on and its 800ms ceiling, the 0.18 area-alpha ceiling, the shared calibrating caption, and the TrendChartPoint index signature."
    - "04-UI-SPEC.md section 5 carries a second dated revision recording every override this quick task makes, including the /trends route and the deliberate non-resurrection of the 5-tab TRENDS tab (D-03, D-04), with the la6 revision block and the rest of the document intact."
    - "pnpm run typecheck is clean and all four vitest suites still pass with no reduction in test count."
  artifacts:
    - apps/mobile/components/home/TrendChart.tsx
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md
  key_links:
    - "constants/Colors.ts is the palette source of truth the containment gate resolves volt from at runtime — the gate is never handed a hardcoded hex, so recoloring or aliasing a token cannot silently defeat it, and the gate fails loudly if the palette shape moves."
    - "The palette exposes more than one key that resolves to the same volt hex (the accent itself plus its tint/tab-selected aliases), so the gate bans by resolved VALUE, not by key name — reaching for an alias is not a way through."
    - "constants/readinessBand.ts (created by 260907-qe6-PLAN.md Task 2) legitimately carries a volt swatch and sits one directory from TrendChart.tsx — the import-aware half of the containment gate exists precisely because that shared constant is the shortest legitimate-looking path to volt on the home chart."
    - "TrendChart.tsx's onPressDetail affordance is created by 260907-qe6-PLAN.md Task 1, so this plan must not start until that plan is committed — otherwise the enrichment is written against a file that is about to change underneath it."
---

<objective>
Enrich the home TrendChart visually, then record the divergence in 04-UI-SPEC.md section 5.

This is the second half of quick task 260907-qe6, split out on checker direction. Two locked
user decisions govern it:

- **D-02 — Home chart: "rich, but keep volt for the ring."** Horizontal AND vertical
  gridlines, area fills under BOTH series, point dots, denser axis labels. ATL line and fill
  stay bone. **No volt on the home chart** — the HssRing keeps the Today screen its single
  focal accent. (The "visible tap-for-detail affordance" half of D-02 is already delivered by
  `260907-qe6-PLAN.md` Task 1.)
- **D-03 — Record the divergence.** The user authorized going against the design doc for the
  visuals; the spec must not be left contradicting the code. Amend `04-UI-SPEC.md` section 5
  with a second dated revision, following the supersede idiom already at ~line 372. The
  amendment also records the `/trends` route and the D-04 navigation shape delivered by the
  sibling plan, because a spec revision that documents half a change is worse than none.

**Execution order (hard).** This plan and `260907-qe6-PLAN.md` are siblings with
`depends_on: []`, executed SEQUENTIALLY by separate executors on the same working tree —
qe6 first, then this one. They are never run in parallel: both edit
`apps/mobile/components/home/TrendChart.tsx`, and the containment gate here also reads
`constants/readinessBand.ts`, which qe6 Task 2 creates. There is no cross-plan coordination
to perform beyond starting after qe6 is committed.

Purpose: the trend chart is the Today screen's second hero and currently under-reads at hero
scale — one thin line over a bare grid — while the design doc still describes a chart the app
no longer draws.

Output: an enriched home chart that stays strictly bone and ash, a palette-containment gate
that cannot be walked around by indirection, and a dated spec amendment.

**Deliberate non-goals (stated so the executor does not sprawl):** no change to the public
`TrendChartPoint` / `TrendChartProps` shapes, the `calibratingCaption` export, the D-19 scrub,
the animate-once draw-on or `CURVE_TYPE`; no change to `/trends` or any file the sibling plan
owns; no engine change; no new npm dependencies; no `git push`; no Phase 06-07 or App Store
artifact edits; no edit to DESIGN-SYSTEM.md; whichever Metro dev server is currently serving
the app is NOT killed or restarted at any point.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@apps/mobile/AGENTS.md
@DESIGN-SYSTEM.md
@.planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md
@apps/mobile/components/home/TrendChart.tsx
@apps/mobile/constants/Colors.ts
@apps/mobile/constants/theme.ts
</context>

<interface_context>
Verified live against installed files at planning time (mutable-scope authority, #3786 —
these were read from `node_modules`, not recalled):

**victory-native 41.26.0** (`node_modules/victory-native/dist/`):
- `Line` props: `SkiaDefaultProps<Pick<PathProps, "color"|"strokeWidth"|…|"opacity"|"start"|"end">, "start"|"end">`
  → `opacity` and `end` are both `AnimatedProp`. Safe to drive from a Reanimated shared value.
- `Area` props: `Partial<Pick<PathProps, "color"|"blendMode"|"opacity"|"antiAlias">>` — plain
  `PathProps`, NOT run through `SkiaDefaultProps`. `Area.opacity` is a plain `number`. This is
  the la6 asymmetry; the existing wrapping Skia `<Group opacity={…}>` is the fix. **Never cast.**
- `Scatter` props: `SkiaDefaultProps<Pick<PathProps, "style"|"color"|…|"opacity"|"start"|"end">, "start"|"end">`
  plus `radius?: number | ((pt) => number)` and `shape?: "circle"|"square"|"star"`.
  `Scatter.opacity` **is** animated-capable — dots can ride `drawProgress` directly, no Group needed.
- `xAxis` accepts `lineColor?: Color` and `lineWidth?: number` (vertical gridlines at tick
  values — currently zeroed out in TrendChart.tsx) alongside `font`, `labelColor`, `tickValues`,
  `tickCount`, `formatXLabel`.
- `yAxis[]` accepts the same keys plus `domain?: YAxisDomain`.
- `Scatter` must be added to the existing `from 'victory-native'` import.

**Palette shape the containment gate depends on** (`apps/mobile/constants/Colors.ts`):
- Colors are declared as top-level `const NAME = '#RRGGBB';` bindings, then collected into a
  `darkPalette` object literal that is exported as both `light` and `dark`. The gate parses
  those two structures to resolve which palette KEYS carry a volt VALUE.
- More than one key resolves to the same volt hex — the accent itself plus its tint/tab-selected
  aliases — and there is a second, darker volt for the pressed CTA state. All of them are
  treated as volt. This is why the gate cannot be a list of key names.
- `TrendChart.tsx` today imports the palette as a default import and reads it only through
  `Colors.dark.TOKEN` dot access, with zero raw hex literals in the file. That is the state the
  gate locks in.

**Existing shapes reused, not redesigned:**
- `TrendChartPoint { day, hss, atl, ctl, tsb, dateLabel, [key: string]: number | string }` and
  `TrendChartProps { data, calibratingDayN?, onPressDetail? }` — the index signature is
  load-bearing for victory-native's generic inference. Do not remove it.
- `hexToRgba(hex, alpha)` is already local to TrendChart.tsx and is how the existing ATL
  gradient is built; reuse it for the CTL gradient rather than introducing a color constant.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich the home TrendChart, then amend 04-UI-SPEC.md section 5</name>
  <files>apps/mobile/components/home/TrendChart.tsx, .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md</files>
  <action>
**Home chart enrichment (D-02).** Edit `apps/mobile/components/home/TrendChart.tsx` only —
the public `TrendChartPoint` / `TrendChartProps` shapes, the `calibratingCaption` export, the
D-19 scrub mechanics, the animate-once `hasDrawnOnceRef` gate, `CURVE_TYPE`, and the
`onPressDetail` affordance added by `260907-qe6-PLAN.md` Task 1 all stay exactly as they are.

1. **Vertical gridlines.** The `xAxis` config currently zeroes its line width, which is what
   suppresses vertical gridlines; give it a real hairline width and `Colors.dark.steel` so
   vertical gridlines render at the tick values, matching the existing horizontal `yAxis` rule.
2. **Denser axis labels.** Introduce a single named constant `X_TICK_EVERY_N_DAYS` for x-tick
   spacing (currently every seventh day, hard-coded in the `xTickValues` memo) and set it to
   every fourth day, so a 28-day window carries seven labels instead of four. Add y-axis tick
   labels too — the
   `yAxis` entry gets the same guarded `axisFont`, an ash `labelColor` and a small `tickCount`
   — so the horizontal gridlines finally carry readable values. Keep both label sets mono 9px
   ash. The x-spacing constant is the tuning knob if seven labels crowd on-device.
3. **Area fill under BOTH series.** Add a second `Area` beneath the CTL series with its own
   vertical gradient in `Colors.dark.mutedText`, governed by its own named top-alpha constant
   `CTL_AREA_FILL_TOP_ALPHA`, set strictly below the existing ATL constant so the acute line
   still reads as the dominant
   one and the crossover stays legible. Paint order inside the render function: CTL area, then
   ATL area, then the two stroked lines, then dots, then the scrub hairline. Both areas go
   inside the existing draw-on `<Group opacity={drawProgress}>` — `Area.opacity` is a plain
   `number` in the installed victory-native typings while `Line.end` is animated-capable, and
   that Group is the sanctioned workaround. Do not cast.
4. **Point dots.** Import `Scatter` from `victory-native` and render one dot layer per series —
   ATL in `Colors.dark.text`, CTL in `Colors.dark.mutedText`, small radius, circle shape,
   explicit fill style. `Scatter.opacity` IS animated-capable in the installed typings, so pass
   `drawProgress` to it directly rather than adding another Group wrapper.
5. **Palette containment.** No volt may reach this file, in any form. The dedicated gate below
   resolves the volt-valued palette tokens from `constants/Colors.ts` at runtime — it is never
   handed a hardcoded hex — and then rejects, in order: any of those token names appearing
   anywhere in the file (plain dot access, a bracketed string key, a destructured binding and
   an import binding all land in that one check); any raw six-digit hex color literal in any
   casing; the decimal rgb channel triple of a volt color with any spacing; any `hsl()`/`hsla()`
   literal; computed or bracket access into `Colors`; destructuring `Colors`; and importing any
   local module that can itself reach a volt value at up to three hops. That last rule
   deliberately covers `constants/readinessBand.ts` — the shared band-to-swatch map
   `260907-qe6-PLAN.md` Task 2 creates one directory away, which is the shortest legitimate-
   looking path to volt on this chart. The only sanctioned color path in this file remains
   `Colors.dark.TOKEN` dot access on a non-volt token, exactly as it reads today; if a color you
   want is not reachable that way, that is the gate telling you it is volt. Comments are
   stripped before scanning, so explanatory comments are safe. The home chart stays bone and ash
   so the HssRing keeps the Today screen its single focal point (D-02).

**Spec amendment (D-03).** Append a SECOND dated revision block to section 5 of
`.planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md`, directly after the existing
`> **Revised 2026-09-07 (quick task 260907-la6) — amends D-18:**` block and following that
block's exact supersede idiom (blockquote, bold dated header, then Changed / Unchanged / Why).
Use `Revised 2026-09-07 (quick task 260907-qe6)`. It must record, explicitly:

- **Changed on the home chart:** vertical gridlines are now permitted alongside horizontal
  ones, superseding "horizontal only"; a gradient area fill now sits under BOTH series,
  superseding la6's "CTL keeps a bare line with no fill beneath it"; point dots now mark every
  data point on both series; x-axis labels densify to every fourth day and the y-axis gains
  tick labels; a tap affordance row now sits beneath the card.
- **Changed elsewhere:** TSB IS drawn as a real third line — **on the new trends detail screen
  only** — superseding D-18's blanket "TSB is NOT drawn as a third line". D-18's prohibition
  still binds the home chart.
- **New surface:** a pushed `/trends` route at `apps/mobile/app/trends.tsx`, a sibling of
  `modal.tsx` outside `(tabs)/`, carrying a 28D/90D/1Y range switcher, the three-series chart,
  a readiness-band history strip and a six-cell stats block. Record explicitly that
  DESIGN-SYSTEM.md section 5's unbuilt five-tab layout with a TRENDS tab is **deliberately not
  resurrected** — this is a pushed route, not a fifth tab (D-04).
- **Accent usage:** the detail screen's two accent usages are the TSB line and the readiness
  strip's green segments, both readiness-semantic, permitted because that screen never renders
  an HssRing; the home chart remains accent-free.
- **Unchanged:** no accent anywhere on the home chart, ~2px line weight, mono 9px ash axis
  labels, the D-20 tooltip copy format, the D-19 scrub mechanics and its ≤150ms opacity fade,
  the ≤800ms animate-once draw-on, and D-22's never-a-fake-line calibrating behavior — now
  generalized to the detail screen's short-history captions.
- **Why:** the user explicitly authorized diverging from the design doc for these visuals; this
  revision exists so the spec and the shipped code do not contradict each other.
- Note, as la6 did, that DESIGN-SYSTEM.md is deliberately left unedited and that its
  "volt primary line" chart guidance remains superseded, not restored.

Do not touch any other section of the document, and do not modify or remove the la6 revision
block — the collateral-damage gate below re-runs unchanged.
  </action>
  <verify>
    <automated>pnpm run typecheck</automated>
    <automated>pnpm --filter @apsis/mobile test</automated>
    <automated>node -e "const s=require('fs').readFileSync('apps/mobile/components/home/TrendChart.tsx','utf8');const code=s.split('\n').filter(l=>!/^\s*(\*|\/\/)/.test(l)).join('\n');const must=['AREA_FILL_TOP_ALPHA','DRAW_ON_DURATION_MS','withTiming','curveType','strokeWidth={2}','TrendChartPoint','calibratingCaption','lineColor: Colors.dark.steel'];const banned=['curveType=\"linear\"'];const missing=must.filter(m=>!code.includes(m));const bad=banned.filter(b=>code.includes(b));if(missing.length)throw new Error('missing: '+missing.join(', '));if(bad.length)throw new Error('forbidden token present: '+bad.join(', '));const m=code.match(/AREA_FILL_TOP_ALPHA\s*=\s*([0-9.]+)/);if(!m||Number(m[1])>0.18)throw new Error('area fill alpha missing or above the 0.18 ceiling');const d=code.match(/DRAW_ON_DURATION_MS\s*=\s*(\d+)/);if(!d||Number(d[1])>800)throw new Error('draw-on duration missing or above the 800ms ceiling');console.log('la6 TrendChart shape gates still OK');"</automated>
    <automated>node -e "const fs=require('fs'),path=require('path'); const F='apps/mobile/components/home/TrendChart.tsx'; const P='apps/mobile/constants/Colors.ts'; const strip=t=>t.split('\n').map(l=>/^\s*(\*|\/\/|\/\*)/.test(l)?'':l.replace(/(^|[^:])\/\/.*/,(x,a)=>a)).join('\n'); const fail=r=>{throw new Error('VOLT CONTAINMENT FAILED (D-02): '+r);}; const code=strip(fs.readFileSync(F,'utf8')); const pal=fs.readFileSync(P,'utf8'); const hex={}; for(const m of pal.matchAll(/const\s+([A-Za-z0-9_]+)\s*=\s*'(#[0-9a-fA-F]{6})'/g))hex[m[1]]=m[2].toLowerCase(); const voltHex=['accent','accentPressed'].map(n=>hex[n]).filter(Boolean); if(voltHex.length<2)fail('cannot resolve the accent hexes from '+P+' -- the palette moved; update this gate to the new source of truth rather than weakening it'); const b=pal.indexOf('darkPalette = {'); if(b<0)fail('cannot locate the dark palette object in '+P); const obj=pal.slice(b,pal.indexOf('};',b)); const voltKeys=new Set(['accent','accentPressed']); for(const m of obj.matchAll(/([A-Za-z0-9_]+)\s*(?::\s*([A-Za-z0-9_]+))?\s*,/g)){const k=m[1],v=m[2]||m[1];if(voltHex.includes(hex[v]))voltKeys.add(k);} for(const t of code.matchAll(/[A-Za-z_][A-Za-z0-9_]*/g))if(voltKeys.has(t[0]))fail('the palette token '+t[0]+' resolves to volt and appears in '+F+' (dot access, bracket string, destructured name and import binding all land here)'); const raw=code.match(/#[0-9a-fA-F]{6}/g); if(raw)fail('raw hex color literal(s) '+raw.join(', ')+' -- colors must come from Colors.dark tokens so this gate can resolve their value'); const norm=code.toLowerCase().replace(/[\s,]+/g,','); for(const h of voltHex){const c=[1,3,5].map(i=>parseInt(h.slice(i,i+2),16)).join(',');if(norm.includes(c))fail('the decimal rgb channels of a volt color ('+c+') appear in '+F);} if(/hsla?\s*\(/i.test(code))fail('an hsl()/hsla() literal appears in '+F+' -- unresolvable by value; use a Colors.dark token'); if(/Colors\s*\[/.test(code)||/Colors\s*\.\s*[A-Za-z0-9_]+\s*\[/.test(code))fail('computed/bracket access into Colors defeats static palette analysis -- use Colors.dark.TOKEN dot access only'); for(const m of code.matchAll(/(?:const|let|var)\s*\{([^}]*)\}\s*=\s*([^;\n]*)/g))if(/Colors/.test(m[2]))fail('destructuring Colors ({'+m[1].trim()+' }) defeats static palette analysis -- use dot access only'); const resolve=(from,spec)=>{if(!spec.startsWith('.'))return null;for(const e of ['.ts','.tsx','/index.ts','/index.tsx']){const p=path.join(from,spec+e).split(path.sep).join('/');if(fs.existsSync(p))return p;}return null;}; const memo=new Map(); const reaches=(p,d)=>{if(memo.has(p))return memo.get(p);memo.set(p,false);if(d>3)return false;const src=strip(fs.readFileSync(p,'utf8'));let v=voltHex.some(h=>src.toLowerCase().includes(h));if(!v)for(const t of src.matchAll(/[A-Za-z_][A-Za-z0-9_]*/g))if(voltKeys.has(t[0])){v=true;break;}if(!v)for(const m of src.matchAll(/from\s*'([^']+)'/g)){const q=resolve(path.dirname(p),m[1]);if(q&&reaches(q,d+1)){v=true;break;}}memo.set(p,v);return v;}; const dir=path.dirname(F); for(const m of code.matchAll(/import\s+([^;]*?)\s*from\s*'([^']+)'/g)){ const binds=m[1].trim(),spec=m[2],p=resolve(dir,spec); if(!p)continue; if(p===P){if(/\{/.test(binds))fail('named/destructured import from the palette module ('+binds+') -- import the palette default and use dot access');continue;} if(reaches(p,0))fail(spec+' can reach a volt value (directly or through its own imports) and is imported into '+F+' as '+binds+' -- the home chart must stay bone and ash, so read non-volt tokens straight off Colors.dark instead'); } console.log('volt containment OK -- volt tokens resolved from the palette: '+[...voltKeys].join(', '));"</automated>
    <automated>node -e "const s=require('fs').readFileSync('apps/mobile/components/home/TrendChart.tsx','utf8');for(const k of ['day: number','hss: number','atl: number','ctl: number','tsb: number','dateLabel: string','[key: string]: number | string','calibratingDayN?: number'])if(!s.includes(k))throw new Error('TrendChartPoint/Props contract broken -- lost: '+k);if(!s.includes('BUILDING TREND · DAY '))throw new Error('calibrating caption changed');console.log('public contract intact');"</automated>
    <automated>node -e "const s=require('fs').readFileSync('apps/mobile/components/home/TrendChart.tsx','utf8');const code=s.split('\n').filter(l=>!/^\s*(\*|\/\/)/.test(l)).join('\n');if(/lineWidth:\s*0\s*[,}\n]/.test(code))throw new Error('the x-axis line width is still zero -- vertical gridlines are not enabled (D-02)');if(!/\bScatter\b/.test(code))throw new Error('point dots missing -- Scatter is not rendered (D-02)');const areas=(code.match(/<Area\b/g)||[]).length;if(areas<2)throw new Error('expected an area fill under BOTH series (D-02), found '+areas);const gradients=(code.match(/<LinearGradient\b/g)||[]).length;if(gradients<2)throw new Error('expected a gradient per area fill, found '+gradients);const ctl=code.match(/CTL_AREA_FILL_TOP_ALPHA\s*=\s*([0-9.]+)/);const atl=code.match(/(?:^|[^_])AREA_FILL_TOP_ALPHA\s*=\s*([0-9.]+)/m);if(!ctl)throw new Error('the CTL fill needs its own named top-alpha constant');if(atl&&Number(ctl[1])>=Number(atl[1]))throw new Error('the CTL fill alpha must sit strictly below the ATL fill alpha so the acute line stays dominant');const tick=code.match(/X_TICK_EVERY_N_DAYS\s*=\s*(\d+)/);if(!tick)throw new Error('x-tick spacing must be a single named constant, not inline');if(Number(tick[1])>=7)throw new Error('axis labels were not densified -- x tick spacing is still '+tick[1]);if(!/labelColor/.test(code.split('yAxis')[1]||''))throw new Error('y-axis tick labels were not added');console.log('home chart enrichment gates OK');"</automated>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md','utf8');const i=s.indexOf('### 5. Trend chart');const j=s.indexOf('### 6. Today');if(i<0||j<0||j<i)throw new Error('section 5 boundaries not found');const sec=s.slice(i,j);if(!sec.includes('Revised 2026-09-07 (quick task 260907-la6)'))throw new Error('the la6 revision block was destroyed -- it must remain');const must=['Revised 2026-09-07 (quick task 260907-qe6)','vertical gridlines','app/trends.tsx','pushed route','not a fifth tab','TRENDS tab','readiness-band','both series','point dots'];const missing=must.filter(m=>!sec.includes(m));if(missing.length)throw new Error('missing from the qe6 amendment: '+missing.join(', '));const revs=(sec.match(/Revised 2026-09-07/g)||[]).length;if(revs<2)throw new Error('expected two dated revision blocks in section 5, found '+revs);console.log('section 5 second amendment OK');"</automated>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md','utf8');for(const h of ['## Design System','## Spacing Scale','## Typography','## Color','## Copywriting Contract','## Registry Safety','### 6. Today','### 7. Run entry form','## Screen Inventory','## Checker Sign-Off'])if(!s.includes(h))throw new Error('collateral damage -- heading lost: '+h);console.log('rest of document intact');"</automated>
    <automated>pnpm --filter @apsis/db test</automated>
    <automated>pnpm --filter @apsis/engine test</automated>
    <automated>pnpm --filter @apsis/shared test</automated>
  </verify>
  <done>The home chart renders horizontal and vertical steel gridlines, gradient fills under both series with the CTL alpha strictly below ATL's, point dots on both series, seven x labels plus y tick labels, and still contains no volt-valued palette token, no raw hex color literal, no volt rgb triple and no import that can reach a volt value; every la6 gate and the public `TrendChart` contract survive unchanged; section 5 of 04-UI-SPEC.md carries both the la6 and the new qe6 dated revision blocks with the rest of the document intact; typecheck is clean and all four vitest suites pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered UI → screen / crash reporter | The home chart now renders numeric y-axis tick labels for health-derived training-load values that were previously only implicit in the line's shape. |
| plan-time palette constants → shipped chart | A locked user decision (D-02) is enforced by a static gate rather than by review; if the gate is weaker than the decision, the decision silently does not ship. |
| spec document → future planning | 04-UI-SPEC.md is read as authoritative by later phases; a bad edit misdirects future work rather than breaking a build. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-qe6b-01 | Information disclosure | `TrendChart.tsx` y-axis tick labels | low | accept | The y-axis exposes ATL/CTL magnitudes numerically. These are the same health-derived values the HssRing, the D-20 tooltip and the readiness copy already render on the same screen, so no new value class is exposed. Nothing new is logged, and `sanitizeSentryEvent`'s existing allowlist (only `exception` + `contexts.device/app` survive) still strips all rendering context from reports. |
| T-qe6b-02 | Tampering | the D-02 palette-containment gate itself | medium | mitigate | The prior gate matched four exact substrings and was defeatable by bracket access, destructuring, an alias key, a mixed-case hex, an rgb triple or an import of the shared band-color constant. The replacement resolves volt by VALUE from `constants/Colors.ts` at runtime, bans volt-resolving token names in every syntactic position, bans raw hex literals outright, bans the rgb triple and `hsl()` forms, bans computed access into and destructuring of the palette, and walks local imports up to three hops for anything that can reach volt. It fails loudly if it cannot resolve the palette rather than passing vacuously. |
| T-qe6b-03 | Tampering | `04-UI-SPEC.md` section 5 | low | mitigate | The amendment is append-only inside section 5. The unchanged collateral-damage gate fails if the la6 revision block is lost, if section 5 does not end with two dated revision blocks, or if any other top-level document heading disappears. |
| T-qe6b-SC | Tampering | npm/pip/cargo installs | high | accept | **No packages are installed by this plan.** `Scatter` and `LinearGradient` come from already-installed victory-native 41.26.0 and @shopify/react-native-skia, verified against their installed `.d.ts` files at planning time. The package-legitimacy gate therefore does not apply; if the task discovers it needs a new dependency, stop and escalate rather than installing. |
</threat_model>

<verification>
Automated (the task's gates, which are this plan's whole surface):
- `pnpm run typecheck` clean at the repo root (`tsc --build`, the authoritative cross-package
  entry point).
- `pnpm --filter @apsis/mobile test`, `@apsis/db`, `@apsis/engine`, `@apsis/shared` all pass.
  Floors — mobile, db, engine and shared each at or above the count `260907-qe6-PLAN.md` left
  them at. No suite may lose a test; this plan adds none.
- The la6 TrendChart shape gates re-run green (curve type, area-alpha ceiling, draw-on ceiling,
  the must-token list) and the public `TrendChart` contract gate re-runs green.
- The hardened palette-containment gate passes, printing the volt token set it resolved from
  `constants/Colors.ts`. If it prints an empty or single-token set, treat that as a failure of
  the gate rather than a pass of the code.
- The 04-UI-SPEC section 5 amendment gate and the collateral-damage gate both pass.
- `packages/engine` is untouched (`git status` shows no change under `packages/engine/`).

On-device (developer, via the running Metro dev server — never killed, never restarted):
- The home chart reads richer — grid in both directions, both series filled, dots on every
  point, labels on both axes — without stealing focus from the HssRing.
- No green anywhere on the home chart; the ring is still the only accent on the Today screen.
- The D-19 scrub still tracks the finger with no lag and the draw-on still animates once.
</verification>

<success_criteria>
- Every `must_haves.truths` entry is observably true on device.
- Every artifact in `must_haves.artifacts` exists and is committed.
- Every automated gate in the task passes, including the palette-containment gate.
- `260907-qe6-PLAN.md` was committed before this plan started.
- No `git push`, no Phase 06-07 or App Store artifact touched, no dependency added, no
  DESIGN-SYSTEM.md edit, the running Metro dev server never killed or restarted.
</success_criteria>

<output>
Create `.planning/quick/260907-qe6-enrich-the-home-trendchart-and-add-a-tap/260907-qe6b-SUMMARY.md` when done.

The SUMMARY must record, at minimum: the exact final values of `X_TICK_EVERY_N_DAYS` and
`CTL_AREA_FILL_TOP_ALPHA` (and the pre-existing ATL alpha they were set against); the volt
token set the containment gate resolved from `constants/Colors.ts` and printed on its green
run; whether the installed victory-native typings matched this plan's `<interface_context>`
for `Scatter` and `Area` opacity; and the resulting per-suite test counts.
</output>
