---
phase: quick
plan: 260709-rq4
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/constants/Colors.ts
  - apps/mobile/constants/theme.ts
  - apps/mobile/components/ScreenHeader.tsx
  - apps/mobile/components/onboarding/WizardStep.tsx
  - apps/mobile/components/onboarding/ProfileReview.tsx
  - apps/mobile/components/session/LiveHssHeader.tsx
  - apps/mobile/components/session/ExerciseCard.tsx
  - apps/mobile/components/session/RestTimerBanner.tsx
  - apps/mobile/components/session/ExercisePickerSheet.tsx
  - apps/mobile/components/session/SetRow.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/log/index.tsx
  - apps/mobile/app/(tabs)/log/session.tsx
  - apps/mobile/app/(tabs)/settings/index.tsx
  - apps/mobile/app/session/finish.tsx
  - apps/mobile/app/onboarding/sex.tsx
  - apps/mobile/app/onboarding/units.tsx
  - apps/mobile/app/onboarding/bodyweight.tsx
  - apps/mobile/app/onboarding/threshold-hr.tsx
  - apps/mobile/app/onboarding/threshold-pace.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Amber/caution readiness resolves to exactly #CDBE4A everywhere warning is used"
    - "Every primary volt button shows the #A9D32B pressed state and a steel-bg/ash-text/opacity-0.7 disabled state (never a plain opacity fade)"
    - "Primary CTA labels render sentence case (Start workout, Save changes), not Title Case"
    - "Buttons/inputs/chips/segments use radius 6-8; a pill radius token (100) exists"
    - "Logger set-row values render in heavy Archivo ~19px with tiny mono unit suffixes; RPE 9-10 selection reads molten"
    - "Tab bar labels render in JetBrains Mono ~9px, uppercase, tracked"
    - "A ScreenHeader (mono kicker + heavy uppercase title) exists and is applied to the Settings screen"
  artifacts:
    - apps/mobile/constants/Colors.ts
    - apps/mobile/constants/theme.ts
    - apps/mobile/components/ScreenHeader.tsx
  key_links:
    - "Colors.ts warning token → every warning/amber consumer (SetRow, onboarding warnings, settings modal warning, ProfileReview error)"
    - "theme.ts Radius.pill + Typography.title + Kicker → ScreenHeader and control styles"
    - "accentPressed token → all Pressable pressed-state style branches"
---

<objective>
Align the already-restyled Apsis mobile UI (void/volt palette from quick task 260709-qmv)
with the concrete "Apsis Design System v1" component specs: exact amber value, volt
pressed/disabled button states, sentence-case CTA labels, corrected radii scale (6/8/10/pill),
logger data typography, mono tab-bar labels, and a reusable mono-kicker + heavy-title screen
header. Styling-only — no new features, no new screens, no image-asset changes, no engine/db
edits.

Purpose: The 260709-qmv pass established the palette + fonts but predates the finalized design
system. This pass closes the component-level deltas so the app matches the authoritative specs.
Output: Refined token layer, a ScreenHeader component, and a styling sweep across the restyled
onboarding, logger, settings, and finish surfaces.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260709-qmv-restyle-app-ui-to-apsis-dark-design-syst/260709-qmv-SUMMARY.md
@apps/mobile/constants/Colors.ts
@apps/mobile/constants/theme.ts

# Authoritative design specs are in the planning_context (extracted from
# "Apsis Design System.dc.html" at repo root). The HTML may be grepped to confirm a value
# (CDBE4A / A9D32B / C6F23D / FF5A1F all confirmed present) but does not need full parsing.

# OUT OF SCOPE (do not build): readiness pill/status light, HSS ring, progress bars, charts,
# stat tiles, session-stress card, and session-row lists. None of these components exist yet —
# the Home tab is a Phase 4 "Coming soon" placeholder. Their design specs govern Phase 4, not
# this styling-alignment pass. Do NOT create them here.

# HARD CONSTRAINTS: Keep the current app icon and every image already wired from
# apsis_icon_pack (icon.png, splash, plate-mark, tab icons) — do NOT replace/regenerate/
# re-export any image asset. Recolor tab icons via tintColor only. Do NOT touch
# packages/engine or packages/db.

# RN NOTE: letterSpacing in React Native is px, not em. Conversions used below:
#   kicker 0.16em @11px ≈ 1.8 · field label 0.16em @10px ≈ 1.6 · segment @11px ≈ 1.5 · tab @9px ≈ 1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refine token layer + add ScreenHeader component</name>
  <files>apps/mobile/constants/Colors.ts, apps/mobile/constants/theme.ts, apps/mobile/components/ScreenHeader.tsx</files>
  <action>
Colors.ts:
- Change the amber/caution token `warning` from `#A8B545` to exactly `#CDBE4A` (Design System v1: amber readiness = #CDBE4A). Update its inline comment value.
- Add a new module const `accentPressed = '#A9D32B'` (volt pressed state) and expose it on `darkPalette` as `accentPressed`. Keep the existing `steel` (#1F2329) and `mutedText` (#8A9098 ash) keys — those are the disabled-button bg and text per spec (steel bg + ash text + opacity 0.7).

theme.ts:
- Add `pill: 100` to the `Radius` token object (readiness pills / progress bars). Update the Radius doc comment to note the 6=buttons/inputs/chips/segments, 8=cards/rows, 10=sheets, 100=pills mapping.
- Add a `DISABLED_OPACITY = 0.7` exported const with a comment (spec: disabled buttons = steel bg + ash text + opacity 0.7).
- Add a `title` role to the `Typography` record: fontFamily `Archivo_900Black`, fontSize 29, fontWeight '600' (family carries the weight, matching how `display`/`heading` are declared), lineHeight 32, textTransform 'uppercase', letterSpacing -0.5 — the screen-header heavy title. Widen the Typography Record key union type to include `'title'`.
- Add an exported `Kicker` style object (same shape as `Mono`): fontFamily `JetBrainsMono_500Medium`, fontSize 11, letterSpacing 1.8, textTransform 'uppercase'. Color is applied by the consumer (ash/mutedText). Add a comment: mono kicker line that sits above a `title`.

ScreenHeader.tsx (new): a small presentational component. Props: `kicker: string`, `title: string`, optional `style`. Renders a `View` containing a `Text` styled with `Kicker` + `color: Colors.dark.mutedText`, above a `Text` styled with `Typography.title` + `color: Colors.dark.text`. No logic, no db/router imports. Import tokens from `../constants/theme` and Colors from `../constants/Colors`. Do NOT put the negative-grep literal amber/pressed hex strings in any comment here.
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const c=require('fs').readFileSync('constants/Colors.ts','utf8'); if(!c.includes('#CDBE4A')||c.includes('#A8B545')) process.exit(1); if(!c.includes('#A9D32B')||!/accentPressed/.test(c)) process.exit(1); const t=require('fs').readFileSync('constants/theme.ts','utf8'); if(!/pill:\s*100/.test(t)||!/Kicker/.test(t)||!/title:/.test(t)) process.exit(1); require('fs').accessSync('components/ScreenHeader.tsx'); console.log('OK')"</automated>
  </verify>
  <done>warning=#CDBE4A (no #A8B545 remaining), accentPressed=#A9D32B present, Radius.pill=100, Typography.title + exported Kicker + DISABLED_OPACITY present, ScreenHeader.tsx exists.</done>
</task>

<task type="auto">
  <name>Task 2: Button/control states, radii, sentence-case labels, dashed add-set, ScreenHeader</name>
  <files>apps/mobile/components/onboarding/WizardStep.tsx, apps/mobile/components/onboarding/ProfileReview.tsx, apps/mobile/components/session/LiveHssHeader.tsx, apps/mobile/components/session/ExerciseCard.tsx, apps/mobile/components/session/RestTimerBanner.tsx, apps/mobile/components/session/ExercisePickerSheet.tsx, apps/mobile/app/(tabs)/log/index.tsx, apps/mobile/app/(tabs)/log/session.tsx, apps/mobile/app/(tabs)/settings/index.tsx, apps/mobile/app/session/finish.tsx, apps/mobile/app/onboarding/sex.tsx, apps/mobile/app/onboarding/units.tsx, apps/mobile/app/onboarding/bodyweight.tsx, apps/mobile/app/onboarding/threshold-hr.tsx, apps/mobile/app/onboarding/threshold-pace.tsx</files>
  <action>
Apply the Design System v1 button/control specs. Import `accentPressed` (Colors) and `DISABLED_OPACITY` / `Radius` (theme) where needed.

PRIMARY BUTTONS (volt fill, void `onAccent` label, radius 6-8, minHeight 48, sentence case). For each primary CTA — WizardStep `button`, ProfileReview `button`, log/index `button`, LiveHssHeader `finishButton`, settings `retryButton`, finish `button`:
- Change container `borderRadius` from `Radius.lg` (10) to `Radius.md` (8).
- Ensure `minHeight` is at least 48 (HIT_TARGET_MIN=44 is fine to keep as the floor, but set the primary CTA containers to `minHeight: 48`).
- Pressed state: where the Pressable uses a style-function with a `pressed` flag (WizardStep, ProfileReview, log/index), replace the `buttonPressed` opacity approach with `backgroundColor: Colors.dark.accentPressed`. For LiveHssHeader `finishButton` (currently a plain style), convert its `style` to a function `({ pressed }) => [styles.finishButton, pressed && { backgroundColor: Colors.dark.accentPressed }]`.
- Disabled state: replace the ad-hoc `buttonDisabled` opacity (0.4/0.5/0.6) with the spec treatment — `backgroundColor: Colors.dark.steel`, label `color: Colors.dark.mutedText`, and `opacity: DISABLED_OPACITY`. Apply the disabled label color by adding a disabled-label style branch on the button's `Text` (e.g. `nextDisabled && styles.buttonLabelDisabled`).

SENTENCE-CASE VISIBLE LABELS (change only the visible `<Text>` copy; DO NOT change any `accessibilityLabel` value — tests/a11y selectors depend on the current strings):
- log/index: "Start Workout" → "Start workout".
- settings: the `primaryLabel="Save Changes"` passed to ProfileReview → "Save changes".
- finish: modal "Keep Training" → "Keep training"; visible "Discard Workout" (both the dropdown menu item text and the modal destructive button text) → "Discard workout".
- Leave already-correct labels unchanged (Continue, Finish, Done, Set, Cancel, Retry, +30s, Skip, Add exercise, Metric, Imperial).

CHOICE / SEGMENT / PILL CONTROLS (radius 6; selected already = volt bg + onAccent label, keep that):
- onboarding option/mode buttons — units.tsx `option`, sex.tsx (its option/mode button style), bodyweight.tsx (any choice/mode button), threshold-hr.tsx `modeButton`, threshold-pace.tsx (its mode button): change `borderRadius` from `Radius.lg` (10) to `Radius.sm` (6).
- settings `choiceButton` and `sexOption`: change `borderRadius` from `Radius.lg` (10) to `Radius.sm` (6).

GHOST / DASHED AFFORDANCES:
- ExerciseCard `addSetRow`/`addSetLabel`: per spec #3 the "Add set" affordance is a full-width DASHED-border ghost. Change `addSetRow` to: `borderWidth: 1`, `borderStyle: 'dashed'`, `borderColor: Colors.dark.accent`, `borderRadius: Radius.md` (8), `marginHorizontal: Spacing.md`, `marginTop: Spacing.sm`, `marginBottom: Spacing.md`, keep `minHeight` and centering; remove the top-hairline-only look (`borderTopWidth`). Keep the volt `addSetLabel` text.
- session.tsx `addExerciseButton` (accent ghost, 1px solid volt border): change `borderRadius` from `Radius.lg` (10) to `Radius.md` (8). Keep solid border.
- RestTimerBanner `ghostButton` (accent ghost): change `borderRadius` from `Radius.lg` (10) to `Radius.md` (8).

INPUTS (radius 6-8):
- ExercisePickerSheet `searchInput`: change `borderRadius` from `Radius.lg` (10) to `Radius.md` (8). Keep void (`background`) bg + ash placeholder.
- Do NOT restructure the big borderless hero-number inputs (settings modal/ onboarding display-size inputs) — they are hero readouts, not bordered form fields; leave them.

SCREEN HEADER:
- settings/index.tsx: replace the current `heading`-styled "Settings" `Text` with `<ScreenHeader kicker="CONFIG" title="Settings" />` (import from `../../../components/ScreenHeader`). Keep it at the top of the ScrollView with the existing horizontal padding (wrap in a `View` with `paddingHorizontal: Spacing.md, paddingTop: Spacing.lg` if needed to preserve current spacing). Remove the now-unused `heading` style only if nothing else references it.

Do NOT introduce any fenced code or the literal amber/pressed hex strings into comment prose in these files.
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const fs=require('fs'); const idx=fs.readFileSync('app/(tabs)/log/index.tsx','utf8'); if(!idx.includes('Start workout')||idx.includes('Start Workout>')) {}; if(!/Start workout/.test(idx)) process.exit(1); const set=fs.readFileSync('app/(tabs)/settings/index.tsx','utf8'); if(!/ScreenHeader/.test(set)||!/Save changes/.test(set)) process.exit(1); const ec=fs.readFileSync('components/session/ExerciseCard.tsx','utf8'); if(!/dashed/.test(ec)) process.exit(1); const fin=fs.readFileSync('app/session/finish.tsx','utf8'); if(!/Keep training/.test(fin)||!/Discard workout/.test(fin)) process.exit(1); console.log('OK')"</automated>
  </verify>
  <done>Primary CTAs use radius 8 + accentPressed pressed state + steel/ash/0.7 disabled state; "Start workout"/"Save changes"/"Keep training"/"Discard workout" render; choice/segment/pill controls use radius 6; add-set is dashed volt; searchInput radius 8; Settings uses ScreenHeader. accessibilityLabels unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Logger data typography + mono tab-bar labels</name>
  <files>apps/mobile/components/session/SetRow.tsx, apps/mobile/app/(tabs)/_layout.tsx</files>
  <action>
SetRow.tsx (spec #9 — set-row values in heavy Archivo with tiny mono unit suffixes; RPE segment turns molten at 9-10):
- `valueInput` style: replace the `...Mono, fontSize: 16, textTransform: 'none'` treatment with heavy Archivo — `fontFamily: 'Archivo_800ExtraBold'`, `fontSize: 19`, `fontWeight: '600'`, `color: Colors.dark.text`. Keep `...tabularNums`, `minWidth`, `textAlign: 'center'`, `paddingVertical: 0`. Drop the Mono spread and the `textTransform` override (no longer needed).
- `unitLabel` style: keep `...Mono` + ash color but set `fontSize: 9` (tiny mono unit suffix per spec).
- Stepper as icon-button (spec #4 — 48x48, steel bg, line border, radius 6): change `stepper` style `borderRadius` from `Radius.lg` (10) to `Radius.sm` (6), `backgroundColor` from `Colors.dark.border` to `Colors.dark.steel`, and add `borderWidth: 1, borderColor: Colors.dark.border`.
- RPE pills (spec #9 — selected reads volt, or molten when RPE ≥ 9): change `rpePill` `borderRadius` from `Radius.lg` (10) to `Radius.sm` (6). Add a `rpePillHeat` style with `backgroundColor: Colors.dark.destructive`. In the RPE map, when a pill is `selected` AND `option >= 9`, apply `rpePillHeat` instead of `rpePillSelected` (keep the `rpePillLabelSelected` onAccent/void label for readability on both fills).
- Leave the commit checkmark, warmup chip (already radius 6), and effective-load label as they are.

_layout.tsx (spec #13 — tab-bar labels JetBrains Mono ~9px, uppercase, tracked; keep existing tab icon images, recolor via tintColor only):
- Add `tabBarLabelStyle: { fontFamily: 'JetBrainsMono_500Medium', fontSize: 9, letterSpacing: 1, textTransform: 'uppercase' }` to the `Tabs` `screenOptions`. Do NOT change any `tabBarIcon` source/image or the `require(...apsis-plate-mark.png)` — only labels change. Keep the existing hairline top border and active/inactive volt/ash tint.

Do NOT inline fenced code or the negative-grep literal hex strings into comment prose here.
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const fs=require('fs'); const sr=fs.readFileSync('components/session/SetRow.tsx','utf8'); if(!/Archivo_800ExtraBold/.test(sr)||!/rpePillHeat/.test(sr)||!/steel/.test(sr)) process.exit(1); const lay=fs.readFileSync('app/(tabs)/_layout.tsx','utf8'); if(!/tabBarLabelStyle/.test(lay)||!/JetBrainsMono_500Medium/.test(lay)) process.exit(1); console.log('OK')"</automated>
  </verify>
  <done>Set-row values render heavy Archivo ~19px, unit suffixes mono 9px, steppers are steel+line-border radius 6, RPE pills radius 6 with molten fill at RPE≥9; tab bar has mono 9px uppercase labels and unchanged icon images.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Styling-only change: no new input parsing, network, storage, or auth surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-RQ4-01 | Tampering | Design-token edits (Colors/theme) | low | accept | Token values are static string/number literals; no runtime input. Typecheck gate catches shape breakage. No package installs in this plan. |
</threat_model>

<verification>
- `pnpm run typecheck` (root) passes with no NEW errors beyond the two pre-existing, unrelated
  typed-routes `Href` mismatches already logged in
  `.planning/quick/260709-qmv-restyle-app-ui-to-apsis-dark-design-syst/deferred-items.md`
  (`onboarding/review.tsx`, `ExternalLink.tsx`). Confirm they are unchanged, not newly introduced.
- Grep: zero occurrences of `#A8B545` under `apps/mobile`; `#CDBE4A`, `#A9D32B`, `Radius.pill`/`pill: 100`,
  `Typography.title`, `Kicker`, `ScreenHeader`, `tabBarLabelStyle`, `Archivo_800ExtraBold` (SetRow value),
  `rpePillHeat`, and `borderStyle: 'dashed'` all present.
- No image asset under `apps/mobile/assets/images` is modified; `git status` shows no changes there.
- No files under `packages/engine` or `packages/db` are modified.
</verification>

<success_criteria>
- Amber resolves to #CDBE4A app-wide; volt pressed (#A9D32B) + steel/ash/0.7 disabled states on primary CTAs.
- Primary CTA labels sentence case; controls on the 6/8/10/pill radii scale.
- Logger set-row values heavy Archivo with mono unit suffixes and molten RPE≥9; mono uppercase tab labels.
- ScreenHeader exists and is applied to Settings.
- Typecheck clean (no new errors); no image/engine/db changes.
</success_criteria>

<output>
Create `.planning/quick/260709-rq4-align-ui-to-apsis-design-system-v1-compo/260709-rq4-SUMMARY.md` when done.
</output>
