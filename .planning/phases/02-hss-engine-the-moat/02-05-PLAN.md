---
phase: 02-hss-engine-the-moat
plan: 05
type: execute
wave: 3
depends_on:
  - 02-03
  - 02-04
files_modified:
  - packages/engine/src/index.ts
  - packages/engine/README.md
autonomous: true
requirements:
  - "Pure-TS engine computes HSS + readiness on-device, fully unit-tested"
  - "cold-start must not show red on a single moderate day"
must_haves:
  truths:
    - "packages/engine/src/index.ts is a pure barrel re-exporting every public symbol: StrengthSet/EnduranceSegment/EngineConfig, DEFAULT_CONFIG, CONFIG_VERSION, ENGINE_VERSION, epley1RM, strengthStress, enduranceStress, sessionHSS, dailyHSS, computeLoadTrend, readinessBand, computeReadiness"
    - "packages/engine/README.md documents each formula and each constant including ctlCalibrationThreshold, defaultRpe, CONFIG_VERSION, the 1.4x calibration target, and the cold-start calibrating rule"
    - "the engine has zero runtime dependencies, >=20 vitest tests pass, and tsc is green"
  artifacts:
    - "packages/engine/src/index.ts (barrel)"
    - "packages/engine/README.md"
  key_links:
    - "the barrel is the public entry point @apsis/engine consumers (db recomputeLoadDaily in Phase 3) import from"
    - "README is the BUILD.md §4.4 acceptance artifact documenting the moat's formulas + constants"
---

<objective>
Close the phase: turn `packages/engine/src/index.ts` into a clean public barrel re-exporting every
engine symbol, write the `packages/engine/README.md` documenting each formula and constant (BUILD.md
§4.4), and run the full acceptance gate — >=20 passing tests including all golden/calibration/cold-start
cases, `tsc` green, and zero runtime dependencies.

Purpose: This is the BUILD.md §4.4 done-definition for the moat. The barrel is the API surface Phase 3
wires to; the README is a required deliverable; the gate proves the whole engine is correct and pure.
Output: barrel `index.ts`, `README.md`, and a verified-green acceptance run.

Artifacts this plan produces: the public `@apsis/engine` barrel and the engine README. This plan also
asserts the full phase artifact set is present: `StrengthSet`, `EnduranceSegment`, `EngineConfig`,
`DEFAULT_CONFIG`, `CONFIG_VERSION`, `strengthStress`, `enduranceStress`, `sessionHSS`, `dailyHSS`,
`computeLoadTrend`, `readinessBand`, `computeReadiness`, `epley1RM`, the `load_daily.config_version`
column, and the generated migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hss-engine-the-moat/02-CONTEXT.md
@.planning/phases/02-hss-engine-the-moat/02-PATTERNS.md
@BUILD.md
@packages/db/src/index.ts
@packages/engine/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite index.ts as the public engine barrel</name>
  <files>packages/engine/src/index.ts</files>
  <read_first>
    - packages/db/src/index.ts (canonical barrel pattern: top doc-comment describing re-export order, then grouped export/export type lines with intent comments)
    - packages/engine/src/index.ts (keep ENGINE_VERSION; raise it from 0.0.1 to reflect the implemented engine)
    - packages/engine/src/config.ts, e1rm.ts, strength.ts, endurance.ts, hss.ts, trend.ts, readiness.ts (the modules to re-export)
    - BUILD.md §4.1 (the full public-API symbol list this barrel must surface)
  </read_first>
  <action>
    Rewrite packages/engine/src/index.ts as a pure barrel mirroring packages/db/src/index.ts: a top
    doc-comment describing the re-export order, then grouped re-exports —
    `export { DEFAULT_CONFIG, CONFIG_VERSION } from './config';`,
    `export { epley1RM } from './e1rm';`, `export { strengthStress } from './strength';`,
    `export { enduranceStress } from './endurance';`, `export { sessionHSS, dailyHSS } from './hss';`,
    `export { computeLoadTrend } from './trend';`, `export { readinessBand, computeReadiness } from './readiness';`.
    Re-export the public input/config types for consumer convenience:
    `export type { StrengthSet, EnduranceSegment, EngineConfig } from '@apsis/shared';`.
    Keep and raise `ENGINE_VERSION` (e.g. to '1.0.0') as a const. No logic in the barrel — re-exports only.
    Zero runtime deps.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/engine test 2>&1 | tail -8</automated>
  </verify>
  <done>index.ts is a barrel exporting all engine public symbols + ENGINE_VERSION; engine tests still green.</done>
  <acceptance_criteria>
    - packages/engine/src/index.ts re-exports DEFAULT_CONFIG, CONFIG_VERSION, epley1RM, strengthStress, enduranceStress, sessionHSS, dailyHSS, computeLoadTrend, readinessBand, computeReadiness, and ENGINE_VERSION
    - index.ts contains no compute logic (re-exports only)
    - `pnpm --filter @apsis/engine test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write packages/engine/README.md documenting formulas + constants</name>
  <files>packages/engine/README.md</files>
  <read_first>
    - BUILD.md §4.2 lines 162-175 (formulas to document), §4.3 lines 177-183 (constants), §4.4 line 191 (README requirement)
    - .planning/phases/02-hss-engine-the-moat/02-CONTEXT.md (decisions D-01..D-10 to reflect: calibration target, calibrating rule, defaultRpe, config versioning)
    - packages/db/src/schema.ts lines 1-14 and packages/db/src/seed.ts lines 1-15 (module doc-comment voice to mirror, since no README exists in the repo)
    - .planning/phases/02-hss-engine-the-moat/02-PATTERNS.md (section "packages/engine/README.md — formula/constant docs", the content checklist)
  </read_first>
  <action>
    Create packages/engine/README.md documenting the engine for a future maintainer. Cover, in the
    house doc-comment voice: (1) purity contract (zero deps, time passed in, no Date.now()); (2) each
    formula from BUILD.md §4.2 — endurance ES = durationMin * IF^2 * kEndurance; per-set strength
    (loadKg/e1rmKg) * reps * rpeFactor with legMultiplier, then * kStrength; session HSS = ES + SS; daily
    HSS = sum of sessions, * doublePenalty when more than one session; ATL/CTL EWMA with atlDays/ctlDays,
    TSB = CTL - ATL; readiness bands on TSB/CTL; (3) every EngineConfig constant and its DEFAULT_CONFIG
    value, including the decision-driven ctlCalibrationThreshold (10, D-03), defaultRpe (8, D-09), and
    CONFIG_VERSION (1, D-08); (4) the calibration target (run ~= 1.4x lift, ±25%, D-01/D-02) and the
    cold-start calibrating rule (calibrating until CTL >= threshold, never red on a cold start, D-03/D-04);
    (5) the public API symbol list. Keep numeric constant values consistent with the final tuned config.ts.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('packages/engine/README.md','utf8'); const need=['kStrength','kEndurance','ctlCalibrationThreshold','defaultRpe','CONFIG_VERSION','TSB','calibrating']; const miss=need.filter(k=>!t.includes(k)); if(miss.length){console.error('README missing: '+miss.join(', '));process.exit(1)} console.log('README OK')"</automated>
  </verify>
  <done>packages/engine/README.md exists and documents every formula and constant (including ctlCalibrationThreshold, defaultRpe, CONFIG_VERSION) plus the calibration target and cold-start rule.</done>
  <acceptance_criteria>
    - packages/engine/README.md exists
    - It documents the endurance, strength, session, daily, ATL/CTL/TSB, and readiness formulas
    - It lists every EngineConfig constant + DEFAULT_CONFIG value, including ctlCalibrationThreshold, defaultRpe, CONFIG_VERSION
    - The README keyword check command exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Full acceptance gate (>=20 tests, tsc green, zero runtime deps)</name>
  <files>packages/engine/src/index.ts, packages/engine/README.md</files>
  <read_first>
    - BUILD.md §4.4 lines 185-191 (the five engine acceptance criteria — all must be true)
    - packages/engine/package.json (must have NO `dependencies` key — only devDependencies — to preserve zero runtime deps)
    - packages/engine/tsconfig.json (composite project — typecheck via build mode honors references and __tests__ exclusion)
  </read_first>
  <action>
    Run the full BUILD.md §4.4 acceptance gate and fix any gap before writing the SUMMARY:
    1. `pnpm --filter @apsis/engine test` — must report >=20 passing tests across config, e1rm, strength,
       endurance, hss, calibration, trend, readiness suites (count the reported total). If under 20, the
       gap is in an earlier plan's coverage — add the missing golden/edge cases here to reach the bar.
    2. Typecheck: `pnpm --filter @apsis/engine exec tsc -b --force` — must exit 0 (composite build mode;
       BUILD.md §4.4 "tsc green"). The engine is the moat — exactOptionalPropertyTypes + noUncheckedIndexedAccess
       must be satisfied.
    3. Zero runtime deps: confirm packages/engine/package.json has no `dependencies` key, and that no engine
       source imports anything other than relative './...' paths and the '@apsis/shared' type-only alias.
    Do not weaken any test to make the gate pass; fix the code or add coverage.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/engine test 2>&1 | tail -10 && pnpm --filter @apsis/engine exec tsc -b --force 2>&1 | tail -5 && node -e "const p=require('./packages/engine/package.json'); if(p.dependencies){console.error('engine has runtime dependencies');process.exit(1)} console.log('zero runtime deps OK')"</automated>
  </verify>
  <done>>=20 engine tests pass; tsc build is green; packages/engine has zero runtime dependencies — BUILD.md §4.4 fully satisfied.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/engine test` reports >=20 passing tests and exits 0
    - `pnpm --filter @apsis/engine exec tsc -b --force` exits 0
    - packages/engine/package.json has no `dependencies` key (zero runtime deps)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @apsis/engine barrel → consumers | Public re-export surface imported by the db layer (Phase 3); no new trust boundary, no I/O introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering (supply chain) | engine public surface + deps | low | mitigate | The acceptance gate asserts zero runtime dependencies (no `dependencies` key) and that imports are relative or the @apsis/shared type alias only — the engine's attack surface stays pure compute. Input-domain guards (e1rmKg=0, ctl=0) were added and tested in plans 02-03/02-04. |
| T-02-05b | Information disclosure | README docs | low | accept | README documents formulas/constants only; no secrets, credentials, or PII. Acceptable. |
</threat_model>

<verification>
- `pnpm --filter @apsis/engine test` reports >=20 passing tests and exits 0
- `pnpm --filter @apsis/engine exec tsc -b --force` exits 0
- packages/engine/package.json has no `dependencies` key
- packages/engine/README.md documents every formula + constant (keyword check passes)
- index.ts re-exports the full public API surface
</verification>

<success_criteria>
The engine is shippable per BUILD.md §4.4: a clean public barrel, a README documenting every formula
and constant, >=20 green tests (golden lift/run, double-day, rest-week decay, calibration, cold-start),
tsc green, and zero runtime dependencies — the moat is built and proven.
</success_criteria>

<output>
Create `.planning/phases/02-hss-engine-the-moat/02-05-SUMMARY.md` when done
</output>
