---
phase: 04-run-logger-home-dashboard
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(tabs)/log/run.tsx
  - apps/mobile/app/(tabs)/history/index.tsx
  - packages/shared/src/units.ts
  - packages/shared/src/__tests__/units.test.ts
autonomous: true
gap_closure: true
requirements: [RUN-02, RUN-04, HOME-05]

must_haves:
  truths:
    - "The iOS date picker closes after the athlete selects a date or re-taps the date row (no permanently-mounted picker) — RUN-04 editable date stays usable."
    - "History shows finished sessions even when load_daily is empty, instead of a false 'No sessions yet' empty state — HOME-05."
    - "Pace never renders 'X:60'; 359.7 s/km displays as '6:00' — RUN-02 live pace / all pace readouts."
  artifacts:
    - apps/mobile/app/(tabs)/log/run.tsx
    - apps/mobile/app/(tabs)/history/index.tsx
    - packages/shared/src/units.ts
  key_links:
    - "formatPaceMinSec round-before-split → the four pace callers (run live pace, finish, detail, TODAY rows)"
    - "history loadHistory → build day entries from sessions/counts when load_daily is empty"
---

<objective>
Close the three lower-severity Phase 4 warnings that the verification flagged as touching the same success criteria (04-VERIFICATION.md "time permitting", 04-REVIEW.md WR-05/WR-07/WR-08):

1. WR-05 — the iOS date picker never closes once opened (`setShowDatePicker(Platform.OS === 'ios')` keeps it permanently mounted).
2. WR-07 — History renders "No sessions yet" whenever `load_daily` is empty even though finished sessions exist in the same fetch.
3. WR-08 — `formatPaceMinSec` rounds seconds to 60 without carrying, rendering "X:60".

Purpose: Harden the RUN-04 date editing flow, the HOME-05 history empty-state honesty, and the RUN-02 pace display correctness.
Output: A dismissible iOS date picker, a sessions-aware History fallback, and a carry-correct pace formatter with a unit test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-run-logger-home-dashboard/04-VERIFICATION.md
@.planning/phases/04-run-logger-home-dashboard/04-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix pace seconds carry in formatPaceMinSec (WR-08)</name>
  <files>packages/shared/src/units.ts, packages/shared/src/__tests__/units.test.ts</files>
  <read_first>
    - packages/shared/src/units.ts (current `formatPaceMinSec` at lines 67-71 — floors minutes but `Math.round`s seconds independently)
    - packages/shared/src/__tests__/units.test.ts (existing test structure/style to mirror — note there is currently no `formatPaceMinSec` test)
    - .planning/phases/04-run-logger-home-dashboard/04-REVIEW.md (WR-08 — diagnosed root cause + fix)
  </read_first>
  <behavior>
    - `formatPaceMinSec(270)` → "4:30" (unchanged whole-second case)
    - `formatPaceMinSec(359.7)` → "6:00" (fractional remainder ≥ 59.5 must carry into minutes, NOT render "5:60")
    - `formatPaceMinSec(59.6)` → "1:00" (carry from 0:60 → 1:00)
    - `formatPaceMinSec(0)` → "0:00"
    - Seconds are always zero-padded to two digits.
  </behavior>
  <action>
    `formatPaceMinSec` currently computes `minutes = Math.floor(totalSec / 60)` and `seconds = Math.round(totalSec % 60)` independently, so any input whose fractional-second remainder rounds to 60 renders "X:60". Round the whole total first, then split: round `totalSec` to the nearest integer, derive minutes via `Math.floor(rounded / 60)` and seconds via `rounded % 60`, then zero-pad seconds to two digits with `String(seconds).padStart(2, '0')`. Keep the function pure and dependency-free (the `@apsis/shared` display-only contract) and keep the return type `string`.

    Add tests to `units.test.ts` covering the behavior cases above (the whole-second baseline, the 359.7 → "6:00" carry, the 59.6 → "1:00" carry, and 0 → "0:00"), matching the existing describe/it style in that file.

    Do not change any other function in units.ts and do not alter the `M:SS` output format for inputs that were already correct.
  </action>
  <verify>
    <automated>cd packages/shared && pnpm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @apsis/shared test -- --run` passes with the new `formatPaceMinSec` cases (previous 17 tests plus the added ones).
    - Behavior: `formatPaceMinSec(359.7) === "6:00"` and `formatPaceMinSec(59.6) === "1:00"` (no "X:60" output).
    - Source assertion: `formatPaceMinSec` rounds the total seconds once before splitting into minutes/seconds (no independent `Math.round(totalSec % 60)`).
    - `formatPaceMinSec(270) === "4:30"` and `formatPaceMinSec(0) === "0:00"` still hold.
  </acceptance_criteria>
  <done>No pace readout in the app (live pace, finish, detail, TODAY rows, erg splits) can render "X:60"; the carry case is proven by unit test.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Close the iOS date picker after selection / re-tap (WR-05)</name>
  <files>apps/mobile/app/(tabs)/log/run.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/log/run.tsx (see `handleDateChange` at lines 168-171 and the date row `onPress` + `DateTimePicker` mount at lines 282-296)
    - .planning/phases/04-run-logger-home-dashboard/04-REVIEW.md (WR-05 — diagnosed root cause + fix)
  </read_first>
  <action>
    `handleDateChange` runs `setShowDatePicker(Platform.OS === 'ios')`, which stays `true` forever on iOS, and the date row `onPress` (line 283) only ever calls `setShowDatePicker(true)` — so on iOS the picker can never be dismissed once opened.

    Fix both entry and exit:
    - In `handleDateChange`, after handling the `dismissed`/no-date guard and calling `setSelectedDate(date)`, close the picker when the user commits a selection: on iOS, when `event.type === 'set'`, call `setShowDatePicker(false)`. On Android the dialog dismisses itself (its `onChange` already fires with the false-write path), so keep the existing Android behavior intact — do not leave Android permanently open.
    - Change the date row `onPress` (line 283) to toggle rather than force-open, e.g. `setShowDatePicker((open) => !open)`, so a second tap on the row also closes an open iOS picker.

    Keep `maximumDate={new Date()}` (RUN-04 "cannot pick the future") and the existing `dateLabel`/`selectedDate` wiring unchanged. Do not introduce a modal wrapper or a new picker library — use the existing `@react-native-community/datetimepicker` inline mount.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | grep -v -E "onboarding/review\.tsx|components/ExternalLink\.tsx" | grep -c "error TS" | grep -qx 0 && echo TYPECHECK_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `handleDateChange` sets `showDatePicker` to `false` on iOS when the user commits a date (`event.type === 'set'`), rather than `setShowDatePicker(Platform.OS === 'ios')` which is always-true on iOS.
    - Source assertion: the date row `onPress` toggles `showDatePicker` (functional updater `(open) => !open`) instead of an unconditional `setShowDatePicker(true)`.
    - Source assertion: `maximumDate={new Date()}` is still present on the `DateTimePicker`.
    - `npx tsc --noEmit` in apps/mobile produces no new errors beyond the two documented pre-existing ones.
    - On-device (human, in verification notes): tapping the date row opens the iOS picker; selecting a date closes it; re-tapping the row toggles it closed.
  </acceptance_criteria>
  <done>The iOS date picker opens and closes; the ledger card is no longer permanently occupied by an un-dismissable picker (RUN-04 date editing usable).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Show finished sessions in History when load_daily is empty (WR-07)</name>
  <files>apps/mobile/app/(tabs)/history/index.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/history/index.tsx (see `loadHistory` at lines 97-134 — the `Promise.all` fetch of `dailyRows`/`counts`/`sessions`, the `if (dailyRows.length === 0) { setAllDays([]); … return; }` early return at lines 106-110, and the day-entry build at 112-138)
    - .planning/phases/04-run-logger-home-dashboard/04-REVIEW.md (WR-07 — diagnosed root cause + fix)
  </read_first>
  <action>
    `loadHistory` early-returns with an empty list whenever `dailyRows.length === 0`, discarding the `counts` and `sessions` it already fetched in the same `Promise.all`. An athlete with finished workouts but an empty `load_daily` (e.g. a recompute that hasn't run, or pre-existing sessions) sees "No sessions yet" while their sessions exist.

    Change the empty-`load_daily` guard so it only short-circuits to the empty state when there are also no finished sessions: keep an empty result ONLY when `dailyRows.length === 0 && sessions.length === 0`. When `dailyRows` is empty but `sessions` is not, build the day entries from `sessions`/`counts` with `dayHss` falling back to `0` for each date (the existing `dayHssByDate.get(localDate) ?? 0` fallback already handles a missing per-date value — the fix is to not bail before reaching that build path). Preserve the existing `sessionsByDate`/`countByDate` grouping, the REST-row bridge logic (lines 121-134), and month grouping; the day rows simply show `dayHss` 0 until the next recompute populates `load_daily`.

    Do not trigger a recompute from the read path here (that would pull the WR-02/WR-01 recompute-on-read concern into scope) — just stop dropping already-fetched sessions. Do not change the double-session ADJUSTED math or the swipe-to-delete wiring.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | grep -v -E "onboarding/review\.tsx|components/ExternalLink\.tsx" | grep -c "error TS" | grep -qx 0 && echo TYPECHECK_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the early-return empty guard requires both `dailyRows.length === 0` AND `sessions.length === 0` (finished sessions are no longer discarded when `load_daily` is empty).
    - Source assertion: when `dailyRows` is empty but `sessions` is non-empty, the day-entry build path runs and derives dates from `sessions`/`counts` with `dayHss` defaulting to 0.
    - Source assertion: the REST-row bridge, month grouping, ADJUSTED double-session line, and swipe-to-delete wiring are unchanged.
    - `npx tsc --noEmit` in apps/mobile produces no new errors beyond the two documented pre-existing ones.
    - On-device (human, in verification notes): with finished sessions but empty `load_daily`, History lists the sessions (day HSS may read 0) instead of "No sessions yet".
  </acceptance_criteria>
  <done>History never hides existing finished sessions behind a false empty state; HOME-05 day-grouped history reflects reality even before the next recompute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → local SQLite reads | History reads `load_daily`/session rows via already-parameterized query builders; no write, no new statement introduced. |
| Display formatting (pure) | `formatPaceMinSec` is a pure display transform with no I/O and no untrusted sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10-01 | Information Disclosure | History empty-state fallback | low | accept | The fallback only renders the user's own already-fetched sessions on-device (offline, single-user). No new data crosses any boundary; only the render gate changes. |
| T-04-10-02 | Tampering | formatPaceMinSec | low | accept | Pure numeric-to-string transform; no input reaches a query or filesystem. Rounding change cannot alter data at rest. |
| T-04-10-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs — all three fixes edit existing files (and one existing test file) only. No supply-chain surface added. |
</threat_model>

<verification>
Automated (run by executor):
- `packages/shared`: `pnpm test -- --run` → 17 prior tests plus the new `formatPaceMinSec` carry cases pass.
- `apps/mobile`: `npx tsc --noEmit` → no new errors beyond the two documented pre-existing route-typing errors.
- `apps/mobile`: `npx vitest run` → existing 9 lib tests still pass.

On-device human verification (carried forward from 04-VERIFICATION.md — native picker/list behavior cannot run under vitest):
1. iOS date picker: open via the date row, select a date → picker closes; re-tap the row → toggles closed. Future dates remain unselectable.
2. History empty-`load_daily`: with finished sessions present but no `load_daily` rows, History lists the sessions instead of "No sessions yet".
3. Pace readouts: live pace on the run form, finish summary, and TODAY session rows never show "X:60".
</verification>

<success_criteria>
- WR-08 closed: `formatPaceMinSec` carries seconds correctly, proven by unit test.
- WR-05 closed: iOS date picker is dismissable (source-asserted; on-device confirmed).
- WR-07 closed: History renders finished sessions when `load_daily` is empty.
- All existing automated suites pass; typecheck clean except the two documented pre-existing errors.
</success_criteria>

<artifacts>
## Artifacts this plan produces

Modified:
- `packages/shared/src/units.ts`
  - `formatPaceMinSec(totalSec: number): string` — rounds total seconds once before splitting (signature/format unchanged; fixes the "X:60" carry).
- `packages/shared/src/__tests__/units.test.ts`
  - New test cases for `formatPaceMinSec` (baseline, carry, zero).
- `apps/mobile/app/(tabs)/log/run.tsx`
  - `handleDateChange` — closes the iOS picker on committed selection; date row `onPress` toggles `showDatePicker`.
- `apps/mobile/app/(tabs)/history/index.tsx`
  - `loadHistory` — empty guard now requires both `dailyRows` and `sessions` empty; builds day entries from `sessions`/`counts` when `load_daily` is empty.
</artifacts>

<output>
Create `.planning/phases/04-run-logger-home-dashboard/04-10-SUMMARY.md` when done.
</output>
