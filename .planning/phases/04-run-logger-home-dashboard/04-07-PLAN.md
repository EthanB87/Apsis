---
phase: 04-run-logger-home-dashboard
plan: 07
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - apps/mobile/components/home/TrendChart.tsx
  - apps/mobile/components/home/TodayBreakdownSheet.tsx
  - apps/mobile/app/(tabs)/index.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04]

must_haves:
  truths:
    - "The TODAY screen shows today's readiness band + today's total HSS prominently above the fold (HOME-01/HOME-02)."
    - "It shows a 28-day ATL/CTL trend chart with a scrub tooltip snapping to the nearest day (HOME-03/D-19/D-20)."
    - "Before 14+ days of data, it shows the calibrating hero (steel ring + plate orbit + BUILDING TREND DAY N/14) and the chart renders whatever real days exist (HOME-04/D-22)."
    - "Before any session today, the ring renders at 0 with the readiness light still live from history plus a LET'S WORK empty state (D-06)."
    - "All load_daily/workout reads use useFocusEffect, never a bare useEffect (Pitfall 5)."
    - "Tapping the ring opens a today-breakdown sheet with per-session rows + the double-day math (D-24)."
  artifacts:
    - apps/mobile/components/home/TrendChart.tsx
    - apps/mobile/components/home/TodayBreakdownSheet.tsx
    - "apps/mobile/app/(tabs)/index.tsx (TODAY dashboard)"
  key_links:
    - "TrendChart reads persisted load_daily rows via last28DaysTrend (04-01) - never re-derives EWMA in the UI."
    - "The screen composes HssRing/ReadinessLight/StatTiles (04-04) and reads today's row + last 28 rows + today's sessions."
    - "readinessBand/calibrating comes from the persisted load_daily.readinessBand, produced by the engine in the recompute pipeline (04-03)."
---

<objective>
Build the TODAY dashboard (HOME-01..04) - the payoff screen where the unified HSS number and readiness
become visible. Implements UI-SPEC scroll order (D-17): greeting -> 200px ring + readiness light ->
ATL/CTL/TSB tiles -> 28-day scrub chart -> today's session rows, plus the calibrating and empty states,
and the ring-tap today-breakdown sheet. Also builds the first chart in the codebase (victory-native
scrub tooltip).

Purpose: Make the engine's output visible - the reason the loggers exist.
Output: TrendChart.tsx, TodayBreakdownSheet.tsx, and the retrofitted index.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md
@.planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md
@.planning/phases/04-run-logger-home-dashboard/04-PATTERNS.md
@apps/mobile/components/session/HSSBreakdownSheet.tsx
@apps/mobile/app/(tabs)/index.tsx
@.planning/phases/04-run-logger-home-dashboard/04-01-SUMMARY.md
@.planning/phases/04-run-logger-home-dashboard/04-04-SUMMARY.md
</context>

<artifacts_produced>
Symbols/files this plan creates (exclude from source-drift verification):
- apps/mobile/components/home/TrendChart.tsx (new): victory-native CartesianChart + useChartPressState scrub tooltip; props data (28 chronological load_daily points), calibratingDayN?
- apps/mobile/components/home/TodayBreakdownSheet.tsx (new): per-session rows + double-day math sheet, opened by the ring tap
- apps/mobile/app/(tabs)/index.tsx: full TODAY dashboard replacing the placeholder
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: TrendChart - victory-native 28-day chart with scrub tooltip</name>
  <files>apps/mobile/components/home/TrendChart.tsx</files>
  <read_first>
    - node_modules/victory-native (installed .d.ts - CONFIRM the CartesianChart + useChartPressState + Line TypeScript signatures against the installed types before coding, per RESEARCH Assumption A2 / the Phase 3 Stack.Protected lesson)
    - apps/mobile/constants/Colors.ts (bone=ATL line, ash=CTL line + axis + hairline cursor; steel gridlines; NO volt on the chart per D-18)
    - apps/mobile/constants/theme.ts (Mono role for tooltip + axis labels; Radius.md for the card + tooltip)
    - .planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md (Pattern 4 useChartPressState scrub tooltip; GestureHandlerRootView already wraps the app root - no extra wrapping)
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md (section 5 chart + tooltip: 2 lines bone/ash, hairline ash cursor, tooltip copy, calibrating caption)
  </read_first>
  <action>
    Create components/home/TrendChart.tsx. Props: data (an array of chronological points { day, hss,
    atl, ctl, tsb, dateLabel }) and an optional calibratingDayN. Render a carbon card (hairline border,
    Radius.md, ~180-200px height) with a victory-native CartesianChart: two Line series - ATL bone, CTL
    ash (~2px, no area fill, D-18) - horizontal steel gridlines only, mono 9px ash sparse axis labels
    (every ~7th day, executor discretion). TSB is NOT a third line. Scrub (D-19/D-20): wire
    useChartPressState so a touch-drag shows a 1px ash vertical hairline cursor snapping to the nearest
    day and a floating carbon tooltip card (hairline, Radius.md) offset above the touch, containing the
    D-20 line: {MON DD} then HSS N then ATL N then CTL N then TSB +/-N, all mono bone/ash, no volt.
    Rest days (hss 0) still show ATL/CTL/TSB. Calibrating (calibratingDayN set): render whatever real
    days exist (never a fake line) plus a BUILDING TREND then DAY N/14 mono ash caption above the card.
    Do NOT hand-roll EWMA - the point values are already-persisted engine output passed in.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "useChartPressState" components/home/TrendChart.tsx && grep -q "CartesianChart" components/home/TrendChart.tsx</automated>
  </verify>
  <acceptance_criteria>
    - TrendChart uses victory-native CartesianChart + useChartPressState (signatures confirmed against the installed .d.ts).
    - Two lines render (ATL bone, CTL ash); no volt anywhere on the chart; TSB is not drawn as a line.
    - Scrubbing shows an ash hairline cursor + a tooltip with the exact D-20 content including a signed TSB.
    - A calibratingDayN prop renders the BUILDING TREND / DAY N/14 caption and only real days.
  </acceptance_criteria>
  <done>The 28-day scrub-tooltip trend chart renders persisted load_daily output (HOME-03) and its calibrating state (HOME-04).</done>
</task>

<task type="auto">
  <name>Task 2: TODAY dashboard scaffold - data load + hero + tiles + chart wiring</name>
  <files>apps/mobile/app/(tabs)/index.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/index.tsx (current placeholder - replace with the dashboard)
    - packages/db/src/index.ts (last28DaysTrend, dayGroupedSessions, loadDaily, workout exports)
    - apps/mobile/lib/localDate.ts (todayLocalDate for today's row lookup)
    - apps/mobile/components/home/HssRing.tsx, ReadinessLight.tsx, StatTiles.tsx (04-04 components + props)
    - apps/mobile/components/home/TrendChart.tsx (Task 1)
    - .planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md (Pitfall 5 useFocusEffect; Code Examples last28DaysTrend)
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md (section 1-5 + D-17 scroll order + D-06 empty state + Copywriting Contract greeting/calibrating/empty copy)
  </read_first>
  <action>
    Replace app/(tabs)/index.tsx with the TODAY dashboard. Load data in a useFocusEffect (NEVER a bare
    useEffect - Pitfall 5): read today's load_daily row (by todayLocalDate()), the last 28 load_daily
    rows (last28DaysTrend, reversed to chronological), and today's finished sessions (filter
    dayGroupedSessions or query workout for today's localDate). Derive: historyDays = count of load_daily
    rows (or days since first session) to know calibrating; today's dayHss (0 if no row yet); today's
    readinessBand from today's row (or the most recent row's band - readiness comes from history, D-06).
    Compose the scroll order (D-17): mono timestamp + LET'S WORK greeting -> HssRing (size 200, hss =
    today's dayHss, band = readiness band, animate gated once-per-day via a persisted
    lastAnimatedHssDate/value in a lightweight zustand slice or AsyncStorage, D-05) -> ReadinessLight
    (band) -> StatTiles (atl/ctl/tsb from today's row) -> TrendChart (28 points, calibratingDayN when
    historyDays < 14). Calibrating hero (D-08/D-22): when band is calibrating the HssRing already renders
    the steel + plate-orbit + BUILDING TREND DAY N/14 and ReadinessLight renders null - pass the day
    count through. Empty state (D-06): before any session today, ring shows 0 with the readiness light
    still live, plus a quiet LET'S WORK prompt and two ghost shortcuts Start Workout (routes to
    /(tabs)/log) and Log Run (routes to /(tabs)/log/run) - buttons on Home go ghost (D-09), never volt.
    Wire the trend chart's today session-row list in Task 3.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "useFocusEffect" 'app/(tabs)/index.tsx' && grep -q "last28DaysTrend" 'app/(tabs)/index.tsx' && grep -q "HssRing" 'app/(tabs)/index.tsx' && ! grep -q "useEffect(" 'app/(tabs)/index.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - Data loads via useFocusEffect (no bare useEffect reading load_daily/workout in this file).
    - The scroll order matches D-17: greeting, ring, readiness light, stat tiles, chart.
    - When historyDays < 14 the calibrating hero renders (steel ring + plate orbit) and ReadinessLight is absent.
    - With no session today, the ring shows 0, the readiness light is still live, and LET'S WORK + two ghost (non-volt) shortcuts render.
    - today's dayHss and ATL/CTL/TSB come from the persisted load_daily row.
  </acceptance_criteria>
  <done>The TODAY hero, tiles, chart, calibrating and empty states render from persisted load_daily (HOME-01..04).</done>
</task>

<task type="auto">
  <name>Task 3: Today's session rows + ring-tap today-breakdown sheet (D-23/D-24)</name>
  <files>apps/mobile/app/(tabs)/index.tsx, apps/mobile/components/home/TodayBreakdownSheet.tsx</files>
  <read_first>
    - apps/mobile/components/session/HSSBreakdownSheet.tsx (bottom-sheet pattern + per-row subtotal + total row to adapt to per-session rows + double-day math)
    - packages/db/src/index.ts (dayGroupedSessions for today's rows; the double-day delta is dailyHSS(scores) - sum(scores))
    - packages/engine/src/daily.ts (dailyHSS - reuse for the today-breakdown double-session math, never hand-roll the multiplier)
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md (section 6 today's session rows - Archivo_500Medium title + mono metadata + bone (not volt) right-aligned HSS; section 1 ring tap; D-24)
  </read_first>
  <action>
    In app/(tabs)/index.tsx, render today's session rows beneath the chart per D-23: a TODAY then N
    SESSION(S) mono ash header, then one row per today session - title (Archivo_500Medium, bone) + a mono
    ash metadata line (lifts: TITLE then N LIFTS then RPE; runs: distance then pace then BPM) + the
    session HSS right-aligned in BONE (NOT volt, One-Volt Discipline). Rows sit on the void background
    with hairline dividers; tapping a row opens the session detail (route /session/detail with the
    workoutId - the screen itself is built in 04-08; wire the navigation now).
    Create components/home/TodayBreakdownSheet.tsx adapting HSSBreakdownSheet to PER-SESSION rows: list
    each of today's sessions with its HSS, then a DAY TOTAL row; when sessionCount > 1 show the honest
    double-day math (DAY TOTAL X then INCL. +Y DOUBLE-DAY LOAD where Y = dailyHSS(scores) - sum(scores)
    via @apsis/engine). Wire the HssRing onPress (D-24) to open this sheet.
  </action>
  <verify>
    <automated>cd apps/mobile && test -f components/home/TodayBreakdownSheet.tsx && grep -q "dailyHSS\|DOUBLE-DAY" components/home/TodayBreakdownSheet.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Today's session rows render with an Archivo_500Medium title, mono metadata, and a BONE right-aligned HSS under a TODAY / N SESSION(S) header.
    - Tapping a session row navigates to /session/detail with the workoutId.
    - Tapping the ring opens TodayBreakdownSheet; with 2+ sessions it shows the +Y DOUBLE-DAY LOAD math computed via dailyHSS (not a hand-rolled multiplier).
  </acceptance_criteria>
  <done>Today's sessions are listed and the ring-tap breakdown surfaces the honest double-day math.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| load_daily/workout reads -> TODAY render | Read-only; a focus-effect miswire could resurrect stale/finished state (Phase 3 P10 lesson) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Tampering | TODAY/breakdown queries | high | mitigate | Parameterized last28DaysTrend/dayGroupedSessions builders only (T-1-01) |
| T-04-14 | Denial of Service | stacked-screen focus rehydrate loop | medium | mitigate | useFocusEffect (never bare useEffect) for all load_daily/workout reads (Pitfall 5 / STATE.md P10) |
| T-04-15 | Information Disclosure | dashboard query error path | low | mitigate | console.error + graceful empty/zero render, no stack trace to user |
</threat_model>

<verification>
- apps/mobile typechecks with no NEW errors from the three files.
- grep confirms useFocusEffect (and absence of bare useEffect), last28DaysTrend, HssRing, and the double-day math.
- On-device UAT (phase gate): ring animates once/day; scrub tooltip snaps to days; calibrating hero shows before 14 days; empty state before today's first session.
</verification>

<success_criteria>
TODAY shows today's HSS + readiness above the fold, a 28-day scrub chart, calibrating and empty states,
and today's sessions - all from persisted engine output (HOME-01..04).
</success_criteria>

<output>
Create `.planning/phases/04-run-logger-home-dashboard/04-07-SUMMARY.md` when done.
</output>
