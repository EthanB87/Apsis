---
phase: 04-run-logger-home-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/mobile/lib/localDate.ts
  - apps/mobile/lib/recomputeLoadDaily.ts
  - apps/mobile/lib/finishWorkout.ts
  - apps/mobile/app/(tabs)/log/index.tsx
autonomous: true
requirements: [HOME-01, HOME-02]

must_haves:
  truths:
    - "After a lifting session is finished, load_daily is fully recomputed from the athlete's first-ever finished session through today so Home reflects lifts, not just runs (Pitfall 1)."
    - "After a session is soft-deleted (discard or history swipe), load_daily is recomputed so the removed session's load disappears from the trend (D-29)."
    - "recomputeLoadDaily upserts one contiguous row per calendar day, keyed on load_daily.localDate, never skipping rest days."
    - "todayLocalDate and addDaysLocal are a single shared local-time (never UTC) helper reused everywhere Phase 4 derives a date string (Pitfall 4)."
  artifacts:
    - apps/mobile/lib/localDate.ts (todayLocalDate, addDaysLocal)
    - apps/mobile/lib/recomputeLoadDaily.ts (recomputeLoadDaily)
  key_links:
    - "recomputeLoadDaily uses @apsis/db computeLoadDailyUpsertRows (04-01) for the pure fold and only owns the op-sqlite read + upsert."
    - "finishWorkout and discardWorkout both call recomputeLoadDaily so every terminal write to workout refreshes load_daily."
---

<objective>
Build the net-new load_daily recompute pipeline (the phase's highest-risk item — RESEARCH Summary
+ Pitfall 1) and wire it into the EXISTING lifting finish/discard flow so Home reflects BOTH lifting
and running. This closes the gap RESEARCH discovered: load_daily is currently never written
anywhere, so without this plan Home would only ever reflect runs — silently breaking the app's
Core Value. Also promotes the local-date helper to a shared module (Pitfall 4 timezone-drift fix).

Purpose: Make load_daily the durable, engine-accurate rollup every Home/History read consumes.
Output: apps/mobile/lib/localDate.ts, recomputeLoadDaily.ts, retrofitted finishWorkout.ts, and a
localDate-deduplicated log/index.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md
@.planning/phases/04-run-logger-home-dashboard/04-PATTERNS.md
@apps/mobile/lib/commitSet.ts
@apps/mobile/lib/finishWorkout.ts
@.planning/phases/04-run-logger-home-dashboard/04-01-SUMMARY.md
</context>

<artifacts_produced>
Symbols/files this plan creates (exclude from source-drift verification):
- apps/mobile/lib/localDate.ts (new): todayLocalDate(): string, addDaysLocal(dateStr: string, n: number): string
- apps/mobile/lib/recomputeLoadDaily.ts (new): recomputeLoadDaily(database: DB): Promise of void
- apps/mobile/lib/finishWorkout.ts: finishWorkout and discardWorkout now call recomputeLoadDaily after their write
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Promote the local-date helper to a shared module (Pitfall 4)</name>
  <files>apps/mobile/lib/localDate.ts, apps/mobile/app/(tabs)/log/index.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/log/index.tsx (existing inline todayLocalDate, lines 28-35)
    - apps/mobile/lib/effectiveLoad.ts (small pure lib-module convention, if present)
    - .planning/phases/04-run-logger-home-dashboard/04-PATTERNS.md (Shared Patterns local date derivation - MUST be promoted and reused verbatim)
    - .planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md (Pitfall 4 - timezone drift correctness, not a style nit)
  </read_first>
  <action>
    Create apps/mobile/lib/localDate.ts exporting two pure helpers: todayLocalDate(): string - the
    EXACT local-time (never UTC) YYYY-MM-DD logic currently inlined in log/index.tsx (getFullYear /
    getMonth()+1 / getDate, each zero-padded), copied verbatim so day boundaries match the existing
    convention; and addDaysLocal(dateStr: string, n: number): string - parse a YYYY-MM-DD string into
    y/m/d, build a local Date, add n days, re-serialize as YYYY-MM-DD. Refactor
    app/(tabs)/log/index.tsx to import todayLocalDate from @/lib/localDate and remove the local
    inline function (single source of truth). No behavior change to the Start Workout flow.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "export function todayLocalDate" lib/localDate.ts && grep -q "export function addDaysLocal" lib/localDate.ts && grep -q "lib/localDate" 'app/(tabs)/log/index.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/lib/localDate.ts exports both todayLocalDate and addDaysLocal.
    - app/(tabs)/log/index.tsx imports todayLocalDate from @/lib/localDate and no longer declares its own function todayLocalDate.
    - The Start Workout flow still inserts workout.localDate from todayLocalDate().
  </acceptance_criteria>
  <done>A single shared local-date helper exists and log/index.tsx consumes it.</done>
</task>

<task type="auto">
  <name>Task 2: recomputeLoadDaily - op-sqlite read + full-history upsert</name>
  <files>apps/mobile/lib/recomputeLoadDaily.ts</files>
  <read_first>
    - apps/mobile/lib/commitSet.ts (recomputeSessionHss - the read-all-rows then call-pure-engine then write-back shape to mirror at day scope)
    - packages/db/src/index.ts (exports: computeLoadDailyUpsertRows, LoadDailyUpsertRow, loadDaily, workout, activeWorkoutFilter, DB)
    - packages/db/src/loadDaily.ts (computeLoadDailyUpsertRows signature - sessions + today to rows)
    - packages/db/src/schema.ts (loadDaily columns: localDate PK, dayHss, atl, ctl, tsb, readinessBand, updatedAt)
    - .planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md (Pattern 1 upsert; Open Question 1 + Assumption A1 - the excluded-column batch-upsert idiom is UNVERIFIED against op-sqlite)
  </read_first>
  <action>
    Create apps/mobile/lib/recomputeLoadDaily.ts exporting recomputeLoadDaily(database: DB) returning
    a Promise of void. Steps: (1) SELECT localDate + hss FROM workout WHERE isNotNull(finishedAt)
    AND isNull(deletedAt) using and(...) with the shared activeWorkoutFilter for the deleted check;
    (2) if zero rows, return early; (3) map rows to localDate + hss (hss defaulting to 0) and call
    computeLoadDailyUpsertRows(sessions, todayLocalDate()) from @apsis/db + @/lib/localDate; (4)
    upsert every returned row into loadDaily keyed on loadDaily.localDate via onConflictDoUpdate
    (target loadDaily.localDate, set dayHss/atl/ctl/tsb/readinessBand/updatedAt).
    IMPORTANT (RESEARCH A1/Open Question 1): the excluded-column batch-upsert idiom is NOT verified
    against drizzle-orm 0.45.2 + op-sqlite. Implement the SAFE guaranteed-correct form: a loop of
    single-row upserts, each insert(loadDaily).values(row).onConflictDoUpdate with a set object of
    that row's LITERAL values (row.dayHss, row.atl, and so on), never a raw interpolated sql template
    and never an excluded reference - keeps it parameterized (T-1-01). Wrap in try/catch:
    console.error for diagnostics then re-throw so the caller shows its own generic user message; do
    NOT swallow. updatedAt uses new Date() at write time (the app I/O edge, not the pure engine).
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "export async function recomputeLoadDaily" lib/recomputeLoadDaily.ts && grep -q "computeLoadDailyUpsertRows" lib/recomputeLoadDaily.ts && grep -q "onConflictDoUpdate" lib/recomputeLoadDaily.ts</automated>
  </verify>
  <acceptance_criteria>
    - recomputeLoadDaily selects only finished, non-deleted workouts and returns early on zero rows.
    - It calls computeLoadDailyUpsertRows with today from todayLocalDate() and upserts each returned row into loadDaily on the localDate conflict target.
    - The upsert uses per-row literal set values (no `excluded` reference, no raw interpolated sql template).
    - Errors are console.error-logged and re-thrown (not swallowed).
  </acceptance_criteria>
  <done>recomputeLoadDaily reads finished workouts, folds them via the engine-backed pure builder, and upserts the full contiguous load_daily table.</done>
</task>

<task type="auto">
  <name>Task 3: Retrofit finishWorkout + discardWorkout to recompute load_daily (Pitfall 1 + D-29)</name>
  <files>apps/mobile/lib/finishWorkout.ts</files>
  <read_first>
    - apps/mobile/lib/finishWorkout.ts (current finishWorkout + discardWorkout, both thin single-write functions)
    - apps/mobile/lib/recomputeLoadDaily.ts (the function added in Task 2)
    - .planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md (Pitfall 1 - the lifting flow must call the recompute or Home only ever reflects runs)
  </read_first>
  <action>
    Edit apps/mobile/lib/finishWorkout.ts so BOTH terminal actions refresh load_daily after their
    write: finishWorkout, after setting workout.finishedAt, awaits recomputeLoadDaily(database);
    discardWorkout, after softDeleteWorkout, also awaits recomputeLoadDaily(database) (D-29 - the
    removed session's load must leave the trend). Import recomputeLoadDaily from './recomputeLoadDaily'.
    Keep the caller-supplied timestamp arguments unchanged (engine-purity convention). Update the file
    header comment to note that both functions now trigger a full-history load_daily recompute so
    Home stays consistent for lifting sessions too (previously load_daily was never written).
  </action>
  <verify>
    <automated>cd apps/mobile && grep -c "recomputeLoadDaily" lib/finishWorkout.ts</automated>
  </verify>
  <acceptance_criteria>
    - finishWorkout awaits recomputeLoadDaily(database) after setting finishedAt.
    - discardWorkout awaits recomputeLoadDaily(database) after softDeleteWorkout.
    - recomputeLoadDaily is imported from './recomputeLoadDaily' (grep count of the symbol in the file is at least 3: import + 2 call sites).
  </acceptance_criteria>
  <done>Finishing or discarding a lifting session refreshes the whole load_daily table, so Home's ring/band/chart include lifts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workout rows -> load_daily upsert | Persisted per-session hss aggregates into the durable trend table |
| error paths -> user | A failed recompute must not leak stack traces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Tampering | load_daily upsert (recomputeLoadDaily) | high | mitigate | Per-row parameterized onConflictDoUpdate with literal set values; no excluded/raw-sql interpolation (T-1-01) |
| T-04-05 | Information Disclosure | recompute error path | medium | mitigate | console.error for diagnostics, re-throw so caller shows a hardcoded generic message (existing T-03-08 convention) |
| T-04-06 | Denial of Service | full-history fold on every terminal write | low | accept | Synchronous op-sqlite fold is cheap at v1.0 scale (RESEARCH A3 / Open Question 2); keep inline, revisit only if UAT shows a stall |
</threat_model>

<verification>
- apps/mobile typechecks with no NEW errors introduced by the three files (only the two documented pre-existing typed-route errors remain).
- grep confirms finishWorkout.ts references recomputeLoadDaily at import + both call sites.
- On-device UAT (phase gate): finishing a LIFT changes Home's ring/band; discarding a session removes its load from the trend.
</verification>

<success_criteria>
The load_daily recompute pipeline exists, is engine-accurate (pure fold from 04-01), and fires on
every terminal workout write including the existing lifting flow.
</success_criteria>

<output>
Create `.planning/phases/04-run-logger-home-dashboard/04-03-SUMMARY.md` when done.
</output>
