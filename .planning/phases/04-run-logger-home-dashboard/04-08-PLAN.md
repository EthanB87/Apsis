---
phase: 04-run-logger-home-dashboard
plan: 08
type: execute
wave: 3
depends_on: ["04-01", "04-03"]
files_modified:
  - apps/mobile/app/(tabs)/history/index.tsx
  - apps/mobile/components/history/DayRow.tsx
  - apps/mobile/app/session/detail.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
autonomous: true
requirements: [HOME-05, HOME-06]

must_haves:
  truths:
    - "History is its own tab: a chronological day-grouped ledger of date + day HSS + session count (HOME-05)."
    - "Days with 2+ sessions show a 'N SESSIONS - ADJUSTED' amber chip and, when expanded, the honest 'DAY TOTAL X - INCL. +Y DOUBLE-DAY LOAD' math (HOME-06/D-26)."
    - "Tapping a day expands its sessions inline (accordion); tapping a session pushes the session-detail screen (D-25)."
    - "Rest days render as quiet ash REST rows (D-27); month header rows group the ledger (D-28)."
    - "Swipe-to-delete a session soft-deletes it (with confirm dialog) and recomputes load_daily forward (D-29)."
    - "The tab bar is 4 tabs: TODAY (renamed from Home) / LOG / HISTORY (new) / SETTINGS (D-07)."
  artifacts:
    - "apps/mobile/app/(tabs)/history/index.tsx"
    - apps/mobile/components/history/DayRow.tsx
    - apps/mobile/app/session/detail.tsx
    - "apps/mobile/app/(tabs)/_layout.tsx (4-tab restructure)"
  key_links:
    - "Day HSS + session counts come from load_daily + sessionCountsByDate/dayGroupedSessions (04-01)."
    - "Swipe-delete reuses discardWorkout (04-03, now recompute-aware) via the Phase 3 react-native-gesture-handler Swipeable pattern."
    - "The double-day delta reuses @apsis/engine dailyHSS, never a hand-rolled multiplier."
---

<objective>
Build the History tab (HOME-05/HOME-06) - the day-grouped training ledger with the honest double-session
math, rest-day rows, month grouping, infinite scroll, and swipe-to-delete - plus the session-detail
screen that History pushes to, and the 4-tab restructure (D-07). Implements UI-SPEC sections 9-11.

Purpose: Let the athlete read their true training calendar with correct double-day labeling.
Output: history/index.tsx, DayRow.tsx, session/detail.tsx, and the 4-tab _layout.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md
@.planning/phases/04-run-logger-home-dashboard/04-PATTERNS.md
@apps/mobile/components/session/SetRow.tsx
@apps/mobile/app/(tabs)/_layout.tsx
@apps/mobile/app/session/finish.tsx
@.planning/phases/04-run-logger-home-dashboard/04-01-SUMMARY.md
@.planning/phases/04-run-logger-home-dashboard/04-03-SUMMARY.md
</context>

<artifacts_produced>
Symbols/files this plan creates (exclude from source-drift verification):
- apps/mobile/app/(tabs)/history/index.tsx (new): day-grouped ledger, accordion, REST rows, month headers, infinite scroll, swipe-delete
- apps/mobile/components/history/DayRow.tsx (new): accordion day row + ADJUSTED chip + double-day math expansion
- apps/mobile/app/session/detail.tsx (new): read-only session detail (branches strength/endurance)
- apps/mobile/app/(tabs)/_layout.tsx: 4-tab bar (TODAY rename + HISTORY tab insertion)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: History ledger + DayRow (accordion, ADJUSTED chip, REST rows, month headers, swipe-delete)</name>
  <files>apps/mobile/app/(tabs)/history/index.tsx, apps/mobile/components/history/DayRow.tsx</files>
  <read_first>
    - apps/mobile/components/session/SetRow.tsx (Swipeable swipe-to-delete + expand/collapse + warning-badge pattern - the concrete swipe analog RESEARCH flagged; row hairline/surface conventions)
    - apps/mobile/components/session/ExerciseCard.tsx (another Swipeable usage reference)
    - packages/db/src/index.ts (loadDaily rows for day HSS/band; sessionCountsByDate; dayGroupedSessions)
    - apps/mobile/lib/finishWorkout.ts (discardWorkout - now recompute-aware; call it on confirmed delete)
    - apps/mobile/lib/localDate.ts (addDaysLocal for building the contiguous month ledger incl. REST days)
    - packages/engine/src/daily.ts (dailyHSS for the +Y double-day delta)
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md (section 9 History tab full spec + Copywriting Contract REST/ADJUSTED/DAY TOTAL/month header/delete-confirm copy; Pitfall 5 useFocusEffect)
  </read_first>
  <action>
    Create app/(tabs)/history/index.tsx: reuse ScreenHeader (Kicker LEDGER + Title History), headerShown
    false on the tab. Load data in a useFocusEffect (Pitfall 5): load_daily rows (day HSS + band) joined
    with sessionCountsByDate for counts, and dayGroupedSessions for per-day session rows on expand. Build
    a calendar-contiguous list per visible month (using addDaysLocal) so REST days appear as explicit
    rows (D-27). Render a SectionList/FlatList with sticky mono ash month header rows in the {MONTH}
    {YEAR} format (D-28); most recent ~30 days load instantly, older months fetch on scroll-near-bottom
    (centered ash ActivityIndicator). Day rows and REST rows come from DayRow.
    Create components/history/DayRow.tsx: props for a day's date, dayHss, sessionCount, band, sessions.
    A normal day row shows date (Body bone) + day HSS (Heading, bone, tabularNums, right-aligned); when
    sessionCount > 1 render an amber-tinted pill N SESSIONS - ADJUSTED inline before the HSS (D-26).
    Tapping the day toggles an inline accordion listing each session (Label-role, indented) followed by
    a DAY TOTAL then INCL. +Y DOUBLE-DAY LOAD line (mono ash) when sessionCount > 1, where Y =
    dailyHSS(scores) - sum(scores) via @apsis/engine. Tapping a session sub-row pushes /session/detail
    with its workoutId. A REST day (no sessions) renders a thinner ash date + REST row, not
    tappable/expandable (D-27). Swipe-to-delete (D-29): reuse the SetRow Swipeable pattern on session
    sub-rows; the reveal shows a molten Delete; on tap show a confirm dialog with the exact UI-SPEC delete
    copy; on confirm call discardWorkout(db, workoutId, new Date()) (soft-delete + forward recompute) and
    refresh the list. Error handling: console.error + the exact History load/error copy from the UI-SPEC.
  </action>
  <verify>
    <automated>cd apps/mobile && test -f components/history/DayRow.tsx && grep -q "useFocusEffect" 'app/(tabs)/history/index.tsx' && grep -q "ADJUSTED" components/history/DayRow.tsx && grep -q "DOUBLE-DAY" components/history/DayRow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - History renders day rows (date + day HSS + count) grouped under mono month headers, loaded via useFocusEffect.
    - Days with 2+ sessions show the amber N SESSIONS - ADJUSTED chip and, on expand, the DAY TOTAL / +Y DOUBLE-DAY LOAD math via dailyHSS.
    - Rest days render as quiet ash REST rows, not expandable.
    - Swipe-to-delete triggers the confirm dialog and calls discardWorkout (soft-delete + recompute) on confirm.
    - Tapping a session sub-row navigates to /session/detail.
  </acceptance_criteria>
  <done>The History ledger shows the true day-grouped calendar with honest double-day labeling and swipe-delete (HOME-05/HOME-06/D-25..D-29).</done>
</task>

<task type="auto">
  <name>Task 2: Session-detail screen (read-only, branches strength/endurance)</name>
  <files>apps/mobile/app/session/detail.tsx</files>
  <read_first>
    - apps/mobile/app/session/finish.tsx (SQLite-truth query + summary render + the endurance branch added in 04-06 - mirror its per-type derivation)
    - apps/mobile/components/session/HSSBreakdownSheet.tsx (per-row subtotal + volt total row pattern to reuse as a pushed screen; volt total is allowed here per One-Volt Discipline since this is not the Home ring screen)
    - packages/db/src/schema.ts (workout.type/title/localDate; strength_set + endurance_segment shapes)
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md (section 10 session-detail screen)
  </read_first>
  <action>
    Create app/session/detail.tsx: a read-only pushed screen (back button via the nav header) reached from
    History. Read the workoutId param, query workout.type, and branch: strength/hybrid renders per-exercise
    subtotal rows; endurance renders per-segment rows (distance/pace/duration or duration/HR per activity
    type). Reuse the HSSBreakdownSheet visual pattern (Label-size subtotal rows ending in a Heading-size
    VOLT total row - volt is fine here, not the Home ring screen). Header: mono ash timestamp + date + a
    Heading bone workout title/type. Re-derive everything from SQLite (never a store). No edit/delete here
    (delete lives in History swipe; editing is deferred).
  </action>
  <verify>
    <automated>cd apps/mobile && test -f app/session/detail.tsx && grep -q "workout" app/session/detail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - detail.tsx reads the workoutId param and branches on workout.type (strength vs endurance).
    - It reuses the breakdown per-row + total-row visual pattern with a volt total.
    - Everything is re-derived from SQLite; no edit/delete controls are present.
  </acceptance_criteria>
  <done>Tapping a session in History opens a read-only detail screen for both activity families.</done>
</task>

<task type="auto">
  <name>Task 3: 4-tab restructure (TODAY rename + HISTORY tab)</name>
  <files>apps/mobile/app/(tabs)/_layout.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/_layout.tsx (current 3-tab config: index Home / log / settings; Settings uses headerShown false + SymbolView glyph pattern)
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md (section 11 tab bar update: 4 tabs, TODAY rename keeps the plate glyph, HISTORY between LOG and SETTINGS with the calendar SymbolView, headerShown false)
  </read_first>
  <action>
    Edit app/(tabs)/_layout.tsx to a 4-tab bar (D-07): (1) rename the index Tabs.Screen title from 'Home'
    to 'TODAY', keep its plate-mark Image icon, and set headerShown false (the TODAY screen renders its own
    in-screen timestamp/greeting header, matching the Settings pattern); (2) keep LOG unchanged (volt
    plus.circle.fill); (3) INSERT a new Tabs.Screen name 'history' between log and settings with title
    'HISTORY', headerShown false, and a SymbolView calendar glyph (ios 'calendar', android/web
    'calendar_month') size 28, same volt/ash active/inactive tint convention; (4) keep SETTINGS unchanged.
    Preserve all existing screenOptions (mono uppercase tabBarLabelStyle, void tabBar/header styling).
    The history route directory (app/(tabs)/history/index.tsx) exists from Task 1, so the new Tabs.Screen
    resolves.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q 'name="history"' 'app/(tabs)/_layout.tsx' && grep -q "TODAY" 'app/(tabs)/_layout.tsx' && grep -q "calendar" 'app/(tabs)/_layout.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - _layout.tsx declares four Tabs.Screen entries in order: index (title TODAY), log, history, settings.
    - The index tab title is TODAY with the plate-mark icon and headerShown false.
    - The new history tab sits between log and settings with a calendar SymbolView and headerShown false.
    - LOG and SETTINGS are otherwise unchanged; the mono uppercase label style is preserved.
  </acceptance_criteria>
  <done>The tab bar is the D-07 four-tab TODAY / LOG / HISTORY / SETTINGS layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| history reads + swipe-delete -> SQLite | Paginated reads + a destructive soft-delete cross into the DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Tampering | history queries + soft-delete | high | mitigate | Parameterized sessionCountsByDate/dayGroupedSessions builders + discardWorkout soft-delete (T-1-01); no raw sql interpolation |
| T-04-17 | Denial of Service | stacked-screen focus rehydrate loop | medium | mitigate | useFocusEffect for all load_daily/workout reads (Pitfall 5) |
| T-04-18 | Repudiation | accidental session deletion | medium | mitigate | Confirm dialog before a whole-session soft-delete (higher-cost than a single-set delete); soft-delete keeps the row recoverable |
</threat_model>

<verification>
- apps/mobile typechecks with no NEW errors from the four files.
- grep confirms the ADJUSTED chip, DOUBLE-DAY math, useFocusEffect, the history tab, and the TODAY rename.
- On-device UAT (phase gate): day-grouped ledger with rest days + month headers; a double-session day shows the ADJUSTED chip and correct +Y math; swipe-delete confirms then removes a session and updates the trend.
</verification>

<success_criteria>
History shows the true day-grouped calendar with correct double-session labeling and swipe-delete, the
session-detail screen works for both types, and the tab bar is the 4-tab D-07 layout (HOME-05/HOME-06).
</success_criteria>

<output>
Create `.planning/phases/04-run-logger-home-dashboard/04-08-SUMMARY.md` when done.
</output>
