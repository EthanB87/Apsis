---
phase: 04-run-logger-home-dashboard
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/components/home/TrendChart.tsx
  - apps/mobile/lib/recomputeLoadDaily.ts
autonomous: true
gap_closure: true
requirements: [HOME-01, HOME-02, HOME-03]

must_haves:
  truths:
    - "Scrubbing the 28-day trend chart does not crash and displays the snapped tooltip (e.g. 'JUL 8 · HSS 142 · ATL 41 · CTL 55 · TSB +14') per D-19/D-20."
    - "Deleting the only session of a day clears that day's load_daily row so TODAY shows the empty/0 readiness state (no ghost HSS/band) per D-29."
    - "Deleting the earliest session leaves no stale load_daily rows outside the recomputed date range."
  artifacts:
    - apps/mobile/components/home/TrendChart.tsx
    - apps/mobile/lib/recomputeLoadDaily.ts
  key_links:
    - "tooltipText useDerivedValue worklet → sign-formatting stays on the UI thread (no captured host-function call)"
    - "recomputeLoadDaily → load_daily DELETE cleanup (empty-set delete + notInArray range-shrink cleanup)"
---

<objective>
Close the two blocking Phase 4 verification gaps (04-VERIFICATION.md, both reproduced from source in 04-REVIEW.md CR-01/CR-02):

1. CR-01 — the 28-day TrendChart scrub tooltip crashes on first touch because a `useDerivedValue` worklet calls a non-worklet module-scope function (`formatSignedTsb`) on the UI thread.
2. CR-02 — `recomputeLoadDaily` is upsert-only and never DELETEs stale `load_daily` rows, so a deleted session's HSS/ATL/CTL/TSB/readinessBand keeps rendering on TODAY (and the earliest-session delete leaves stale rows feeding the trend/history).

Purpose: Restore HOME-03's headline scrub interaction and the HOME-01/HOME-02 "reflects reality" honesty invariant (D-29 soft-delete-then-recompute contract).
Output: A worklet-safe TrendChart tooltip and a delete-aware `recomputeLoadDaily`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-run-logger-home-dashboard/04-VERIFICATION.md
@.planning/phases/04-run-logger-home-dashboard/04-REVIEW.md
@.planning/phases/04-run-logger-home-dashboard/04-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Make the TrendChart scrub tooltip worklet-safe (CR-01)</name>
  <files>apps/mobile/components/home/TrendChart.tsx</files>
  <read_first>
    - apps/mobile/components/home/TrendChart.tsx (see current `formatSignedTsb` at lines 71-74 and its call site inside the `tooltipText` `useDerivedValue` worklet body at line 106)
    - .planning/phases/04-run-logger-home-dashboard/04-REVIEW.md (CR-01 — diagnosed root cause + fix)
    - .planning/phases/04-run-logger-home-dashboard/04-VERIFICATION.md (gap #1 truth + missing item)
  </read_first>
  <action>
    The `tooltipText` `useDerivedValue` at line 100 is marked `'worklet'` and at line 106 calls `formatSignedTsb(point.tsb)`. `formatSignedTsb` (lines 71-74) is an ordinary module-scope function with no `'worklet'` directive, so the react-native-worklets runtime throws "Tried to synchronously call a non-worklet function on the UI thread" on the first scrub (fires once `state.matchedIndex.value >= 0`).

    Fix by adding the `'worklet'` directive as the first statement of the `formatSignedTsb` function body (the exact literal directive string is `'worklet';`), keeping its existing sign-formatting behavior unchanged: round via `Math.round`, return `+N` for positive, the U+2212 minus form for negative (`−` followed by `Math.abs`), and `+0` for zero. Do not alter the returned string format — the D-20 tooltip contract ("… · TSB +14") depends on the leading sign. The alternative accepted form is to inline the identical sign-formatting logic directly inside the `tooltipText` worklet body and remove the standalone helper; prefer the directive approach to keep the single formatter.

    Do not touch the tooltip `translateX` clamping (IN-07 is an out-of-scope follow-up) or any other part of the component.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | grep -v -E "onboarding/review\.tsx|components/ExternalLink\.tsx" | grep -c "error TS" | grep -qx 0 && echo TYPECHECK_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `formatSignedTsb`'s function body begins with the directive `'worklet';` as its first statement (OR `formatSignedTsb` no longer exists and the sign-formatting is inlined inside the `tooltipText` worklet with no captured module-scope formatter).
    - Source assertion: no non-worklet host function is invoked inside the `tooltipText` `useDerivedValue` body (only `Math.round`, `Math.abs`, template literals, and worklet-directive'd helpers).
    - `npx tsc --noEmit` in apps/mobile produces no new errors beyond the two documented pre-existing ones (`app/onboarding/review.tsx`, `components/ExternalLink.tsx`).
    - The tooltip string still renders the signed TSB form (e.g. `TSB +14` / `TSB −6` / `TSB +0`) — the return format is byte-identical to the pre-fix helper.
  </acceptance_criteria>
  <done>Scrubbing the chart no longer calls a non-worklet function on the UI thread; the D-19/D-20 tooltip can render without throwing (on-device confirmation carried in verification notes below).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete stale load_daily rows on recompute (CR-02)</name>
  <files>apps/mobile/lib/recomputeLoadDaily.ts</files>
  <read_first>
    - apps/mobile/lib/recomputeLoadDaily.ts (current upsert-only body; empty-set early return at lines 35-37; upsert loop at 42-66)
    - .planning/phases/04-run-logger-home-dashboard/04-REVIEW.md (CR-02 — diagnosed root cause + fix, and WR-02 note on transactions)
    - .planning/phases/04-run-logger-home-dashboard/04-VERIFICATION.md (gap #2 truth + both missing items)
    - packages/db/src/loadDaily.ts (confirm `computeLoadDailyUpsertRows` returns `[]` for empty input and re-anchors `firstDate` to the earliest remaining session — this is why range-shrink leaves stale rows)
  </read_first>
  <action>
    The recompute is upsert-only, so two delete paths are missing (D-29 requires a discarded session never appears in any HSS/load computation):

    1. Empty-set path: replace the current `if (rows.length === 0) { return; }` early return (lines 35-37) with a full clear — `await database.delete(loadDaily);` — then return. This removes the last deleted session's ghost row so TODAY falls back to the empty/0 state.

    2. Range-shrink cleanup: after computing `upsertRows` (line 40) and before/around the upsert loop, delete every `load_daily` row whose `localDate` is not in the new output. Build the keep-list as `const keep = upsertRows.map((r) => r.localDate);` and run `await database.delete(loadDaily).where(notInArray(loadDaily.localDate, keep));`. Import `notInArray` from `drizzle-orm` by adding it to the existing named import on line 19 (currently `and, isNotNull, isNull`) — it is a standard drizzle-orm 0.45.2 operator.

    Ordering/atomicity: run the `notInArray` cleanup DELETE and the upsert loop together so a reader never sees a partially-cleaned table — wrap the cleanup DELETE plus the upsert loop in a single `await database.transaction(async (tx) => { … })` and issue the deletes/inserts against `tx`. If the op-sqlite/drizzle transaction API surface is uncertain at implementation time, keep the sequential (non-transactional) form but issue the cleanup DELETE immediately before the upsert loop so the window is minimal — do NOT expand this into the broader WR-02 transaction refactor (out of scope).

    Preserve the existing behavior otherwise: the same finished-and-not-deleted filter (`and(isNotNull(workout.finishedAt), isNull(workout.deletedAt))`), the same per-row literal-value upsert (never a raw interpolated `sql` template, never an `excluded` reference — per the file's T-1-01 security note), and the existing catch that console.errors then re-throws (never swallow).

    Do not use a raw interpolated SQL string for the DELETE; use the drizzle `notInArray(loadDaily.localDate, keep)` operator so the keep-list is parameterized.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | grep -v -E "onboarding/review\.tsx|components/ExternalLink\.tsx" | grep -c "error TS" | grep -qx 0 && echo TYPECHECK_CLEAN</automated>
    <automated>cd packages/db && pnpm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `notInArray` is imported from `drizzle-orm` in recomputeLoadDaily.ts.
    - Source assertion: the `rows.length === 0` branch calls `database.delete(loadDaily)` (full clear) instead of a bare `return`.
    - Source assertion: after `computeLoadDailyUpsertRows`, a `database.delete(loadDaily).where(notInArray(loadDaily.localDate, keep))` cleanup exists where `keep` is derived from `upsertRows.map((r) => r.localDate)`.
    - Source assertion: no raw `sql\`…\`` interpolation is used for the DELETE (parameterized `notInArray` only), and the upsert loop still uses per-row literal `set` values.
    - `packages/db` test suite still passes (27 tests) — `computeLoadDailyUpsertRows` empty-input and first-date-anchoring behavior is unchanged; this task only adds the op-sqlite DELETE glue around it.
    - `npx tsc --noEmit` in apps/mobile produces no new errors beyond the two documented pre-existing ones.
    - Note: recomputeLoadDaily itself is not unit-testable under apps/mobile's vitest harness — that harness is scoped to `lib/**` pure modules and must never import `@apsis/db` (opens a native SQLite connection at import time, per vitest.config.mts). Behavioral proof of the delete paths is the on-device regression check below.
  </acceptance_criteria>
  <done>Deleting the only finished session clears load_daily (TODAY shows empty/0); deleting the earliest session removes rows outside the re-anchored range; the D-29 "discarded sessions never appear in any HSS/load computation" invariant holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI thread ↔ JS thread (Reanimated worklet) | Worklet code runs on the UI runtime; capturing/calling a non-worklet host function crosses this boundary and hard-throws. |
| App code → local SQLite (op-sqlite/drizzle) | The only data store; DELETE/UPSERT statements cross here. No network, no remote party (offline-only app). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09-01 | Tampering | recomputeLoadDaily DELETE cleanup | low | mitigate | Use the parameterized drizzle `notInArray(loadDaily.localDate, keep)` operator — never a raw interpolated `sql` template — so the keep-list cannot inject SQL. Preserves the file's existing T-1-01 literal-value discipline. |
| T-04-09-02 | Denial of Service | tooltipText worklet | low | mitigate | Adding the `'worklet'` directive removes the UI-thread exception that otherwise crashes the render on first scrub (availability of the chart). No untrusted input involved. |
| T-04-09-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this plan — both fixes edit existing files only. No supply-chain surface added. |
</threat_model>

<verification>
Automated (run by executor):
- `apps/mobile`: `npx tsc --noEmit` → no new errors beyond the two documented pre-existing route-typing errors.
- `packages/db`: `pnpm test -- --run` → 27 tests pass (pure builder unchanged).
- `apps/mobile`: `npx vitest run` → existing 9 lib tests still pass (no regression).

On-device human verification (carried forward from 04-VERIFICATION.md "Human Verification Required" — these boundaries cannot be exercised under Node/vitest):
1. Scrub tooltip (post CR-01): drag across the 28-day chart — the hairline cursor + tooltip appear and snap to the nearest day without a red screen / crash; TSB shows the signed form.
2. Delete-then-view regression (post CR-02): log a session, confirm it appears on TODAY, delete it via History swipe, return to TODAY — the ring shows 0 / empty state with no stale readiness band.
3. Earliest-session delete: with sessions across several days, delete the earliest — the 28-day trend and History no longer show a stale value for that removed day.
</verification>

<success_criteria>
- TrendChart's `tooltipText` worklet invokes no non-worklet host function (CR-01 closed).
- `recomputeLoadDaily` DELETEs on the empty-set path and cleans up out-of-range rows via `notInArray` (CR-02 closed).
- All existing automated suites pass; typecheck clean except the two documented pre-existing errors.
- HOME-01/HOME-02 "reflects reality" and HOME-03 scrub interaction are unblocked pending the on-device human pass.
</success_criteria>

<artifacts>
## Artifacts this plan produces

Modified:
- `apps/mobile/components/home/TrendChart.tsx`
  - `formatSignedTsb(tsb: number): string` — gains a `'worklet'` directive (signature unchanged) OR is removed and inlined into the `tooltipText` worklet.
- `apps/mobile/lib/recomputeLoadDaily.ts`
  - `recomputeLoadDaily(database: DB): Promise<void>` — signature unchanged; behavior extended to DELETE stale `load_daily` rows (full clear on empty input; `notInArray` cleanup after upsert).
  - New named import `notInArray` from `drizzle-orm`.
</artifacts>

<output>
Create `.planning/phases/04-run-logger-home-dashboard/04-09-SUMMARY.md` when done.
</output>
