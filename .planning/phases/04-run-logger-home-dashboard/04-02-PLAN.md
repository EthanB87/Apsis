---
phase: 04-run-logger-home-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/ios/
autonomous: false
requirements: [RUN-04, HOME-03]
user_setup: []

must_haves:
  truths:
    - "victory-native, @shopify/react-native-skia (pinned exactly 2.6.9), react-native-svg, and @react-native-community/datetimepicker are installed at the versions RESEARCH verified."
    - "The @shopify/react-native-skia package was human-verified as legitimate on npmjs.com before install (flagged SUS by the legitimacy audit)."
    - "The native project has been prebuilt so the four native modules are linked (Metro bundles them without a missing-native-module error)."
  artifacts:
    - apps/mobile/package.json (four new native deps)
  key_links:
    - "@shopify/react-native-skia is pinned to an exact 2.6.9 (no caret) so a mid-phase patch bump can't silently change chart rendering (RESEARCH Pitfall 7)."
    - "victory-native peer range requires Skia <3.0.0 — the exact pin must stay inside >=1.2.3 <3.0.0."
---

<objective>
Install and link the four native modules Phase 4's UI depends on (the trend chart's Skia stack,
the HSS ring's SVG primitive, and the run-form date sheet), gated behind a mandatory human
legitimacy checkpoint for the SUS-flagged Skia package. No product code changes here — this plan
exists solely so the deps/native-link work is isolated from feature logic (and because it carries
a blocking-human checkpoint).

Purpose: De-risk the one supply-chain flag and the native-rebuild step before any consuming UI is
written.
Output: Updated apps/mobile/package.json + a successful prebuild.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md
</context>

<artifacts_produced>
Symbols/files this plan creates (exclude from source-drift verification):
- `apps/mobile/package.json`: new dependencies `victory-native@41.26.0`, `@shopify/react-native-skia` (exact `2.6.9`), `react-native-svg@15.15.5`, `@react-native-community/datetimepicker@9.1.0`
- Prebuilt native iOS project artifacts under `apps/mobile/ios/` (generated)
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Human-verify @shopify/react-native-skia legitimacy (SUS) before install</name>
  <action>Blocking-human supply-chain gate: a human must confirm @shopify/react-native-skia is legitimate on npmjs.com before it is installed. Never auto-approve (workflow.auto_advance is ignored for legitimacy checkpoints). See how-to-verify for the exact steps.</action>
  <what-built>Nothing yet — this is a pre-install supply-chain gate. The legitimacy audit (04-RESEARCH.md) flagged @shopify/react-native-skia [SUS] on the "too-new latest-patch" heuristic (2.6.9 published ~2 weeks before research). The other three packages (victory-native, react-native-svg, @react-native-community/datetimepicker) were audited OK/Approved and need no checkpoint.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@shopify/react-native-skia in a browser.
    2. Confirm it is Shopify's official library (repo github.com/Shopify/react-native-skia), ~1.25M weekly downloads, long multi-major version history — i.e. the SUS flag is a false-positive from frequent patch cadence, not a slopsquat.
    3. Confirm version 2.6.9 exists and is a normal published patch.
    4. Reply "approved" to proceed with the pinned install, or describe any concern to halt.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install the four native deps (Skia pinned exact) and prebuild</name>
  <files>apps/mobile/package.json, apps/mobile/app.json, apps/mobile/ios/</files>
  <read_first>
    - apps/mobile/package.json (current installed versions — RESEARCH confirmed reanimated 4.3.1 and gesture-handler 3.0.2 already satisfy every peer range; do NOT bump them)
    - apps/mobile/AGENTS.md (Expo SDK 56 — read the versioned docs before touching native config)
    - .planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md (Standard Stack table + Installation + Pitfall 7)
  </read_first>
  <action>
    From apps/mobile, install the three expo-resolved native deps plus Skia:
    `npx expo install victory-native react-native-svg @react-native-community/datetimepicker`.
    Then install Skia at an EXACT pin (no caret) so a later `pnpm install` cannot pick up a newer
    patch (RESEARCH Pitfall 7): add `@shopify/react-native-skia` at exactly `2.6.9` — after
    resolution, edit apps/mobile/package.json so the version string is literally `2.6.9` (not
    `^2.6.9` / `~2.6.9`). Confirm the resolved Skia version stays inside victory-native's peer range
    `>=1.2.3 <3.0.0` (do NOT let it resolve to 3.x). Do NOT change react-native-reanimated or
    react-native-gesture-handler versions — the installed 4.3.1 / 3.0.2 already satisfy all peers.
    Run the project's native rebuild (`npx expo prebuild` then the existing dev-build step, per the
    Phase 1 op-sqlite/HealthKit precedent) so the four modules are linked; these are native modules
    and cannot run in Expo Go. If Node emits an Expo-CLI version warning (installed 20.16.0 vs
    requested >=20.19.4, RESEARCH Environment table), note it in the SUMMARY but proceed unless
    prebuild actually fails.
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const p=require('./package.json').dependencies; if(p['@shopify/react-native-skia']!=='2.6.9') throw new Error('skia not pinned exact: '+p['@shopify/react-native-skia']); for(const k of ['victory-native','react-native-svg','@react-native-community/datetimepicker']){ if(!p[k]) throw new Error('missing '+k);} console.log('deps ok');"</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/package.json dependencies include `victory-native`, `react-native-svg`, `@react-native-community/datetimepicker`, and `@shopify/react-native-skia` with the Skia value exactly `2.6.9` (no `^`/`~`).
    - `react-native-reanimated` and `react-native-gesture-handler` versions are unchanged from before this plan.
    - Prebuild completes without a missing-native-module error; the SUMMARY records the exact resolved versions and any Node-version warning.
  </acceptance_criteria>
  <done>All four native deps are installed at the RESEARCH-verified versions (Skia exact-pinned) and the native project is prebuilt/linked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → app bundle | Third-party native code is compiled into the shipped binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | @shopify/react-native-skia (SUS install) | high | mitigate | Blocking-human legitimacy checkpoint on npmjs.com before install + exact 2.6.9 pin (never auto-approved; RESEARCH Pitfall 7) |
| T-04-SC2 | Tampering | victory-native / react-native-svg / datetimepicker | low | accept | Audited OK/Approved (RESEARCH Package Legitimacy Audit) — installed via `npx expo install` at verified versions |
</threat_model>

<verification>
- The legitimacy checkpoint was approved by a human before install.
- package.json shows Skia pinned to exactly 2.6.9 and the other three deps present.
- Prebuild succeeds; app bundles without a missing-native-module error.
</verification>

<success_criteria>
Four native deps installed at verified versions with Skia exact-pinned, human-approved, and linked
via prebuild.
</success_criteria>

<output>
Create `.planning/phases/04-run-logger-home-dashboard/04-02-SUMMARY.md` when done.
</output>
