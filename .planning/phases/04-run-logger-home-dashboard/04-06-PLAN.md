---
phase: 04-run-logger-home-dashboard
plan: 06
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - apps/mobile/app/session/finish.tsx
autonomous: true
requirements: [RUN-06]

must_haves:
  truths:
    - "The finish screen branches on workout.type: strength renders the existing per-exercise summary; endurance renders a per-segment summary and computes session HSS from enduranceSegments (RUN-06)."
    - "Both lift and run finish screens show the 84px HssRing mini-ring counting up the session HSS (D-04)."
    - "The endurance summary line reads distance/pace/duration for run+erg, and duration/HR only for conditioning (D-16)."
    - "Done returns to the TODAY tab (not the Log tab)."
  artifacts:
    - "apps/mobile/app/session/finish.tsx (endurance branch + mini-ring + TODAY routing)"
  key_links:
    - "The mini-ring reuses the HssRing component (04-04) at size 84 - the same ring used on Home."
    - "The endurance HSS is re-derived from SQLite endurance_segment rows (never from a store), matching finish.tsx's existing re-derive-from-truth invariant."
---

<objective>
Retrofit the existing lift-only finish screen so it serves BOTH lifting and running sessions
(D-04/D-14), replacing the bare 40px HSS number with the shared 84px mini-ring and adding an endurance
summary branch that satisfies RUN-06 (session HSS shown on the run finish screen). This is a retrofit
of one existing file, not a new screen.

Purpose: One finish flow for both activity families, tying the finish moment to the Home hero.
Output: A branched, mini-ring finish.tsx.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md
@.planning/phases/04-run-logger-home-dashboard/04-PATTERNS.md
@apps/mobile/app/session/finish.tsx
@.planning/phases/04-run-logger-home-dashboard/04-04-SUMMARY.md
</context>

<artifacts_produced>
Symbols/files this plan creates (exclude from source-drift verification):
- apps/mobile/app/session/finish.tsx: new endurance summary branch (queries endurance_segment, sessionHSSDetailed over enduranceSegments), 84px HssRing mini-ring replacing the bare number, Done routes to TODAY
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Branch finish summary on workout.type + endurance HSS/summary</name>
  <files>apps/mobile/app/session/finish.tsx</files>
  <read_first>
    - apps/mobile/app/session/finish.tsx (current lift-only summary fetch useEffect lines 76-158; re-derive-from-SQLite invariant lines 11-15; Done handler lines 160-172)
    - packages/db/src/schema.ts (endurance_segment columns activityType/distanceM/durationS/avgHr/intensityFactor; workout.type)
    - packages/engine/src/session.ts (sessionHSSDetailed input enduranceSegments of durationS + intensityFactor)
    - packages/shared/src/units.ts (formatPaceMinSec, paceSecPerKmToSecPerMi, km/mi display for the summary line)
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md (section 8 finish retrofit - summary line formats per type)
  </read_first>
  <action>
    Edit the finish.tsx summary-fetch effect to first read workout.type for the workoutId, then branch:
    for type strength (or hybrid) keep the existing strength_set query + per-exercise summary path
    unchanged; for type endurance, query endurance_segment rows for the workoutId and compute session
    HSS via sessionHSSDetailed with enduranceSegments mapped from each row's durationS + intensityFactor,
    then build a summary line from the segment(s): run/erg show distance + pace + duration (pace via
    @apsis/shared, /KM or /MI per profile units for run, /500M for erg); conditioning shows duration +
    AVG HR only (D-16). Keep the re-derive-from-SQLite invariant (never read a store). Preserve the
    existing warnings section (endurance sessions may carry IF/duration warnings from the engine). Read
    profile units via fetchProfileSummary to format the summary line.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "enduranceSegment" app/session/finish.tsx</automated>
  </verify>
  <acceptance_criteria>
    - finish.tsx reads workout.type and keeps the strength path unchanged for strength/hybrid.
    - For endurance workouts it queries endurance_segment and computes HSS via sessionHSSDetailed over the segments.
    - The summary line format matches per-type rules: distance+pace+duration for run/erg, duration+HR for conditioning.
    - Session HSS is derived from SQLite, not from any store.
  </acceptance_criteria>
  <done>The finish screen computes and shows the correct session HSS + summary for endurance sessions (RUN-06) while leaving the lift path intact.</done>
</task>

<task type="auto">
  <name>Task 2: 84px mini-ring + Done-to-TODAY routing</name>
  <files>apps/mobile/app/session/finish.tsx</files>
  <read_first>
    - apps/mobile/app/session/finish.tsx (bare HSS Text at lines 224-226 + styles.hss; handleDone router.replace at line 171; handleConfirmDiscard router.replace at line 186)
    - apps/mobile/components/home/HssRing.tsx (default export, props size/hss/band/animate - size 84 for the mini-ring; animate always true here since a finish screen is a one-time view)
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md (section 8 - mini-ring replaces the bare number, always animates; Done returns to TODAY)
  </read_first>
  <action>
    Replace the bare HSS Text (styles.hss / Typography.display) with the HssRing component sized to 84
    (D-04), passing the computed session hss and animate=true (a finish screen is a one-time view, so
    the D-05 once-per-day gate does not apply here). Since this ring is a single-session HSS and not a
    readiness band, pass a non-calibrating band so the volt arc renders (the session HSS ring is never
    in a calibrating state). Beneath the mini-ring render the per-type summary line built in Task 1.
    Change the Done handler (handleDone) and the discard handler (handleConfirmDiscard) router.replace
    target from '/(tabs)/log' to the TODAY tab route '/(tabs)' (index) so Done lands on TODAY after both
    a lift and a run (UI-SPEC section 8 routing change). Keep the existing warnings section and the
    Discard menu chrome unchanged.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "HssRing" app/session/finish.tsx && grep -q "size={84}\|size: 84\|84" app/session/finish.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The bare Typography.display HSS number is replaced by an 84px HssRing rendering the session HSS with a volt arc and count-up.
    - The per-type summary line renders beneath the mini-ring.
    - handleDone and handleConfirmDiscard route to the TODAY tab route '/(tabs)' instead of '/(tabs)/log'.
    - The warnings section and Discard menu are unchanged.
  </acceptance_criteria>
  <done>Both finish flows show the 84px mini-ring and return to TODAY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite endurance_segment -> finish render | Re-derived summary must come from persisted rows, not a mutable store |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Tampering | endurance summary query | high | mitigate | Parameterized drizzle select by workoutId; no raw sql interpolation (T-1-01) |
| T-04-12 | Information Disclosure | finish summary error path | low | mitigate | Existing console.error + fallback hss=0 pattern retained (no stack trace to user) |
</threat_model>

<verification>
- apps/mobile typechecks with no NEW errors from finish.tsx.
- grep confirms the endurance branch + HssRing at size 84 + TODAY routing.
- On-device UAT (phase gate): after saving a run, the finish screen shows the mini-ring counting up the session HSS and Done lands on TODAY.
</verification>

<success_criteria>
The finish screen serves both lifting and running, shows the 84px session-HSS mini-ring (RUN-06/D-04),
and returns to TODAY.
</success_criteria>

<output>
Create `.planning/phases/04-run-logger-home-dashboard/04-06-SUMMARY.md` when done.
</output>
