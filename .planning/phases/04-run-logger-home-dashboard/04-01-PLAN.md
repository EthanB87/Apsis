---
phase: 04-run-logger-home-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema.ts
  - packages/db/drizzle/
  - packages/db/src/queries.ts
  - packages/db/src/loadDaily.ts
  - packages/db/src/index.ts
  - packages/db/package.json
  - packages/db/src/__tests__/load-daily.test.ts
  - packages/db/src/__tests__/history-queries.test.ts
  - packages/shared/src/duration.ts
  - packages/shared/src/index.ts
  - packages/shared/src/__tests__/duration.test.ts
autonomous: true
requirements: [RUN-02, RUN-05, HOME-03, HOME-04, HOME-05, HOME-06]

must_haves:
  truths:
    - "A finished, non-deleted workout's localDate + hss can be rolled into a per-day HSS that applies the double-session penalty when a day has >1 session."
    - "The load_daily upsert-row builder produces a calendar-contiguous array (rest days = explicit 0) from the first-ever session date through a caller-supplied today, and every row carries dayHss/atl/ctl/tsb/readinessBand from the engine."
    - "The 28-most-recent load_daily rows and per-day session counts are queryable via parameterized builders that never leak soft-deleted workouts."
    - "workout.note (nullable text) exists in the schema and a committed drizzle migration adds it."
    - "parseDurationDigits('4530') yields 45:30 / 2730s and parseDurationDigits('13000') yields 1:30:00 / 5400s."
  artifacts:
    - packages/db/src/schema.ts (workout.note column)
    - packages/db/drizzle/0002_*.sql (generated migration)
    - packages/db/src/loadDaily.ts (pure computeLoadDailyUpsertRows)
    - packages/db/src/queries.ts (last28DaysTrend, sessionCountsByDate, dayGroupedSessions)
    - packages/shared/src/duration.ts (parseDurationDigits)
  key_links:
    - "computeLoadDailyUpsertRows composes @apsis/engine dailyHSS + computeLoadTrendSeries — never a hand-rolled EWMA."
    - "The migration is generated by drizzle-kit and re-exported through packages/db/src/migrations.ts's existing importer, so useMigrations() applies it on app startup."
---

<objective>
Build the pure, unit-testable data foundation the rest of Phase 4 consumes: the `workout.note`
schema column + committed migration (RUN-05), the `load_daily` recompute-row builder and
history query builders (feeding HOME-03/04/05/06), and the smart duration-digit parser
(RUN-02). All logic here is pure/parameterized and covered by vitest in `packages/db` and
`packages/shared` — the two packages that HAVE a test harness (apps/mobile does not).

Purpose: Isolate every piece of Phase-4 logic that can be proven correct off-device into one
foundation plan, so the UI plans downstream are thin consumers.
Output: New/edited files in `packages/db` and `packages/shared`, a generated drizzle
migration, and passing vitest suites.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md
@.planning/phases/04-run-logger-home-dashboard/04-PATTERNS.md
</context>

<artifacts_produced>
Symbols/files this plan creates (exclude from source-drift verification):
- `packages/db/src/schema.ts`: new column `workout.note` (`text('note')`, nullable)
- `packages/db/drizzle/0002_*.sql` + `packages/db/drizzle/meta/0002_snapshot.json` + `_journal.json` entry (generated)
- `packages/db/src/loadDaily.ts` (new): `computeLoadDailyUpsertRows(sessions: { localDate: string; hss: number }[], today: string): LoadDailyUpsertRow[]`; type `LoadDailyUpsertRow`
- `packages/db/src/queries.ts`: new builders `last28DaysTrend(db)`, `sessionCountsByDate(db)`, `dayGroupedSessions(db)`
- `packages/db/src/index.ts`: re-exports for the three new builders + `computeLoadDailyUpsertRows` + `LoadDailyUpsertRow`
- `packages/db/package.json`: adds `@apsis/engine` workspace dependency
- `packages/shared/src/duration.ts` (new): `parseDurationDigits(raw: string): { totalSeconds: number; display: string }`
- `packages/shared/src/index.ts`: `export * from './duration'`
- New test files: `packages/db/src/__tests__/load-daily.test.ts`, `packages/db/src/__tests__/history-queries.test.ts`, `packages/shared/src/__tests__/duration.test.ts`
</artifacts_produced>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1: [BLOCKING] Add workout.note column and generate the committed migration</name>
  <files>packages/db/src/schema.ts, packages/db/drizzle/</files>
  <read_first>
    - packages/db/src/schema.ts (current workout table def, lines 59-72)
    - packages/db/drizzle.config.ts (driver:'expo', out:'./drizzle' — do NOT change)
    - packages/db/src/migrations.ts (how drizzle/migrations.js is re-exported for useMigrations)
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md is NOT needed; RESEARCH.md Assumption A4 (workout.note placement) is the source of truth
  </read_first>
  <action>
    Add a nullable free-text column `note: text('note')` to the `workout` table in
    packages/db/src/schema.ts (RUN-05, per RESEARCH A4 — workout-level, NOT endurance_segment,
    so CONDITIONING sessions with no segment still carry a note). Do not add NOT NULL and do not
    set a default. Then generate the migration by running `npx drizzle-kit generate` from
    `packages/db` (non-interactive; produces the next-numbered `drizzle/0002_*.sql`, a new
    `drizzle/meta/0002_snapshot.json`, and appends `drizzle/meta/_journal.json`). This is the
    mandatory schema-push step: build/type checks pass WITHOUT it because drizzle types come from
    schema.ts, not the live DB — only the generated .sql actually adds the column at app startup
    via the existing `useMigrations` hook. Commit the generated artifacts (drizzle/ is
    append-only — never hand-edit generated files). Verify the new .sql contains an
    `ALTER TABLE ... ADD ... note` statement referencing the workout table.
  </action>
  <verify>
    <automated>cd packages/db && npx drizzle-kit generate && ls drizzle/0002_*.sql && grep -il "note" drizzle/0002_*.sql</automated>
  </verify>
  <acceptance_criteria>
    - `packages/db/src/schema.ts` `workout` table contains `note: text('note')` with no `.notNull()` and no `.default(...)`.
    - A new `packages/db/drizzle/0002_*.sql` file exists and contains an ALTER/ADD statement adding a `note` column to the workout table.
    - `packages/db/drizzle/meta/_journal.json` has a new entry appended (idx 2).
    - Re-running `npx drizzle-kit generate` produces no additional migration (schema and snapshot are in sync — proves the migration captured the change).
  </acceptance_criteria>
  <done>workout.note column exists in schema and in a committed, journal-registered drizzle migration that useMigrations will apply on startup.</done>
</task>

<task type="auto">
  <name>Task 2: load_daily recompute-row builder + history query builders (+ engine dependency)</name>
  <files>packages/db/src/loadDaily.ts, packages/db/src/queries.ts, packages/db/src/index.ts, packages/db/package.json, packages/db/src/__tests__/load-daily.test.ts, packages/db/src/__tests__/history-queries.test.ts</files>
  <read_first>
    - packages/db/src/queries.ts (QueryableDB type + parameterized-builder convention + activeWorkoutFilter, lines 1-70)
    - packages/db/src/__tests__/previous-session-query.test.ts (sqlite-proxy .toSQL() test pattern)
    - packages/engine/src/daily.ts (dailyHSS signature: dailyHSS(sessionScores: number[]) — applies config.doublePenalty when length>1)
    - packages/engine/src/trend.ts (computeLoadTrendSeries(dailyHSSByDay: number[]): LoadTrendPoint[], lines 113-135 — one point per input day, historyDays = i+1)
    - packages/shared/src/index.ts (LoadTrendPoint { atl, ctl, tsb, band }, ReadinessBand union — lines 13, 97-102)
    - packages/db/src/schema.ts (loadDaily columns localDate/dayHss/atl/ctl/tsb/readinessBand; workout.localDate/hss/finishedAt/deletedAt)
    - .planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md (Pattern 1 + Pitfall 2/3 — full-history contiguous gap-fill, never a forward-only delta)
  </read_first>
  <action>
    Add `@apsis/engine` to packages/db/package.json dependencies (workspace, e.g. `"@apsis/engine": "workspace:*"` matching how @apsis/shared is referenced across the monorepo — check an existing package.json for the exact protocol string).

    Create packages/db/src/loadDaily.ts exporting a PURE function
    `computeLoadDailyUpsertRows(sessions: { localDate: string; hss: number }[], today: string): LoadDailyUpsertRow[]`
    where `LoadDailyUpsertRow = { localDate: string; dayHss: number; atl: number; ctl: number; tsb: number; readinessBand: ReadinessBand }`.
    Behavior (RESEARCH Pattern 1, Pitfalls 2 & 3):
      1. Group session hss values by localDate into a Map<string, number[]>.
      2. If sessions is empty, return [].
      3. firstDate = lexicographically smallest localDate key (YYYY-MM-DD sorts correctly as string).
      4. Build a CALENDAR-CONTIGUOUS list of date strings from firstDate through `today` inclusive,
         incrementing one calendar day at a time via a pure local date-string increment helper
         (parse YYYY-MM-DD into y/m/d, construct a Date, add 1 day, re-serialize — keep it pure and
         timezone-stable; today is passed in, never read from the wall clock here).
      5. dailyArray = dates.map(d => dailyHSS(byDate.get(d) ?? []))  — rest days become dailyHSS([]) = 0.
      6. series = computeLoadTrendSeries(dailyArray) (same length).
      7. Return dates.map((localDate, i) => ({ localDate, dayHss: dailyArray[i], atl: series[i].atl,
         ctl: series[i].ctl, tsb: series[i].tsb, readinessBand: series[i].band })).
    Do NOT perform any I/O or DB access here — this is the testable pure core; the op-sqlite
    read+upsert wrapper lives in apps/mobile (Plan 04-03).

    Add three parameterized query builders to packages/db/src/queries.ts (reuse activeWorkoutFilter
    and QueryableDB; import desc/sql/and/isNotNull/isNull from drizzle-orm as the existing builders do):
      - `last28DaysTrend(db)`: select all loadDaily columns, orderBy desc(loadDaily.localDate), limit 28 (caller reverses to chronological). (HOME-03/04)
      - `sessionCountsByDate(db)`: select { localDate: workout.localDate, sessionCount: sql<number>`count(*)`.as('sessionCount') } from workout where and(isNotNull(finishedAt), isNull(deletedAt)) groupBy workout.localDate. (HOME-05/06)
      - `dayGroupedSessions(db)`: select { id, localDate, type, title, hss } from workout where and(isNotNull(finishedAt), activeWorkoutFilter) orderBy desc(localDate), desc(createdAt) — the per-session rows History expands per day. (HOME-05)
    Every builder MUST use drizzle parameterized builders only — no raw sql template literal with
    interpolated user data (T-1-01). The only `sql` fragment permitted is the `count(*)` aggregate
    (no user input).

    Re-export the new builders and `computeLoadDailyUpsertRows` + `LoadDailyUpsertRow` from
    packages/db/src/index.ts alongside the existing query exports.

    Add tests:
      - packages/db/src/__tests__/load-daily.test.ts (pure, no DB): assert gap-filling (a 3-day gap
        between two sessions yields explicit 0 dayHss rows for the missing days), double-session
        penalty (two sessions same day → dayHss > sum of the two raw hss values), first-session-date
        anchoring, empty-input → [], and that the last row's atl/ctl/tsb equal computeLoadTrendSeries
        run over the same contiguous array's final point.
      - packages/db/src/__tests__/history-queries.test.ts (sqlite-proxy .toSQL()): assert
        sessionCountsByDate groups by local_date and filters finished_at IS NOT NULL / deleted_at IS
        NULL, and that last28DaysTrend limits to 28 and orders by local_date desc.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/db test</automated>
  </verify>
  <acceptance_criteria>
    - `computeLoadDailyUpsertRows([{localDate:'2026-07-01',hss:100},{localDate:'2026-07-04',hss:100}], '2026-07-04')` returns 4 rows (Jul 1,2,3,4) with dayHss 100,0,0,100 respectively.
    - Two sessions on the same date produce a dayHss strictly greater than the arithmetic sum of their hss (double-session penalty applied via engine).
    - `computeLoadDailyUpsertRows([], '2026-07-04')` returns `[]`.
    - `pnpm --filter @apsis/db test` passes including the two new test files.
    - `packages/db/package.json` lists `@apsis/engine` as a dependency and loadDaily.ts imports `dailyHSS`, `computeLoadTrendSeries` from `@apsis/engine`.
    - No new query builder contains a raw `sql\`...${userValue}...\`` interpolation; the only sql fragment is `count(*)`.
  </acceptance_criteria>
  <done>The pure load_daily row builder and the three history/trend query builders exist, are re-exported, and pass vitest.</done>
</task>

<task type="auto">
  <name>Task 3: Smart duration-digit parser in packages/shared</name>
  <files>packages/shared/src/duration.ts, packages/shared/src/index.ts, packages/shared/src/__tests__/duration.test.ts</files>
  <read_first>
    - packages/shared/src/units.ts (module-shape convention: small pure exported functions, no React/IO — formatPaceMinSec at line 67 is the closest analog)
    - packages/shared/src/index.ts (barrel — how units is re-exported via `export * from './units'`)
    - packages/shared/src/__tests__/units.test.ts (vitest style for pure shared functions)
    - .planning/phases/04-run-logger-home-dashboard/04-RESEARCH.md (Pattern 2 — reference implementation of parseDurationDigits)
  </read_first>
  <action>
    Create packages/shared/src/duration.ts exporting the pure function
    `parseDurationDigits(raw: string): { totalSeconds: number; display: string }` implementing the
    right-to-left h:mm:ss fill from RESEARCH Pattern 2: strip non-digits, keep the last 6 digits,
    left-pad to 6, split into hh/mm/ss two-digit groups, `totalSeconds = h*3600 + m*60 + s`, and a
    `display` of `H:MM:SS` when h>0 else `M:SS`. Do NOT clamp individual mm/ss groups to 0-59 during
    entry (a momentary "99" self-corrects as more digits arrive — totalSeconds is a flat arithmetic
    sum). Add `export * from './duration'` to packages/shared/src/index.ts.
    Add packages/shared/src/__tests__/duration.test.ts asserting the RESEARCH examples and edges:
    '4530' → { totalSeconds: 2730, display: '45:30' }; '13000' → { totalSeconds: 5400, display: '1:30:00' };
    '' → { totalSeconds: 0, display: '0:00' }; '5' → { totalSeconds: 5, display: '0:05' };
    a 7+ digit input keeps only the last 6 digits.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/shared test</automated>
  </verify>
  <acceptance_criteria>
    - `parseDurationDigits('4530')` deep-equals `{ totalSeconds: 2730, display: '45:30' }`.
    - `parseDurationDigits('13000')` deep-equals `{ totalSeconds: 5400, display: '1:30:00' }`.
    - `parseDurationDigits('')` returns `{ totalSeconds: 0, display: '0:00' }`.
    - `parseDurationDigits` is imported successfully from `@apsis/shared` (barrel export wired).
    - `pnpm --filter @apsis/shared test` passes including the new test file.
  </acceptance_criteria>
  <done>parseDurationDigits is exported from @apsis/shared with passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → SQLite (note, distance, duration, HR, dates) | Free-text and numeric fields cross into persisted queries |
| generated migration → app startup | Migration SQL executes against the local DB via useMigrations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | new query builders (queries.ts) | high | mitigate | Parameterized drizzle builders only; the sole `sql` fragment is `count(*)` (no user input) — extends the existing T-1-01 convention |
| T-04-02 | Tampering | drizzle migration (0002) | medium | mitigate | Migration is drizzle-kit generated, never hand-edited; append-only drizzle/ dir; verified in-sync via a second generate producing no diff |
| T-04-03 | Denial of Service | computeLoadDailyUpsertRows contiguous fill | low | accept | Full-history fold is O(days-since-first-session); acceptable at v1.0 scale (RESEARCH A3); revisit only if on-device UAT shows a stall |
</threat_model>

<verification>
- `pnpm --filter @apsis/db test` and `pnpm --filter @apsis/shared test` both green.
- `npx drizzle-kit generate` (from packages/db) run twice: second run reports no changes.
- `grep` confirms the 0002 migration adds a `note` column.
</verification>

<success_criteria>
workout.note is migrated, the pure load_daily row builder + history query builders are exported and
tested, parseDurationDigits is exported and tested, and all package test suites pass.
</success_criteria>

<output>
Create `.planning/phases/04-run-logger-home-dashboard/04-01-SUMMARY.md` when done.
</output>
