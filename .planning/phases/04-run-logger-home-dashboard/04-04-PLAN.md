---
phase: 04-run-logger-home-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - apps/mobile/constants/theme.ts
  - apps/mobile/components/home/HssRing.tsx
  - apps/mobile/components/home/PlateOrbit.tsx
  - apps/mobile/components/home/ReadinessLight.tsx
  - apps/mobile/components/home/StatTiles.tsx
autonomous: true
requirements: [HOME-01, HOME-02]

must_haves:
  truths:
    - "HssRing renders a react-native-svg stroke ring whose volt arc fill is capped at RING_FILL_REFERENCE_HSS=200 while the displayed count-up number is the exact rounded HSS."
    - "HssRing supports a calibrating variant: entire ring steel, no volt arc, plate-orbit mark inside, BUILDING TREND caption."
    - "HssRing works at both the 200px home size and the 84px finish mini size from a single component."
    - "D-02: ReadinessLight is the status light under the ring — pulsing dot + mono label in the correct semantic color per band (PRIMED · GREEN LIGHT volt / CAUTION · HOLD STEADY amber / OVERREACHING · RED ZONE molten) and renders nothing in the calibrating state (the steel ring + BUILDING TREND · DAY N/14 label owns that space)."
    - "D-21: StatTiles renders ATL · CTL · TSB tiles as bone (not volt) big numbers with mono captions (ATL · ACUTE / CTL · CHRONIC / TSB · BALANCE), TSB always sign-prefixed, and flips to a one-line explainer on tap."
  artifacts:
    - apps/mobile/components/home/HssRing.tsx
    - apps/mobile/components/home/PlateOrbit.tsx
    - apps/mobile/components/home/ReadinessLight.tsx
    - apps/mobile/components/home/StatTiles.tsx
    - "Typography.displayXl role in constants/theme.ts"
  key_links:
    - "HssRing count-up reuses the LiveHssHeader Reanimated withTiming + AnimatedTextInput pattern."
    - "All colors come from constants/Colors.ts (volt/steel/bone/ash/amber/molten) - palette is binding (D-01/D-09)."
---

<objective>
Build the four presentational home components (the signature hero + its parts) as pure, props-driven
RN components with no DB access, so the TODAY screen (04-07) and the finish mini-ring (04-06) both
consume them. Implements the DESIGN-SYSTEM hero per UI-SPEC sections 1-4: the HSS ring (capped fill,
count-up, calibrating variant), the plate-orbit mark, the readiness status light, and the ATL/CTL/TSB
stat tiles. Adds the one justified new typography role (displayXl 60px) for the ring number.

Purpose: Isolate the reusable visual primitives so both consuming screens stay thin.
Output: components/home/*.tsx + one new Typography role.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md
@.planning/phases/04-run-logger-home-dashboard/04-PATTERNS.md
@apps/mobile/components/session/LiveHssHeader.tsx
@apps/mobile/constants/theme.ts
@apps/mobile/constants/Colors.ts
</context>

<artifacts_produced>
Symbols/files this plan creates (exclude from source-drift verification):
- apps/mobile/components/home/HssRing.tsx (new): default export HssRing; prop-driven size/hss/band/calibratingDayN; const RING_FILL_REFERENCE_HSS = 200
- apps/mobile/components/home/PlateOrbit.tsx (new): default export PlateOrbit (nested bone ellipses, ~6s linear rotation)
- apps/mobile/components/home/ReadinessLight.tsx (new): default export ReadinessLight; prop band: ReadinessBand
- apps/mobile/components/home/StatTiles.tsx (new): default export StatTiles; props atl/ctl/tsb
- apps/mobile/constants/theme.ts: new Typography.displayXl role (60px, Archivo_900Black, lineHeight ~54, uppercase)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Typography.displayXl + HssRing (capped fill, count-up, calibrating variant) + PlateOrbit</name>
  <files>apps/mobile/constants/theme.ts, apps/mobile/components/home/HssRing.tsx, apps/mobile/components/home/PlateOrbit.tsx</files>
  <read_first>
    - apps/mobile/constants/theme.ts (existing Typography roles, Mono, tabularNums, Spacing, Radius - add displayXl alongside display without redefining existing sizes)
    - apps/mobile/constants/Colors.ts (volt=accent, steel, bone=text, ash=mutedText - exact token names)
    - apps/mobile/components/session/LiveHssHeader.tsx (Reanimated count-up: addWhitelistedNativeProps, AnimatedTextInput, useSharedValue + withTiming, useAnimatedProps text - reuse this exact mechanism)
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md (section 1 HSS Ring geometry/fill/number/animation; section 2 Plate-orbit; Typography table displayXl row)
  </read_first>
  <action>
    Add a new Typography.displayXl role to constants/theme.ts: 60px, fontFamily Archivo_900Black (no
    fontWeight key, single-face heavy convention), lineHeight ~54, textTransform uppercase. Do NOT
    change any existing role.
    Create components/home/PlateOrbit.tsx: 2-3 nested bone ellipses tilted -30deg, continuous ~6s
    linear infinite rotation (Reanimated), sized to a passed diameter; NO runner glyph (D-08). It
    renders only when used by the ring's calibrating variant.
    Create components/home/HssRing.tsx: a react-native-svg ring. Props: size (200 home | 84 mini),
    hss (number), band (ReadinessBand), and an optional calibratingDayN. Behavior:
      - Track: full steel circle drawn first (strokeWidth 14 at 200px, 8 at 84px, round linecap).
      - Non-calibrating: volt progress arc via stroke-dashoffset with
        fillFraction = Math.min(hss / RING_FILL_REFERENCE_HSS, 1) where RING_FILL_REFERENCE_HSS = 200
        (single exported const). The DISPLAYED number is always the exact Math.round(hss), volt,
        Typography.displayXl at 200px or Typography.heading at 84px, tabularNums, centered, with a
        Mono ash HYBRID STRESS sub-label (inside for 200px; executor discretion inside-vs-below for 84px).
      - Calibrating (band === 'calibrating'): entire ring steel (no volt arc at all), render PlateOrbit
        centered inside, and a Mono ash caption BUILDING TREND then DAY {calibratingDayN}/14 in place
        of HYBRID STRESS - no numeric HSS shown.
      - Count-up: reuse LiveHssHeader's withTiming AnimatedTextInput pattern (500ms ease-out-cubic).
        Accept an optional animate prop (default true) so the parent can gate D-05 once-per-day; when
        false, render the final value instantly.
      - The whole ring is wrapped in a Pressable that calls an optional onPress prop (D-24 ring tap).
    Import Colors + theme via the same relative depth as LiveHssHeader.tsx.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "RING_FILL_REFERENCE_HSS" components/home/HssRing.tsx && grep -q "react-native-svg" components/home/HssRing.tsx && grep -q "displayXl" constants/theme.ts && test -f components/home/PlateOrbit.tsx</automated>
  </verify>
  <acceptance_criteria>
    - constants/theme.ts exports a Typography.displayXl role at fontSize 60 using Archivo_900Black.
    - HssRing.tsx defines RING_FILL_REFERENCE_HSS = 200 and computes fillFraction = Math.min(hss/200, 1); the displayed number is Math.round(hss) (uncapped).
    - When band === 'calibrating', HssRing renders no volt arc and mounts PlateOrbit + a BUILDING TREND / DAY N/14 caption.
    - HssRing renders correctly at size 200 and size 84 from the same component.
    - PlateOrbit contains no runner glyph and rotates continuously.
  </acceptance_criteria>
  <done>The HSS ring (both sizes + calibrating variant) and plate-orbit mark exist and honor the UI-SPEC geometry, fill cap, and palette.</done>
</task>

<task type="auto">
  <name>Task 2: ReadinessLight (pulsing dot + semantic mono label)</name>
  <files>apps/mobile/components/home/ReadinessLight.tsx</files>
  <read_first>
    - apps/mobile/components/session/SetRow.tsx (semantic-color-by-threshold pattern + mono label styling)
    - apps/mobile/constants/Colors.ts (volt/amber/molten token names)
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md (section 3 Readiness status light: exact copy + colors + pulse; Copywriting Contract readiness rows)
  </read_first>
  <action>
    Create components/home/ReadinessLight.tsx. Prop: band (ReadinessBand). Layout: an 8px circular
    dot + 4px gap + a Mono-role label, horizontally centered, ~24px row height. Mapping:
      - green -> dot+label volt, label PRIMED then GREEN LIGHT.
      - amber -> dot+label amber, label CAUTION then HOLD STEADY.
      - red -> dot+label molten, label OVERREACHING then RED ZONE.
      - calibrating -> render null (the space is owned by the ring's internal plate-orbit; D-08).
    The dot pulses opacity 1.0 to 0.5 on a ~1.2s ease-in-out infinite Reanimated loop; do NOT speed
    up the pulse for red (calm-but-honest brand voice). Use the exact copy strings from the UI-SPEC
    Copywriting Contract.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "PRIMED" components/home/ReadinessLight.tsx && grep -q "CAUTION" components/home/ReadinessLight.tsx && grep -q "OVERREACHING" components/home/ReadinessLight.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ReadinessLight maps green/amber/red to volt/amber/molten with the exact PRIMED/CAUTION/OVERREACHING copy.
    - band === 'calibrating' renders null.
    - The dot pulses via a single infinite Reanimated loop at the same speed for all bands.
  </acceptance_criteria>
  <done>ReadinessLight renders the correct semantic dot + label per band and hides during calibration.</done>
</task>

<task type="auto">
  <name>Task 3: StatTiles (ATL/CTL/TSB, bone numbers, tap-to-explain flip)</name>
  <files>apps/mobile/components/home/StatTiles.tsx</files>
  <read_first>
    - apps/mobile/app/session/finish.tsx (label + tabular-nums value row styling; card/hairline conventions)
    - apps/mobile/constants/theme.ts (Radius.md=8 card radius, Spacing, Mono, Typography.heading, tabularNums)
    - .planning/phases/04-run-logger-home-dashboard/04-UI-SPEC.md (section 4 Stat tiles; One-Volt Discipline - numbers are BONE not volt; Copywriting Contract captions + explainer copy)
  </read_first>
  <action>
    Create components/home/StatTiles.tsx. Props: atl, ctl, tsb (numbers). Render three equal-width
    tiles in a row, Spacing.lg gaps, carbon surface, hairline border, Radius.md (8). Each tile: big
    number top using Typography.heading (22px/800), color BONE (NOT volt, per One-Volt Discipline),
    tabularNums; mono ash caption beneath (ATL then ACUTE / CTL then CHRONIC / TSB then BALANCE). TSB
    ALWAYS shows an explicit sign (+14 or the minus form, never a bare number). Tap interaction (D-24):
    tapping a tile cross-fades (~200ms opacity) to show a one-line Label-size explainer in place of the
    number+caption, tap again to flip back. Use the verbatim explainer copy from the UI-SPEC
    Copywriting Contract (ATL 7-day acute load; CTL 28-day chronic load; TSB CTL minus ATL balance).
    Full tile is the tap target.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "ACUTE" components/home/StatTiles.tsx && grep -q "CHRONIC" components/home/StatTiles.tsx && grep -q "BALANCE" components/home/StatTiles.tsx</automated>
  </verify>
  <acceptance_criteria>
    - StatTiles renders three tiles with heading-size BONE numbers (no accent/volt color on the numbers) and mono ash captions ATL/ACUTE, CTL/CHRONIC, TSB/BALANCE.
    - The TSB number is rendered with an explicit leading sign for both positive and negative values.
    - Tapping a tile flips it to the correct verbatim explainer line and back.
  </acceptance_criteria>
  <done>StatTiles renders ATL/CTL/TSB per the UI-SPEC with sign-aware TSB and tap-to-explain flips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentational components; all inputs are already-persisted numbers passed as props |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Information Disclosure | ring/tile rendering | low | accept | Components render only already-computed local HSS/trend numbers; no new data source, no network, no PII beyond what the app already displays |
</threat_model>

<verification>
- apps/mobile typechecks with no NEW errors from the four new component files + theme.ts.
- grep confirms RING_FILL_REFERENCE_HSS, displayXl, and the readiness/stat-tile copy strings.
- On-device UAT (phase gate): ring animates once, calibrating shows plate orbit, readiness light shows correct color, stat tiles flip.
</verification>

<success_criteria>
Four reusable home components exist, are palette-correct and props-driven, and honor the UI-SPEC hero
spec (capped fill, calibrating variant, semantic readiness light, bone stat numbers).
</success_criteria>

<output>
Create `.planning/phases/04-run-logger-home-dashboard/04-04-SUMMARY.md` when done.
</output>
