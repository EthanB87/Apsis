---
phase: 03-onboarding-lifting-logger
plan: 09
type: execute
wave: 4
depends_on: [03-05, 03-02]
files_modified:
  - apps/mobile/app/(tabs)/settings/_layout.tsx
  - apps/mobile/app/(tabs)/settings/index.tsx
  - apps/mobile/hooks/useProfile.ts
  - apps/mobile/lib/settingsStore.ts
autonomous: true
requirements: [ONB-02, ONB-04]
must_haves:
  truths:
    - "The Settings tab shows the profile editor (the same review pattern as onboarding) where the user can view and edit sex, bodyweight, threshold HR, and threshold pace (ONB-02/D-04)."
    - "Profile edits apply to FUTURE calculations only — stored/version-stamped scores are never rewritten (D-05)."
    - "The user can toggle display units (km<->mi, kg<->lb) from Settings; all data stays stored in metric (ONB-04/D-05)."
    - "Settings exposes the global default rest-timer duration; scope is limited to profile editor + units + rest default + about/version footer (D-32)."
  artifacts:
    - apps/mobile/app/(tabs)/settings/index.tsx
    - apps/mobile/hooks/useProfile.ts
  key_links:
    - "Settings profile editor -> ProfileReview (Plan 05) -> useSaveProfile update (edits-forward, D-05)"
    - "units toggle -> user_profile.units -> app-wide display conversion (Plan 01 units.ts)"
    - "rest default -> user_profile.restTimerDefaultSec -> rest timer (Plan 07)"
---

<objective>
Build the Settings tab: a profile editor reusing the onboarding review pattern (ONB-02), a units toggle (ONB-04), the global default rest-timer duration, and an about/version footer.

Purpose: ONB-02 (view/edit profile) and ONB-04 (units toggle) live in Settings. D-05 requires edits to apply forward only (never rewrite stored scores). D-32 bounds the scope. The rest default feeds Plan 07's timer.
Output: A functional Settings tab within the D-32 scope.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md
@.planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md
@apps/mobile/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile read hook + settings store + units-aware display</name>
  <files>apps/mobile/hooks/useProfile.ts, apps/mobile/lib/settingsStore.ts</files>
  <read_first>
    - packages/db/src/schema.ts (userProfile columns incl units + restTimerDefaultSec — Plan 02)
    - packages/db/src/client.ts + index.ts (db singleton + drizzle select — parameterized, T-1-01)
    - apps/mobile/hooks/useSaveProfile.ts (Plan 05 — reuse for the update path; edits-forward D-05)
    - packages/shared/src/units.ts (km/mi, kg/lb display conversions — Plan 01)
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md (D-05 edits apply forward only, stored scores never rewritten; ONB-04 units toggle, storage stays metric)
  </read_first>
  <action>
    Create apps/mobile/hooks/useProfile.ts: reads the single user_profile row via drizzle (parameterized) and exposes the current values + units for display. Editing goes through the existing useSaveProfile update path (Plan 05) — an UPDATE to user_profile that changes future calculations only; it must NOT touch any persisted workout.hss or load_daily rows (D-05 edits-forward; scores are version-stamped per Phase 02 D-06).
    Create apps/mobile/lib/settingsStore.ts (or a thin selector): expose the active `units` preference so any screen can convert metric storage to display (kg/lb, km/mi) via the Plan 01 units helpers; changing units updates user_profile.units and re-renders display without mutating stored metric values.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>useProfile reads the profile row; edits route through useSaveProfile (update, forward-only) and never rewrite stored scores; units preference drives display conversion; typecheck clean.</done>
  <acceptance_criteria>
    - useProfile.ts reads user_profile via a drizzle builder (no raw sql).
    - The edit/update path does not write to workout.hss or load_daily (grep: no update on those tables in this plan).
    - Units change updates user_profile.units only; stored values stay metric.
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Settings screen — profile editor + units toggle + rest default + about footer</name>
  <files>apps/mobile/app/(tabs)/settings/_layout.tsx, apps/mobile/app/(tabs)/settings/index.tsx</files>
  <read_first>
    - apps/mobile/components/onboarding/ProfileReview.tsx (Plan 05 — reuse the review editor for the profile section)
    - apps/mobile/hooks/useProfile.ts + useSaveProfile.ts (Task 1 / Plan 05)
    - apps/mobile/lib/settingsStore.ts (Task 1 units preference)
    - packages/shared/src/units.ts (display conversions)
    - apps/mobile/constants/theme.ts + Colors.ts (dark tokens)
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Typography/Color; reuse review-screen row pattern)
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md (D-32 settings scope: profile editor + units toggle + default rest-timer duration + about/version footer AT MOST; no notification prefs, no data management)
  </read_first>
  <action>
    Create apps/mobile/app/(tabs)/settings/_layout.tsx (Stack for the settings group) and settings/index.tsx: a scrollable Settings screen with (1) a Profile section reusing ProfileReview (tap a field to edit it, saving via the forward-only update path — ONB-02); (2) a Units toggle section (km<->mi and kg<->lb) writing user_profile.units, storage staying metric (ONB-04); (3) a Default Rest Timer control writing user_profile.restTimerDefaultSec (D-25 global default consumed by Plan 07); (4) an about/version footer showing the app + engine version. Do NOT add notification preferences or data-management/export controls (D-32 — those are deferred).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Settings shows profile editor (reused review), units toggle, rest default, and about footer — nothing outside the D-32 scope; typecheck clean.</done>
  <acceptance_criteria>
    - settings/index.tsx renders ProfileReview, a units toggle, a rest-default control, and a version footer.
    - No notification-preference or data-export UI is present (grep: none).
    - Editing a profile field routes through the forward-only save path.
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 09)

New symbols/files created here (exclude from source-drift verification):
- `apps/mobile/hooks/useProfile.ts`
- `apps/mobile/lib/settingsStore.ts` (units preference selector)
- `apps/mobile/app/(tabs)/settings/_layout.tsx`, `settings/index.tsx`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user input -> user_profile update | Profile edits + units + rest default |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-19 | Tampering | profile update rewriting historical scores | low | mitigate | edits apply forward only; update touches user_profile only, never workout.hss/load_daily (D-05, version-stamped scores) |
| T-03-20 | Tampering | profile update injection | low | mitigate | drizzle parameterized update, no raw sql with user values (T-1-01) |
| T-03-08 | Information Disclosure | profile values in logs | low | mitigate | no raw bodyweight/HR logging; generic error strings only (V7) |
</threat_model>

<verification>
- `npx tsc --noEmit` in apps/mobile clean.
- Manual (phase UAT): edit bodyweight in Settings, then log a set — future effective-load uses the new value while past sets are unchanged; toggle units and confirm display flips without changing stored metric.
</verification>

<success_criteria>
- ONB-02 + ONB-04 complete: profile view/edit + units toggle in Settings, within the D-32 scope, edits-forward.
</success_criteria>

<output>
Create `.planning/phases/03-onboarding-lifting-logger/03-09-SUMMARY.md` when done.
</output>
