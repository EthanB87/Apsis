---
phase: 03-onboarding-lifting-logger
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/constants/Colors.ts
  - apps/mobile/constants/theme.ts
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/two.tsx
autonomous: false
requirements: [ONB-03]
user_setup: []
must_haves:
  truths:
    - "The app has a Home / Log / Settings tab bar (D-30) with the dark-only palette from the UI-SPEC."
    - "The Home tab is a branded 'coming soon' placeholder that shows NO readiness band or session list (D-31, ONB-03 structural)."
    - "The four new native-module deps (@gorhom/bottom-sheet, expo-crypto, expo-notifications, expo-haptics) + zustand are installed and vetted at a blocking human checkpoint."
  artifacts:
    - apps/mobile/constants/theme.ts
    - apps/mobile/app/(tabs)/_layout.tsx
  key_links:
    - "package legitimacy checkpoint -> npm install of [ASSUMED]/[SUS] packages"
    - "Colors.ts dark palette -> every screen background/surface token"
---

<objective>
Install and vet this phase's new native-module dependencies, collapse the theme to the dark-only design system defined in the UI-SPEC, and replace the default two-tab Expo template with the real Home / Log / Settings tab bar whose Home tab is a branded placeholder.

Purpose: Every subsequent app plan (onboarding, logger, settings) needs the deps installed, the design tokens defined, and the tab shell present so no navigation rework happens later (D-30). The package-legitimacy checkpoint is mandatory because four of the five packages are [ASSUMED]/[SUS] on the legitimacy gate.
Output: Installed+vetted deps, dark theme tokens, three-tab shell, Home placeholder.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md
@.planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md
@.planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md
@apps/mobile/AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Package legitimacy verification before install</name>
  <files>apps/mobile/package.json</files>
  <what-built>Nothing yet — this checkpoint gates the install of the phase's new packages. Four of them are tagged [ASSUMED] (discovered via WebSearch, not an authoritative lookup) and three are [SUS] on the too-new-publish-date heuristic (RESEARCH Package Legitimacy Audit).</what-built>
  <action>Halt before Task 2's install. Present the five packages and their registry evidence to the human for approval; do not run any npm/expo install until approval is given. This checkpoint is never auto-approvable (a supply-chain gate).</action>
  <how-to-verify>
    Confirm each package is legitimate before install:
    1. @gorhom/bottom-sheet — open https://www.npmjs.com/package/@gorhom/bottom-sheet (expect ~2M+ weekly downloads, repo github.com/gorhom/react-native-bottom-sheet, v5.x).
    2. expo-crypto — https://www.npmjs.com/package/expo-crypto (repo github.com/expo/expo).
    3. expo-notifications — https://www.npmjs.com/package/expo-notifications (repo github.com/expo/expo).
    4. expo-haptics — https://www.npmjs.com/package/expo-haptics (repo github.com/expo/expo).
    5. zustand — https://www.npmjs.com/package/zustand (already project-approved in CLAUDE.md/BUILD.md §3).
    The three expo-* [SUS] flags are almost certainly false positives from Expo's weekly monorepo re-publish cadence (RESEARCH audit). Approve only if the repos + download counts match.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or name any package to hold.</resume-signal>
  <verify>Human types "approved" after confirming repos + download counts for all five packages.</verify>
  <done>All five packages confirmed legitimate (repo + downloads match) and human approval recorded; install may proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Install deps via expo, dark-only theme tokens</name>
  <files>apps/mobile/package.json, apps/mobile/app.json, apps/mobile/constants/Colors.ts, apps/mobile/constants/theme.ts</files>
  <read_first>
    - apps/mobile/package.json (current deps + Expo SDK 56 versions; use `npx expo install` for expo-* + skia-ecosystem alignment, `npm install` for @gorhom/bottom-sheet)
    - apps/mobile/app.json (plugins array — expo-notifications needs a plugin entry)
    - apps/mobile/constants/Colors.ts (existing light/dark scaffold to collapse to dark-only)
    - apps/mobile/AGENTS.md (verify all Expo API shapes at https://docs.expo.dev/versions/v56.0.0/ before writing code)
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Color, Spacing, Typography tables — copy exact hex + token values)
    - .planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md (Installation block + version-verification note: do NOT hand-pin the 57.x tags; let `npx expo install` resolve SDK-56-line versions)
  </read_first>
  <action>
    From apps/mobile run `npx expo install zustand expo-crypto expo-notifications expo-haptics` then `npm install @gorhom/bottom-sheet`. Do not hand-pin expo-* to their 57.x "latest" tags — let expo install resolve the SDK-56-line versions (RESEARCH version note). Add the expo-notifications config plugin to app.json plugins.
    Collapse apps/mobile/constants/Colors.ts to the dark-only palette from the UI-SPEC: background/dominant #0C0E12, secondary surface #181B20, accent #3E8EF7, destructive #E5484D, warning #D9A441, primary text #F2F3F5, muted text #8A8F98, border/divider #2A2E35. Keep the light/dark key shape but resolve both to the same dark values (UI-SPEC theming decision).
    Create apps/mobile/constants/theme.ts exporting the spacing scale (xs 4, sm 8, md 16, lg 24, xl 32, xxl 48, xxxl 64), the 44 hit-target minimum, and typography roles (body 16/400/24, label 13/400/17, heading 20/600/24, display 32/600/35) per the UI-SPEC tables. Numeric UI must set fontVariant ['tabular-nums'] — expose a `tabularNums` style helper.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Deps installed; app.json has the expo-notifications plugin; Colors.ts is dark-only with exact UI-SPEC hex; theme.ts exports spacing + typography tokens; typecheck clean.</done>
  <acceptance_criteria>
    - apps/mobile/package.json dependencies include @gorhom/bottom-sheet, zustand, expo-crypto, expo-notifications, expo-haptics.
    - Colors.ts contains the exact strings `#0C0E12`, `#181B20`, `#3E8EF7`, `#E5484D`, `#D9A441`.
    - theme.ts exports spacing tokens and a 44 hit-target constant.
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Home / Log / Settings tab shell + branded Home placeholder</name>
  <files>apps/mobile/app/(tabs)/_layout.tsx, apps/mobile/app/(tabs)/index.tsx, apps/mobile/app/(tabs)/two.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/_layout.tsx (current two-tab Tabs/Tabs.Screen/tabBarIcon + SymbolView + Colors imports — keep this shape, retarget to three tabs)
    - apps/mobile/app/(tabs)/index.tsx (existing themed Text/View placeholder to rebuild as the Home placeholder)
    - apps/mobile/app/(tabs)/two.tsx (delete this file — replaced by log/settings groups created in later plans)
    - apps/mobile/components/Themed.tsx (themed Text/View convention to reuse)
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Copywriting: Home placeholder points to the Log tab; Color/Typography for the placeholder)
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md (D-30 real tab bar this phase; D-31 Home placeholder shows NO readiness band or session list — ONB-03 structural)
    - apps/mobile/AGENTS.md (verify Tabs API at https://docs.expo.dev/versions/v56.0.0/)
  </read_first>
  <action>
    Replace the two-tab template in (tabs)/_layout.tsx with three Tabs.Screen entries in order: `index` (title Home, SF Symbol e.g. house.fill), `log` (title Log, SF Symbol e.g. plus.circle.fill), `settings` (title Settings, SF Symbol e.g. gearshape.fill). Use the dark Colors accent (#3E8EF7) for tabBarActiveTintColor and the secondary surface (#181B20) for the tab bar background. The `log` and `settings` routes resolve to route groups created in later plans (log/ and settings/ folders) — expo-router will warn until those exist; that is expected within this wave and resolved by Plans 06/09. Remove the headerRight info/modal link.
    Rebuild (tabs)/index.tsx as the Home placeholder (D-31): a branded 'coming soon' screen on the #0C0E12 background with a Heading-size title and a Label-size line inviting the user to the Log tab to start a workout. It must render NO readiness band, NO trend chart, NO session list (that is Phase 4). This structurally satisfies ONB-03 (no readiness ever shown this phase).
    Delete apps/mobile/app/(tabs)/two.tsx.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Three-tab shell (Home/Log/Settings) renders with the dark palette; Home is a placeholder with no readiness UI; two.tsx removed; typecheck clean.</done>
  <acceptance_criteria>
    - (tabs)/_layout.tsx defines exactly three Tabs.Screen with names index, log, settings.
    - (tabs)/index.tsx contains no reference to readiness/band/trend/chart (grep returns nothing).
    - apps/mobile/app/(tabs)/two.tsx no longer exists.
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 03)

New symbols/files created here (exclude from source-drift verification):
- Deps: @gorhom/bottom-sheet, zustand, expo-crypto, expo-notifications, expo-haptics in apps/mobile/package.json
- `apps/mobile/constants/theme.ts` (spacing scale, hit-target constant, typography roles, tabularNums helper)
- Dark-only palette in `apps/mobile/constants/Colors.ts`
- Three-tab shell in `apps/mobile/app/(tabs)/_layout.tsx`; Home placeholder in `(tabs)/index.tsx`
- Removed: `apps/mobile/app/(tabs)/two.tsx`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| npm registry -> app bundle | New third-party/native packages enter the trusted build |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | npm installs (@gorhom/bottom-sheet, expo-crypto/notifications/haptics, zustand) | high | mitigate | blocking-human legitimacy checkpoint (Task 1) verifying repo + downloads for every [ASSUMED]/[SUS] package before install; never auto-approvable |
| T-03-06 | Spoofing | weak primary-key generation | low | mitigate | install expo-crypto now so later plans use its CSPRNG-backed randomUUID, never Math.random() (V6) |
</threat_model>

<verification>
- Legitimacy checkpoint approved before any install.
- `npx tsc --noEmit` in apps/mobile clean.
- Manual: app launches to a dark three-tab shell; Home shows the placeholder only (verified in phase UAT).
</verification>

<success_criteria>
- Deps installed and vetted; dark design tokens defined; three-tab shell present; Home placeholder shows no readiness UI (ONB-03 structural).
</success_criteria>

<output>
Create `.planning/phases/03-onboarding-lifting-logger/03-03-SUMMARY.md` when done.
</output>
