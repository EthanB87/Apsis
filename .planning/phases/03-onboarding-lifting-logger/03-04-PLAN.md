---
phase: 03-onboarding-lifting-logger
plan: 04
type: execute
wave: 2
depends_on: [03-02, 03-03]
files_modified:
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app/onboarding/_layout.tsx
  - apps/mobile/app/onboarding/sex.tsx
  - apps/mobile/app/onboarding/bodyweight.tsx
  - apps/mobile/app/onboarding/units.tsx
  - apps/mobile/hooks/useProfileExists.ts
  - apps/mobile/lib/onboardingDraft.ts
  - apps/mobile/components/onboarding/WizardStep.tsx
  - apps/mobile/components/BootStates.tsx
autonomous: true
requirements: [ONB-01, ONB-03]
must_haves:
  truths:
    - "On first launch with no profile row, the app renders the onboarding wizard and blocks the tabs entirely (D-01, Stack.Protected gate)."
    - "Once a profile row exists, the tabs render and onboarding is unreachable (ONB-03: no readiness ever shown before onboarding completes)."
    - "The wizard captures sex, bodyweight (kg/lb), and unit preference one input per screen with big touch targets + a progress indicator, using soft validation (accept any numeric input, warn out-of-range, never block) (D-01/D-03)."
    - "On relaunch with an unfinished workout row, a resume prompt (Resume / Finish Now / Discard) shows before the tabs (D-14 detection)."
  artifacts:
    - apps/mobile/app/_layout.tsx
    - apps/mobile/app/onboarding/_layout.tsx
    - apps/mobile/hooks/useProfileExists.ts
    - apps/mobile/lib/onboardingDraft.ts
  key_links:
    - "_layout.tsx Stack.Protected guard -> useProfileExists (drizzle profile query)"
    - "_layout.tsx boot -> openWorkout query -> resume prompt (D-14)"
    - "wizard steps -> onboardingDraft store -> review screen (Plan 05)"
---

<objective>
Make onboarding mandatory-before-app via an expo-router `Stack.Protected` gate, scaffold the onboarding wizard stack, build the first input steps (sex, bodyweight, units) with soft validation and a shared step chrome, and add crash/kill resume detection to the boot sequence.

Purpose: D-01 requires onboarding to block the entire app on first launch so all downstream code can assume a profile row exists (no null-profile branches). ONB-03 is satisfied structurally: no readiness UI is reachable until the profile exists. The wizard draft store + shared step component set up Plan 05's threshold steps + review.
Output: Working gate, wizard scaffold, three input steps, resume-prompt detection.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md
@.planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md
@.planning/phases/03-onboarding-lifting-logger/03-PATTERNS.md
@.planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md
@apps/mobile/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding gate + wizard stack scaffold + resume detection</name>
  <files>apps/mobile/app/_layout.tsx, apps/mobile/app/onboarding/_layout.tsx, apps/mobile/hooks/useProfileExists.ts, apps/mobile/components/BootStates.tsx</files>
  <read_first>
    - apps/mobile/app/_layout.tsx (current boot: useMigrations -> seedExercises -> Slot; error/loading gates; preserve this exact structure and the V7/T-1-02 no-raw-error discipline)
    - apps/mobile/components/BootStates.tsx (LoadingScreen/ErrorScreen hardcoded-string pattern; extend with a ResumePrompt in the same no-raw-detail style)
    - packages/db/src/queries.ts (openWorkout builder from Plan 02 for resume detection)
    - packages/db/src/schema.ts (userProfile table; a profile-exists query = count rows)
    - .planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md (Pattern 1 Stack.Protected — Assumption A1: VERIFY exact prop names at docs.expo.dev/router/advanced/protected/ before finalizing; fall back to a nested <Redirect> if the API differs)
    - .planning/phases/03-onboarding-lifting-logger/03-PATTERNS.md (_layout.tsx extend pattern)
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Copywriting: resume prompt "Resume workout from {time}? [Resume] [Finish Now] [Discard]")
  </read_first>
  <action>
    Create apps/mobile/hooks/useProfileExists.ts: a hook that, after migrations succeed, runs a drizzle count query over userProfile and returns 'loading' | true | false. Use the parameterized query builder (T-1-01).
    Extend apps/mobile/app/_layout.tsx: after the existing useMigrations/seedExercises success gate, read useProfileExists and an openWorkout query. Wrap routing in a `<Stack screenOptions={{ headerShown: false }}>` with two `Stack.Protected` groups — guard `!hasProfile` renders `<Stack.Screen name="onboarding" />`, guard `hasProfile` renders `<Stack.Screen name="(tabs)" />` (verify the exact Stack.Protected prop shape against the live docs first per A1; if it differs, use a nested <Redirect> fallback and note the deviation). Keep the existing ErrorScreen/LoadingScreen gates and the console.error-only error discipline. Never log raw bodyweight/HR values (T-03 info-disclosure).
    On boot, if openWorkout returns an unfinished workout row, render the ResumePrompt (new BootStates component) before the tab shell; its buttons: Resume (navigate to the log session route with the workoutId param — that route lands in Plan 06), Finish Now (navigate to finish route — Plan 08), Discard (soft-delete via Plan 02 softDeleteWorkout then continue to tabs). Format {time} as a local 12-hour clock.
    Create apps/mobile/app/onboarding/_layout.tsx: a plain expo-router `<Stack>` (no tab bar, headerShown false) that auto-registers the step screen files. Do NOT import from @react-navigation/* (SDK 56 fork — Pitfall 1).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Root layout gates onboarding vs tabs by profile existence; onboarding stack exists; resume prompt renders on an unfinished workout; typecheck clean; no @react-navigation imports.</done>
  <acceptance_criteria>
    - apps/mobile/app/_layout.tsx references useProfileExists and renders a profile-gated route tree.
    - No `@react-navigation/` import string appears in _layout.tsx or onboarding/_layout.tsx (grep returns nothing).
    - BootStates.tsx exports a ResumePrompt component that renders no raw error/profile detail.
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Shared wizard step chrome + draft store + sex/bodyweight/units steps</name>
  <files>apps/mobile/components/onboarding/WizardStep.tsx, apps/mobile/lib/onboardingDraft.ts, apps/mobile/app/onboarding/sex.tsx, apps/mobile/app/onboarding/bodyweight.tsx, apps/mobile/app/onboarding/units.tsx</files>
  <read_first>
    - apps/mobile/constants/theme.ts (spacing xs..xxxl, typography roles, 44 hit-target, tabularNums — from Plan 03)
    - apps/mobile/constants/Colors.ts (dark palette from Plan 03)
    - packages/shared/src/units.ts (lbToKgExact/kgToDisplayLb for the bodyweight step imperial entry — from Plan 01)
    - packages/shared/src/index.ts (Sex, Units types)
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Wizard step screens spec: one question per screen, Heading question, 3xl top padding, large centered input, progress dots, Accent primary button pinned to bottom safe area; Color: warning #D9A441 for out-of-range notice)
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md (D-01 one input per screen; D-03 soft validation ranges: bodyweight 30-250 kg; D-05 units asked in wizard)
  </read_first>
  <action>
    Create apps/mobile/lib/onboardingDraft.ts: a zustand store (in-memory, NO persist) holding the wizard draft {sex, bodyweightKg, units, thresholdHr, thresholdPaceSecPerKm, and estimate-provenance flags} plus setters and a step-order constant array listing the wizard route names in order (sex, bodyweight, units, threshold-hr, threshold-pace, review) so the progress indicator can compute position. Plan 05 appends its steps' logic against this same draft.
    Create apps/mobile/components/onboarding/WizardStep.tsx: shared step chrome — Heading-size question prop, 3xl top padding, a centered content slot, a progress-dots row computed from the step-order constant + current route, and an Accent primary button pinned to the bottom safe area (label prop, default 'Continue') that runs an onNext callback. All controls >= 44x44.
    Create the three step screens using WizardStep, each writing to onboardingDraft and routing to the next step:
    - sex.tsx: three big segment buttons (male/female/other) -> draft.sex.
    - bodyweight.tsx: numeric entry honoring draft.units (kg direct, or lb via lbToKgExact to store metric per D-05); soft-validation warning (warning color #D9A441, "are you sure?") when the entered kg is outside 30-250, but never block Continue (D-03).
    - units.tsx: km/mi + kg/lb display toggle -> draft.units (ONB-04 capture in wizard; storage stays metric).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Wizard draft store + shared step chrome exist; sex/bodyweight/units steps capture into the draft with soft validation; progress indicator advances; typecheck clean.</done>
  <acceptance_criteria>
    - onboardingDraft.ts exports a zustand store with no persist middleware and a step-order array.
    - bodyweight.tsx shows a warning-colored notice for out-of-range input but still allows Continue.
    - WizardStep primary button and all interactive controls meet the 44 hit-target.
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 04)

New symbols/files created here (exclude from source-drift verification):
- `apps/mobile/hooks/useProfileExists.ts`
- `apps/mobile/lib/onboardingDraft.ts` (zustand draft store + step-order constant)
- `apps/mobile/components/onboarding/WizardStep.tsx`
- `apps/mobile/app/onboarding/_layout.tsx`, `sex.tsx`, `bodyweight.tsx`, `units.tsx`
- `ResumePrompt` component in `apps/mobile/components/BootStates.tsx`
- Stack.Protected onboarding gate in `apps/mobile/app/_layout.tsx`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user input -> profile draft | Sex/bodyweight/units entered in the wizard |
| DB -> boot routing | Profile-exists + open-workout queries decide routing |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering / DoS | onboarding numeric inputs (bodyweight) | low | mitigate | soft validation (accept any numeric, warn out-of-range, never block) mirrors engine clamp-and-warn (D-03/D-15); no crash on odd input |
| T-03-08 | Information Disclosure | profile values in logs/errors | low | mitigate | ErrorScreen renders hardcoded strings only; never console.log raw bodyweight/HR (V7/T-1-02) |
| T-03-09 | Spoofing/Elevation | bypassing onboarding to reach app | low | mitigate | Stack.Protected gate blocks tabs until a profile row exists; onboarding is unreachable once it does (ONB-03) |
</threat_model>

<verification>
- `npx tsc --noEmit` in apps/mobile clean.
- Manual (phase UAT): fresh install shows the wizard, not the tabs; completing later steps reaches the tabs; relaunch with an open workout shows the resume prompt.
</verification>

<success_criteria>
- Onboarding is mandatory and gates the whole app (D-01); ONB-03 satisfied structurally.
- Sex/bodyweight/units captured one-per-screen with soft validation; draft feeds Plan 05.
- Resume detection wired into boot (D-14).
</success_criteria>

<output>
Create `.planning/phases/03-onboarding-lifting-logger/03-04-SUMMARY.md` when done.
</output>
