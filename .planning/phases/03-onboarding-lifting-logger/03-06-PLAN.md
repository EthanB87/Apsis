---
phase: 03-onboarding-lifting-logger
plan: 06
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - apps/mobile/stores/sessionStore.ts
  - apps/mobile/lib/effectiveLoad.ts
  - apps/mobile/lib/commitSet.ts
  - apps/mobile/app/(tabs)/log/_layout.tsx
  - apps/mobile/app/(tabs)/log/index.tsx
  - apps/mobile/app/(tabs)/log/session.tsx
  - apps/mobile/components/session/ExercisePickerSheet.tsx
  - apps/mobile/components/session/SetRow.tsx
  - apps/mobile/components/session/ExerciseCard.tsx
  - apps/mobile/components/session/LiveHssHeader.tsx
  - apps/mobile/components/session/RestTimerBanner.tsx
  - apps/mobile/components/session/HSSBreakdownSheet.tsx
  - apps/mobile/app/session/finish.tsx
autonomous: true
requirements: [LIFT-01, LIFT-02, LIFT-03, LIFT-04, LIFT-06, LIFT-08]
must_haves:
  truths:
    - "The user starts a workout from the Log tab, which creates a workout row (expo-crypto UUID) and opens one scrollable active-session screen of exercise cards (D-22)."
    - "The user finds any seeded exercise in a search-first bottom sheet in under 2 taps (LIFT-01/D-10) and logs a set (load/reps/RPE/warmup) in <= 3 taps (LIFT-02/D-06)."
    - "Each new set clones the previous set; the first set of an exercise pre-fills from that exercise's previous session; RPE is a persistent inline 6-10 quick-row pre-selected to last-used (LIFT-03/LIFT-04/D-07/D-08)."
    - "Checking a set commits it: persist to SQLite, recompute sessionHSSDetailed over ALL committed sets (incl bodyweight effective-load + timed carries), update workout.hss, and tick the live HSS header up (LIFT-08/D-13/D-23)."
    - "The user can add and remove sets inline (LIFT-06/D-11)."
  artifacts:
    - apps/mobile/stores/sessionStore.ts
    - apps/mobile/lib/commitSet.ts
    - apps/mobile/lib/effectiveLoad.ts
    - apps/mobile/app/(tabs)/log/session.tsx
    - apps/mobile/components/session/SetRow.tsx
  key_links:
    - "checkmark -> commitSet -> db insert strength_set -> sessionHSSDetailed(all sets) -> update workout.hss -> sessionStore.liveHss -> LiveHssHeader count-up"
    - "SetRow first set -> previousSessionSet query (Plan 02) -> pre-fill"
    - "bwFactor != null -> effectiveLoad -> estimateE1RMFromRepMaxTable (Plan 01); entryMode timed -> carrySet path"
---

<objective>
Build the working lifting logger: session store, effective-load + commit pipeline, Log-tab entry, exercise picker, set row, exercise card, active-session screen with a live HSS header, and add/remove sets. This is the differentiator — the athlete watches training load accumulate as they log.

Purpose: LIFT-01/02/03/04/06/08 are the core logging loop (search, log a set fast, pre-fill, RPE quick-row, add/remove, live HSS). It must match Strong/Hevy entry speed (existential per CLAUDE.md). This plan also scaffolds stub components (RestTimerBanner, HSSBreakdownSheet, finish) that Plans 07/08 flesh out, so those plans never edit session.tsx (mediated through the store).
Output: A fully functional log-a-workout flow with live HSS, plus mount-point stubs for timer/breakdown/finish.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md
@.planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md
@.planning/phases/03-onboarding-lifting-logger/03-PATTERNS.md
@.planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md
@apps/mobile/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Session store + effective-load + commit pipeline + Log entry</name>
  <files>apps/mobile/stores/sessionStore.ts, apps/mobile/lib/effectiveLoad.ts, apps/mobile/lib/commitSet.ts, apps/mobile/app/(tabs)/log/_layout.tsx, apps/mobile/app/(tabs)/log/index.tsx</files>
  <read_first>
    - .planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md (Pattern 2 persist-then-recompute — write set, re-run sessionHSSDetailed over the FULL accumulated set list, update workout.hss, then update store; Anti-Patterns: no zustand persist/AsyncStorage, DB is the source of truth; Code Examples effective-load §1; use expo-crypto randomUUID, not Math.random)
    - packages/engine/src/index.ts (sessionHSSDetailed, estimateE1RM, estimateE1RMFromRepMaxTable, carryStress — Plan 01 additions)
    - packages/db/src/queries.ts (previousSessionSet, openWorkout — Plan 02) and schema.ts (strength_set columns incl addedLoadKg, durationS; exercise.bwFactor/entryMode)
    - packages/shared/src/index.ts (StrengthSet, CarrySet, SessionInput shapes the engine expects)
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md (D-13 persist-per-set; D-15/D-16/D-17 effective load = bodyweight x bwFactor + added, snapshot at log time; D-19 timed carries)
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Log tab CTA "Start Workout")
  </read_first>
  <action>
    Create apps/mobile/stores/sessionStore.ts: a zustand store (in-memory ONLY, no persist) holding the active workoutId, an ordered list of exercise cards each with their set rows (draft values + committed flag), liveHss, warnings[], and UI flags used by sibling components (restTimerEndsAt, breakdownOpen). Include actions: startSession(workoutId), addExercise, addSet, removeSet, updateSetDraft, setCommitted, setLiveHss, rehydrateFromDb(workoutId). No AsyncStorage — DB is the durable store (D-13/D-14).
    Create apps/mobile/lib/effectiveLoad.ts: `computeEffectiveLoad(exercise: {bwFactor:number|null}, addedKg, profileBodyweightKg)` = bwFactor == null ? addedKg : profileBodyweightKg * bwFactor + addedKg (D-15/D-16, snapshot at log time). Pure.
    Create apps/mobile/lib/commitSet.ts: the persist-then-recompute pipeline (D-13). Given a set draft + its exercise + profile bodyweight: compute effective loadKg (effectiveLoad); compute e1rmKg via estimateE1RMFromRepMaxTable when exercise.bwFactor != null else estimateE1RM (Epley); for entryMode 'timed' build a CarrySet (loadKg effective, bodyweightKg, durationS, rpe) instead. Insert the strength_set row (drizzle, expo-crypto randomUUID for id, store addedLoadKg + durationS where relevant). Then select all committed non-deleted sets for the workout, partition into StrengthSet[] (reps mode) + CarrySet[] (timed mode), call sessionHSSDetailed({strengthSets, carrySets}), update workout.hss, and return {hss, warnings}. Order: persist THEN recompute (never reverse). Parameterized builders only (T-1-01).
    Create apps/mobile/app/(tabs)/log/_layout.tsx (Stack for the log group) and log/index.tsx: the "Start Workout" entry (Accent primary CTA) that creates a workout row (expo-crypto UUID, localDate today, type strength, finishedAt null) and navigates to the session route with the workoutId.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Store, effectiveLoad, commitSet pipeline, and Log entry exist; commit persists before recompute; carries + bodyweight movements route to the correct estimator; typecheck clean.</done>
  <acceptance_criteria>
    - sessionStore.ts uses zustand with no persist middleware (grep: no AsyncStorage import).
    - commitSet.ts calls db insert BEFORE sessionHSSDetailed and recomputes over all committed sets (not an incremental delta).
    - effectiveLoad returns addedKg unchanged when bwFactor is null.
    - Set/workout ids use expo-crypto randomUUID (grep: no Math.random for ids).
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Exercise picker sheet + SetRow (steppers/keypad/RPE/checkmark/W/carry variant)</name>
  <files>apps/mobile/components/session/ExercisePickerSheet.tsx, apps/mobile/components/session/SetRow.tsx</files>
  <read_first>
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Set row spec: 44x44 -/+ steppers, RPE 6·7·8·9·10 segment control Accent on selected, 44x44 checkmark circle outline->Accent-filled, neutral W chip; Exercise picker: auto-focused search + Recents + A-Z by body part; Copywriting empty state "No matches"; effective-load "≈ {value} kg effective")
    - packages/db/src/queries.ts (previousSessionSet for first-set pre-fill — Plan 02)
    - packages/db/src/seed.ts (STARTER_EXERCISES with bodyPart for grouping; local in-memory filter, no per-keystroke query — RESEARCH Architectural Responsibility Map)
    - packages/shared/src/units.ts (kg/lb stepper round-trip: display lb steps by 5, store exact kg via lbToKgExact — D-12)
    - apps/mobile/stores/sessionStore.ts (Task 1 store actions the row calls)
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md (D-06 steppers weight ±2.5kg/±5lb reps ±1, tap number = keypad; D-07 clone prev set, first set from previous session, first-ever blank+keypad focus no fabricated defaults; D-08 RPE inline last-used; D-09 checkmark commits + W chip warmup; D-12 imperial 5lb whole-lb; D-17 BW "+0 kg" added weight + effective shown; D-19 timed carry load+duration+RPE no reps)
  </read_first>
  <action>
    Create apps/mobile/components/session/ExercisePickerSheet.tsx using @gorhom/bottom-sheet v5: auto-focused search TextInput, a Recents/frequents section above the full A-Z list grouped by body-part Label headers, filtering the in-memory seeded exercise list instantly (no query per keystroke). Empty state uses the exact "No matches" copy. Selecting an exercise calls sessionStore.addExercise. All rows/controls >= 44 hit-target.
    Create apps/mobile/components/session/SetRow.tsx: a single horizontal row on the secondary surface with tabular-nums load/reps flanked by 44x44 -/+ steppers (weight ±2.5kg or ±5lb per units, reps ±1), a tap-to-open numeric keypad on the number, an inline RPE 6-10 segment control (Accent on selected, pre-selected to last-used, never modal — D-08/LIFT-04), a leading neutral W chip toggling warmup, and a trailing 44x44 checkmark that commits the set via commitSet (outline #2A2E35 -> Accent-filled + white glyph when checked; unchecking undoes). Pre-fill: a new set clones the previous set's values; the first set of an exercise pulls previousSessionSet; a first-ever exercise shows blank with keypad focus, no fabricated defaults (D-07). For exercises with bwFactor != null render the load field as "+{added} kg" added-weight with a Label "≈ {effective} kg effective" beneath (D-17). For entryMode 'timed' render a load + duration (mm:ss) + RPE variant with no reps field (D-19). Imperial display steps whole lb and stores exact kg (D-12). Render a subtle warning badge slot (populated by Plan 08 via store warnings, D-29).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Picker filters instantly with Recents + A-Z grouping and the exact empty-state copy; SetRow supports steppers/keypad/RPE/checkmark/W, pre-fill, bodyweight effective display, and the timed-carry variant; typecheck clean.</done>
  <acceptance_criteria>
    - ExercisePickerSheet uses @gorhom/bottom-sheet and an in-memory filter (no db query inside the keystroke handler).
    - SetRow RPE control renders 6,7,8,9,10 inline (not in a modal) and all touch targets meet 44.
    - SetRow renders the "≈ {value} kg effective" label for bwFactor != null exercises.
    - A timed-entry exercise renders duration instead of reps.
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Active session screen + live HSS header + add/remove sets + stub mount points</name>
  <files>apps/mobile/app/(tabs)/log/session.tsx, apps/mobile/components/session/ExerciseCard.tsx, apps/mobile/components/session/LiveHssHeader.tsx, apps/mobile/components/session/RestTimerBanner.tsx, apps/mobile/components/session/HSSBreakdownSheet.tsx, apps/mobile/app/session/finish.tsx</files>
  <read_first>
    - apps/mobile/stores/sessionStore.ts + lib/commitSet.ts (Task 1)
    - apps/mobile/components/session/SetRow.tsx + ExercisePickerSheet.tsx (Task 2)
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Sticky header: elapsed time left + live HSS Display size Accent right with Reanimated withTiming ~400-600ms count-up on each checked set, tap opens breakdown; Exercise card: rounded secondary surface, Heading name + Label last-session summary, "+ Add set" ghost row; Copywriting active CTA "Finish")
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md (D-22 one scrollable screen + Add exercise at bottom + sticky header; D-23 count-up tick + tap for breakdown; D-11 add set clones prev / swipe-left delete; D-14 resume rehydrate)
    - apps/mobile/AGENTS.md (verify expo-router + reanimated 4 APIs at docs.expo.dev/versions/v56.0.0/)
  </read_first>
  <action>
    Create apps/mobile/components/session/ExerciseCard.tsx: a secondary-surface rounded card with a Heading name + Label last-session summary header, its SetRows stacked with hairline dividers, and a full-width "+ Add set" ghost row (Accent text) that clones the previous set's values (D-11); support removing a set inline via react-native-gesture-handler swipe-left -> Delete (D-11, LIFT-06) calling sessionStore.removeSet.
    Create apps/mobile/components/session/LiveHssHeader.tsx: a pinned secondary-surface bar with elapsed time (tabular-nums, left) and the live HSS (Display size, Accent, right) animated with a Reanimated withTiming count-up (~400-600ms ease-out) whenever store.liveHss changes (D-23). Tapping the HSS number sets sessionStore.breakdownOpen = true.
    Create apps/mobile/app/(tabs)/log/session.tsx: the active-session screen (D-22) reading the workoutId param. On mount, if the store isn't initialized for this workoutId, call rehydrateFromDb (D-14 resume). Render LiveHssHeader (with a "Finish" button navigating to /session/finish), a scrollable list of ExerciseCards, an "Add exercise" button at the bottom opening ExercisePickerSheet, and MOUNT the three stub components RestTimerBanner + HSSBreakdownSheet + (Finish routes to finish.tsx) so Plans 07/08 flesh out those files WITHOUT editing session.tsx. Wire commitSet results into sessionStore.setLiveHss so the header ticks.
    Create STUB files that later plans own: components/session/RestTimerBanner.tsx (reads store.restTimerEndsAt; for now returns null — Plan 07 implements), components/session/HSSBreakdownSheet.tsx (reads store.breakdownOpen; for now a minimal bottom sheet showing the session total — Plan 08 fleshes per-exercise rollup), and app/session/finish.tsx (reads workout.hss and shows the Display-size session HSS + "Done" — Plan 08 adds per-exercise volume, warnings list, discard menu). Keep the stubs importable and mounted so session.tsx stays owned solely by this plan.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>The active session screen composes cards + set rows + picker with a live HSS count-up header, add/remove sets work, resume rehydrates from the DB, and the timer/breakdown/finish stubs are mounted; typecheck clean.</done>
  <acceptance_criteria>
    - session.tsx renders LiveHssHeader, ExerciseCards, an "Add exercise" opener, and mounts RestTimerBanner + HSSBreakdownSheet.
    - LiveHssHeader animates the HSS via Reanimated withTiming (grep: withTiming present).
    - ExerciseCard supports "+ Add set" (clones prev) and swipe-left delete.
    - session.tsx reads the workoutId param and calls rehydrateFromDb when uninitialized.
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 06)

New symbols/files created here (exclude from source-drift verification):
- `apps/mobile/stores/sessionStore.ts` (zustand active-session store)
- `apps/mobile/lib/effectiveLoad.ts` (computeEffectiveLoad), `apps/mobile/lib/commitSet.ts` (commit pipeline)
- `apps/mobile/app/(tabs)/log/_layout.tsx`, `log/index.tsx`, `log/session.tsx`
- `apps/mobile/components/session/ExercisePickerSheet.tsx`, `SetRow.tsx`, `ExerciseCard.tsx`, `LiveHssHeader.tsx`
- STUB files owned by later plans: `components/session/RestTimerBanner.tsx` (Plan 07), `HSSBreakdownSheet.tsx` (Plan 08), `app/session/finish.tsx` (Plan 08)
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user input -> strength_set / workout | Load/reps/RPE/duration values persisted per set |
| store -> engine -> DB | Recompute and hss write on every commit |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Tampering | per-set drizzle insert / workout.hss update | medium | mitigate | parameterized builders only (T-1-01); recompute via engine (single source of truth) not incremental delta |
| T-03-06 | Spoofing | primary-key generation for set/workout ids | low | mitigate | expo-crypto randomUUID (CSPRNG), never Math.random() (V6) |
| T-03-14 | DoS | malformed numeric set input | low | mitigate | engine clamp-and-warn absorbs bad load/reps/rpe/duration; UI never blocks the logging loop |
</threat_model>

<verification>
- `npx tsc --noEmit` in apps/mobile clean.
- Manual (phase UAT): Start Workout -> add exercise in <2 taps -> log a pre-filled set with 1 tap -> live HSS ticks up -> add/remove sets -> pre-fill from previous session works.
</verification>

<success_criteria>
- LIFT-01/02/03/04/06/08 functional: fast search, <=3-tap set logging, pre-fill, inline RPE quick-row, add/remove sets, live HSS count-up.
- Bodyweight + timed-carry movements score correctly through the engine.
- Stub mount points let Plans 07/08 extend without touching session.tsx.
</success_criteria>

<output>
Create `.planning/phases/03-onboarding-lifting-logger/03-06-SUMMARY.md` when done.
</output>
