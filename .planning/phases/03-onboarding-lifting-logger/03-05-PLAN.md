---
phase: 03-onboarding-lifting-logger
plan: 05
type: execute
wave: 3
depends_on: [03-04]
files_modified:
  - apps/mobile/app/onboarding/threshold-hr.tsx
  - apps/mobile/app/onboarding/threshold-pace.tsx
  - apps/mobile/app/onboarding/review.tsx
  - apps/mobile/lib/thresholdEstimates.ts
  - apps/mobile/components/onboarding/ProfileReview.tsx
  - apps/mobile/hooks/useSaveProfile.ts
autonomous: true
requirements: [ONB-01]
must_haves:
  truths:
    - "The user can set threshold HR and threshold pace by direct entry OR an 'estimate for me' path (race-time picker for pace; max-HR-or-age fallback for HR) (D-02)."
    - "The wizard ends with a review screen showing every captured value (estimates labeled 'estimated'), each row tap-to-edit, then a 'Save & Start Training' button (D-04)."
    - "Saving inserts the user_profile row (drizzle), which flips the Stack.Protected gate to the tabs (ONB-01)."
    - "The review screen is a reusable component so Settings (Plan 09) renders the same editor (D-04/ONB-02)."
  artifacts:
    - apps/mobile/components/onboarding/ProfileReview.tsx
    - apps/mobile/hooks/useSaveProfile.ts
    - apps/mobile/lib/thresholdEstimates.ts
  key_links:
    - "review Save -> useSaveProfile -> insert user_profile -> gate flips to tabs"
    - "estimate paths -> thresholdEstimates helpers -> onboardingDraft"
---

<objective>
Complete the onboarding wizard: threshold HR and threshold pace capture with dual direct/estimate paths, a reusable review-and-save screen, and the profile insert that unlocks the app.

Purpose: These are the remaining ONB-01 inputs (threshold HR + pace) plus the D-04 review pattern that Settings reuses (ONB-02). Saving the profile flips the Plan 04 gate so the tabs become reachable.
Output: Two threshold steps with estimates, a reusable ProfileReview component, a save hook, and the wizard's terminal review screen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md
@.planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md
@.planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md
@apps/mobile/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Threshold estimate helpers + threshold-hr + threshold-pace steps</name>
  <files>apps/mobile/lib/thresholdEstimates.ts, apps/mobile/app/onboarding/threshold-hr.tsx, apps/mobile/app/onboarding/threshold-pace.tsx</files>
  <read_first>
    - apps/mobile/lib/onboardingDraft.ts (draft store + estimate-provenance flags — from Plan 04)
    - apps/mobile/components/onboarding/WizardStep.tsx (shared step chrome to reuse — from Plan 04)
    - packages/shared/src/units.ts (pace sec/km <-> sec/mi for display honoring draft.units)
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md (D-02 dual paths; pace estimate = race-time picker (5k/10k/half + finish time) x ~1.05 conversion; HR estimate = max-HR x 0.90 else (220-age) x 0.90; D-03 soft ranges: HR 100-220, pace 2:30-12:00 /km)
    - .planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md (Assumption A6 race-pace-to-threshold offsets: 5K x~1.03-1.05, 10K x~1.01-1.02, half x~1.0 — Claude's discretion on exact constants)
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Copywriting: "I know my numbers" vs "Estimate for me")
  </read_first>
  <action>
    Create apps/mobile/lib/thresholdEstimates.ts (pure helpers): `thresholdPaceFromRace(distanceKey, finishSec)` where distanceKey in {'5k','10k','half'} maps distance meters, computes race pace sec/km, and applies the threshold offset (5k x1.05, 10k x1.02, half x1.00 — document the chosen constants); `thresholdHrFromMax(maxHr)` returns Math.round(maxHr * 0.90); `thresholdHrFromAge(age)` returns Math.round((220 - age) * 0.90). All pure, no wall-clock.
    Create threshold-hr.tsx using WizardStep: two-path UI — "I know my numbers" direct numeric HR entry, or "Estimate for me" collecting max HR (if known) else age, then computing via the helpers and writing the result + an estimated=true provenance flag into the draft. Soft-validate HR to 100-220 (warn, never block, D-03).
    Create threshold-pace.tsx using WizardStep: "I know my numbers" direct pace entry (mm:ss per km or per mi per draft.units), or "Estimate for me" race-time picker (distance segment + finish-time entry) -> thresholdPaceFromRace -> draft with estimated=true. Soft-validate pace to 2:30-12:00 /km (warn, never block).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Both threshold steps offer direct + estimate paths, write results + provenance to the draft, and soft-validate; estimate math is in pure tested-shape helpers; typecheck clean.</done>
  <acceptance_criteria>
    - thresholdEstimates.ts exports thresholdPaceFromRace, thresholdHrFromMax, thresholdHrFromAge as pure functions (no Date/IO).
    - threshold-hr.tsx and threshold-pace.tsx each render both an "I know my numbers" and an "Estimate for me" path.
    - Out-of-range HR/pace shows a warning-colored notice but Continue still works.
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reusable ProfileReview + save hook + review screen (ONB-01 completion)</name>
  <files>apps/mobile/components/onboarding/ProfileReview.tsx, apps/mobile/hooks/useSaveProfile.ts, apps/mobile/app/onboarding/review.tsx</files>
  <read_first>
    - apps/mobile/lib/onboardingDraft.ts (draft fields + provenance flags to render)
    - packages/db/src/schema.ts (userProfile columns: sex, bodyweightKg, thresholdHr, thresholdPaceSecPerKm, units, restTimerDefaultSec)
    - packages/db/src/client.ts + index.ts (db singleton + drizzle insert; parameterized builder only, T-1-01)
    - apps/mobile/components/BootStates.tsx (no-raw-error discipline for the save-failure path)
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Review screen spec: Label-caption + Body-value rows, tap-to-edit jumps to the step, "estimated" tag; Copywriting: primary CTA "Save & Start Training", error "Couldn't save your profile. Check available storage and try again.")
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md (D-04 review + tap-to-edit + Save; reused as Settings editor ONB-02)
  </read_first>
  <action>
    Create apps/mobile/components/onboarding/ProfileReview.tsx: a reusable scrollable list of Label-caption + Body-value rows (sex, bodyweight in display units, threshold HR, threshold pace in display units, units) driven by props (values + onEditField callback + primary button label + onSubmit). Estimated values render a small Label-styled "estimated" tag (D-04, honest-number thesis). Each row is tappable to invoke onEditField(field). This same component backs the Settings editor (Plan 09), so keep it presentational — no direct DB/router coupling inside it.
    Create apps/mobile/hooks/useSaveProfile.ts: takes the draft values and inserts (or updates, for Settings) the user_profile row via drizzle (parameterized, T-1-01), storing bodyweight/pace in metric (D-05). On failure, console.error the raw error only and surface the hardcoded UI-SPEC error string; never render raw error detail (V7/T-1-02). Never log raw bodyweight/HR values.
    Create apps/mobile/app/onboarding/review.tsx: renders ProfileReview with the draft, wires onEditField to router.push back to the relevant wizard step, and onSubmit to useSaveProfile with the "Save & Start Training" label. A successful insert makes useProfileExists return true, flipping the Plan 04 gate to the tabs (ONB-01 complete).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Review screen shows all captured values with estimated tags + tap-to-edit; Save inserts the profile row and unlocks the tabs; ProfileReview is reusable by Settings; save failure shows the generic string; typecheck clean.</done>
  <acceptance_criteria>
    - ProfileReview.tsx is presentational (takes values + callbacks as props; no db/router import inside it).
    - useSaveProfile inserts user_profile via a drizzle builder (no raw sql with user values) and stores metric.
    - review.tsx primary button label is exactly "Save & Start Training".
    - Save-failure path renders the exact UI-SPEC error string and console.errors the raw error only.
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 05)

New symbols/files created here (exclude from source-drift verification):
- `apps/mobile/lib/thresholdEstimates.ts` (thresholdPaceFromRace, thresholdHrFromMax, thresholdHrFromAge)
- `apps/mobile/components/onboarding/ProfileReview.tsx` (reusable review editor, also used by Settings)
- `apps/mobile/hooks/useSaveProfile.ts`
- `apps/mobile/app/onboarding/threshold-hr.tsx`, `threshold-pace.tsx`, `review.tsx`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user input -> user_profile | Threshold HR/pace + estimate-derived values persisted |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Tampering | threshold numeric inputs / estimate math | low | mitigate | soft validation warn-not-block (D-03); estimate helpers are pure and clamp implicitly via UI ranges |
| T-03-11 | Information Disclosure | profile save error leaking storage paths | low | mitigate | generic UI-SPEC error string only; raw error console.error'd, never rendered (V7/T-1-02) |
| T-03-12 | Tampering | profile insert | low | mitigate | drizzle parameterized insert, no raw sql interpolation (T-1-01) |
</threat_model>

<verification>
- `npx tsc --noEmit` in apps/mobile clean.
- Manual (phase UAT): complete the full wizard incl estimates; verify a user_profile row is written and the app advances to the tabs; verify Settings later shows the same values.
</verification>

<success_criteria>
- ONB-01 complete: sex + bodyweight + threshold HR + threshold pace captured (direct or estimated) and saved.
- Review/save is reusable for ONB-02 Settings.
- Saving unlocks the app (gate flips).
</success_criteria>

<output>
Create `.planning/phases/03-onboarding-lifting-logger/03-05-SUMMARY.md` when done.
</output>
