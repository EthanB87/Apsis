---
phase: 03-onboarding-lifting-logger
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema.ts
  - packages/db/src/seed.ts
  - packages/db/src/queries.ts
  - packages/db/src/index.ts
  - packages/db/drizzle/
  - packages/db/src/__tests__/previous-session-query.test.ts
  - packages/db/src/__tests__/soft-delete.test.ts
  - packages/db/src/__tests__/seed.test.ts
autonomous: true
requirements: [LIFT-03, LIFT-07]
must_haves:
  truths:
    - "The exercise table carries bwFactor + entryMode + restTimerSec per movement, seeded with literature-grounded values (D-15/D-21)."
    - "A workout can be marked finished (finishedAt) and soft-deleted (deletedAt); a soft-deleted workout is excluded from every load/HSS query (D-14/D-28)."
    - "A query returns an exercise's most recent prior committed set for previous-session pre-fill (LIFT-03/D-07)."
    - "New migration .sql is generated, committed, and applied by the existing useMigrations pipeline."
  artifacts:
    - packages/db/src/queries.ts
    - packages/db/drizzle/ (new numbered migration + meta snapshot)
  key_links:
    - "seedExercises -> STARTER_EXERCISES rows now carry bwFactor/entryMode/restTimerSec"
    - "every active-workout read -> activeWorkoutFilter (isNull(deletedAt))"
---

<objective>
Extend the drizzle schema for Phase 3 (per-exercise bwFactor/entryMode/restTimerSec, workout finishedAt/deletedAt, per-set addedLoadKg/durationS, profile restTimerDefaultSec), seed the new per-exercise metadata, add reusable parameterized query builders (previous-session pre-fill + soft-delete filtering), and generate + commit the migration so the app applies it on boot.

Purpose: The lifting logger's pre-fill (LIFT-03), soft-delete discard (LIFT-07/D-28), crash recovery (D-14), bodyweight/carry logging (D-15/D-19/D-21), and rest-timer defaults (D-25) all need durable columns. Types compile without the migration, so a [BLOCKING] migration-generation task is mandatory or the phase passes verification against a schema the device never received.
Output: Migrated schema, seeded metadata, tested query builders, committed migration files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md
@.planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md
@.planning/phases/03-onboarding-lifting-logger/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend schema + seed the new per-exercise metadata</name>
  <files>packages/db/src/schema.ts, packages/db/src/seed.ts, packages/db/src/__tests__/seed.test.ts</files>
  <read_first>
    - packages/db/src/schema.ts (six tables; add columns following the existing sqliteTable/real/integer/text patterns)
    - packages/db/src/seed.ts (STARTER_EXERCISES array + idempotent count-before-insert seeder — extend rows, keep seeder unchanged)
    - packages/db/src/__tests__/seed.test.ts (extend assertions for the new fields)
    - .planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md (Open Questions: the full bwFactor/entryMode table for every seeded movement; Pitfall 5: leave endurance rows null)
    - .planning/phases/03-onboarding-lifting-logger/03-PATTERNS.md (schema column-add pattern; nullable, no non-constant default = safe ALTER TABLE ADD COLUMN)
  </read_first>
  <action>
    Add nullable columns to the `exercise` table: `bwFactor: real('bw_factor')` (null = not a bodyweight movement), `entryMode: text('entry_mode', { enum: ['reps', 'timed'] })`, `restTimerSec: integer('rest_timer_sec')` (per-exercise rest override, null = use global default, D-25).
    Add nullable columns to the `workout` table: `finishedAt: integer('finished_at', { mode: 'timestamp' })` (null = open session, D-14) and `deletedAt: integer('deleted_at', { mode: 'timestamp' })` (null = active, D-28).
    Add nullable columns to the `strength_set` table: `addedLoadKg: real('added_load_kg')` (user-entered added weight for bodyweight/implement movements, D-17; null for barbell lifts) and `durationS: integer('duration_s')` (for timed carry/sled sets, D-19; null for rep sets). Keep `reps` NOT NULL — timed carry sets store reps=0 (avoids a NOT-NULL-drop table recreation).
    Add to the `user_profile` table: `restTimerDefaultSec: integer('rest_timer_default_sec').default(120)` (global default rest, D-25/D-32).
    Update STARTER_EXERCISES in seed.ts with bwFactor + entryMode values from the RESEARCH proposed table: pull-up 0.95/reps, chin-up 0.95/reps, dip 0.95/reps, push-up 0.65/reps, lunge 0.85/reps, split-squat 0.85/reps, step-up 0.90/reps, box-step-over 0.90/reps, box-jump 1.0/reps, burpee-broad-jump 1.0/reps, wall-ball 0.30/reps, kb-swing 0.30/reps, sandbag-lunge 0.85/reps; farmers-carry / yoke-carry / sled-push / sled-pull entryMode 'timed' with bwFactor null (D-19 carry path); all barbell/machine lifts (squat, deadlift, bench, ohp, row, etc.), goblet-squat, thruster, atlas-stone → bwFactor null, entryMode 'reps'; plank/battle-rope entryMode 'timed' bwFactor null; ab-wheel/hanging-leg-raise entryMode 'reps' bwFactor null; all `type: 'endurance'` rows (run, ski-erg, rowing-erg, assault-bike) → bwFactor null, entryMode null (Pitfall 5, not logged as strength this phase). Leave `restTimerSec` unset (null) for all seed rows.
    Extend seed.test.ts: assert every bodyweight movement listed above has a numeric bwFactor, assert every timed movement has entryMode 'timed', assert endurance rows have entryMode null.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/db test</automated>
  </verify>
  <done>Schema has all new nullable columns; STARTER_EXERCISES carries bwFactor/entryMode per the table; seed.test.ts green.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/db test` exits 0.
    - seed.test.ts asserts the pull-up row has bwFactor 0.95 and entryMode 'reps'.
    - seed.test.ts asserts the farmers-carry row has entryMode 'timed' and bwFactor null.
    - workout table definition contains finished_at and deleted_at columns.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reusable parameterized query builders (previous-session prefill + soft-delete filter)</name>
  <files>packages/db/src/queries.ts, packages/db/src/index.ts, packages/db/src/__tests__/previous-session-query.test.ts, packages/db/src/__tests__/soft-delete.test.ts</files>
  <read_first>
    - packages/db/src/schema.ts (table refs after Task 1)
    - packages/db/src/client.ts (NOTE: client.ts is NOT imported in vitest — op-sqlite JSI needs a device; tests must NOT open the real db)
    - packages/db/src/index.ts (public barrel; add queries exports)
    - packages/db/src/__tests__/seed.test.ts (vitest structure; assertions run in node)
    - .planning/phases/03-onboarding-lifting-logger/03-PATTERNS.md (Parameterized drizzle queries only T-1-01; Idempotent/soft-delete filtering — every read filters deletedAt IS NULL)
    - .planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md (Validation Architecture: these are the LIFT-03 + LIFT-07 test files)
  </read_first>
  <action>
    Create packages/db/src/queries.ts exporting pure drizzle query-builder factories that take the `db` (DB type) instance as a parameter (so they are testable via `.toSQL()` without opening op-sqlite):
    (a) `activeWorkoutFilter` — an exported SQL condition `isNull(workout.deletedAt)` reused everywhere; and `selectActiveWorkouts(db)` builder that applies it.
    (b) `previousSessionSet(db, exerciseId)` — selects the most recent committed strength_set for that exerciseId belonging to a finished, non-deleted workout, ordered by workout.finishedAt (or createdAt) desc, limit 1, returning loadKg/addedLoadKg/reps/rpe/durationS for pre-fill (LIFT-03/D-07). Use drizzle's eq/and/isNull/isNotNull/desc — never raw sql string interpolation with user values (T-1-01).
    (c) `openWorkout(db)` — selects a workout with finishedAt IS NULL and deletedAt IS NULL, limit 1 (D-14 crash recovery).
    (d) `softDeleteWorkout(db, workoutId)` — update setting deletedAt to a caller-passed timestamp where id = workoutId (D-28; timestamp passed in, never Date.now() inside a reusable builder).
    Export all from index.ts.
    Create previous-session-query.test.ts: build previousSessionSet(mockDb, 'squat') and assert its `.toSQL()` output contains the exercise_id parameter binding, an `is null` on deleted_at, an order-by desc, and a limit of 1. Use a lightweight drizzle instance for SQL generation only (drizzle's query builder `.toSQL()` needs no live connection).
    Create soft-delete.test.ts: assert selectActiveWorkouts(mockDb).toSQL() and previousSessionSet(...).toSQL() both include `deleted_at` `is null`; assert softDeleteWorkout(mockDb, id).toSQL() sets deleted_at and binds the id parameter (no literal interpolation).
  </action>
  <verify>
    <automated>pnpm --filter @apsis/db test</automated>
  </verify>
  <done>queries.ts exposes previousSessionSet, openWorkout, selectActiveWorkouts, softDeleteWorkout, activeWorkoutFilter; both new test files green; every workout read filters deletedAt IS NULL.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/db test` exits 0.
    - soft-delete.test.ts asserts selectActiveWorkouts SQL contains `deleted_at` and `is null`.
    - previous-session-query.test.ts asserts the generated SQL has `limit ?`/limit 1 and an order-by desc.
    - No raw `sql\`...\`` template with an interpolated user value appears in queries.ts (grep: parameterized builders only).
  </acceptance_criteria>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Generate + commit the drizzle migration and confirm the boot pipeline applies it</name>
  <files>packages/db/drizzle/, packages/db/src/index.ts</files>
  <read_first>
    - packages/db/drizzle.config.ts (driver MUST stay 'expo' — Pitfall 4/RESEARCH Pitfall 5; do not change it)
    - packages/db/src/migrations.ts (re-exports generated drizzle/migrations; never hand-edit generated files)
    - packages/db/drizzle/0000_mushy_satana.sql (existing migration format to expect a sibling of)
    - packages/db/drizzle/meta/ (journal + snapshot the generator appends to)
    - .planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md (Pitfall 4: wrong driver silently no-ops; verify the new .sql contents before commit)
  </read_first>
  <action>
    This task runs AFTER Tasks 1-2 (all schema.ts edits complete). From packages/db run `npx drizzle-kit generate`. This must produce a new numbered migration (e.g. 0001_*.sql) containing ALTER TABLE ADD COLUMN statements for: exercise.bw_factor, exercise.entry_mode, exercise.rest_timer_sec, workout.finished_at, workout.deleted_at, strength_set.added_load_kg, strength_set.duration_s, user_profile.rest_timer_default_sec. Open the generated .sql and confirm it contains those ADD COLUMN statements and NO destructive table-recreation (all new columns are nullable / constant-default, so plain ADD COLUMN is expected). Confirm drizzle/meta/_journal.json gained the new entry and a new snapshot file exists. Do NOT hand-edit any generated file. The existing useMigrations(db, migrations) call in apps/mobile/app/_layout.tsx + the migrations re-export in src/migrations.ts already wire these into the boot sequence — no code change needed there; confirm migrations.ts still imports `../drizzle/migrations`.
    Commit the generated migration + meta snapshots together with the schema change.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/db test</automated>
  </verify>
  <done>A new numbered migration .sql exists with the 8 ADD COLUMN statements, meta journal updated, db test suite green, migration is append-only (0000 untouched).</done>
  <acceptance_criteria>
    - A new `packages/db/drizzle/0001_*.sql` (or next number) file exists and contains `ADD COLUMN` for `bw_factor`, `entry_mode`, `finished_at`, `deleted_at`, `added_load_kg`, `duration_s`, `rest_timer_default_sec`, `rest_timer_sec`.
    - `packages/db/drizzle/meta/_journal.json` references the new migration.
    - `packages/db/drizzle/0000_mushy_satana.sql` is byte-for-byte unchanged.
    - `pnpm --filter @apsis/db test` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 02)

New symbols/files created here (exclude from source-drift verification):
- Columns: `exercise.bwFactor`, `exercise.entryMode`, `exercise.restTimerSec`, `workout.finishedAt`, `workout.deletedAt`, `strength_set.addedLoadKg`, `strength_set.durationS`, `user_profile.restTimerDefaultSec`
- `packages/db/src/queries.ts` exporting `activeWorkoutFilter`, `selectActiveWorkouts`, `previousSessionSet`, `openWorkout`, `softDeleteWorkout`
- New numbered drizzle migration `.sql` + meta snapshot under `packages/db/drizzle/`
- Tests: `previous-session-query.test.ts`, `soft-delete.test.ts`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| app -> SQLite | User-entered exercise ids, workout ids, and set values reach the DB via drizzle builders |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | queries.ts (previous-session, soft-delete, open-workout) | medium | mitigate | drizzle parameterized query builders only — eq/and/isNull, never raw sql interpolation with user values (T-1-01); tests assert parameter binding in generated SQL |
| T-03-04 | Tampering | drizzle migration integrity | medium | mitigate | append-only migrations, driver stays 'expo', generated files never hand-edited, new .sql contents reviewed before commit (Pitfall 4) |
| T-03-05 | Information Disclosure | soft-deleted workouts leaking into trend/history | low | mitigate | activeWorkoutFilter (isNull deletedAt) centralized and applied to every workout read (D-28) |
</threat_model>

<verification>
- `pnpm --filter @apsis/db test` green (seed + both new query test files).
- New migration `.sql` inspected: only ADD COLUMN, no table recreation.
- `pnpm -r build` succeeds — schema types compile.
</verification>

<success_criteria>
- Schema, seed, and query builders support pre-fill, soft-delete, crash recovery, bodyweight/carry logging, and rest defaults.
- Migration generated, reviewed, and committed; boot pipeline unchanged and still valid.
- Every workout read path filters deletedAt IS NULL via a shared helper.
</success_criteria>

<output>
Create `.planning/phases/03-onboarding-lifting-logger/03-02-SUMMARY.md` when done.
</output>
