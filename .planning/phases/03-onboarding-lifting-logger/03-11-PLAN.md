---
phase: 03-onboarding-lifting-logger
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/constants/theme.ts
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/** (Spacing token remap sweep — every file consuming Spacing tokens)
  - apps/mobile/components/** (Spacing token remap sweep — every file consuming Spacing tokens)
autonomous: true
requirements: [ONB-01, ONB-02, LIFT-02]
must_haves:
  truths:
    - "theme.ts spacing tokens match the DESIGN-SYSTEM.md contract scale (xs 4 · sm 8 · md 12 · lg 16 · xl 24 · then 32/48/64), and every pre-existing usage still renders the SAME pixel value it did before (pure rename, zero visual drift)."
    - "Typography matches the contract: heading (H3) ~22, title (H2) ~30, uppercase mono labels tracked toward 0.2em (up from ~0.08em), and no fontWeight key on the single-face heavy Archivo roles."
    - "The existing Home/Log/Settings tab bar conforms: mono uppercase labels, active volt / inactive ash, void background, hairline top border."
    - "Text fields on existing Phase-3 surfaces show a volt border on focus; existing segmented-style controls (units toggle, wizard selectors) follow the contract's steel-track/active-fill pattern."
    - "No Phase-4 surface (HSS ring, run logger, trends chart, TODAY/TRENDS/HISTORY tabs) and no paywall is built — DESIGN-SYSTEM.md's Phase-applicability notes are respected."
  artifacts:
    - apps/mobile/constants/theme.ts
    - apps/mobile/app/(tabs)/_layout.tsx
  key_links:
    - "ordered token rename sweep (largest-first) -> every old usage maps to the new name with the same value -> zero rendered-layout change"
    - "new Spacing.md=12 token -> consumed by Plan 03-10's SetRow/ExerciseCard geometry (03-10 depends on this plan)"
    - "theme.ts tokens -> all components (DESIGN-SYSTEM.md hard rule: no hardcoded colors/fonts/spacing/radii)"
---

<!-- planner-discipline-allow: Spacing.md -->
<!-- planner-discipline-allow: fontWeight -->

<objective>
Bring the Phase-3 token layer and existing Phase-3 surfaces into conformance with DESIGN-SYSTEM.md (repo root, commit d8df06a) — the user-provided authoritative design contract that supersedes 03-UI-SPEC.md where they conflict.

Three deltas: (1) the spacing scale changes shape (the contract inserts md=12 and shifts every name down one slot: contract lg=16 where the current md=16, etc.) — a mechanical rename sweep that must keep every rendered value identical; (2) typography moves to the contract ladder (H3 22, H2 30, mono tracking 0.2em+) and drops the redundant fontWeight on single-face heavy Archivo roles; (3) the existing tab bar, text-field focus states, and segmented-style controls adopt the contract patterns — only on surfaces that already exist in Phase 3 (per DESIGN-SYSTEM.md's own Phase-applicability notes: no HSS ring, no run logger, no trends chart, no paywall).

Purpose: Plan 03-10 (the UAT Test 9 set-row fix) builds its geometry on the contract's md=12 token, and the user explicitly directed "update the UI as part of execute phase 3" per this contract.
Output: contract-conformant theme.ts with all consumers remapped 1:1, conformant tab bar and input states on existing screens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN-SYSTEM.md
@.planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md
@apps/mobile/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract spacing scale + ordered token remap sweep (zero visual drift)</name>
  <files>apps/mobile/constants/theme.ts, apps/mobile/app/** and apps/mobile/components/** (every Spacing consumer)</files>
  <read_first>
    - DESIGN-SYSTEM.md §3 (spacing base-4 scale: xs 4 · sm 8 · md 12 · lg 16 · xl 24 · 2xl 32 · 3xl 48 · 4xl 64; radius btn 6 · card 8 · sheet 10 · pill 9999; hairline borders 1px)
    - apps/mobile/constants/theme.ts (current Spacing: xs4 sm8 md16 lg24 xl32 xxl48 xxxl64; HAIRLINE_WIDTH=2; Radius pill:100)
    - Output of `grep -rn "Spacing\." apps/mobile/app apps/mobile/components apps/mobile/hooks apps/mobile/lib` (the full consumer inventory — enumerate before renaming)
  </read_first>
  <action>
    Adopt the contract spacing scale in theme.ts and remap ALL existing usages so every rendered value is unchanged. TS identifiers cannot start with a digit, so keep camel-safe key names and document the contract-name equivalence in the token comment: xs:4, sm:8, md:12, lg:16, xl:24, xxl:32 (contract "2xl"), xxxl:48 (contract "3xl"), xxxxl:64 (contract "4xl").

    Rename mapping (old name -> new name, SAME rendered value):
    - old md (16) -> lg · old lg (24) -> xl · old xl (32) -> xxl · old xxl (48) -> xxxl · old xxxl (64) -> xxxxl · xs/sm unchanged.

    CRITICAL — perform the usage sweep in LARGEST-FIRST order to avoid chained double-renames within a pass: first rename all `Spacing.xxxl` usages to `Spacing.xxxxl`, then xxl->xxxl, then xl->xxl, then lg->xl, then md->lg. Only after the sweep is complete, rewrite the Spacing object in theme.ts to the new scale (which introduces md:12 with, at this point, zero consumers — Plan 03-10 is its first consumer). Sweep scope: everything the grep inventory found (app/, components/, and any hooks/lib consumers).

    Also in theme.ts: set HAIRLINE_WIDTH to 1 (contract: hairline borders are 1px `line`), and set Radius.pill to 9999 (contract; visually equivalent to the current 100 on all controls that use it — verify none relies on exactly 100).

    Do NOT change any other value or any consumer's layout intent. This task is a pure rename plus three token-value updates (md added, hairline, pill); if any usage cannot be mapped 1:1, stop and flag it rather than eyeballing a new value.

    COUNT-INVARIANT GUARD (covers all five renamed tokens, not just md): BEFORE the sweep, record per-token usage counts across app/ components/ hooks/ lib/ (`grep -roE 'Spacing\.(md|lg|xl|xxl|xxxl|xxxxl)\b' app components hooks lib 2>/dev/null | sort | uniq -c` — approximately md 52 / lg 26 / xl 12 / xxl 7 / xxxl 3 / xxxxl 0 at plan time; re-measure at execution time). AFTER the sweep, assert the exact shift invariant: new lg count == old md count, new xl == old lg, new xxl == old xl, new xxxl == old xxl, new xxxxl == old xxxl, new md == 0, and the TOTAL Spacing.* usage count is unchanged. Any deviation means a missed or double rename — a silently changed rendered value that tsc cannot catch. Fix before proceeding.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && grep -q "md: 12" constants/theme.ts && grep -q "xxxxl: 64" constants/theme.ts && ! grep -rE 'Spacing\.md\b' app components hooks lib 2>/dev/null | grep -q . && grep -q "HAIRLINE_WIDTH = 1" constants/theme.ts && echo VERIFY_OK</automated>
  </verify>
  <done>theme.ts carries the contract scale (md=12 present, xxxxl=64 present, hairline 1, pill 9999); zero remaining `Spacing.md` usages anywhere outside theme.ts (the stale-name gate — every old md=16 usage became lg); typecheck clean; no rendered spacing value changed.</done>
  <acceptance_criteria>
    - `npx tsc --noEmit` in apps/mobile exits 0.
    - theme.ts Spacing is xs4/sm8/md12/lg16/xl24/xxl32/xxxl48/xxxxl64 with the contract-name equivalence documented.
    - Stale-name gate: `Spacing.md` has zero usages in app/, components/, hooks/, lib/ after the sweep (old-16 consumers all renamed to lg; nothing consumes the new 12 yet).
    - HAIRLINE_WIDTH is 1; Radius.pill is 9999.
    - Every rename preserved the numeric value (spot-check the diff: each changed line swaps only the token name).
    - Count-invariant holds for ALL five renamed tokens: post-sweep per-token counts equal the pre-sweep counts shifted one name up (lg==old md, xl==old lg, xxl==old xl, xxxl==old xxl, xxxxl==old xxxl), md==0, total unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Typography conformance — contract sizes, mono tracking, fontWeight cleanup</name>
  <files>apps/mobile/constants/theme.ts</files>
  <read_first>
    - DESIGN-SYSTEM.md §2 (Archivo Expanded 800-900 display: H2 ~30, H3 ~22, letter-spacing -0.01 to -0.02em, tight line-height 0.86-0.95; JetBrains Mono for ALL data/telemetry with wide tracking 0.2-0.34em on uppercase labels; Archivo 400-600 body ~1.55 line-height)
    - apps/mobile/constants/theme.ts (Typography roles: heading 20 / title 29 / display 40, each with a redundant fontWeight '600' on a single-face family; Mono letterSpacing 1 at 13px ≈ 0.077em; Kicker 1.8 at 11px)
  </read_first>
  <action>
    Conform theme.ts typography to the contract:

    1. Sizes: heading (H3) fontSize 20 -> 22 with lineHeight ~21 (contract 0.86-0.95 ratio); title (H2) fontSize 29 -> 30 with lineHeight ~28-29. Leave display at 40 (the contract's hero ≈68 is the Phase-4 home HSS ring, not the session header — do not build it now). Keep uppercase transforms. Set letterSpacing on heading/title/display within the contract's -0.01 to -0.02em band for their sizes (e.g. heading 22px -> approximately -0.3; title 30px -> approximately -0.45).

    2. Mono tracking: widen Mono letterSpacing from 1 toward the contract's 0.2em floor at its 13px size (≈ 2.6); widen Kicker from 1.8 toward 0.2em at 11px (≈ 2.2). These roles are label/metadata treatments — the numeric VALUE styling used by set-row inputs must NOT inherit this wide tracking (Plan 03-10 uses the mono family + tabular-nums directly for values, without the label tracking, to protect row width). Keep the tabularNums helper exactly as-is.

    3. fontWeight cleanup: remove the redundant `fontWeight: '600'` from the heading, display, and title roles — those families (Archivo_800ExtraBold / Archivo_900Black) are single-face, and a non-matching fontWeight can shift face selection/metrics on iOS. Leave body/label roles unchanged (multi-face-safe, out of scope).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && grep -q "fontSize: 22" constants/theme.ts && grep -q "fontSize: 30" constants/theme.ts && sed -n '/heading: {/,/},/p;/display: {/,/},/p;/title: {/,/},/p' constants/theme.ts | grep -c "fontWeight" | grep -qx 0 && echo VERIFY_OK</automated>
  </verify>
  <done>heading is 22 / title is 30 with contract-band tracking and tight line-height; Mono/Kicker tracking widened toward 0.2em; heading/display/title carry no fontWeight key (region-scoped grep over those three blocks: zero fontWeight lines); tabularNums untouched; typecheck clean.</done>
  <acceptance_criteria>
    - `npx tsc --noEmit` in apps/mobile exits 0.
    - Typography.heading fontSize 22; Typography.title fontSize 30; Typography.display unchanged at 40.
    - Mono letterSpacing >= 2.5 and Kicker letterSpacing >= 2.2 (0.2em at their sizes).
    - Region-scoped grep over the heading/display/title blocks finds zero fontWeight lines.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Existing-surface conformance — tab bar, text-field focus, segmented controls</name>
  <files>apps/mobile/app/(tabs)/_layout.tsx, existing Phase-3 input/segment components (discover via read_first; e.g. onboarding wizard inputs, exercise-picker search field, Settings units toggle)</files>
  <read_first>
    - DESIGN-SYSTEM.md §5 Inputs + Bottom nav (text field: carbon fill, hairline border, mono uppercase floating label, VOLT border on focus, ash placeholder; segmented control: steel track, active segment volt-or-bone with void text; nav: mono uppercase labels, active volt icon+label, inactive ash, void bg, hairline top border) and §7 Hard rules (one volt-filled element per screen; tokens only)
    - apps/mobile/app/(tabs)/_layout.tsx (current tab bar — quick task 260709-rq4 already added mono tab labels; verify the remaining contract properties: active/inactive colors, void bg, hairline top border)
    - Output of `grep -rln "TextInput" apps/mobile/app apps/mobile/components` (the Phase-3 text-field inventory: wizard steps, exercise-picker search, settings fields — SetRow is Plan 03-10's file, do NOT touch it here)
    - apps/mobile/app/(tabs)/settings/index.tsx and the onboarding sex/units steps (existing segmented-style selectors)
  </read_first>
  <action>
    Conform ONLY surfaces that already exist (DESIGN-SYSTEM.md Phase-applicability notes: Phase-3 = tokens, buttons, inputs, steppers, set rows, logging screens, onboarding, tab bar of existing tabs, settings):

    1. Tab bar (`(tabs)/_layout.tsx`): mono uppercase labels (verify — rq4 may have done this), active tint volt / inactive ash, void background, hairline (1px `line`) top border, no shadow. Do NOT add TODAY/TRENDS/HISTORY tabs — the 5-tab layout is Phase 4+; style only the tabs that exist today.

    2. Text-field focus state: every Phase-3 TextInput that renders as a form field (wizard steps, exercise-picker search, settings edits) gets the contract treatment — carbon fill, hairline border, volt border on focus (onFocus/onBlur-driven border color or a focused style variant), ash placeholderTextColor. Skip SetRow.tsx entirely (Plan 03-10 owns it and runs after this plan).

    3. Segmented-style controls that already exist (Settings units toggle, onboarding sex/units selectors if rendered as segments): steel track, active segment volt or bone fill with void text, radius from tokens. Respect the one-volt-per-screen rule — if a screen already has a volt-filled primary CTA, use the bone active-segment variant.

    Do NOT build: HSS ring, run logger, run-type chips (no run surface exists in Phase 3), trends chart, session-row home list, paywall. Tokens only — no hardcoded hex/sizes (contract hard rule).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit && grep -q "borderTopWidth" "app/(tabs)/_layout.tsx" && grep -rq "onFocus" app components && echo VERIFY_OK</automated>
  </verify>
  <done>Tab bar matches the contract (mono uppercase, volt/ash, void bg, hairline top border) for the existing tabs; Phase-3 form fields show volt focus borders; existing segments follow the steel-track pattern; zero Phase-4/paywall surfaces created; typecheck clean. Final visual confirmation rides Plan 03-10's on-device checkpoint.</done>
  <acceptance_criteria>
    - `npx tsc --noEmit` in apps/mobile exits 0.
    - (tabs)/_layout.tsx sets a hairline top border and volt-active/ash-inactive tints on a void background.
    - At least the wizard, picker-search, and settings text fields implement a focus-driven volt border (grep: onFocus handling present).
    - No new route/screen files are created (git status shows edits to existing files only, except none).
    - SetRow.tsx and ExerciseCard.tsx are untouched by this plan (they belong to 03-10).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Token/style-only changes; no new inputs, parsing, storage, or network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-24 | Tampering | Spacing remap silently changing rendered values (wrong old->new mapping) | low | mitigate | Largest-first ordered rename with an explicit mapping table; stale-name gate (zero Spacing.md consumers post-sweep); diff spot-check that each changed line swaps only the token name; on-device confirmation at 03-10's checkpoint |
| T-03-25 | Denial of Service | Focus-state rework breaking keyboard/submit flows fixed in 260709-r4z | low | mitigate | Focus styling is border-color only — no layout/keyboard-avoidance changes to WizardStep; onboarding retested at the 03-10 checkpoint |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after each task.
- Stale-name gate: zero `Spacing.md` usages outside theme.ts post-sweep; theme.ts shows md:12 and xxxxl:64.
- Count-invariant gate: per-token usage counts measured pre-sweep shift exactly one name up post-sweep (all five renamed tokens), total unchanged.
- Region-scoped grep: no fontWeight in heading/display/title blocks; heading 22 / title 30 present.
- Tab bar greps: hairline top border present; no new tab routes.
- Rendered-value invariant: the remap diff only swaps token names (values unchanged) — visually confirmed on-device at Plan 03-10's blocking checkpoint, which covers both plans.
</verification>

<success_criteria>
- theme.ts and all consumers conform to DESIGN-SYSTEM.md's token contract with zero visual drift from the rename; typography and tab bar/inputs match the contract on existing Phase-3 surfaces only.
- Plan 03-10 can consume Spacing.md=12 for its set-row geometry.
</success_criteria>

<output>
Create `.planning/phases/03-onboarding-lifting-logger/03-11-SUMMARY.md` when done.
</output>
