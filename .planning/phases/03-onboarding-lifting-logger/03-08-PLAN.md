---
phase: 03-onboarding-lifting-logger
plan: 08
type: execute
wave: 4
depends_on: [03-06, 03-02]
files_modified:
  - apps/mobile/app/session/finish.tsx
  - apps/mobile/components/session/HSSBreakdownSheet.tsx
  - apps/mobile/components/session/SetRow.tsx
  - apps/mobile/lib/finishWorkout.ts
autonomous: true
must_haves:
  truths:
    - "Finishing a workout marks it finished (finishedAt) and shows a summary: big session HSS, per-exercise volume/set counts, and the engine warnings list (LIFT-07/D-27)."
    - "Discard lives behind a menu (never adjacent to Finish) with a confirm dialog, and is a soft delete (deletedAt) that excludes the session from ALL history/HSS/load queries (D-27/D-28)."
    - "Tapping the live HSS opens a breakdown sheet showing per-exercise stress subtotals + set counts + session total, no raw formula internals (D-24)."
    - "Engine warnings surface as a subtle badge on the affected set row (tap for message) plus the full list on finish (D-29)."
  artifacts:
    - apps/mobile/app/session/finish.tsx
    - apps/mobile/components/session/HSSBreakdownSheet.tsx
    - apps/mobile/lib/finishWorkout.ts
  key_links:
    - "Finish -> finishWorkout (set finishedAt) -> finish summary"
    - "Discard menu -> confirm -> softDeleteWorkout (Plan 02) -> excluded everywhere"
    - "sessionHSSDetailed perSetStress/warnings -> breakdown sheet + finish + SetRow badge"
requirements: [LIFT-07]
---

<objective>
Complete the session lifecycle: a finish summary (session HSS, per-exercise volume, warnings), a menu-gated confirmed discard that soft-deletes, the tappable HSS breakdown sheet, and engine-warning badges on set rows.

Purpose: LIFT-07 requires save-or-discard with a confirmed discard and the session HSS on finish. D-28 makes discard a soft delete excluded from every computation. D-24/D-29 surface the honest breakdown + warnings. This plan fleshes out the finish.tsx and HSSBreakdownSheet.tsx stubs Plan 06 mounted, so it does not edit session.tsx.
Output: Finish summary, breakdown sheet, soft-delete discard, warning badges.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md
@.planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md
@.planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md
@apps/mobile/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finish summary + finishWorkout + menu-gated confirmed soft-delete discard</name>
  <files>apps/mobile/app/session/finish.tsx, apps/mobile/lib/finishWorkout.ts</files>
  <read_first>
    - apps/mobile/app/session/finish.tsx (the stub Plan 06 created showing session HSS + Done — flesh it out here; do NOT edit session.tsx)
    - packages/db/src/queries.ts (softDeleteWorkout — Plan 02) and schema.ts (workout.finishedAt/deletedAt)
    - apps/mobile/stores/sessionStore.ts (committed sets + per-exercise rollup + warnings)
    - packages/engine/src/index.ts (sessionHSSDetailed perSetStress + warnings for the summary)
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Finish summary spec; Copywriting: Done; Discard menu item "Discard Workout" + dialog "Discard this workout? All logged sets will be removed and can't be recovered from the app. [Keep Training] [Discard Workout]"; Destructive color #E5484D)
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md (D-27 finish summary big HSS + per-exercise volume + warnings, Done returns to tabs, Discard behind a menu NOT adjacent to Finish; D-28 soft delete deletedAt excluded from all load/HSS)
  </read_first>
  <action>
    Create apps/mobile/lib/finishWorkout.ts: `finishWorkout(db, workoutId, finishedAt)` sets workout.finishedAt (timestamp passed in, not Date.now() inside the helper) marking the session closed; `discardWorkout(db, workoutId, deletedAt)` calls the Plan 02 softDeleteWorkout builder (deletedAt) — a soft delete, never a hard row delete (D-28). Parameterized builders only (T-1-01).
    Flesh out apps/mobile/app/session/finish.tsx (D-27): show the session HSS at Display size (big), a per-exercise volume/set-count list, and the engine warnings list (D-29). "Done" calls finishWorkout then returns to the tabs. A separate menu (e.g. a header "..." menu, deliberately NOT adjacent to Done/Finish) contains "Discard Workout" which opens the confirm dialog with the exact UI-SPEC copy; confirming calls discardWorkout (soft delete) and returns to tabs; the discarded session then never appears in history or any HSS/load computation (D-28). Use the destructive color for the discard action + confirm primary.
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Finish shows big HSS + per-exercise volume + warnings; Done marks finishedAt; Discard is menu-gated + confirmed + soft-deletes; typecheck clean.</done>
  <acceptance_criteria>
    - finishWorkout.ts uses softDeleteWorkout (sets deletedAt) for discard — no db.delete() hard delete (grep: no `.delete(` on workout).
    - finish.tsx Discard dialog contains the exact string "All logged sets will be removed and can't be recovered from the app."
    - Discard action is not rendered adjacent to Done/Finish (separate menu).
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: HSS breakdown sheet (D-24) + set-row warning badges (D-29)</name>
  <files>apps/mobile/components/session/HSSBreakdownSheet.tsx, apps/mobile/components/session/SetRow.tsx</files>
  <read_first>
    - apps/mobile/components/session/HSSBreakdownSheet.tsx (the minimal stub Plan 06 mounted — flesh out per-exercise rollup here; do NOT edit session.tsx)
    - apps/mobile/components/session/SetRow.tsx (the warning-badge slot Plan 06 left — populate it from store warnings)
    - apps/mobile/stores/sessionStore.ts (breakdownOpen flag + per-set stress + warnings)
    - packages/shared/src/index.ts (SessionHSSResult perSetStress + warnings shape)
    - .planning/phases/03-onboarding-lifting-logger/03-UI-SPEC.md (Breakdown sheet: @gorhom/bottom-sheet, per-exercise Label subtotal rows [name + set count + stress subtotal] ending in a Heading-size session total, NO raw formula/internals; Warning color #D9A441 for the badge)
    - .planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md (D-24 rollup from sessionHSSDetailed per-set output; D-29 subtle badge on affected set row, tap for message, never interrupts logging)
  </read_first>
  <action>
    Flesh out apps/mobile/components/session/HSSBreakdownSheet.tsx (D-24): a @gorhom/bottom-sheet opened by store.breakdownOpen, listing per-exercise Label-size subtotal rows (exercise name + set count + summed stress subtotal, rolled up from the per-set stress the store holds) ending in a Heading-size session total row. Never show raw formula internals or config constants — the number, not the math (honest-number voice). Store-driven only; must not import session.tsx.
    Update apps/mobile/components/session/SetRow.tsx to populate the warning-badge slot Plan 06 left: when the store carries a warning attributable to this set/exercise, render a subtle warning-color (#D9A441) badge; tapping it shows the warning message (small popover/inline), and it never blocks the logging loop (D-29).
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit</automated>
  </verify>
  <done>Breakdown sheet shows per-exercise subtotals + total with no formula internals; set rows show a tappable warning badge; both store-driven; typecheck clean.</done>
  <acceptance_criteria>
    - HSSBreakdownSheet lists per-exercise subtotal rows and a session total, driven by store.breakdownOpen.
    - HSSBreakdownSheet does not render any engine config constant or formula string (grep: no kStrength/kEndurance/kCarry).
    - SetRow renders a warning badge from store warnings and does not block on it.
    - `npx tsc --noEmit` in apps/mobile exits 0.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 08)

New symbols/files created here (exclude from source-drift verification):
- `apps/mobile/lib/finishWorkout.ts` (finishWorkout, discardWorkout)
- Fleshed-out `apps/mobile/app/session/finish.tsx` (summary + discard menu)
- Fleshed-out `apps/mobile/components/session/HSSBreakdownSheet.tsx` (per-exercise rollup)
- Warning-badge rendering added to `apps/mobile/components/session/SetRow.tsx`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user action -> workout lifecycle | Finish/discard mutate workout state |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Tampering / data loss | accidental discard of a logged session | medium | mitigate | discard is menu-gated (not adjacent to Finish) + confirm dialog + SOFT delete (deletedAt) so data survives as a debug safety net (D-28) |
| T-03-05 | Information Disclosure | soft-deleted session leaking into history/trend | low | mitigate | every load/HSS/history read filters deletedAt IS NULL via the Plan 02 activeWorkoutFilter |
| T-03-18 | Information Disclosure | breakdown sheet exposing formula internals | low | mitigate | breakdown shows only rolled-up numbers, never engine constants/formulas (D-24) |
</threat_model>

<verification>
- `npx tsc --noEmit` in apps/mobile clean.
- `pnpm --filter @apsis/db test` still green (soft-delete filtering unchanged).
- Manual (phase UAT): finish shows HSS + volume + warnings; discard requires confirm and removes the session from history; breakdown sheet opens from the HSS tap.
</verification>

<success_criteria>
- LIFT-07 complete: save (finish) shows session HSS; discard is confirmed + soft-deletes.
- D-24 breakdown + D-28 soft-delete exclusion + D-29 warning badges implemented.
</success_criteria>

<output>
Create `.planning/phases/03-onboarding-lifting-logger/03-08-SUMMARY.md` when done.
</output>
