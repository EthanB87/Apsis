---
phase: 03-onboarding-lifting-logger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/index.ts
  - packages/shared/src/units.ts
  - packages/shared/package.json
  - packages/shared/vitest.config.mts
  - packages/shared/src/__tests__/units.test.ts
  - packages/engine/src/bodyweight.ts
  - packages/engine/src/carry.ts
  - packages/engine/src/config.ts
  - packages/engine/src/session.ts
  - packages/engine/src/index.ts
  - packages/engine/src/__tests__/bodyweight.test.ts
  - packages/engine/src/__tests__/carry.test.ts
  - packages/engine/src/__tests__/session.test.ts
autonomous: true
requirements: [ONB-04, LIFT-02, LIFT-08]
must_haves:
  truths:
    - "The engine returns a bodyweight-movement e1RM from a rep-max table that does not blow up above 12 reps (D-18)."
    - "The engine returns a loaded-carry/sled stress score from load+duration+RPE (D-20), and a 4x40m heavy farmer's carry lands well under the ~100 HSS threshold-run anchor."
    - "sessionHSSDetailed composes strength sets, endurance segments, AND timed carry sets into one session HSS."
    - "Pure km<->mi and kg<->lb display conversions round-trip exactly (225 lb never displays as 224.9)."
  artifacts:
    - packages/engine/src/bodyweight.ts
    - packages/engine/src/carry.ts
    - packages/shared/src/units.ts
    - packages/shared/vitest.config.mts
  key_links:
    - "sessionHSSDetailed(session.ts) -> carryStressDetailed(carry.ts) for carrySets"
    - "app effective-load path -> estimateE1RMFromRepMaxTable when exercise.bwFactor != null"
---

<objective>
Add the two pure engine functions Phase 3 needs (D-18 bodyweight rep-max e1RM, D-20 loaded-carry/sled stress), compose timed carries into session HSS, and stand up the pure unit-conversion module (km<->mi, kg<->lb exact round-trip) with its own test harness in `@apsis/shared`.

Purpose: The lifting logger cannot honestly score bodyweight movements (pull-ups, push-ups) or loaded carries (farmer's carry, sled) without these engine additions, and cannot display imperial units without exact round-trip conversion. All of this is pure, testable logic that belongs in the moat packages — no UI, no I/O.
Output: Two new tested engine modules, an extended session composer, a new tested shared units module, and a new `@apsis/shared` vitest config.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding-lifting-logger/03-CONTEXT.md
@.planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md
@.planning/phases/03-onboarding-lifting-logger/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend @apsis/shared types + pure units module + shared vitest harness</name>
  <files>packages/shared/src/index.ts, packages/shared/src/units.ts, packages/shared/package.json, packages/shared/vitest.config.mts, packages/shared/src/__tests__/units.test.ts</files>
  <read_first>
    - packages/shared/src/index.ts (current types: Sex, Units, EngineConfig, StrengthSet, EnduranceSegment, SessionInput, SessionHSSResult, EnduranceStressDetail — replicate the JSDoc + interface style exactly)
    - packages/shared/package.json (currently has no test script/devDeps — you will add them)
    - packages/engine/vitest.config.mts (copy this config shape; @apsis/shared has NO workspace deps so omit the alias block)
    - packages/engine/package.json (mirror its scripts.test + devDeps: typescript 6.0.3, vite ^6.4.3, vitest 4.1.9)
    - .planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md (Code Examples: imperial round-trip LB_PER_KG 2.2046226218; D-12)
  </read_first>
  <behavior>
    - Test: lbToKgExact(225) then kgToDisplayLb(result) returns exactly 225 (no 224.9 drift).
    - Test: kgToDisplayLb(102.058...) returns 225; kgToDisplayLb is Math.round of kg*LB_PER_KG.
    - Test: km<->mi round-trips a whole-number mile value back to itself after display rounding.
    - Test: paceSecPerKm converted to paceSecPerMi and back is within 1s tolerance for a 300 s/km input.
  </behavior>
  <action>
    Add to packages/shared/src/index.ts (append, matching existing JSDoc style): interface `CarrySet` with fields loadKg:number, bodyweightKg:number, durationS:number, rpe:number, isWarmup:boolean; interface `CarryStressDetail` with cs:number and warnings:string[]; add optional `carrySets?: CarrySet[]` to `SessionInput`; add `kCarry: number` to `EngineConfig` (place after doublePenalty); add optional `cs?: number` to `SessionHSSResult`.
    Create packages/shared/src/units.ts as a zero-dependency pure module exporting: const LB_PER_KG = 2.2046226218; const KM_PER_MI = 1.609344; function lbToKgExact(lb) returns lb / LB_PER_KG; function kgToDisplayLb(kg) returns Math.round(kg * LB_PER_KG); function kmToDisplayMi(km) returns km / KM_PER_MI; function miToKmExact(mi) returns mi * KM_PER_MI; function paceSecPerKmToSecPerMi(secPerKm) returns secPerKm * KM_PER_MI; function paceSecPerMiToSecPerKm(secPerMi) returns secPerMi / KM_PER_MI. Every function is a plain arithmetic transform; storage stays metric, display converts (D-05/D-12). Re-export units.ts from index.ts via `export * from './units';`.
    Add packages/shared/vitest.config.mts using vitest/config defineConfig with test.globals true, test.environment 'node', test.include ['src/**/*.{test,spec}.{ts,mts}'] — no resolve.alias (shared has no workspace deps).
    Update packages/shared/package.json: add "scripts": { "test": "vitest run" } and devDependencies typescript "6.0.3", vite "^6.4.3", vitest "4.1.9".
    Create packages/shared/src/__tests__/units.test.ts covering the four behaviors above plus the exact-round-trip invariant for 225 lb and 315 lb.
    Run `pnpm install` at repo root if needed so the new devDeps resolve.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/shared test</automated>
  </verify>
  <done>packages/shared exports CarrySet, CarryStressDetail, units helpers; EngineConfig has kCarry; SessionInput has carrySets; SessionHSSResult has cs; `pnpm --filter @apsis/shared test` passes with the round-trip test green.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/shared test` exits 0.
    - units.test.ts asserts kgToDisplayLb(lbToKgExact(225)) === 225.
    - `export * from './units'` present in packages/shared/src/index.ts.
    - EngineConfig interface contains a `kCarry: number` member; SessionInput contains `carrySets?`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bodyweight rep-max e1RM estimator (D-18)</name>
  <files>packages/engine/src/bodyweight.ts, packages/engine/src/index.ts, packages/engine/src/__tests__/bodyweight.test.ts</files>
  <read_first>
    - packages/engine/src/strength.ts (estimateE1RM Epley + MAX_REPS clamp-and-warn shape; mirror the never-throw + warnings[] discipline exactly)
    - packages/engine/src/clamp.ts (clampRange signature + NaN-safe `!(x>0)` idiom)
    - packages/engine/src/config.ts (mergeConfig import pattern)
    - packages/engine/src/index.ts (barrel; add `export * from './bodyweight';`)
    - .planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md (Pattern 3 REP_MAX_TABLE starter + Pitfall 3 on Brzycki/Epley high-rep divergence)
    - packages/engine/src/__tests__/strength.test.ts (test file structure to mirror)
  </read_first>
  <behavior>
    - Test: an exact table-row rep count returns loadKg / pct for that row (e.g. reps=10 uses pct 0.75).
    - Test: a rep count between two rows returns a linearly interpolated pct.
    - Test: reps above the 30-rep floor row clamps to 30 and pushes a warning (never divides by a negative/zero denominator).
    - Test: reps below 1 clamps to 1 with a warning; NaN reps clamps with a warning (never throws, never returns NaN).
  </behavior>
  <action>
    Create packages/engine/src/bodyweight.ts mirroring strength.ts structure. Define a module-level readonly REP_MAX_TABLE of {reps, pct} rows: 1/1.0, 2/0.95, 3/0.93, 5/0.87, 8/0.80, 10/0.75, 12/0.70, 15/0.65, 20/0.55, 25/0.50, 30/0.45 (30 is the floor row — do not extrapolate below it). Add a private `interpolateRepMaxPct(reps)` doing linear interpolation between the two bracketing rows (exact match returns the row pct; at/above 30 returns the 30-row pct). Export `estimateE1RMFromRepMaxTableDetailed(loadKg, reps)` returning { e1rm, warnings }: clampRange(reps, 1, 30, 'reps') and clampRange(loadKg, 0, POSITIVE_INFINITY, 'loadKg'), push any warnings, compute e1rm = clampedLoad / interpolateRepMaxPct(clampedReps); guard against a non-finite result. Export a bare facade `estimateE1RMFromRepMaxTable(loadKg, reps)` returning `.e1rm`. Never throw. Add `export * from './bodyweight';` to index.ts. This estimator is used by the app ONLY when exercise.bwFactor != null (RESEARCH Pattern 2); barbell lifts keep Epley (estimateE1RM) per Phase 02 D-10.
    Create packages/engine/src/__tests__/bodyweight.test.ts covering the four behaviors.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/engine test</automated>
  </verify>
  <done>estimateE1RMFromRepMaxTable + Detailed exported from the engine barrel; bodyweight.test.ts green; a 25-rep push-up no longer produces an inflated/undefined e1RM.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/engine test` exits 0.
    - bodyweight.test.ts asserts estimateE1RMFromRepMaxTable at reps=10 equals loadKg/0.75.
    - bodyweight.test.ts asserts reps=40 clamps to the 30-row and returns a warning.
    - `export * from './bodyweight'` present in packages/engine/src/index.ts.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Loaded-carry/sled stress (D-20) + kCarry config + session composition</name>
  <files>packages/engine/src/carry.ts, packages/engine/src/config.ts, packages/engine/src/session.ts, packages/engine/src/index.ts, packages/engine/src/__tests__/carry.test.ts, packages/engine/src/__tests__/session.test.ts</files>
  <read_first>
    - packages/engine/src/endurance.ts (enduranceStressDetailed IF^2 shape + Detailed+facade pattern to clone)
    - packages/engine/src/config.ts (DEFAULT_CONFIG + calibration-anchor comment style for kStrength/kEndurance)
    - packages/engine/src/session.ts (sessionHSSDetailed composition of strength + endurance; you will add carrySets)
    - packages/engine/src/__tests__/calibration.test.ts (golden-test structure to mirror for the carry anchor)
    - .planning/phases/03-onboarding-lifting-logger/03-RESEARCH.md (D-20 formula shape + Assumption A5: golden test is authoritative over the suggested shape)
    - packages/shared/src/index.ts (CarrySet, CarryStressDetail, EngineConfig.kCarry, SessionHSSResult.cs added in Task 1)
  </read_first>
  <behavior>
    - Test: carryStressDetailed clamps durationS>=0, rpe into [1,10], loadKg>=0, and returns cs plus warnings; never throws.
    - Test (golden, D-20): a 4x40m heavy farmer's carry (model each set as ~20s, loadKg = 1.5 x bodyweight, rpe 8) summed across 4 sets lands WELL under the ~100 HSS threshold-run anchor (assert total cs is in a hard-accessory band, e.g. > 8 and < 60) — tune kCarry to satisfy this and record the chosen value in the config comment.
    - Test: sessionHSSDetailed with only carrySets returns hss === cs (ss and es zero); with strengthSets + carrySets returns hss === ss + cs.
  </behavior>
  <action>
    Add `kCarry` to DEFAULT_CONFIG in packages/engine/src/config.ts with a calibration-anchor comment matching the kStrength/kEndurance style; pick a starting value then tune it so the carry golden test passes.
    Create packages/engine/src/carry.ts cloning enduranceStressDetailed: export `carryStressDetailed(seg: CarrySet, cfg?)` returning { cs, warnings }. Clamp durationS into [0, POSITIVE_INFINITY], rpe into [1, 10], loadKg into [0, POSITIVE_INFINITY]; compute durationMin = durationS/60; compute a load-vs-bodyweight multiplier loadRatioMultiplier = clampRange(1 + (bodyweightKg > 0 ? loadKg / bodyweightKg : 0), 1, 4, 'loadRatioMultiplier').value (guards a zero/negative bodyweight to a neutral 1x); cs = durationMin * (rpe/10)**2 * config.kCarry * loadRatioMultiplier. Export bare facade `carryStress(seg, cfg?)` returning `.cs`. Never throw. Add `export * from './carry';` to index.ts.
    Extend sessionHSSDetailed in session.ts: after the endurance loop, iterate `input.carrySets ?? []` summing carryStressDetailed(seg, config).cs into a new `cs` accumulator and concatenating warnings; set hss = ss + es + cs and include `cs` in the returned SessionHSSResult.
    Create packages/engine/src/__tests__/carry.test.ts (clamp behaviors + the golden anchor). Extend packages/engine/src/__tests__/session.test.ts with a carry-only and a strength+carry composition case.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/engine test</automated>
  </verify>
  <done>carryStress + carryStressDetailed exported; kCarry tuned via golden test; sessionHSSDetailed folds carrySets into hss; full engine suite green (>=20 tests still pass).</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/engine test` exits 0 with the new carry + session cases green.
    - carry.test.ts golden asserts the 4x40m farmer's carry total is > 8 and < 60.
    - session.test.ts asserts a carry-only session hss equals its cs.
    - DEFAULT_CONFIG contains kCarry with a calibration comment naming the chosen value.
  </acceptance_criteria>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 01)

New symbols created here (exclude from source-drift verification):
- `estimateE1RMFromRepMaxTable`, `estimateE1RMFromRepMaxTableDetailed`, `REP_MAX_TABLE`, `interpolateRepMaxPct` (packages/engine/src/bodyweight.ts)
- `carryStress`, `carryStressDetailed` (packages/engine/src/carry.ts)
- `kCarry` field on `EngineConfig` + `DEFAULT_CONFIG`
- `CarrySet`, `CarryStressDetail` interfaces; `carrySets?` on `SessionInput`; `cs?` on `SessionHSSResult` (packages/shared/src/index.ts)
- `lbToKgExact`, `kgToDisplayLb`, `kmToDisplayMi`, `miToKmExact`, `paceSecPerKmToSecPerMi`, `paceSecPerMiToSecPerKm`, `LB_PER_KG`, `KM_PER_MI` (packages/shared/src/units.ts)
- `packages/shared/vitest.config.mts`, `packages/shared/src/__tests__/units.test.ts`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| app -> engine | App passes numeric set/carry inputs (loadKg, reps, rpe, durationS) that originate from user entry |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Denial of Service | bodyweight.ts / carry.ts numeric inputs | low | mitigate | clampRange every numeric input; NaN-safe `!(x>0)` guards; never throw, never divide by zero (mirrors existing engine D-15 discipline) |
| T-03-02 | Tampering | high-rep e1RM extrapolation poisoning HSS | low | mitigate | rep-max table with a hard 30-rep floor row + clamp-and-warn instead of extrapolating Epley/Brzycki past their valid range (D-18) |
</threat_model>

<verification>
- `pnpm --filter @apsis/shared test` green (units round-trip).
- `pnpm --filter @apsis/engine test` green (bodyweight, carry, session, and all prior >=20 tests).
- `pnpm -r build` (or tsc) succeeds — no type errors from the new shared interfaces.
</verification>

<success_criteria>
- Engine exposes estimateE1RMFromRepMaxTable and carryStress(Detailed) as pure, tested functions.
- sessionHSSDetailed returns a session HSS that includes timed carry sets.
- Unit conversions round-trip exactly and are covered by a passing test.
- Engine purity preserved: no I/O, no Date.now(), zero new runtime deps.
</success_criteria>

<output>
Create `.planning/phases/03-onboarding-lifting-logger/03-01-SUMMARY.md` when done.
</output>
