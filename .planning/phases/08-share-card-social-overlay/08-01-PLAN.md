---
phase: 08-share-card-social-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - pnpm-lock.yaml
autonomous: false
requirements: [D-06, D-08, D-15]
user_setup:
  - service: eas
    why: "The three new native modules only link into the app after a fresh EAS dev-client build (Pitfall 3); on-device UAT in later plans depends on it. Windows host cannot prebuild ios/ locally (STATE.md Phase 04 P02)."
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "expo-image-picker, expo-file-system, and expo-sharing appear in apps/mobile/package.json at their SDK-56 lines"
    - "app.json declares the expo-image-picker config plugin with a photo-library permission string (NSPhotoLibraryUsageDescription)"
    - "The three new native deps are recorded as a 06-06 production-build gate"
  artifacts:
    - apps/mobile/package.json
    - apps/mobile/app.json
    - pnpm-lock.yaml
  key_links:
    - "package.json dep entries wired to pnpm-lock.yaml (EAS installs with --frozen-lockfile; a drift blocks the build)"
    - "app.json expo-image-picker plugin wired to NSPhotoLibraryUsageDescription in the built binary"
---

<objective>
Land the three new native dependencies (expo-image-picker, expo-file-system, expo-sharing)
into the app so they ride the single fresh 06-06 EAS build, and configure the
expo-image-picker photo-library permission string (per D-06/D-08/D-15). This is the native
prerequisite the rest of Phase 8 builds on — the render/export pipeline (08-02) and the
photo-first flow (08-04) cannot be verified on-device until these modules are compiled into
a dev-client binary (Pitfall 3).

All three packages carry a SUS legitimacy verdict (RESEARCH Package Legitimacy Audit — a
false positive of the "too-new" heuristic against Expo's lockstep SDK versioning), so the
install is gated behind a blocking-human legitimacy checkpoint (T-08-SC), consistent with
Phase 05/07 precedent for expo-camera and react-native-healthkit.

Purpose: Get the native surface committed before the production build so no dependency is
stranded outside the single submission binary.
Output: Updated package.json, app.json, and pnpm-lock.yaml; a documented 06-06 gate entry.

Note: This plan touches NO drizzle schema (share.tsx only reads existing tables) — the
schema-push/migration gate does not apply to Phase 8.
Note: The spec-less probe fallback was visibly skipped for Phase 8 (no requirement IDs to
probe) — must_haves are derived from the phase goal and CONTEXT.md D-01..D-16 decisions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-share-card-social-overlay/08-CONTEXT.md
@.planning/phases/08-share-card-social-overlay/08-RESEARCH.md
@.planning/phases/08-share-card-social-overlay/08-PATTERNS.md
@apps/mobile/app.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (3 SUS packages)</name>
  <read_first>
    - .planning/phases/08-share-card-social-overlay/08-RESEARCH.md (Package Legitimacy Audit table plus the "Note on the SUS verdicts")
  </read_first>
  <what-built>Nothing installed yet. RESEARCH flagged expo-image-picker (~56.0.22), expo-file-system (~56.0.8), and expo-sharing (~56.0.23) as SUS under the "too-new" heuristic — each is published by the official expo npm org, part of the multi-year Expo SDK family, with millions of weekly downloads and no postinstall script. This is almost certainly a false positive, but the legitimacy gate requires human confirmation before install.</what-built>
  <how-to-verify>
    For each of the three packages, open its registry page and confirm it is the official Expo package (publisher expo, repository github.com/expo/expo, no suspicious postinstall):
    - https://www.npmjs.com/package/expo-image-picker
    - https://www.npmjs.com/package/expo-file-system
    - https://www.npmjs.com/package/expo-sharing
    Confirm each resolves to its SDK-56 line (56.0.x) and shows large weekly download counts.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all three are the official Expo packages and approves the install
    - This checkpoint is blocking-human and is NOT auto-approvable (workflow.auto_advance is ignored for legitimacy checkpoints)
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the install, or name any package to reject</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install native deps via expo install and configure app.json photo permission</name>
  <files>apps/mobile/package.json, apps/mobile/app.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/mobile/app.json (current plugins array plus ios.infoPlist shape — the expo-camera plugin entry is the exact shape to mirror)
    - apps/mobile/AGENTS.md (mandate: read exact versioned Expo docs at docs.expo.dev/versions/v56.0.0/ before writing code)
    - .planning/phases/08-share-card-social-overlay/08-PATTERNS.md (app.json section — expo-image-picker plugin config shape)
    - .planning/phases/08-share-card-social-overlay/08-RESEARCH.md (Installation plus Environment Availability sections)
  </read_first>
  <reversibility rating="costly">D-06 photo-first commits expo-image-picker into the 06-06 production binary; backing out later means a new EAS build and a compose-screen redesign. Flagged, not gated (costly, not one-way).</reversibility>
  <action>
    From apps/mobile, run the expo install command for expo-image-picker expo-file-system expo-sharing so each resolves to its SDK-56-compatible line (never hand-pin a 57.x tag — Phase 03/07 precedent). Confirm all three land in apps/mobile/package.json dependencies (expected lines: expo-image-picker ~56.0.22, expo-file-system ~56.0.8, expo-sharing ~56.0.23) and that pnpm-lock.yaml is updated in the same change (EAS installs with a frozen lockfile — an out-of-sync lockfile blocks the build, STATE.md Phase 04 lesson).

    Add an expo-image-picker config-plugin entry to app.json's plugins array in the same array-with-options shape the existing expo-camera entry uses. Provide a photo-library permission string that honestly describes the single use (adding a background photo to a share card). Verify the exact plugin option key name for the photo-library usage description against the installed expo-image-picker plugin schema and the v56.0.0 imagepicker docs — it maps to NSPhotoLibraryUsageDescription in the built Info.plist. expo-file-system and expo-sharing need no plugin entry and no permission string.

    Do NOT run prebuild locally (Windows host cannot prebuild ios/). The native link is deferred to the EAS cloud dev build (per Phase 04 P02 precedent); the local gate here is that typecheck still passes with the new deps declared.

    Record in the SUMMARY that expo-image-picker, expo-file-system, and expo-sharing are added to the 06-06 native-dependency gate list (they MUST all land in the single fresh production build alongside any already-listed native deps).
  </action>
  <verify>
    <automated>cd /c/Users/ethan/Development/Apsis && node -e "const d=require('./apps/mobile/package.json').dependencies; ['expo-image-picker','expo-file-system','expo-sharing'].forEach(k=>{if(!d[k]) throw new Error('missing '+k)}); console.log('deps present')" && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - apps/mobile/package.json dependencies include expo-image-picker, expo-file-system, and expo-sharing at 56.0.x lines
    - pnpm-lock.yaml contains resolved entries for all three packages
    - app.json plugins array contains an expo-image-picker entry with a photo-library usage-description string
    - `pnpm run typecheck` exits 0
    - SUMMARY records the three deps as 06-06 build-gate items
  </acceptance_criteria>
  <done>All three native deps are declared and lockfile-synced, the photo-library permission string is configured in app.json, typecheck is clean, and the 06-06 gate is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to build | Third-party package code enters the production binary at install/build time |
| user photo library to app | (Downstream, configured here) photo-library access declared via NSPhotoLibraryUsageDescription |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-SC | Tampering | expo-image-picker / expo-file-system / expo-sharing installs | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifying each on npmjs.com before install; SDK-56-line via expo install only |
| T-08-01 | Information Disclosure | NSPhotoLibraryUsageDescription honesty | low | accept | Permission string discloses the single on-device use (share-card background); no photo data leaves the device |
</threat_model>

<verification>
- `pnpm run typecheck` passes with the three new deps declared
- package.json + pnpm-lock.yaml in sync (no --frozen-lockfile drift)
- app.json expo-image-picker plugin present with permission string
- Legitimacy checkpoint approved before install
</verification>

<success_criteria>
- Three native deps installed at SDK-56 lines, lockfile synced
- Photo-library permission configured
- 06-06 native-dependency gate documented in SUMMARY
</success_criteria>

<artifacts_this_phase_produces>
This plan creates/modifies (no new code symbols — dependency + config only):
- apps/mobile/package.json — new dependencies: expo-image-picker, expo-file-system, expo-sharing
- apps/mobile/app.json — new plugins entry: expo-image-picker (+ NSPhotoLibraryUsageDescription)
- pnpm-lock.yaml — resolved entries for the three packages
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-share-card-social-overlay/08-01-SUMMARY.md` when done
</output>
