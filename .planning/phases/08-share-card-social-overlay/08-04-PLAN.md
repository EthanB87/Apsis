---
phase: 08-share-card-social-overlay
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - apps/mobile/lib/shareCardExport.ts
  - apps/mobile/app/session/share.tsx
  - apps/mobile/components/share/ShareCardCanvas.tsx
autonomous: false
requirements: [D-06, D-07, D-08, D-10, D-15, D-16]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The compose flow leads with picking a photo from the library; the picked photo becomes the card background with the stat overlay on top (D-06/D-08)"
    - "Non-square photos are cropped square via the iOS native crop UI at pick time (D-15)"
    - "The stat overlay sits bottom-anchored over a void-to-transparent gradient scrim so text stays legible on bright/busy photos (D-10)"
    - "Denied or empty photo access shows an alert with an Open Settings deep-link, then falls back to the void branded card — sharing never hard-blocks (D-16/D-07)"
  artifacts:
    - apps/mobile/lib/shareCardExport.ts
    - apps/mobile/app/session/share.tsx
    - apps/mobile/components/share/ShareCardCanvas.tsx
  key_links:
    - "expo-image-picker requestMediaLibraryPermissionsAsync wired through a never-throws wrapper to share.tsx"
    - "picked photo URI wired to ShareCardCanvas Skia Image background under the scrim + overlay"
    - "permission-denied / empty-result wired to the void-card fallback (never a dead end)"
---

<objective>
Complete the phase with the photo-first flow (D-06): the compose screen leads with picking a
photo from the library (D-08), the iOS native square-crop UI frames non-square photos at pick
time (D-15), and the picked photo becomes the card background with the stat overlay
bottom-anchored over a void-to-transparent gradient scrim (D-10). Every permission edge —
denied, limited-access-yields-nothing, or skip — falls back to the fully-composed void
branded card without ever hard-blocking sharing (D-16/D-07).

This is the final horizontal expansion: it adds the photo background layer to the already-
proven render/export pipeline and the already-composed overlay. It ends with the phase's
full on-device UAT checkpoint.

Purpose: Deliver the Strava-style photo-first card and lock down the permission edge cases.
Output: The complete photo-first compose flow, verified end-to-end on device.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-share-card-social-overlay/08-CONTEXT.md
@.planning/phases/08-share-card-social-overlay/08-RESEARCH.md
@.planning/phases/08-share-card-social-overlay/08-PATTERNS.md
@apps/mobile/lib/nutritionCameraAuth.ts
@apps/mobile/constants/Colors.ts
@apps/mobile/constants/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Photo-library permission wrapper + pick helper in shareCardExport.ts</name>
  <files>apps/mobile/lib/shareCardExport.ts</files>
  <read_first>
    - apps/mobile/lib/shareCardExport.ts (the export module from 08-02 — add the permission + pick helpers alongside exportAndShareCard)
    - apps/mobile/lib/nutritionCameraAuth.ts (the never-throws / [Apsis]-prefixed / return-safe-fallback wrapper convention to copy)
    - apps/mobile/AGENTS.md (read exact v56.0.0 expo-image-picker docs before writing)
    - .planning/phases/08-share-card-social-overlay/08-RESEARCH.md (Pattern 2 photo pick + permission fallback; Pitfall 5 limited-access state)
    - .planning/phases/08-share-card-social-overlay/08-PATTERNS.md (Native permission + graceful fallback section)
  </read_first>
  <action>
    Add to apps/mobile/lib/shareCardExport.ts (keeping the never-throws convention):

    requestPhotoLibraryPermission(): async wrapper around expo-image-picker's requestMediaLibraryPermissionsAsync. Return a small result the caller can branch on: whether access is usable (granted or limited) versus denied. Per Pitfall 5, distinguish the iOS limited-access state (accessPrivileges / limited flag) from a clean deny — treat limited as usable (the picker still works), not as denied. On any thrown error, log [Apsis]-prefixed and report denied. Never throw.

    pickShareBackgroundPhoto(): async wrapper around launchImageLibraryAsync({ mediaTypes: ['images'], allowsEditing: true, quality: 1 }). allowsEditing:true gives the iOS native square crop for free (D-15 — the aspect option is Android-only; iOS crop is always square with allowsEditing). Return the selected asset uri, or null when the user canceled or the result has no assets (D-07 skip / Pitfall 5 empty-under-limited). Never throw.

    Keep exportAndShareCard unchanged.
  </action>
  <verify>
    <automated>cd /c/Users/ethan/Development/Apsis && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - shareCardExport.ts exports requestPhotoLibraryPermission and pickShareBackgroundPhoto
    - requestPhotoLibraryPermission distinguishes limited access from denied (does not treat limited as denied)
    - pickShareBackgroundPhoto returns null on cancel/empty result and never throws
    - Both new helpers log [Apsis]-prefixed on error and return a safe fallback
    - `pnpm run typecheck` exits 0
  </acceptance_criteria>
  <done>The photo-permission and photo-pick native wrappers exist, honor the limited-access edge, and never throw to their caller.</done>
</task>

<task type="auto">
  <name>Task 2: Photo-first compose flow + scrim + void fallback in share.tsx and ShareCardCanvas</name>
  <files>apps/mobile/app/session/share.tsx, apps/mobile/components/share/ShareCardCanvas.tsx</files>
  <read_first>
    - apps/mobile/app/session/share.tsx (the compose screen from 08-02/08-03 — add the photo-first pick flow + fallback state)
    - apps/mobile/components/share/ShareCardCanvas.tsx (the composed void card — add the photo background layer + scrim beneath the existing overlay)
    - apps/mobile/lib/shareCardExport.ts (Task 1's requestPhotoLibraryPermission + pickShareBackgroundPhoto)
    - apps/mobile/app/(tabs)/nutrition/scan.tsx (screen-level permission guard-flag state + Linking.openSettings denied UI to mirror — PATTERNS Native permission section)
    - .planning/phases/08-share-card-social-overlay/08-RESEARCH.md (Pattern 2; Pitfall 5; the Skia Image/LinearGradient imports for the background + scrim)
    - .planning/phases/08-share-card-social-overlay/08-PATTERNS.md (ShareCardCanvas photo/scrim imports; Native permission + graceful fallback)
  </read_first>
  <action>
    Make the compose flow photo-first (D-06). On entering share.tsx (or via a lead "Choose photo" affordance that is the primary action), call requestPhotoLibraryPermission then pickShareBackgroundPhoto. Track a selectedPhotoUri state plus a swap affordance so the athlete can pick/swap a photo (D-12). Provide an explicit skip path (D-07) that leaves selectedPhotoUri null. On permission denied: show an Alert explaining photo access with a "Not now" and an "Open Settings" action that calls Linking.openSettings() (D-16 — copy scan.tsx's denied-UI shape), THEN fall back to the void card (selectedPhotoUri stays null) — never hard-block. On limited access that yields an empty pick (Pitfall 5), fall back to the void card the same way. Sharing (exportAndShareCard) stays available in every branch.

    Extend ShareCardCanvas.tsx to accept an optional backgroundPhotoUri. When present, load it via useImage and draw it as a full-bleed 1080x1080 Skia <Image> background (cover fit), then draw a void-to-transparent LinearGradient scrim (D-10) anchored at the bottom (opaque void near the bottom edge fading to transparent toward the top) so the bottom-anchored HSS ring + stat trio + footer stay legible on bright/busy photos — the footer branding sits inside the scrim zone. When backgroundPhotoUri is null, render the existing void-fill background (the same overlay composition, D-07). Keep the exact same overlay layout (ring + trio + footer) in both branches so the void and photo cards share one composition. Scrim gradient stops/opacity are Claude's Discretion — tune for volt/bone legibility at feed size. Guard useImage/matchFont so a failed photo load or font miss degrades to the void card rather than crashing.
  </action>
  <verify>
    <automated>cd /c/Users/ethan/Development/Apsis && pnpm run typecheck</automated>
    <human-check>Full on-device flow validated in Task 3's checkpoint.</human-check>
  </verify>
  <acceptance_criteria>
    - share.tsx leads with a photo pick (permission -> pick) and offers pick/swap plus an explicit skip
    - Permission-denied path shows an alert with an Open Settings deep-link (Linking.openSettings) then falls back to the void card
    - Limited-access empty-result path also falls back to the void card
    - Sharing remains available whether or not a photo is selected
    - ShareCardCanvas draws a Skia Image background + a bottom-anchored void-to-transparent scrim when backgroundPhotoUri is set, and the void fill otherwise, with the identical overlay composition in both
    - `pnpm run typecheck` exits 0
  </acceptance_criteria>
  <done>The compose screen is photo-first with a Strava-style bottom scrim over the photo, and every permission edge falls back to the void card without blocking sharing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full on-device UAT of the share-card feature</name>
  <read_first>
    - .planning/phases/08-share-card-social-overlay/08-CONTEXT.md (D-01..D-16 — the full acceptance surface)
    - .planning/phases/08-share-card-social-overlay/08-RESEARCH.md (Validation Architecture Phase gate; Pitfall 5 limited-access)
  </read_first>
  <what-built>The complete share-card feature: photo-first compose (pick/crop/swap/skip), full fixed card composition (HSS ring hero + stat trio + branded footer), scrim over photos, void fallback, and share via the iOS share sheet — reachable from both the finish screen and History session detail.</what-built>
  <how-to-verify>
    On a fresh EAS dev build with all three native deps linked:
    1. Photo-first happy path: from a finished lift, tap Share card -> pick a non-square photo -> confirm the iOS native crop is square (D-15) -> the card shows the photo background with the HSS ring + VOLUME/SETS/DURATION trio + footer over a legible bottom scrim -> Share -> the share sheet attaches the PNG.
    2. Run session: repeat and confirm the DISTANCE/PACE/DURATION trio and "RUN — MON D" caption.
    3. Swap photo: pick a different photo and confirm the preview + export update.
    4. Skip photo: skip the pick -> confirm the pure void branded card with the same overlay -> Share works.
    5. Permission denied: in iOS Settings set Photos access to None for Apsis -> attempt a pick -> confirm the alert + Open Settings deep-link appears, then the void card fallback (sharing still works).
    6. Limited access: set Photos to Limited with zero selected -> attempt a pick -> confirm it falls back to the void card, not a dead end (Pitfall 5).
    7. Second entry point: open a session from History -> Share -> same compose flow.
  </how-to-verify>
  <acceptance_criteria>
    - Photo-first pick + square crop + swap + skip all work; card composes correctly for both session types
    - Scrim keeps ring/trio/footer legible over a bright/busy photo
    - Denied and limited-access both fall back to the void card without hard-blocking share
    - Both entry points (finish + History detail) reach the compose flow
    - Share sheet attaches a valid 1080x1080 PNG in every branch
  </acceptance_criteria>
  <resume-signal>Type "approved" if the full flow passes on-device, or describe what failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user photo library to app | Athlete's photo enters the app via the OS picker (on-device only) |
| app to filesystem | Photo-backed PNG written to Paths.cache |
| app to crash reporting | A picked-photo URI could reach Sentry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Information Disclosure | Sentry capturing a picked-photo URI or HSS from the compose screen | medium | mitigate | Add no new Sentry.setContext/breadcrumb calls in share.tsx/shareCardExport.ts; existing sentrySanitize allowlist (only exception + contexts.device/app survive) scrubs the rest |
| T-08-03 | Information Disclosure | Exported photo-backed PNG lingering in Paths.cache | low | accept | Paths.cache is OS-reclaimable per Expo semantics; on-device only, no network egress |
| T-08-05 | Denial of Service | Malformed/huge photo crashing the Skia snapshot | low | mitigate | useImage/makeImageSnapshot guarded (null-checks + try/catch in shareCardExport); a failed load degrades to the void card rather than crashing |
</threat_model>

<verification>
- `pnpm run typecheck` clean after each task
- Full on-device UAT (Task 3) passes all 7 scenarios
- No new Sentry calls in the photo/compose/export path
</verification>

<success_criteria>
- Photo-first compose flow with native square crop (D-06/D-08/D-15)
- Bottom scrim keeps the overlay legible over photos (D-10)
- Every permission edge falls back to the void card without blocking share (D-16/D-07)
- Feature reachable from both entry points; share sheet attaches a valid PNG
</success_criteria>

<artifacts_this_phase_produces>
New symbols created by this plan (excluded from downstream drift verification):
- apps/mobile/lib/shareCardExport.ts — functions requestPhotoLibraryPermission, pickShareBackgroundPhoto
- apps/mobile/components/share/ShareCardCanvas.tsx — extended prop backgroundPhotoUri; Skia Image background + LinearGradient scrim sub-elements
Modified:
- apps/mobile/app/session/share.tsx — photo-first pick/swap/skip flow + permission-denied alert + void fallback state
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-share-card-social-overlay/08-04-SUMMARY.md` when done
</output>
