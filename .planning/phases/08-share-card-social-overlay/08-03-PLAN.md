---
phase: 08-share-card-social-overlay
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/mobile/lib/shareCard.ts
  - apps/mobile/lib/__tests__/shareCard.test.ts
  - apps/mobile/components/share/ShareCardCanvas.tsx
  - apps/mobile/app/session/share.tsx
  - apps/mobile/app/session/detail.tsx
autonomous: true
requirements: [D-03, D-04, D-09, D-11]
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The share card shows a fixed core stat trio by session type — lift: total volume, sets, duration; run: distance, pace, duration (D-03)"
    - "The share card shows a subtle footer: APSIS wordmark + plate-mark + the mono session-type/date caption (D-04)"
    - "History session detail has a Share entry point that opens the same compose screen (D-11)"
  artifacts:
    - apps/mobile/lib/shareCard.ts
    - apps/mobile/components/share/ShareCardCanvas.tsx
    - apps/mobile/app/session/detail.tsx
  key_links:
    - "share.tsx stat query wired to shareCard.ts pure formatters wired to ShareCardCanvas stat trio"
    - "detail.tsx Share entry wired to router.push /session/share with workoutId"
    - "stat-trio numbers match finish.tsx/detail.tsx formatting conventions (no drift)"
---

<objective>
Expand the proven void-card tracer (08-02) into the full fixed card composition and add the
second entry point. Build the fixed core stat trio by session type (D-03: lift = total
volume, sets, duration; run = distance, pace, duration — no HR, no exercise names, no
toggles), the subtle footer branding (D-04: APSIS wordmark + plate-mark + the mono
session-type/date caption), and the recomposed share-edition sizing (D-09), then wire the
History session detail screen as the second Share entry point (D-11).

This is horizontal expansion out from the tracer's proven render->export skeleton: after
this plan a real user can generate a fully-composed void branded card (still no photo — that
is 08-04) from BOTH the finish screen and History detail.

Purpose: Deliver the complete fixed card content and the second entry point.
Output: A fully-composed void card + a Share affordance on session detail.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-share-card-social-overlay/08-CONTEXT.md
@.planning/phases/08-share-card-social-overlay/08-RESEARCH.md
@.planning/phases/08-share-card-social-overlay/08-PATTERNS.md
@apps/mobile/app/session/finish.tsx
@apps/mobile/app/session/detail.tsx
@apps/mobile/constants/Colors.ts
@apps/mobile/constants/theme.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stat-trio + duration formatters in lib/shareCard.ts</name>
  <files>apps/mobile/lib/shareCard.ts, apps/mobile/lib/__tests__/shareCard.test.ts</files>
  <read_first>
    - apps/mobile/lib/shareCard.ts (the tracer's existing module — extend in place)
    - apps/mobile/lib/__tests__/shareCard.test.ts (existing caption tests — extend)
    - apps/mobile/app/session/finish.tsx (formatVolume line 59-63, formatSessionDuration lines 74-81, formatEnduranceSummary distance/pace math lines 86-119 — copy these conventions, do not invent new formatting)
    - apps/mobile/app/session/detail.tsx (formatDuration lines 65-72, formatEnduranceMeta lines 77-109 — same duplicated convention)
    - .planning/phases/08-share-card-social-overlay/08-RESEARCH.md (Code Examples: ShareCardStrengthStats/ShareCardEnduranceStats interfaces)
  </read_first>
  <behavior>
    - buildStrengthStatTrio({ totalVolumeKg, setCount, durationS }, units): returns 3 labeled stat strings — total volume (kg or lb per units, whole-number rounding matching finish.tsx's formatVolume), set count (e.g. "12 SETS"), duration (m:ss / h:mm:ss matching formatSessionDuration).
    - buildEnduranceStatTrio({ distanceM, paceSecPerKm, durationS }, units): returns 3 labeled stat strings — distance (KM/MI per units, 1 decimal), pace (/KM or /MI via formatPaceMinSec + paceSecPerKmToSecPerMi), duration. Null distance/pace degrade gracefully (omit or dash) exactly as finish.tsx's filter(Boolean) join does.
    - Edge: setCount singular/plural ("1 SET" vs "2 SETS"); zero/near-zero duration formats as "0:00".
  </behavior>
  <action>
    Extend apps/mobile/lib/shareCard.ts (still pure, still @apsis/db-free) with the D-03 stat-trio formatters. Export interfaces ShareCardStrengthStats { totalVolumeKg; setCount; durationS } and ShareCardEnduranceStats { distanceM: number|null; paceSecPerKm: number|null; durationS } (RESEARCH Code Examples). Add buildStrengthStatTrio and buildEnduranceStatTrio returning a small fixed-length array (3) of { label, value } pairs (label mono-uppercase like "VOLUME"/"SETS"/"DURATION" and "DISTANCE"/"PACE"/"DURATION"). Reuse the exact volume/duration/distance/pace math from finish.tsx's formatVolume / formatSessionDuration / formatEnduranceSummary (import the shared @apsis/shared helpers kgToDisplayLb, kmToDisplayMi, paceSecPerKmToSecPerMi, formatPaceMinSec that finish.tsx already uses; keep a local duration formatter mirroring formatSessionDuration). Do NOT introduce a new rounding or unit convention — the card must match what finish.tsx/detail.tsx already show so the numbers never appear to drift.

    Extend shareCard.test.ts with cases for both trios: a metric + imperial volume case, singular/plural sets, a run distance/pace/duration case, and a null-distance conditioning-style case. Write the new expectations first (red), then implement.
  </action>
  <verify>
    <automated>cd /c/Users/ethan/Development/Apsis && pnpm --filter @apsis/mobile test -- shareCard && pnpm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - shareCard.ts exports buildStrengthStatTrio, buildEnduranceStatTrio, ShareCardStrengthStats, ShareCardEnduranceStats
    - Each trio builder returns exactly 3 label/value pairs
    - `pnpm --filter @apsis/mobile test -- shareCard` exits 0 with new metric/imperial, singular/plural, and null-distance cases
    - shareCard.ts still imports no @apsis/db
    - `pnpm run typecheck` exits 0
  </acceptance_criteria>
  <done>The fixed stat-trio formatters exist, are unit-tested for both session types and unit systems, and reuse the app's existing number conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Full card composition — stat trio + footer branding in ShareCardCanvas + share.tsx stat data</name>
  <files>apps/mobile/components/share/ShareCardCanvas.tsx, apps/mobile/app/session/share.tsx</files>
  <read_first>
    - apps/mobile/components/share/ShareCardCanvas.tsx (the tracer's void+ring+caption canvas — extend the render tree in place)
    - apps/mobile/app/session/share.tsx (the tracer's SQLite-is-truth useEffect — extend the query to fetch stat inputs)
    - apps/mobile/app/session/finish.tsx (strength per-exercise volume/set query lines 194-250; endurance segment query lines 154-170 — copy the query shapes for the stat inputs)
    - packages/db/src/schema.ts (workout.createdAt/finishedAt for lift session duration; strengthSet.loadKg/reps/isWarmup/durationS; enduranceSegment.distanceM/durationS/intensityFactor)
    - apps/mobile/components/home/HssRing.tsx (visual language reference for the recomposed ring)
    - apps/mobile/constants/theme.ts (Mono, Typography faces for the footer/stat labels)
    - .planning/phases/08-share-card-social-overlay/08-PATTERNS.md (ShareCardCanvas + Skia matchFont sections)
  </read_first>
  <action>
    Extend share.tsx's SQLite-is-truth useEffect to also fetch the stat-trio inputs (still never re-deriving HSS). For a strength/hybrid workout: read the strengthSet rows (same join finish.tsx uses) and aggregate total non-warmup volume (sum of loadKg*reps over reps-mode non-warmup sets), the work-set count, and the lift session duration derived from workout.createdAt -> workout.finishedAt (the same two session-boundary timestamps healthkitWriteback uses). For an endurance workout: read the endurance_segment row(s) for distanceM, durationS, and compute paceSecPerKm from distance/duration (guard divide-by-zero, matching finish.tsx). Fetch the profile units (fetchProfileSummary from lib/commitSet, as finish.tsx does) for unit-aware formatting. Build the correct trio via buildStrengthStatTrio / buildEnduranceStatTrio and pass the resulting 3 pairs into ShareCardCanvas.

    Extend ShareCardCanvas.tsx to render the D-03 stat trio and the D-04 footer within the 1080x1080 tree, recomposed for social scale (D-09 — bigger type, tuned spacing; not a pixel copy of in-app components): draw the three stat pairs as a bottom-anchored row of value+label columns using guarded matchFont (mono for labels/values, heavy face for values as appropriate), and a subtle footer containing a small APSIS wordmark + the plate-mark branding asset (source from the existing icon asset family, e.g. apps/mobile/assets/images/) plus the mono session-type/date caption already produced by buildShareCaption. Keep the athlete's numbers the visual star and branding subtle (D-04). Everything stays volt/bone/void per the palette; guard every matchFont in try/catch so a font-resolution miss never crashes the export.
  </action>
  <verify>
    <automated>cd /c/Users/ethan/Development/Apsis && pnpm run typecheck</automated>
    <human-check>On device, from a finished lift the card shows VOLUME / SETS / DURATION with correct numbers; from a finished run it shows DISTANCE / PACE / DURATION; the footer shows the APSIS wordmark + plate-mark + "LIFT/RUN — MON D" caption; the exported PNG matches the preview. (Full on-device UAT is 08-04's checkpoint.)</human-check>
  </verify>
  <acceptance_criteria>
    - share.tsx aggregates strength volume/sets + lift duration (from workout.createdAt/finishedAt) and endurance distance/pace/duration, and still does not call sessionHSSDetailed
    - ShareCardCanvas renders exactly 3 stat pairs plus a footer (wordmark + plate-mark + caption)
    - All font resolution in ShareCardCanvas is guarded (matchFont in try/catch)
    - `pnpm run typecheck` exits 0
  </acceptance_criteria>
  <done>The void card is fully composed: HSS ring hero, the correct fixed stat trio per session type, and the subtle branded footer — reading persisted stats from SQLite.</done>
</task>

<task type="auto">
  <name>Task 3: Share entry point on History session detail (D-11)</name>
  <files>apps/mobile/app/session/detail.tsx</files>
  <read_first>
    - apps/mobile/app/session/detail.tsx (nested Stack.Screen options lines 280-289; totalRow layout lines 309-312; workoutId from useLocalSearchParams line 112-113; styles)
    - apps/mobile/app/session/finish.tsx (the bone-filled Share card button added in 08-02 — mirror its style/route-push)
    - .planning/phases/08-share-card-social-overlay/08-PATTERNS.md (detail.tsx section)
  </read_first>
  <action>
    Add a Share entry point to detail.tsx (D-11) that pushes the same compose route with the workoutId already held in useLocalSearchParams. Follow the PATTERNS guidance: either a headerRight button in the existing nested Stack.Screen options object, OR a bone-filled Pressable appended after the totalRow following the same Spacing rhythm — pick one and keep the void/volt/bone palette (bone fill = Colors.dark.text, void label = Colors.dark.onAccent) so detail's existing volt total row keeps sole volt ownership. onPress: router.push({ pathname: '/session/share', params: { workoutId } }) (add useRouter from expo-router). Minimum 44pt hit target.
  </action>
  <verify>
    <automated>cd /c/Users/ethan/Development/Apsis && pnpm run typecheck</automated>
    <human-check>Open a session from History -> a Share affordance is visible -> tapping it opens the same compose screen with the correct session's card.</human-check>
  </verify>
  <acceptance_criteria>
    - detail.tsx has a Share affordance that calls router.push /session/share with the workoutId
    - The affordance uses bone fill (Colors.dark.text), not volt; detail's total row remains the only volt element
    - Hit target is at least 44pt
    - `pnpm run typecheck` exits 0
  </acceptance_criteria>
  <done>History session detail opens the same compose screen for its session, giving the phase its second entry point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite to card | Persisted session stats read for display (no re-derivation) |
| app to crash reporting | Stat values could reach Sentry breadcrumbs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | Information Disclosure | Sentry capturing stat/HSS values from the compose/detail screens | medium | mitigate | Add no new Sentry.setContext/breadcrumb calls in share.tsx/detail.tsx; existing sentrySanitize allowlist covers the rest |
| T-08-04 | Tampering | Stat-trio numbers drifting from finish.tsx/detail.tsx | low | mitigate | Reuse the exact @apsis/shared formatters + rounding conventions finish.tsx uses; unit tests pin both session types/unit systems |
</threat_model>

<verification>
- `pnpm --filter @apsis/mobile test -- shareCard` passes (trio formatters, both session types + unit systems)
- `pnpm run typecheck` clean
- Card shows the correct fixed trio + footer per session type
- Detail screen opens the compose screen
</verification>

<success_criteria>
- Fixed stat trio + footer branding composed into the card (D-03/D-04)
- Recomposed share-edition sizing (D-09)
- Second entry point live on session detail (D-11)
- Numbers match the app's existing conventions (no drift)
</success_criteria>

<artifacts_this_phase_produces>
New symbols created by this plan (excluded from downstream drift verification):
- apps/mobile/lib/shareCard.ts — interfaces ShareCardStrengthStats, ShareCardEnduranceStats; functions buildStrengthStatTrio, buildEnduranceStatTrio
- apps/mobile/components/share/ShareCardCanvas.tsx — extended props (statTrio pairs); footer + stat-trio render sub-elements
Modified:
- apps/mobile/app/session/share.tsx — extended stat query + profile units fetch
- apps/mobile/app/session/detail.tsx — new Share entry point + useRouter import
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-share-card-social-overlay/08-03-SUMMARY.md` when done
</output>
