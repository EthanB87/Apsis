---
phase: 05-healthkit-integration
plan: 08
type: execute
wave: 4
depends_on: [05-05]
files_modified:
  - apps/mobile/app/(tabs)/settings/index.tsx
autonomous: false
requirements: [HK-01, HK-02]
must_haves:
  truths:
    - "Settings has a permanent Apple Health section: a Connect row when not connected, a Connected info row + sync toggle + last-sync line when connected."
    - "Toggling sync off pauses HK reads/writes; already-imported sessions stay."
    - "The sync-status line shows LAST SYNC time when on and SYNC PAUSED when off."
  artifacts:
    - "Apple Health section in settings/index.tsx (connect row / connected+toggle / last-sync line)"
  key_links:
    - "the Connect row calls the same requestHealthKitAuthorization used at onboarding (D-03 permanent re-entry point)"
    - "toggle + last-sync read/write via getSyncState/setSyncState (D-19/D-21/D-24)"
---

<objective>
Add the permanent Settings "Apple Health" section (D-03) so existing installs and onboarding
decliners can connect, and connected users can pause sync and see last-sync status. Implements D-19
(connected = sheet completed), D-21 (sync toggle pauses reads/writes, imports stay), and D-24
(LAST SYNC line), plus the resolved SYNC PAUSED off-state copy.

Purpose: The permanent home for HealthKit connection state — the front door is onboarding, this is
where it lives forever.
Output: A new Apple Health section in the Settings screen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-healthkit-integration/05-UI-SPEC.md
@.planning/phases/05-healthkit-integration/05-RESEARCH.md
@apps/mobile/app/(tabs)/settings/index.tsx
@apps/mobile/lib/healthkitAuth.ts
@apps/mobile/lib/healthkitSyncState.ts
@apps/mobile/lib/healthkitImport.ts
@apps/mobile/constants/theme.ts
@apps/mobile/constants/Colors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apple Health section — connect row + connected/toggle/last-sync (D-03/19/21/24)</name>
  <read_first>
    - apps/mobile/app/(tabs)/settings/index.tsx (sectionLabel style; Units section ChoiceButton row lines 295-299; handleUnitsChange optimistic-rollback lines 227-237; footerText mono style lines 607-610; hairline dividers)
    - apps/mobile/lib/healthkitAuth.ts (requestHealthKitAuthorization), apps/mobile/lib/healthkitSyncState.ts (getSyncState/setSyncState — healthkitConnected, healthkitLastSyncAt)
    - .planning/phases/05-healthkit-integration/05-UI-SPEC.md (Component Notes §2 — full State A/State B spec, Switch trackColor/thumbColor, copy; Copywriting Contract; Color/Typography/Spacing tables)
    - apps/mobile/constants/theme.ts + constants/Colors.ts (Spacing, Mono, steel/volt/void/bone/ash tokens, Radius)
  </read_first>
  <action>
    Insert a new "Apple Health" section (existing sectionLabel style) after "Default Rest Timer" and
    before the footer, separated by hairline dividers like every other section boundary. State A (not
    connected, healthkitConnected false): one tappable row "Connect Apple Health" (Typography.body,
    bone) + sub-copy "Import runs and bodyweight. Your logged sessions go to Health too."
    (Typography.label, ash); tapping calls requestHealthKitAuthorization then
    setSyncState({ healthkitConnected: true }) — the permanent D-03 re-entry point for decliners /
    existing installs; optionally kick an initial runHealthKitSync in the background. State B
    (connected): a non-tappable "Connected" info row + sub-copy "Manage permissions in the Health
    app." (no false precision about specific read grants, D-19); a toggle row beneath (full row is the
    44px tap target) labelled "Sync with Apple Health" with a native RN Switch — trackColor { false:
    Colors.dark.steel, true: Colors.dark.accent }, thumbColor value ? Colors.dark.background :
    Colors.dark.text (void thumb on volt when ON, bone on steel when OFF). Toggling OFF pauses all HK
    reads/writes (D-21 — it gates the foreground sync + write-back tails already reading
    healthkitConnected; already-imported sessions are NOT removed or re-marked). Use the existing
    optimistic-set-then-rollback-on-failure pattern for the toggle write. Beneath the toggle, a Mono
    ash status line (footerText style): toggle ON shows "LAST SYNC {H:MM AM/PM}" (or "LAST SYNC —"
    before the first successful sync) from healthkitLastSyncAt (D-24); toggle OFF shows "SYNC PAUSED"
    (resolved gap — never a frozen stale timestamp). One-volt discipline: the Switch's volt-when-ON
    track is a state indicator, not a competing CTA. No error UI (D-25 — silent failures).
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "Apple Health" 'app/(tabs)/settings/index.tsx' && grep -q "SYNC PAUSED" 'app/(tabs)/settings/index.tsx' && grep -q "requestHealthKitAuthorization" 'app/(tabs)/settings/index.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - An "Apple Health" section exists between "Default Rest Timer" and the footer with hairline separators.
    - Not-connected state renders a tappable "Connect Apple Health" row calling `requestHealthKitAuthorization`.
    - Connected state renders a "Connected" info row + a "Sync with Apple Health" Switch with the specified trackColor/thumbColor tokens.
    - The status line shows a `LAST SYNC` mono line when ON and `SYNC PAUSED` when OFF (grep matches both strings).
    - The toggle write uses optimistic-set-then-rollback; no error banner/dialog is added (D-25).
  </acceptance_criteria>
  <done>Settings has a permanent Apple Health section that connects, toggles sync (pausing reads/writes without removing imports), and shows last-sync / paused status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device verification of the Settings Apple Health section</name>
  <action>
    On a physical device with the dev build, exercise the Settings Apple Health section end to
    end (connect → connected, toggle off → SYNC PAUSED + no sync on foreground, toggle on →
    LAST SYNC + sync runs, imports persist across toggle) and confirm each behaves per
    D-19/D-21/D-24 before sign-off.
  </action>
  <what-built>
    The Settings Apple Health section: connect row, connected info row, sync toggle, and
    LAST SYNC / SYNC PAUSED status line.
  </what-built>
  <how-to-verify>
    1. On a physical device with the dev build, open Settings and scroll to "Apple Health".
    2. If not connected, tap "Connect Apple Health" → the iOS permission sheet appears; completing it
       flips the row to "Connected" with the sync toggle visible.
    3. Toggle sync OFF → the status line reads "SYNC PAUSED"; foreground the app and confirm no import
       occurs while paused. Toggle ON → status returns to "LAST SYNC …" and a foreground sync runs.
    4. Confirm already-imported sessions remain in History after toggling off (D-21 — pause, not unlink).
  </how-to-verify>
  <resume-signal>Type "approved" once connect/toggle/last-sync behave per D-19/D-21/D-24 on device, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS permission sheet → app | Connect row requests scoped identifiers |
| toggle state → sync engine | Local flag gating all HK reads/writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | Settings connect authorization | low | mitigate | Reuses the shared minimal HK_READ_TYPES/HK_WRITE_TYPES; no broader scope |
| T-05-01 | Information Disclosure | last-sync/status rendering | low | mitigate | Renders only a timestamp/paused label — never raw bodyweight/HR values |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)
- Settings "Apple Health" section in `app/(tabs)/settings/index.tsx` (connect row, connected info row, sync Switch, LAST SYNC / SYNC PAUSED status line)
</artifacts_produced>

<verification>
- `grep` confirms the section, SYNC PAUSED copy, and requestHealthKitAuthorization call.
- On-device connect/toggle/last-sync behavior human-verified.
</verification>

<success_criteria>
The Settings Apple Health section connects (permanent D-03 re-entry), toggles sync on/off (pausing reads/writes while keeping imports), and shows LAST SYNC or SYNC PAUSED status.
</success_criteria>

<output>
Create `.planning/phases/05-healthkit-integration/05-08-SUMMARY.md` when done.
</output>