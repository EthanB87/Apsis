---
phase: 05-healthkit-integration
plan: 04
type: execute
wave: 2
depends_on: [05-02]
files_modified:
  - apps/mobile/components/history/DayRow.tsx
  - apps/mobile/app/(tabs)/history/index.tsx
  - apps/mobile/app/session/detail.tsx
autonomous: true
requirements: [HK-03]
must_haves:
  truths:
    - "A session sourced from Apple Health shows a subtle mono APPLE HEALTH chip in History and on session detail."
    - "Manually logged sessions show no chip (the default, expected case)."
  artifacts:
    - "APPLE HEALTH provenance chip rendered source-conditionally on DayRow session sub-rows and session detail"
    - "DaySession interface carries a source field threaded from dayGroupedSessions"
  key_links:
    - "workout.source flows dayGroupedSessions (05-02) -> history/index.tsx map -> DaySession.source -> DayRow chip"
---

<objective>
Render the D-08 provenance badge so imported sessions are visibly distinguished from manual ones —
the visible half of the "honest number extends to provenance" thesis. Reuses the existing
DayRow.tsx pill-chip shape with an ash tint (no new component, no new token) on two existing
surfaces: History session sub-rows and the session detail header.

Purpose: Provenance transparency (D-08) — a user must be able to tell which sessions came from
Health vs. what they logged by hand.
Output: A source-conditional APPLE HEALTH chip on History + session detail; DaySession threads source.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-healthkit-integration/05-UI-SPEC.md
@.planning/phases/05-healthkit-integration/05-CONTEXT.md
@apps/mobile/components/history/DayRow.tsx
@apps/mobile/app/(tabs)/history/index.tsx
@apps/mobile/app/session/detail.tsx
@apps/mobile/constants/theme.ts
@apps/mobile/constants/Colors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: APPLE HEALTH chip on History day-session rows (D-08)</name>
  <read_first>
    - apps/mobile/components/history/DayRow.tsx (DaySession interface line 32, sessionLabel line 57, the existing day-level "N SESSIONS · ADJUSTED" pill-chip shape to mirror)
    - apps/mobile/app/(tabs)/history/index.tsx (dayGroupedSessions usage line 103, DaySession map building line 114)
    - .planning/phases/05-healthkit-integration/05-UI-SPEC.md (Component Notes §3 — chip color formula, Mono fontSize 11, Radius.pill; Typography/Color tables)
    - apps/mobile/constants/theme.ts (Mono role, Radius.pill, Spacing.xs)
    - apps/mobile/constants/Colors.ts (mutedText/ash token)
  </read_first>
  <action>
    Add a `source: 'manual' | 'healthkit'` field to the `DaySession` interface in DayRow.tsx and
    render a mono APPLE HEALTH chip inline to the right of each session sub-row's label
    (Spacing.xs/4px gap), reusing the existing day-level pill-chip shape with an ash tint:
    backgroundColor 'rgba(138,144,152,0.15)', text color Colors.dark.mutedText (ash), Mono role at
    fontSize 11, Radius.pill. Render the chip ONLY when session.source === 'healthkit' (no chip for
    manual is the default/expected case, not an error). In history/index.tsx, thread `source` from
    the `dayGroupedSessions` query row (now selected by 05-02) into the constructed DaySession
    objects. Do not use volt/amber — ash tint only (D-08 "ledger language: mono captions, ash tint").
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "APPLE HEALTH" components/history/DayRow.tsx && grep -q "source" 'app/(tabs)/history/index.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `DaySession` in DayRow.tsx has a `source: 'manual' | 'healthkit'` field.
    - `grep -n "APPLE HEALTH" apps/mobile/components/history/DayRow.tsx` matches, rendered inside a `Radius.pill` chip using `Colors.dark.mutedText` and `rgba(138,144,152,0.15)`.
    - The chip renders only when `source === 'healthkit'` (guarded conditional), never for manual.
    - history/index.tsx populates `source` on each DaySession from the dayGroupedSessions row.
  </acceptance_criteria>
  <done>History session sub-rows show an ash APPLE HEALTH chip for HealthKit-sourced sessions and nothing for manual ones.</done>
</task>

<task type="auto">
  <name>Task 2: APPLE HEALTH chip on the session detail header (D-08)</name>
  <read_first>
    - apps/mobile/app/session/detail.tsx (the workout select query + the timestamp/title header lines)
    - apps/mobile/components/history/DayRow.tsx (the chip component/styles created in Task 1, to reuse the exact shape)
    - .planning/phases/05-healthkit-integration/05-UI-SPEC.md (Component Notes §3 — session detail placement: inline right of the timestamp line, before the title)
  </read_first>
  <action>
    Add `source: workout.source` to session/detail.tsx's existing workout select, and render the same
    ash APPLE HEALTH chip (identical shape to Task 1 — Mono fontSize 11, Radius.pill, ash tint) inline
    to the right of the timestamp line (flex-row, Spacing.sm/8px gap), before the title line, only when
    the loaded workout's source === 'healthkit'. Extract or share the chip so both surfaces stay
    visually identical rather than duplicating divergent styles. Does not alter the per-segment rows or
    the total row below.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "APPLE HEALTH" app/session/detail.tsx && grep -q "source" app/session/detail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - session/detail.tsx selects `workout.source` and conditionally renders the APPLE HEALTH chip inline with the timestamp line.
    - The chip only appears when the workout's source is 'healthkit'.
    - The chip visual (radius, mono size 11, ash color + tint) matches the History chip from Task 1.
  </acceptance_criteria>
  <done>Session detail shows the same ash APPLE HEALTH provenance chip for imported sessions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB read → UI | Rendering the source column value; no untrusted external input at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Information Disclosure | provenance chip | low | accept | Displaying a session's own provenance to the single local user is the intended behavior; no cross-user exposure exists (offline single-user app) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)
- `DaySession.source` field on the DayRow interface
- APPLE HEALTH provenance chip component/style (ash-tinted pill) reused on History + session detail
</artifacts_produced>

<verification>
- `grep "APPLE HEALTH"` matches in DayRow.tsx and session/detail.tsx.
- Chip renders source-conditionally on both surfaces; manual sessions show no chip.
</verification>

<success_criteria>
Imported sessions carry a subtle, consistent ash APPLE HEALTH chip in History and on session detail; manual sessions are unmarked.
</success_criteria>

<output>
Create `.planning/phases/05-healthkit-integration/05-04-SUMMARY.md` when done.
</output>