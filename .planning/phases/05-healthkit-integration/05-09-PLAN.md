---
phase: 05-healthkit-integration
plan: 09
type: execute
wave: 4
depends_on: [05-05]
files_modified:
  - apps/mobile/app/(tabs)/log/run.tsx
  - apps/mobile/app/(tabs)/index.tsx
autonomous: true
requirements: [HK-03]
must_haves:
  truths:
    - "When the run form's selected date already has an imported session of similar duration, a soft amber hint appears without blocking Save."
    - "When a foreground sync imports sessions, TODAY shows a quiet transient mono notice above the ring; silent when nothing new."
  artifacts:
    - "D-09 soft dedupe hint beneath the run form Date row"
    - "D-10 transient IMPORTED N SESSIONS FROM APPLE HEALTH notice on TODAY"
  key_links:
    - "the hint reuses the same D-06 dedupe tolerance the import pipeline uses (not a second constant)"
    - "the TODAY notice reads the last-import signal exported by useForegroundHealthKitSync (05-05)"
---

<objective>
Add the two reactive UI feedback touch-points that make imports legible: the D-09 reverse-order
dedupe hint on the run form (so a user saving a run that an import already covers is warned, never
blocked) and the D-10 transient TODAY notice (so the ring visibly moving after a foreground sync is
explained). Both follow clamp-and-warn / quiet-line brand voice; neither blocks anything.

Purpose: Honest provenance and no-silent-surprises — warn on a likely double, explain a moving ring.
Output: Soft hint on run.tsx + transient notice on TODAY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-healthkit-integration/05-UI-SPEC.md
@.planning/phases/05-healthkit-integration/05-CONTEXT.md
@apps/mobile/app/(tabs)/log/run.tsx
@apps/mobile/app/(tabs)/index.tsx
@apps/mobile/lib/healthkitMapping.ts
@apps/mobile/lib/healthkitImport.ts
@apps/mobile/hooks/useForegroundHealthKitSync.ts
@packages/db/src/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Soft dedupe hint on the run form (D-09)</name>
  <read_first>
    - apps/mobile/app/(tabs)/log/run.tsx (existing warningLabel style for HR/pace out-of-range warnings; the Date row; the selected localDate + duration state; the Save button enabled/disabled logic)
    - apps/mobile/lib/healthkitMapping.ts (isDuplicateOfExisting + DUPE_TOLERANCE — reuse, do not introduce a second tolerance)
    - packages/db/src/queries.ts (candidatesForDedupe — same-day + activity-type rows, filter to source='healthkit' for this hint)
    - .planning/phases/05-healthkit-integration/05-UI-SPEC.md (Component Notes §4 — placement beneath the Date row, amber warningLabel, exact copy; Copywriting Contract)
  </read_first>
  <action>
    On the run form, when the currently-selected date already has an imported (source==='healthkit')
    session for the same activity type whose duration is within the D-06 tolerance (reuse
    isDuplicateOfExisting / DUPE_TOLERANCE — NOT a second constant), render a soft hint directly
    beneath the Date row using the existing warningLabel style (Typography.label, amber
    Colors.dark.warning), copy exactly: "An imported run already covers this — saving will count
    both." Query the candidate imported sessions via candidatesForDedupe filtered to healthkit source
    for the selected localDate + activityType. The hint NEVER blocks Save (clamp-and-warn continuity,
    Phase 3 D-03) — the Save button's enabled/disabled state must be unaffected by the hint's
    presence. Silent when there is no matching imported session.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "saving will count both" 'app/(tabs)/log/run.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - The hint copy "An imported run already covers this — saving will count both." appears in run.tsx beneath the Date row using the amber warningLabel style.
    - The dedupe check reuses `isDuplicateOfExisting`/`DUPE_TOLERANCE` from healthkitMapping (no new tolerance constant is declared in run.tsx).
    - The candidate query filters to `source === 'healthkit'` for the selected localDate + activityType.
    - The Save button's enabled/disabled logic is unchanged by the hint (grep/inspection: no new Save-disabling condition tied to the hint).
  </acceptance_criteria>
  <done>The run form warns (never blocks) when the selected date already has a similar imported session, reusing the import pipeline's own tolerance.</done>
</task>

<task type="auto">
  <name>Task 2: Transient TODAY import notice (D-10)</name>
  <read_first>
    - apps/mobile/app/(tabs)/index.tsx (the "LET'S WORK" greeting line; the HssRing block; the existing module-scoped lastAnimatedHssDate/lastAnimatedHssValue animate-once gate to mirror)
    - apps/mobile/hooks/useForegroundHealthKitSync.ts (the last-import summary signal exported by 05-05 — importedCount + a batch id)
    - .planning/phases/05-healthkit-integration/05-UI-SPEC.md (Component Notes §5 — placement beneath the greeting/above the ring, Mono ash line, singular/plural copy, in-memory last-shown-batch lifecycle; Copywriting Contract)
  </read_first>
  <action>
    On TODAY, directly beneath the "LET'S WORK" greeting and above the HssRing, render a single-line
    Mono ash centered notice when the most recent foreground sync imported one or more sessions:
    "IMPORTED {N} SESSION{S} FROM APPLE HEALTH" (singular "1 SESSION", plural "N SESSIONS" per the
    Copywriting Contract). Read the last-import summary signal exported by useForegroundHealthKitSync
    (05-05). No card/background — a quiet inline line, not a banner. Render nothing (zero layout) when
    the sync imported zero sessions (the common case). Tie the notice to a specific completed batch id
    via a module-scoped "last shown batch" check (mirror index.tsx's existing lastAnimatedHssDate/
    lastAnimatedHssValue pattern) so it shows once per batch, disappears on the next fresh focus, and
    never reappears for the same batch — no explicit dismiss, no persisted dismissal table.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "FROM APPLE HEALTH" 'app/(tabs)/index.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - The notice copy "IMPORTED {N} SESSION(S) FROM APPLE HEALTH" (with correct singular/plural) appears in TODAY beneath the greeting and above the ring, Mono ash.
    - It reads the import-summary signal from useForegroundHealthKitSync (not a re-query of the DB).
    - It renders nothing when the last sync imported zero sessions.
    - A module-scoped last-shown-batch guard makes it show once per batch and not reappear for that batch.
  </acceptance_criteria>
  <done>TODAY shows a quiet transient notice explaining a just-completed import, and stays silent when nothing new arrived.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import result → UI | Rendering counts/labels derived from local import state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | hint/notice text | low | mitigate | Render only counts and generic copy — never raw imported HR/distance/bodyweight values |
| T-05-05 | Tampering | dedupe hint query | low | mitigate | candidatesForDedupe is a parameterized builder; no raw sql interpolation |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)
- D-09 soft dedupe hint block in `app/(tabs)/log/run.tsx`
- D-10 transient import notice in `app/(tabs)/index.tsx` (module-scoped last-shown-batch gate)
</artifacts_produced>

<verification>
- `grep` confirms both copy strings are present in their target files.
- On-device: hint appears on a colliding date (non-blocking); TODAY notice appears once after a real foreground import (UAT/manual per Pitfall 6).
</verification>

<success_criteria>
The run form warns without blocking on a likely double-count, and TODAY quietly explains a just-completed import while staying silent when nothing new arrived.
</success_criteria>

<output>
Create `.planning/phases/05-healthkit-integration/05-09-SUMMARY.md` when done.
</output>