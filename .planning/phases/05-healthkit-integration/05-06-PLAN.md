---
phase: 05-healthkit-integration
plan: 06
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - apps/mobile/lib/healthkitWriteback.ts
  - apps/mobile/lib/runEntry.ts
  - apps/mobile/lib/finishWorkout.ts
autonomous: true
requirements: [HK-04]
must_haves:
  truths:
    - "Saving a run or finishing a lift writes that session to Apple Health with distance/duration + an ApsisHSS metadata key and no calories."
    - "The HealthKit sample UUID we authored is stored back on the workout row."
    - "Discarding an Apsis-authored session also deletes our sample from Health; imported sessions' samples are never deleted."
    - "A failed write/delete never blocks or fails the local save (fire-and-forget)."
  artifacts:
    - "apps/mobile/lib/healthkitWriteback.ts (writeBackRun, writeBackLift, deleteHealthKitSample)"
    - "saveRun + finishWorkout extended with fire-and-forget write-back; discardWorkout extended with delete-sync"
  key_links:
    - "totals.distance is passed as raw meters, never through km/mi display conversion (Pitfall 4)"
    - "delete-sync only fires for source==='manual' rows we authored (D-14), never for imported samples we don't own"
---

<objective>
Write logged sessions back to Apple Health and keep Health in sync when Apsis sessions are discarded.
Implements D-12 (immediate fire-and-forget on save/finish), D-13 (basics + ApsisHSS metadata, no
calories), D-14 (delete our authored sample on soft-delete), D-15 (go-forward only), and D-25
(silent failure, never blocks the local save).

Purpose: Requirement HK-04 — a saved Apsis session appears in the Health app, and removing it in
Apsis removes our copy from Health.
Output: healthkitWriteback.ts + write-back tails on saveRun/finishWorkout + delete-sync on discardWorkout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-healthkit-integration/05-RESEARCH.md
@.planning/phases/05-healthkit-integration/05-PATTERNS.md
@apps/mobile/lib/runEntry.ts
@apps/mobile/lib/finishWorkout.ts
@apps/mobile/lib/healthkitMapping.ts
@apps/mobile/lib/healthkitAuth.ts
@packages/db/src/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: healthkitWriteback.ts — writeBackRun / writeBackLift / deleteHealthKitSample (D-12/13/14/25)</name>
  <read_first>
    - apps/mobile/lib/finishWorkout.ts (existing discardWorkout soft-delete shape lines 42-45; doc-comment decision-citation convention)
    - apps/mobile/lib/healthkitMapping.ts (buildHSSMetadata / HSS_METADATA_KEY — the ApsisHSS metadata builder, D-13)
    - .planning/phases/05-healthkit-integration/05-RESEARCH.md (Pattern 3 saveWorkoutSample; Code Examples delete-sync; Pitfall 4 totals.distance meters; Open Question 2 empty quantities array)
    - .planning/phases/05-healthkit-integration/05-PATTERNS.md (healthkitWriteback.ts section — fire-and-forget, the ONE place a caught error must NOT rethrow)
  </read_first>
  <action>
    Create healthkitWriteback.ts with three fire-and-forget helpers. writeBackRun(distanceM,
    durationS, hss, startedAt): call saveWorkoutSample(running, [] empty quantities per Open
    Question 2, startedAt, startedAt+durationS, distanceM != null ? { distance: distanceM } :
    undefined, buildHSSMetadata(hss)) — pass distanceM as RAW METERS, never through any km/mi display
    conversion (Pitfall 4); return the created sample uuid. writeBackLift(durationS, hss, startedAt):
    same shape with traditionalStrengthTraining and no distance. deleteHealthKitSample(uuid): call
    deleteObjects for the workout type identifier + uuid. NO calorie/energy fields anywhere (D-13 —
    keeps Health energy data clean for v1.1 nutrition). Every function wraps its native call in
    try/.catch and console.error's the Error object only (never raw values, T-05-01) and NEVER
    rethrows — this is the one codebase location where a caught error is swallowed, not re-thrown
    (contrast saveRun/recomputeLoadDaily). Return undefined on failure so callers skip storing a uuid.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "saveWorkoutSample" lib/healthkitWriteback.ts && grep -q "deleteObjects" lib/healthkitWriteback.ts</automated>
  </verify>
  <acceptance_criteria>
    - `writeBackRun` passes `distanceM` directly into `totals.distance` with no `km`/`mi` helper call in the path (Pitfall 4).
    - The metadata argument uses `buildHSSMetadata`/`HSS_METADATA_KEY` and contains no calorie/energy key (D-13).
    - Every native call is inside a `.catch`/try-catch that logs an Error and does not rethrow.
    - `deleteHealthKitSample` calls `deleteObjects` with the workout type identifier and the uuid.
  </acceptance_criteria>
  <done>Write-back and delete helpers exist, pass metric distance raw, attach only ApsisHSS metadata, and never throw.</done>
</task>

<task type="auto">
  <name>Task 2: Wire write-back into saveRun + finishWorkout; delete-sync into discardWorkout (D-12/14/15)</name>
  <read_first>
    - apps/mobile/lib/runEntry.ts (saveRun — the successful-save tail after recomputeLoadDaily; workoutId + startedAt availability)
    - apps/mobile/lib/finishWorkout.ts (finishWorkout + discardWorkout; the row.source/healthkitUuid select shown in RESEARCH Code Examples)
    - apps/mobile/lib/healthkitWriteback.ts (Task 1 helpers)
    - apps/mobile/lib/healthkitSyncState.ts (getSyncState — gate write-back on healthkitConnected)
    - .planning/phases/05-healthkit-integration/05-RESEARCH.md (Code Examples discardWorkout delete-sync; Architecture reverse-direction diagram)
  </read_first>
  <action>
    In saveRun: after the existing recomputeLoadDaily success, if healthkitConnected (getSyncState),
    fire writeBackRun (do NOT await it in a way that can fail the save — fire-and-forget, D-12), and
    when it resolves with a uuid, UPDATE workout SET healthkitUuid for that workoutId. In finishWorkout:
    same fire-and-forget writeBackLift tail (compute duration from the session; session HSS already on
    the row) storing the returned uuid on the workout. In discardWorkout: extend per RESEARCH Code
    Examples — before/after soft-delete, read the row's source + healthkitUuid; only when
    source === 'manual' AND healthkitUuid != null (a sample WE authored, D-14) call
    deleteHealthKitSample(healthkitUuid) as a fire-and-forget tail. An imported session's healthkitUuid
    refers to a sample we do NOT own and must NEVER be deleted. Write-back is go-forward only (D-15 — no
    backfill of pre-existing sessions). None of these tails may block or fail the local save/finish/
    discard (D-25). Keep the existing throw-after-log behavior for the DB operations themselves.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "writeBackRun" lib/runEntry.ts && grep -q "writeBackLift" lib/finishWorkout.ts && grep -q "deleteHealthKitSample" lib/finishWorkout.ts</automated>
  </verify>
  <acceptance_criteria>
    - saveRun fires `writeBackRun` after a successful save and stores the returned uuid on the workout row; the write-back cannot fail the save.
    - finishWorkout fires `writeBackLift` and stores the returned uuid.
    - discardWorkout deletes the HK sample ONLY when `source === 'manual' && healthkitUuid != null` — never for imported (`source === 'healthkit'`) rows.
    - Write-back tails are gated on `healthkitConnected` and are go-forward only (D-15).
    - The existing DB-operation error handling (throw-after-log) is preserved; only the HK tails are fire-and-forget.
  </acceptance_criteria>
  <done>Runs and lifts write to Health on save/finish with their uuid stored; discarding an Apsis-authored session deletes our Health sample; imported samples are never deleted; nothing blocks the local save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → HealthKit write | Apsis writes workout samples + metadata into the shared Health store |
| discard → HealthKit delete | Only self-authored samples may be deleted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | delete-sync ownership | medium | mitigate | Delete only fires for source==='manual' rows Apsis authored (D-14) — imported samples we don't own are never deleted |
| T-05-01 | Information Disclosure | write-back logging | medium | mitigate | Log Error objects/booleans only; never raw HSS/HR/bodyweight values (feeds REL-03) |
| T-05-08 | Information Disclosure | write-back payload | low | accept | Writing distance/duration + ApsisHSS metadata to the user's own Health store is the intended HK-04 behavior; no calories fabricated (D-13) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)
- `apps/mobile/lib/healthkitWriteback.ts`: `writeBackRun`, `writeBackLift`, `deleteHealthKitSample`
- Fire-and-forget write-back tails added to `saveRun` and `finishWorkout` (store returned uuid on workout)
- Delete-sync tail added to `discardWorkout` (source==='manual' only)
</artifacts_produced>

<verification>
- `grep` confirms write-back calls in runEntry.ts/finishWorkout.ts and delete-sync in discardWorkout.
- Actual appearance in the Health app + delete-on-discard is UAT/manual-only (physical device, Pitfall 6).
</verification>

<success_criteria>
Saved runs and finished lifts appear in Health with ApsisHSS metadata and no calories; discarding an Apsis-authored session removes our Health sample; imported samples are untouched; no HK operation ever blocks a local save.
</success_criteria>

<output>
Create `.planning/phases/05-healthkit-integration/05-06-SUMMARY.md` when done.
</output>