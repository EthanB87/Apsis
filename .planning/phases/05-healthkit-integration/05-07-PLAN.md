---
phase: 05-healthkit-integration
plan: 07
type: execute
wave: 4
depends_on: [05-05]
files_modified:
  - apps/mobile/app/onboarding/healthkit.tsx
  - apps/mobile/app/onboarding/review.tsx
  - apps/mobile/hooks/useSaveProfile.ts
autonomous: false
requirements: [HK-01]
must_haves:
  truths:
    - "After saving their profile, a new user sees a terminal Connect Apple Health step they can accept or skip."
    - "Granting permission kicks off the 90-day import in the background while onboarding finishes immediately."
    - "Declining advances to the app with one quiet line and never re-prompts."
  artifacts:
    - "apps/mobile/app/onboarding/healthkit.tsx (terminal, skippable step)"
    - "review.tsx pushes to /onboarding/healthkit after save; profile-version bump moved into healthkit.tsx"
  key_links:
    - "the Stack.Protected gate must not flip until the HealthKit step completes (Pitfall 2 race)"
---

<objective>
Append the terminal HealthKit onboarding step (D-23) and fix the profile-version-bump race that
would otherwise make the step unreachable (RESEARCH Pitfall 2). Implements D-03 (onboarding as the
front door), D-20 (skippable, quiet decline, no re-prompt), and D-22 (grant then background 90-day
import, onboarding finishes immediately).

Purpose: The new-user permission entry point — and the delicate gate-timing fix without which the
step never shows.
Output: healthkit.tsx terminal step + review.tsx push + relocated version bump.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-healthkit-integration/05-UI-SPEC.md
@.planning/phases/05-healthkit-integration/05-RESEARCH.md
@.planning/phases/05-healthkit-integration/05-PATTERNS.md
@apps/mobile/app/onboarding/review.tsx
@apps/mobile/app/onboarding/bodyweight.tsx
@apps/mobile/hooks/useSaveProfile.ts
@apps/mobile/lib/profileVersion.ts
@apps/mobile/lib/healthkitAuth.ts
@apps/mobile/lib/healthkitImport.ts
@apps/mobile/lib/healthkitSyncState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the profile-version bump out of useSaveProfile; review.tsx pushes to the HK step (Pitfall 2)</name>
  <read_first>
    - apps/mobile/hooks/useSaveProfile.ts (bumpProfileVersion call at line 57 inside save())
    - apps/mobile/app/onboarding/review.tsx (handleSubmit lines 37-55 — currently relies on the gate auto-navigating)
    - apps/mobile/lib/profileVersion.ts (useProfileVersion bump signal)
    - .planning/phases/05-healthkit-integration/05-RESEARCH.md (Pitfall 2 — HIGH confidence, reproduced from live code)
    - .planning/phases/05-healthkit-integration/05-PATTERNS.md (healthkit.tsx MANDATORY deviation section)
  </read_first>
  <action>
    Remove the `bumpProfileVersion()` call from useSaveProfile.save() so inserting the profile no
    longer immediately flips Plan 04's Stack.Protected gate to the tab shell (Pitfall 2). Update the
    useSaveProfile doc comment to note the bump now happens in the terminal HealthKit step. In
    review.tsx handleSubmit, after a successful save() (the insert), call
    router.push('/onboarding/healthkit') instead of relying on the gate to auto-navigate; update the
    stale "no manual navigation needed here" comment. The gate must NOT flip until the HK step's
    handlers bump the version (Task 2). Do not change the save payload or the ProfileReview props.
  </action>
  <verify>
    <automated>cd apps/mobile && ! grep -q "bumpProfileVersion" hooks/useSaveProfile.ts && grep -q "onboarding/healthkit" app/onboarding/review.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bumpProfileVersion" apps/mobile/hooks/useSaveProfile.ts` returns 0 (the call is removed from save()).
    - review.tsx `handleSubmit` calls `router.push('/onboarding/healthkit')` after a successful save.
    - The save payload and ProfileReview props are unchanged.
  </acceptance_criteria>
  <done>Saving the profile no longer flips the gate; review.tsx routes to the new HealthKit step instead.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Build the terminal healthkit.tsx onboarding step (D-20/D-22/D-23)</name>
  <read_first>
    - apps/mobile/app/onboarding/bodyweight.tsx (WizardStep shell + screen chrome, SafeAreaView edges, button chrome)
    - apps/mobile/app/onboarding/review.tsx (terminal-step SafeAreaView + heading style, void background)
    - apps/mobile/lib/profileVersion.ts (bump signal to call on BOTH accept and skip)
    - apps/mobile/lib/healthkitAuth.ts (requestHealthKitAuthorization), apps/mobile/lib/healthkitSyncState.ts (setSyncState healthkitConnected), apps/mobile/lib/healthkitImport.ts (runHealthKitSync initial background import)
    - .planning/phases/05-healthkit-integration/05-UI-SPEC.md (Component Notes §1 — full screen spec, copy, spacing, one-volt discipline; Copywriting Contract)
    - apps/mobile/constants/theme.ts + constants/Colors.ts (Spacing.xxxl top pad, Typography.heading/body/label, volt/ash/bone tokens)
  </read_first>
  <action>
    Create app/onboarding/healthkit.tsx as a standalone terminal screen (NOT a WizardStep — no
    progress dots; the profile is already saved, UI-SPEC §1). SafeAreaView void background, top pad
    Spacing.xxxl (48px, quieter than WizardStep's 64px). Headline "Connect Apple Health?"
    (Typography.heading, bone). Body copy per Copywriting Contract. Optional mono benefit list
    (executor discretion, cuttable). Pinned action block: primary volt button "Connect Apple Health"
    (WizardStep button chrome — minHeight 48, Radius.md, volt fill, void text, accentPressed) whose
    onPress calls requestHealthKitAuthorization, then setSyncState({ healthkitConnected: true }), then
    fires runHealthKitSync(db, { initial: true }) in the BACKGROUND (do not await/block — D-22), then
    bumps the profile version and advances; Spacing.sm gap; then "Not now" ash text link
    (Typography.label, 44px hit target) that simply bumps the profile version and advances (D-20 —
    declining still completes onboarding); and a static ash sub-copy line "You can connect anytime in
    Settings." always visible (resolved D-20 interpretation — static, not a toast). BOTH paths bump
    the profile version (that is what flips the gate now, per Task 1) and must never re-show this
    screen (D-20 no re-prompt). One-volt discipline: the primary button is the only volt element.
  </action>
  <what-built>
    A terminal onboarding screen offering Connect Apple Health (triggers the iOS permission sheet +
    background 90-day import) or Not now (quiet skip), after which onboarding completes.
  </what-built>
  <how-to-verify>
    1. Fresh install / reset onboarding on a physical device; complete the wizard through
       "Save & Start Training".
    2. Confirm the HealthKit step appears AFTER save (does not jump straight to the tab shell —
       Pitfall 2 warning sign).
    3. Tap "Connect Apple Health" → the iOS Health permission sheet appears; completing it lands on
       the TODAY tab (calibrating), which then live-updates as the background import lands (D-22).
    4. Reset and repeat, tapping "Not now" → advances to the tab shell with no dialog; confirm the
       step never re-appears on subsequent launches (D-20).
  </how-to-verify>
  <resume-signal>Type "approved" once both accept and skip paths behave per D-20/D-22/D-23 on device, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS permission sheet → app | User-mediated grant; app requests scoped identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | onboarding authorization | low | mitigate | Uses the shared minimal HK_READ_TYPES/HK_WRITE_TYPES from healthkitAuth (05-03); no broader request |
| T-05-09 | Denial of Service | background import on onboarding | low | mitigate | 90-day import runs off the main completion path (D-22); onboarding finishes immediately and never blocks on it |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)
- `apps/mobile/app/onboarding/healthkit.tsx` (terminal onboarding route)
- Relocation of the profile-version bump from `useSaveProfile.save()` into the HealthKit step handlers
- `review.tsx` navigation to `/onboarding/healthkit`
</artifacts_produced>

<verification>
- `grep` confirms bump removed from useSaveProfile and review.tsx pushes to the HK step.
- On-device: HK step reachable after save; accept/skip both complete onboarding without re-prompt (human-verified).
</verification>

<success_criteria>
New users reach a terminal, skippable Connect Apple Health step after saving their profile; granting starts a background 90-day import while onboarding finishes immediately; declining advances quietly and never re-prompts.
</success_criteria>

<output>
Create `.planning/phases/05-healthkit-integration/05-07-SUMMARY.md` when done.
</output>