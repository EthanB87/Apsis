---
phase: 05-healthkit-integration
plan: 05
type: execute
wave: 3
depends_on: [05-03]
files_modified:
  - apps/mobile/lib/healthkitImport.ts
  - apps/mobile/hooks/useForegroundHealthKitSync.ts
  - apps/mobile/app/(tabs)/_layout.tsx
autonomous: true
requirements: [HK-01, HK-02, HK-03]
must_haves:
  truths:
    - "On first connection the app imports up to 90 days of Health runs/cardio, deduped against manual entries, updating the ring/band once."
    - "On every app foreground the app anchor-syncs new Health workouts and the most-recent bodyweight, silently."
    - "Apsis never re-imports its own write-backs, and deleted imports never resurrect."
  artifacts:
    - "apps/mobile/lib/healthkitImport.ts (impure batch import + bodyweight import)"
    - "apps/mobile/hooks/useForegroundHealthKitSync.ts (AppState foreground trigger)"
  key_links:
    - "recomputeLoadDaily runs exactly once at the end of the batch (Pitfall 10)"
    - "anchor + lastSyncAt persist only on full batch success (D-25) so a failed sync retries the same window"
    - "echo-exclusion = sources filter + healthkitUuid tombstone incl. soft-deleted rows (Pitfall 8/9)"
---

<objective>
Build the import engine: the initial 90-day backfill and the ongoing foreground anchor-sync that
pull Health workouts + bodyweight into Apsis, deduped and echo-excluded, ending in a single
recompute. Implements D-01 (90-day), D-02 (foreground delta), D-04/D-05 mapping (via 05-03),
D-06/D-07 (dedupe, manual wins), D-11 (echo exclusion), D-16/D-17 (bodyweight sync + recency),
and D-25 (silent failure, anchor advances only on success).

Purpose: This is the core value — existing Apple Watch history flowing in so a new user sees a real
readiness band on day one, with no duplicates and no self-echo.
Output: healthkitImport.ts + the AppState foreground sync hook mounted in the tab shell.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-healthkit-integration/05-RESEARCH.md
@.planning/phases/05-healthkit-integration/05-PATTERNS.md
@apps/mobile/lib/runEntry.ts
@apps/mobile/lib/runEntryLogic.ts
@apps/mobile/lib/recomputeLoadDaily.ts
@apps/mobile/components/session/RestTimerBanner.tsx
@apps/mobile/lib/healthkitMapping.ts
@apps/mobile/lib/healthkitAuth.ts
@apps/mobile/lib/healthkitSyncState.ts
@packages/db/src/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: healthkitImport.ts — anchored batch import + dedupe + bodyweight (D-01/02/06/07/11/16/17)</name>
  <read_first>
    - apps/mobile/lib/runEntry.ts (saveRun insert -> HSS -> recompute sequence; fetchThresholds; endurance_segment shape; console.error+rethrow at batch boundary)
    - apps/mobile/lib/runEntryLogic.ts (resolveRunSegment — reuse for imported-run IF resolution, pace gated to run)
    - apps/mobile/lib/recomputeLoadDaily.ts (single recompute contract, Pitfall 10)
    - apps/mobile/lib/healthkitMapping.ts (mapHKActivityType, isDuplicateOfExisting, bodyweightSampleIsNewer, sanitizeHKNumeric)
    - apps/mobile/lib/healthkitAuth.ts (HK_READ_TYPES), apps/mobile/lib/healthkitSyncState.ts (getSyncState/setSyncState)
    - packages/db/src/queries.ts (candidatesForDedupe — tombstone-inclusive; activeWorkoutFilter contrast)
    - .planning/phases/05-healthkit-integration/05-RESEARCH.md (Pattern 2 anchored sync; Pattern 1; Code Examples bodyweight import; Pitfalls 4/8/9/10; Bodyweight import; Architecture diagram)
    - .planning/phases/05-healthkit-integration/05-PATTERNS.md (healthkitImport.ts section — critical batch deviation)
  </read_first>
  <action>
    Create healthkitImport.ts exporting a batch sync function (e.g. runHealthKitSync(db, { initial }))
    that: (1) reads stored anchor via getSyncState; (2) queries workouts with
    queryWorkoutSamplesWithAnchor using a NOT sources filter for the current app source (D-11 primary
    echo defense) and, on initial connect, a 90-day date window (D-01) with anchor undefined; (3) for
    each returned workout, maps activity type via mapHKActivityType (skip null — D-05 strength and
    unknowns), sanitizes distance/duration/HR via sanitizeHKNumeric (V5/T-05-02), and checks
    candidatesForDedupe(db, localDate, activityType): skip when isDuplicateOfExisting matches a manual
    row (D-06/D-07 manual wins) OR when the sample uuid already exists in candidatesForDedupe's
    healthkitUuid values including soft-deleted tombstones (D-11 second layer + deleted-stays-deleted,
    Pitfall 8/9); (4) for each non-duplicate, resolve IF via resolveRunSegment (pace gated to run),
    insert workout(source='healthkit', healthkitUuid=<sample uuid>, finishedAt=<sample end>) +
    endurance_segment, write workout.hss via sessionHSSDetailed — mirroring saveRun but WITHOUT calling
    recompute per row; (5) after the loop call recomputeLoadDaily(db) EXACTLY ONCE (Pitfall 10); (6)
    pull the most-recent bodyweight via getMostRecentQuantitySample(BodyMass,'kg') and, if
    bodyweightSampleIsNewer than the stored bodyweightSetAt, update user_profile.bodyweightKg +
    bodyweightSetAt=<sample.startDate> (D-16/D-17 — sample time, not import time); (7) persist newAnchor
    + healthkitLastSyncAt via setSyncState ONLY after the whole batch succeeds (D-25 — a thrown error
    leaves the anchor unadvanced so the next foreground sync retries the same window). Return a summary
    (e.g. importedCount) for the TODAY notice. Log counts/Errors only, never raw HK values (T-05-01).
    console.warn per-row dedupe skips; console.error + rethrow at the outer batch boundary.
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "recomputeLoadDaily" lib/healthkitImport.ts && grep -q "candidatesForDedupe" lib/healthkitImport.ts && grep -q "getMostRecentQuantitySample" lib/healthkitImport.ts</automated>
  </verify>
  <acceptance_criteria>
    - `recomputeLoadDaily` is called exactly once (grep shows a single call site, after the insert loop — not inside it).
    - Imported rows are inserted with `source: 'healthkit'` and a non-null `healthkitUuid`.
    - Dedupe consults `candidatesForDedupe` and skips both duration-overlap manual matches (D-06/07) and already-seen healthkitUuid tombstones (D-11/Pitfall 9).
    - Bodyweight update sets `bodyweightSetAt` to the sample's startDate, gated by `bodyweightSampleIsNewer`.
    - `setSyncState` writing the new anchor/lastSyncAt occurs only on the success path (not in a catch/finally that runs on failure).
    - No log statement interpolates a raw bodyweight or HR value.
  </acceptance_criteria>
  <done>The import engine backfills 90 days, foreground-delta-syncs via anchor, dedupes and echo-excludes, imports bodyweight by recency, and recomputes once — all silent on failure.</done>
</task>

<task type="auto">
  <name>Task 2: useForegroundHealthKitSync hook + mount in tab shell (D-02)</name>
  <read_first>
    - apps/mobile/components/session/RestTimerBanner.tsx (AppState.addEventListener('change') + cleanup-on-unmount pattern lines 63-70)
    - apps/mobile/lib/healthkitSyncState.ts (getSyncState — read healthkitConnected gate)
    - apps/mobile/lib/healthkitImport.ts (runHealthKitSync from Task 1)
    - apps/mobile/app/(tabs)/_layout.tsx (tab shell — mount point that is always alive while the app is usable, and out of the logging screens)
    - .planning/phases/05-healthkit-integration/05-PATTERNS.md (useForegroundHealthKitSync.ts section — debounce/in-flight guard is a new discretion item)
  </read_first>
  <action>
    Create useForegroundHealthKitSync.ts mirroring RestTimerBanner's AppState 'change' listener +
    cleanup shape. On nextState === 'active', if getSyncState reports healthkitConnected === true
    (D-21 toggle gate), call runHealthKitSync(db, { initial: false }); guard against re-entrancy with
    an in-flight ref/flag and a short debounce so a rapid background/foreground bounce doesn't
    double-trigger (new discretion item — not in the analog). Never surface errors — the hook swallows
    a rejected sync as a silent no-op (D-25). Mount the hook once in app/(tabs)/_layout.tsx (the tab
    shell, deliberately NOT inside a logging screen so sync stays off the logging path — local-first).
    Do not add any UI. Expose the last import summary through the mechanism 05-09's TODAY notice will
    read (module-scoped signal or a lightweight store — coordinate the exported symbol name in the
    SUMMARY so 05-09 can consume it).
  </action>
  <verify>
    <automated>cd apps/mobile && grep -q "AppState" hooks/useForegroundHealthKitSync.ts && grep -q "useForegroundHealthKitSync" 'app/(tabs)/_layout.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - The hook adds an `AppState` 'change' listener and removes it on cleanup (mirrors RestTimerBanner).
    - Sync only runs when `healthkitConnected` is true (D-21 gate).
    - An in-flight guard prevents a second concurrent sync from a rapid foreground bounce.
    - The hook is mounted exactly once in `app/(tabs)/_layout.tsx` and renders no UI.
    - A rejected `runHealthKitSync` does not throw out of the hook (silent, D-25).
  </acceptance_criteria>
  <done>Foreground app activation triggers a gated, debounced, silent HealthKit sync from the tab shell — never on the logging path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HealthKit samples → SQLite/engine | External workout/bodyweight/HR numbers are persisted and scored |
| Apsis write-backs → next import | Own-authored samples must not re-enter as imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Tampering | imported numeric values | medium | mitigate | sanitizeHKNumeric clamps negative distance / NaN duration / bad HR before insert or engine (V5) |
| T-05-04 | Tampering | echo / resurrection | low | mitigate | sources NOT-filter + healthkitUuid tombstone check incl. soft-deleted rows (Pitfall 8/9) |
| T-05-01 | Information Disclosure | sync logging | medium | mitigate | Log counts/Errors only — never raw bodyweight/HR sample values (feeds REL-03) |
| T-05-05 | Tampering | import inserts | low | mitigate | Parameterized drizzle builders only; no raw sql with interpolated values |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)
- `apps/mobile/lib/healthkitImport.ts`: `runHealthKitSync` (+ import summary type)
- `apps/mobile/hooks/useForegroundHealthKitSync.ts`: `useForegroundHealthKitSync` (+ last-import signal exported for the TODAY notice)
- Mount of `useForegroundHealthKitSync` in `app/(tabs)/_layout.tsx`
</artifacts_produced>

<verification>
- `grep` confirms single `recomputeLoadDaily` call, dedupe + bodyweight queries in healthkitImport.ts.
- Hook mounted in tab shell with AppState listener.
- On-device behavior (actual import/dedupe/bodyweight) is UAT/manual-only per RESEARCH (Simulator has no Health data — Pitfall 6).
</verification>

<success_criteria>
Health workouts and bodyweight import on connect (90 days) and on every foreground, deduped and echo-excluded, recomputing once and advancing the anchor only on success.
</success_criteria>

<output>
Create `.planning/phases/05-healthkit-integration/05-05-SUMMARY.md` when done.
</output>