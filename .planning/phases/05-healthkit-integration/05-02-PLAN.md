---
phase: 05-healthkit-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema.ts
  - packages/db/src/queries.ts
  - packages/db/src/index.ts
  - packages/db/drizzle/
  - packages/db/src/__tests__/dedupe-candidate-query.test.ts
autonomous: true
requirements: [HK-02, HK-03, HK-04]
must_haves:
  truths:
    - "The workout table records provenance (source) and the HealthKit sample UUID of every Apsis-authored or imported HK row."
    - "user_profile records when bodyweight was last set and the HealthKit connection/sync state."
    - "A dedupe-candidate query returns same-day, same-activity-type sessions INCLUDING soft-deleted tombstones."
  artifacts:
    - "packages/db/src/schema.ts extended: workout.source, workout.healthkitUuid, user_profile.bodyweightSetAt/healthkitConnected/healthkitLastSyncAt/healthkitAnchor"
    - "packages/db/drizzle/0003_*.sql generated migration"
    - "candidatesForDedupe query builder exported from @apsis/db"
  key_links:
    - "The generated .sql is picked up by babel-plugin-inline-import + migrations.js so it ships and runs at startup"
    - "dayGroupedSessions selects workout.source so History can render the provenance chip"
---

<objective>
Add the schema columns and query builder that provenance, dedupe, echo-exclusion, write-back
UUID tracking, and bodyweight-recency all depend on, then materialize the change as a bundled
drizzle migration. Implements the storage side of D-06 (dedupe), D-08 (source badge), D-11/D-14
(healthkitUuid), D-17 (bodyweightSetAt), and D-19/D-21/D-24 (connection + last-sync + anchor).

Purpose: Every downstream import/write-back/UI plan reads these columns; the migration is the one
step that makes the columns exist in the live device DB (types alone come from the schema file and
would falsely pass build/typecheck without it).
Output: Extended schema + `candidatesForDedupe` builder + a generated `0003_*.sql` migration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-healthkit-integration/05-RESEARCH.md
@.planning/phases/05-healthkit-integration/05-PATTERNS.md
@packages/db/src/schema.ts
@packages/db/src/queries.ts
@packages/db/src/index.ts
@packages/db/drizzle.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend schema + dedupe query builder</name>
  <read_first>
    - packages/db/src/schema.ts (workout table lines 59-75, userProfile lines 23-34, column-with-inline-decision-comment convention)
    - packages/db/src/queries.ts (previousSessionSet builder, activeWorkoutFilter doc comment lines 31-35, QueryableDB type)
    - packages/db/src/index.ts (barrel export list — queries block)
    - packages/db/src/__tests__/previous-session-query.test.ts (existing .toSQL()/sqlite-proxy test convention to mirror)
    - .planning/phases/05-healthkit-integration/05-RESEARCH.md (Code Examples: candidatesForDedupe; Pitfall 9; Recommended Project Structure)
    - .planning/phases/05-healthkit-integration/05-PATTERNS.md (schema.ts + queries.ts sections)
  </read_first>
  <behavior>
    - candidatesForDedupe(db, localDate, activityType) builds a SQL selecting durationS + healthkitUuid, joining workout↔endurance_segment, filtered by workout.localDate AND enduranceSegment.activityType, with NO deletedAt filter (tombstone check must see soft-deleted rows — Pitfall 9).
    - dayGroupedSessions now selects workout.source alongside its existing columns.
  </behavior>
  <action>
    In schema.ts add to the `workout` table: `source` as `text('source', { enum: ['manual','healthkit'] }).default('manual')` (D-08) and `healthkitUuid` as `text('healthkit_uuid')` (D-11/D-14), each with an inline comment citing its decision ID exactly like the existing finishedAt/deletedAt rows. Add to `userProfile`: `bodyweightSetAt` (`integer('bodyweight_set_at',{mode:'timestamp'})`, D-17/Pitfall 3), `healthkitConnected` (`integer('healthkit_connected',{mode:'boolean'}).default(false)`, D-19/D-21), `healthkitLastSyncAt` (`integer('healthkit_last_sync_at',{mode:'timestamp'})`, D-24), `healthkitAnchor` (`text('healthkit_anchor')`, D-02 anchor token). In queries.ts add `candidatesForDedupe` exactly per RESEARCH Code Examples (import QueryableDB from the same file; parameterized eq/and only — no raw sql) and add a doc comment explaining the deliberate absence of activeWorkoutFilter per Pitfall 9. Add `workout.source` to the `dayGroupedSessions` select. Export `candidatesForDedupe` from index.ts's queries block. Do NOT run drizzle-kit here (Task 2 owns migration generation).
  </action>
  <verify>
    <automated>cd packages/db && pnpm vitest run src/__tests__/dedupe-candidate-query.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "healthkit_uuid" packages/db/src/schema.ts` and `grep -n "source'" packages/db/src/schema.ts` both match inside the workout table.
    - `grep -n "bodyweight_set_at\|healthkit_connected\|healthkit_last_sync_at\|healthkit_anchor" packages/db/src/schema.ts` returns all four columns.
    - New test `packages/db/src/__tests__/dedupe-candidate-query.test.ts` asserts the built `candidatesForDedupe` SQL has NO `deleted_at` in its WHERE (tombstone inclusion) and DOES filter local_date + activity_type — and it passes.
    - `candidatesForDedupe` is exported from `@apsis/db` (present in index.ts).
    - `dayGroupedSessions` select includes `source: workout.source`.
  </acceptance_criteria>
  <done>Schema carries all six new columns with cited comments, candidatesForDedupe is exported and tombstone-inclusive, dayGroupedSessions exposes source, and the new builder test passes.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Generate + bundle the drizzle migration</name>
  <read_first>
    - packages/db/drizzle.config.ts (driver: 'expo' — required for useMigrations consumption)
    - packages/db/drizzle/ (existing 0000/0001/0002 .sql naming convention + meta/journal.json)
    - .planning/phases/05-healthkit-integration/05-PATTERNS.md (migration file convention note)
  </read_first>
  <action>
    From packages/db run `npx drizzle-kit generate` to produce the next append-only migration
    (`0003_<generated-name>.sql`) plus the updated `meta/journal.json` and `migrations.js`
    importer. Never hand-write the .sql. Confirm the generated SQL contains ALTER TABLE statements
    adding all six new columns (workout.source, workout.healthkit_uuid, and the four user_profile
    columns). Do not edit the generated file afterward. This task is mandatory — without it, build
    and typecheck pass (types come from schema.ts) while the live device DB lacks the columns, a
    false-positive verification state the phase cannot ship with.
  </action>
  <verify>
    <automated>ls packages/db/drizzle/0003_*.sql >/dev/null && grep -l "healthkit_uuid" packages/db/drizzle/0003_*.sql && grep -l "bodyweight_set_at" packages/db/drizzle/0003_*.sql</automated>
  </verify>
  <acceptance_criteria>
    - A new `packages/db/drizzle/0003_*.sql` file exists.
    - That .sql references `healthkit_uuid`, `source`, `bodyweight_set_at`, `healthkit_connected`, `healthkit_last_sync_at`, and `healthkit_anchor`.
    - `packages/db/drizzle/meta/journal.json` includes the new migration entry.
    - `packages/db/src/migrations.js` (or the generated importer) references the new .sql so it ships via babel-plugin-inline-import.
  </acceptance_criteria>
  <done>The 0003 migration is generated, adds all six columns, and is wired into the journal + importer so it runs on app startup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app code → SQLite | Provenance/UUID columns are trusted local state used for echo-exclusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | candidatesForDedupe / new columns | low | mitigate | Parameterized drizzle builders only (eq/and), no raw sql with interpolated values (T-1-01 convention) |
| T-05-04 | Tampering | healthkitUuid tombstone | low | mitigate | candidatesForDedupe deliberately omits activeWorkoutFilter so soft-deleted imported rows still block re-import (Pitfall 9) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)
- Column: `workout.source` (`'manual' | 'healthkit'`, default `'manual'`)
- Column: `workout.healthkitUuid` (text, nullable)
- Column: `user_profile.bodyweightSetAt` (timestamp, nullable)
- Column: `user_profile.healthkitConnected` (boolean, default false)
- Column: `user_profile.healthkitLastSyncAt` (timestamp, nullable)
- Column: `user_profile.healthkitAnchor` (text, nullable)
- Query builder: `candidatesForDedupe(db, localDate, activityType)` exported from `@apsis/db`
- Migration: `packages/db/drizzle/0003_*.sql`
- `dayGroupedSessions` select gains `source`
</artifacts_produced>

<verification>
- `pnpm --filter @apsis/db test` passes (new dedupe-candidate test + existing suite).
- The 0003 migration exists and contains all six columns.
</verification>

<success_criteria>
Schema and queries carry provenance, UUID, bodyweight-recency, and connection-state columns; the tombstone-inclusive dedupe builder is exported; and a bundled migration materializes the columns for the device DB.
</success_criteria>

<output>
Create `.planning/phases/05-healthkit-integration/05-02-SUMMARY.md` when done.
</output>
