---
phase: 05-healthkit-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - pnpm-lock.yaml
autonomous: false
requirements: [HK-01]
user_setup:
  - service: apple-developer
    why: "HealthKit capability must be enabled on the com.apsis.app bundle id in the Apple Developer account before any HealthKit permission request can succeed (BUILD.md §8, human-only)."
    dashboard_config:
      - task: "Enable the HealthKit capability for App ID com.apsis.app"
        location: "Apple Developer → Certificates, Identifiers & Profiles → Identifiers → com.apsis.app → Capabilities → HealthKit"
must_haves:
  truths:
    - "The mobile app compiles with the HealthKit native module and its Nitro peer dependency resolvable."
    - "A fresh EAS dev build embeds the HealthKit entitlement and Info.plist usage descriptions."
  artifacts:
    - "apps/mobile/package.json declaring @kingstinct/react-native-healthkit@^14.0.2 and react-native-nitro-modules@^0.36.1"
    - "apps/mobile/app.json plugin entry for @kingstinct/react-native-healthkit with NSHealthShareUsageDescription + NSHealthUpdateUsageDescription"
  key_links:
    - "pnpm-lock.yaml regenerated so EAS --frozen-lockfile install succeeds (Phase 04 failure precedent)"
---

<objective>
Install and configure the HealthKit native stack so every downstream plan can import, write, and delete Health samples. Installs `@kingstinct/react-native-healthkit@^14.0.2` (per D-01..D-25 enabling library) plus its mandatory `react-native-nitro-modules@^0.36.1` peer, wires the Expo config plugin (entitlement + usage descriptions), and de-risks the first Nitro-based build before any logic is written (RESEARCH Pitfall 7).

Purpose: This is the first native module requiring runtime permission and the project's first Nitro dependency — discovering build friction here, not after all import/write-back logic is written, is the whole point of sequencing it first.
Output: Updated package.json/app.json/lockfile; a fresh EAS dev build validated on device via a trivial HealthKit-availability smoke check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-healthkit-integration/05-RESEARCH.md
@.planning/phases/05-healthkit-integration/05-CONTEXT.md
@apps/mobile/app.json
@apps/mobile/package.json
@apps/mobile/AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — react-native-nitro-modules [SUS]</name>
  <action>
    Pause before any install. Present the RESEARCH.md Package Legitimacy Audit verdicts to the
    developer and require explicit approval for `react-native-nitro-modules` [SUS] before Task 2
    runs. This gate is never auto-approvable (workflow.auto_advance is ignored per protocol).
  </action>
  <what-built>
    Nothing yet — this is a mandatory pre-install verification gate. RESEARCH.md's Package
    Legitimacy Audit flagged `react-native-nitro-modules` as [SUS] on a "too-new" recency
    heuristic. `@kingstinct/react-native-healthkit` is audited OK (no checkpoint needed).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/react-native-nitro-modules — confirm ~1M weekly
       downloads, maintainer `mrousavy` (VisionCamera author), repo github.com/mrousavy/nitro.
    2. Confirm version 0.36.1 exists and there is no install-time postinstall script that
       fetches remote code.
    3. Open https://www.npmjs.com/package/@kingstinct/react-native-healthkit — confirm 14.0.2,
       repo github.com/kingstinct/react-native-healthkit, ~126k weekly downloads.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install HealthKit + Nitro deps and wire the config plugin</name>
  <read_first>
    - apps/mobile/package.json (current dependency versions — RN 0.85.3, React 19.2.3, expo ~56.0.12)
    - apps/mobile/app.json (current plugins array + ios.infoPlist shape)
    - .planning/phases/05-healthkit-integration/05-RESEARCH.md (Standard Stack, Pattern 4, Environment Availability, Pitfall 1)
    - apps/mobile/AGENTS.md (read exact Expo SDK 56 versioned docs before writing code)
  </read_first>
  <action>
    From apps/mobile install exactly `@kingstinct/react-native-healthkit@^14.0.2` and
    `react-native-nitro-modules@^0.36.1` via pnpm (NOT the CLAUDE.md-stated ~8.2.0 pin — that
    predates the Nitro rewrite; RESEARCH Pitfall 1 is authoritative for this one package).
    Add the healthkit config plugin to app.json `plugins` with `NSHealthShareUsageDescription`
    and `NSHealthUpdateUsageDescription` strings describing read (runs, heart rate, bodyweight)
    and write (workout sessions) usage. Do NOT enable the background-delivery option (D-02 rejects
    background delivery). Regenerate pnpm-lock.yaml so an EAS `--frozen-lockfile` install matches
    (the exact Phase 04 failure documented in STATE.md). Verify `npm view
    @kingstinct/react-native-healthkit peerDependencies` still lists a `react-native-nitro-modules`
    entry (RESEARCH warning sign for a wrong-major resolve). Do not request or configure any
    bodyweight WRITE permission (D-18: bodyweight import is one-way).
  </action>
  <verify>
    <automated>cd apps/mobile && node -e "const p=require('./package.json').dependencies; if(!/^\^?14\./.test(p['@kingstinct/react-native-healthkit']||''))throw new Error('healthkit version');if(!p['react-native-nitro-modules'])throw new Error('nitro missing');console.log('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/package.json` dependencies include `@kingstinct/react-native-healthkit` at a `^14.x` range and `react-native-nitro-modules` at `^0.36.x`.
    - `apps/mobile/app.json` `plugins` contains the `@kingstinct/react-native-healthkit` plugin entry with non-empty `NSHealthShareUsageDescription` and `NSHealthUpdateUsageDescription`.
    - The healthkit plugin config does NOT enable background delivery.
    - `pnpm-lock.yaml` is regenerated (git shows it modified) and `pnpm install --frozen-lockfile` in apps/mobile succeeds.
  </acceptance_criteria>
  <done>Both packages install cleanly, the config plugin is present with usage-description strings, and the lockfile is in sync.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fresh EAS dev build + on-device HealthKit-availability smoke check</name>
  <action>
    Trigger a fresh EAS dev build (or `expo run:ios` on a Mac), install it on a physical iOS
    device, and confirm the app boots with the HealthKit native module linked (no redbox/crash).
    A fresh build is mandatory — the stale dev client will not include the new native module
    (STATE.md Phase 04 blocker precedent). Confirm the Apple Developer HealthKit capability
    (user_setup) is enabled first, or the build's permission requests will fail.
  </action>
  <what-built>
    A fresh EAS dev build (or `expo run:ios` on a Mac) that includes the newly-added HealthKit
    native module + Nitro codegen + the HealthKit entitlement/usage descriptions. This validates
    the first Nitro-based build against the project's customized Metro/babel `.sql` bundling
    config (RESEARCH Pitfall 7) BEFORE any import/write-back logic exists.
  </what-built>
  <how-to-verify>
    1. Confirm the Apple Developer account has HealthKit capability enabled for com.apsis.app
       (user_setup item above) — the build will fail to request permission otherwise.
    2. Trigger a fresh EAS dev build (`eas build --profile development --platform ios`) — a fresh
       build is required, not the stale dev client (STATE.md Phase 04 blocker precedent).
    3. Install on a physical iOS device (Simulator has minimal Health data — Pitfall 6).
    4. In the running app confirm the HealthKit native module loads without a redbox/crash on
       startup (the trivial availability check: the app boots and reaches TODAY as before).
  </how-to-verify>
  <resume-signal>Type "approved" once the dev build boots on device with the HealthKit module linked, or describe the build error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Third-party package code enters the app's trusted native build |
| iOS HealthKit sheet → app | System-mediated permission boundary (Apsis cannot circumvent it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm install of react-native-nitro-modules [SUS] + healthkit | high | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies both packages on npmjs.com before install; never auto-approved |
| T-05-03 | Elevation of Privilege | HealthKit authorization scope | low | mitigate | Usage descriptions declare only read (workouts/HR/bodyweight) + workout write; no bodyweight write permission (D-18) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (introduced by this plan)
- Dependency: `@kingstinct/react-native-healthkit@^14.0.2`
- Dependency: `react-native-nitro-modules@^0.36.1`
- app.json plugin entry: `@kingstinct/react-native-healthkit` with `NSHealthShareUsageDescription`, `NSHealthUpdateUsageDescription`
</artifacts_produced>

<verification>
- `apps/mobile/package.json` and `app.json` reflect the new dependency + plugin.
- `pnpm install --frozen-lockfile` succeeds in apps/mobile.
- EAS dev build boots on a physical device with the HealthKit module linked (human-verified).
</verification>

<success_criteria>
HealthKit + Nitro are installed, the config plugin declares the entitlement and usage strings, the lockfile is synced, and a fresh dev build runs on device — unblocking all downstream HealthKit plans.
</success_criteria>

<output>
Create `.planning/phases/05-healthkit-integration/05-01-SUMMARY.md` when done.
</output>
