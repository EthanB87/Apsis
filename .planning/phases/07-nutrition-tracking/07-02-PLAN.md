---
phase: 07-nutrition-tracking
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/index.ts
  - packages/engine/src/config.ts
  - packages/engine/src/nutrition.ts
  - packages/engine/src/index.ts
  - packages/engine/src/__tests__/nutrition.test.ts
autonomous: true
requirements: [NUTR-16, NUTR-17, NUTR-18]
must_haves:
  truths:
    - "Given a profile, a day-type, and a session-kcal number, the engine returns {kcal,p,c,f,warnings} deterministically — no I/O, no Date.now(), never throws."
    - "classifyDayType maps a day's session types to heavy_lift/long_run/double/rest/mixed using the same >1-session-wins precedence dailyHSS uses."
    - "Invalid bodyweight (<=0 or non-finite) yields an all-zero result plus a warning — never NaN or a divide-by-zero."
    - "The aggressive-cut fat-floor clamp triggers, pins fat at fatFloorGPerKg*bodyweight, and reduces carbs to compensate — with a warning."
    - "Every new EngineConfig constant is overridable via the cfg?: Partial<EngineConfig> argument, exactly like kStrength."
  artifacts:
    - "packages/shared/src/index.ts: NutritionProfile, DayType, MacroTargetResult types + EngineConfig field additions"
    - "packages/engine/src/config.ts: DEFAULT_CONFIG additions with literature/heuristic provenance comments"
    - "packages/engine/src/nutrition.ts: classifyDayType, trainingKcalFromHss, dailyMacroTarget"
    - "packages/engine/src/__tests__/nutrition.test.ts: 5 golden-file cases from 07-RESEARCH Pattern 4"
  key_links:
    - "nutrition.ts → mergeConfig(cfg) first line of every function (config override path preserved)"
    - "packages/engine/src/index.ts barrel → re-exports nutrition.ts (consumers import from @apsis/engine)"
    - "EngineConfig interface (shared) ↔ DEFAULT_CONFIG value (engine) — new fields added to BOTH, matching the split-to-avoid-circular-dep decision from Phase 02"
---

<objective>
Build the differentiator: pure `packages/engine` functions that compute day-type-adaptive daily
kcal/macro targets from profile + goal mode + the day's logged training. Since the PRD's referenced
§6 nutrition model does not exist in this repo, 07-RESEARCH Pattern 4 IS the model — implement it
exactly, with every constant tunable via EngineConfig and locked by golden-file tests, mirroring the
kStrength calibration precedent.

Purpose: NUTR-16/17/18 — the "targets that adapt to training" no macro app offers, kept pure and
testable (the IP/moat discipline).
Output: NutritionProfile/DayType/MacroTargetResult types, EngineConfig constants, three pure
functions, five golden-file tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-nutrition-tracking/07-RESEARCH.md
@.planning/phases/07-nutrition-tracking/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add nutrition types + EngineConfig constants (shared + config)</name>
  <files>packages/shared/src/index.ts, packages/engine/src/config.ts</files>
  <read_first>
    - packages/shared/src/index.ts (existing EngineConfig interface + Sex type — extend in place)
    - packages/engine/src/config.ts (DEFAULT_CONFIG object + provenance-comment convention for kStrength/kCarry)
    - .planning/phases/07-nutrition-tracking/07-RESEARCH.md (Pattern 4 — exact constant names + values + provenance; NutritionProfile shape; MacroTargetResult shape)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (engine purity + config-constant-with-provenance-comment patterns)
  </read_first>
  <behavior>
    - EngineConfig gains: proteinGPerKgCut, proteinGPerKgMaintain, proteinGPerKgBulk, neatMultiplier, cutDeltaKcal, bulkDeltaKcal, kcalPerHssPoint, carbGPerKgHeavyLift, carbGPerKgLongRun, carbGPerKgDouble, carbGPerKgRest, carbGPerKgMixed, fatFloorGPerKg.
    - DEFAULT_CONFIG supplies each with the exact starting values from 07-RESEARCH Pattern 4 (2.4/1.8/1.8/1.2/-500/300/5/4/7/8/2.5/5/0.4).
    - mergeConfig still resolves any partial override for the new fields (no code change needed if the object spread is preserved).
  </behavior>
  <action>
    In packages/shared/src/index.ts add three exported types: `DayType` (union
    'heavy_lift'|'long_run'|'double'|'rest'|'mixed'), `NutritionProfile` (sex: Sex; bodyweightKg:
    number; heightCm: number; age: number; goalMode: 'cut'|'maintain'|'bulk'), and
    `MacroTargetResult` (kcal, p, c, f: number; warnings: string[]). Extend the existing
    `EngineConfig` interface with the 13 new numeric fields listed above (do not reorder existing
    fields). In packages/engine/src/config.ts add those 13 fields to DEFAULT_CONFIG with the exact
    starting values from 07-RESEARCH Pattern 4, each with a doc comment naming its literature source
    or "starting guess" status and which golden test calibrates it — matching the existing
    kStrength/kCarry comment style. Tag kcalPerHssPoint explicitly as the lowest-confidence constant
    (A4). Do NOT change mergeConfig.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/shared exec tsc --noEmit && grep -cE "proteinGPerKgCut|kcalPerHssPoint|carbGPerKgLongRun|fatFloorGPerKg" packages/engine/src/config.ts</automated>
  </verify>
  <done>3 new shared types + 13 EngineConfig fields present in both interface and DEFAULT_CONFIG; provenance comments added; shared typechecks.</done>
  <acceptance_criteria>
    - `grep -cE "export type DayType|export interface NutritionProfile|export interface MacroTargetResult" packages/shared/src/index.ts` returns 3.
    - DEFAULT_CONFIG contains all 13 new fields with the Pattern-4 default values (e.g. `kcalPerHssPoint: 5`, `cutDeltaKcal: -500`, `fatFloorGPerKg: 0.4`).
    - Each new DEFAULT_CONFIG field has an adjacent doc comment (provenance).
    - `pnpm --filter @apsis/shared exec tsc --noEmit` is clean.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: nutrition.ts pure functions + golden-file tests (RED→GREEN)</name>
  <files>packages/engine/src/nutrition.ts, packages/engine/src/__tests__/nutrition.test.ts, packages/engine/src/index.ts</files>
  <read_first>
    - packages/engine/src/daily.ts (dailyHSS: mergeConfig-first, defend-every-input, never-throw signature convention)
    - packages/engine/src/__tests__/calibration.test.ts (golden-file anchor-test discipline: compute the real number, assert a defensible range, document why)
    - packages/engine/src/index.ts (public barrel — add the nutrition re-export)
    - .planning/phases/07-nutrition-tracking/07-RESEARCH.md (Pattern 4 formula pseudocode + the 5 recommended golden-file cases, lines ~495-579)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (nutrition.ts + nutrition.test.ts sections)
  </read_first>
  <behavior>
    - classifyDayType([]) → 'rest'; length>1 → 'double'; ['strength'] → 'heavy_lift'; ['endurance'] → 'long_run'; ['hybrid'] → 'mixed'.
    - trainingKcalFromHss(dayHss) → max(dayHss,0) * kcalPerHssPoint; non-finite → 0.
    - dailyMacroTarget: protein anchored to bodyweight by goal mode; kcal = neat(=BMR*neatMultiplier) + sessionKcal + goalDelta, floored at BMR; carbs by dayType g/kg; fat fills remaining kcal but never below fatFloor (clamp reduces carbs + warns).
    - Invalid bodyweight → {0,0,0,0, warnings:['invalid bodyweight …']}.
    - Every function takes cfg?: Partial<EngineConfig> and resolves via mergeConfig first line; never reads Date.now(); never throws.
  </behavior>
  <action>
    Write packages/engine/src/__tests__/nutrition.test.ts FIRST (RED) covering the 5 golden-file
    cases from 07-RESEARCH: (1) maintain+rest sanity-range check, (2) cut+heavy_lift — kcal reduced
    by exactly cutDeltaKcal and protein highest relative to kcal, (3) bulk+double — highest carb
    target and sessionKcal clearly additive, (4) invalid bodyweight (<=0 or non-finite) → all-zero +
    warning, no NaN, (5) extreme aggressive-cut tripping the fat-floor clamp → warnings populated and
    f pinned at fatFloorGPerKg*bodyweight. Compute the real expected numbers by running the formula
    (do not hand-wave); assert defensible ranges with an explaining comment, kStrength-style. Also
    cover classifyDayType's 5 branches. Confirm RED. Then implement packages/engine/src/nutrition.ts
    exporting `classifyDayType`, `trainingKcalFromHss`, `dailyMacroTarget`, and a module-private
    `mifflinStJeorBmr`, following 07-RESEARCH Pattern 4 exactly (Mifflin-St Jeor BMR; 'other' sex
    constant -78 per A2; round kcal to nearest 5; round p/c/f to nearest integer). Add
    `export * from './nutrition';` to packages/engine/src/index.ts. Make GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/engine test -- nutrition</automated>
  </verify>
  <done>All 5 macro-target golden cases + classifyDayType branches pass; nutrition.ts is pure (no Date.now, no I/O, never throws); re-exported from the barrel.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/engine test -- nutrition` passes green.
    - `grep -c "Date.now\|require(\|import.*fs\|fetch(" packages/engine/src/nutrition.ts` returns 0.
    - A test asserting `dailyMacroTarget({...bodyweightKg:0...}, 'rest', 0)` returns all-zero with a non-empty `warnings` array passes.
    - A test asserting the fat-floor clamp pins `f` to `0.4 * bodyweightKg` and pushes a warning passes.
    - `packages/engine/src/index.ts` contains `export * from './nutrition'`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → engine | Untrusted numeric inputs (bodyweight, hss, sessionKcal) can be NaN/negative/absurd |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering / data integrity | dailyMacroTarget inputs | medium | mitigate | Defend every input (non-finite → 0/clamped, invalid bodyweight → zero+warning), never throw — golden test 4 proves it (D-15 convention) |
| T-07-04 | Denial of Service | engine purity | low | accept | Pure synchronous compute, no I/O or wall-clock; cannot hang the UI |
| T-07-SC | Tampering | npm installs | high | accept | No installs in this plan (types/logic only) |
</threat_model>

<verification>
- `pnpm --filter @apsis/engine test` full suite green (existing HSS tests + new nutrition tests).
- `pnpm --filter @apsis/engine exec tsc --noEmit` clean.
- Root `pnpm typecheck` clean (shared EngineConfig change propagates to engine + db).
</verification>

<success_criteria>
Three pure adaptive-target functions ship with 5 golden-file tests and tunable EngineConfig
constants; NUTR-16/17/18 satisfied at the engine layer.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (created by this plan)

- Types: `DayType`, `NutritionProfile`, `MacroTargetResult` (@apsis/shared)
- EngineConfig fields: proteinGPerKgCut/Maintain/Bulk, neatMultiplier, cutDeltaKcal, bulkDeltaKcal, kcalPerHssPoint, carbGPerKgHeavyLift/LongRun/Double/Rest/Mixed, fatFloorGPerKg
- Functions: `classifyDayType`, `trainingKcalFromHss`, `dailyMacroTarget` (+ private `mifflinStJeorBmr`) in `packages/engine/src/nutrition.ts`
- Test file: `packages/engine/src/__tests__/nutrition.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/07-nutrition-tracking/07-02-SUMMARY.md` when done
</output>
