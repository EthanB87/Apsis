---
phase: 07-nutrition-tracking
plan: 06
type: execute
wave: 3
depends_on: [07-03, 07-04]
files_modified:
  - apps/mobile/app/(tabs)/nutrition/search.tsx
  - apps/mobile/app/(tabs)/nutrition/log.tsx
  - apps/mobile/components/FoodConfirmSheet.tsx
  - apps/mobile/lib/logFood.ts
  - apps/mobile/lib/__tests__/logFood.test.ts
autonomous: true
requirements: [NUTR-03, NUTR-04, NUTR-05, NUTR-06, NUTR-07]
must_haves:
  truths:
    - "A user can search the local food cache inline and log a matching food in ≤3 taps (recents/favorites surfaced first)."
    - "A user can create a custom food via full manual macro entry (name + per-100g kcal/P/C/F + serving) and log it."
    - "A user can quick-add a macro-only entry (no food row) — food_log.quickAdd=true, foodId null."
    - "Every food_log entry is tagged to a meal (breakfast/lunch/dinner/snack) and today's local date, with kcal/p/c/f frozen at log time (denormalized)."
  artifacts:
    - "apps/mobile/app/(tabs)/nutrition/search.tsx (local search + recents/favorites list)"
    - "apps/mobile/app/(tabs)/nutrition/log.tsx (custom food + quick-add entry)"
    - "apps/mobile/components/FoodConfirmSheet.tsx (shared confirm/edit + serving/qty sheet)"
    - "apps/mobile/lib/logFood.ts (pure macro-at-qty computation + food_log insert row builder)"
  key_links:
    - "search.tsx → searchLocalFoods/recentFoods/favoriteFoods (07-03) → tap → FoodConfirmSheet → logFood → food_log insert → nutritionTargetSignal.bump()"
    - "logFood freeze-at-log-time: food.per100g × qtyGrams → food_log.kcal/p/c/f (never a live join, Pattern 2)"
    - "custom food create → food (source='user') → food_log; quick-add → food_log with foodId null + quickAdd true"
---

<objective>
Deliver the offline manual-logging core: inline local search with recents/favorites for ≤3-tap
repeat logging, full manual custom-food creation, quick-add macro-only entries, and a shared
confirm/edit sheet — all writing denormalized food_log rows tagged to meal + date, fully offline.

Purpose: NUTR-03/04/05/06/07 — the fast-entry bar (match MacroFactor/MFP) that makes the increment
sellable, with logging that never blocks on network.
Output: search + log screens, shared FoodConfirmSheet, pure logFood row builder + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-nutrition-tracking/07-RESEARCH.md
@.planning/phases/07-nutrition-tracking/07-PATTERNS.md
@DESIGN-SYSTEM.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure logFood row builder (freeze-at-log-time) + tests</name>
  <files>apps/mobile/lib/logFood.ts, apps/mobile/lib/__tests__/logFood.test.ts</files>
  <read_first>
    - apps/mobile/lib/runEntryLogic.ts (pure native-import-free lib for vitest; never-divide-by-zero guard style)
    - packages/db/src/schema.ts (food + food_log column names/shapes from 07-01)
    - .planning/phases/07-nutrition-tracking/07-RESEARCH.md (Pattern 2 freeze-at-log-time; Pitfall 7 missing-field defence)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (labelOcrParse bounds-check style to reuse)
  </read_first>
  <behavior>
    - buildFoodLogRow({ food, qtyGrams, meal, localDate }): kcal/p/c/f = food.per100g × (qtyGrams/100), rounded sanely; foodId = food.id; quickAdd false.
    - buildQuickAddRow({ kcal, p, c, f, meal, localDate }): foodId null, quickAdd true, qtyGrams 0.
    - Non-finite / negative qty or macro → clamped to 0 (never NaN); guards on divide-by-zero.
  </behavior>
  <action>
    Create apps/mobile/lib/logFood.ts (zero @apsis/db / expo native imports) exporting a pure
    `buildFoodLogRow` and `buildQuickAddRow` that compute a food_log insert row. buildFoodLogRow
    multiplies each per-100g macro by qtyGrams/100 and freezes the result onto the row (Pattern 2 —
    no live join ever recomputes it). Clamp every numeric to a sane non-negative value; treat missing
    per-100g fields as 0 without producing NaN (Pitfall 7). buildQuickAddRow returns a
    foodId-null/quickAdd-true row from directly-entered macros. Write logFood.test.ts asserting: a
    150g food at 200kcal/100g logs 300 kcal; a quick-add row has foodId null + quickAdd true; a
    negative/NaN qty clamps to a zero-macro row without throwing.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile test -- logFood</automated>
  </verify>
  <done>Pure row builders freeze macros at log time, clamp bad input, and are tested; no native imports.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/mobile test -- logFood` passes green.
    - A test asserting 150g × 200kcal/100g === 300 kcal on the built row passes.
    - A test asserting buildQuickAddRow yields `{ foodId: null, quickAdd: true }` passes.
    - `grep -c "@apsis/db\|expo-" apps/mobile/lib/logFood.ts` returns 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Search + custom-food/quick-add screens + shared FoodConfirmSheet</name>
  <files>apps/mobile/app/(tabs)/nutrition/search.tsx, apps/mobile/app/(tabs)/nutrition/log.tsx, apps/mobile/components/FoodConfirmSheet.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/log/index.tsx (exercise picker/recents list pattern; ≤3-tap entry; tap-to-type ledger fields)
    - apps/mobile/lib/logFood.ts (Task 1 row builders)
    - apps/mobile/lib/nutritionTargetSignal.ts (bump after a food_log write so targets screen refreshes)
    - packages/db/src/nutritionQueries.ts (searchLocalFoods/recentFoods/favoriteFoods from 07-03)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (nutrition/search.tsx + nutrition/index.tsx analogs; shared confirm-sheet reused by scan/OCR later)
    - DESIGN-SYSTEM.md (void/volt, one-volt-per-screen, ledger entry)
  </read_first>
  <action>
    Create components/FoodConfirmSheet.tsx — a shared confirm/edit sheet taking a candidate food +
    serving/qty and a meal selector (breakfast/lunch/dinner/snack), letting the user adjust
    qtyGrams/serving before confirming; on confirm it builds a row via logFood, inserts into
    food_log (today's localDate via todayLocalDate()), and bumps nutritionTargetSignal. This sheet is
    the single confirm surface reused by barcode (07-08) and OCR (07-09), so keep it food-source-
    agnostic. Create nutrition/search.tsx: a debounced local search box (searchLocalFoods) with
    recents + favorites surfaced above results (recentFoods/favoriteFoods) for ≤3-tap repeat logging;
    tapping a result opens FoodConfirmSheet. Create nutrition/log.tsx: full manual custom-food entry
    (name + brand? + per-100g kcal/P/C/F + optional serving) that inserts a food (source='user') then
    opens the confirm sheet, PLUS a quick-add mode (macro-only, foodId null, quickAdd true) via
    buildQuickAddRow. All logging is local-only (no network in this plan — remote OFF/USDA fallback
    is added in 07-08). Style per DESIGN-SYSTEM.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile exec tsc --noEmit 2>&1 | head -5 && grep -c "searchLocalFoods\|recentFoods\|favoriteFoods" apps/mobile/app/(tabs)/nutrition/search.tsx && grep -c "quickAdd\|buildQuickAddRow" apps/mobile/app/(tabs)/nutrition/log.tsx</automated>
  </verify>
  <done>Search surfaces recents/favorites for ≤3-tap logging; custom-food + quick-add write denormalized food_log rows via the shared confirm sheet; fully offline.</done>
  <acceptance_criteria>
    - search.tsx calls searchLocalFoods and surfaces recentFoods + favoriteFoods above results.
    - Tapping a search result and confirming inserts a food_log row with frozen kcal/p/c/f and today's localDate.
    - log.tsx supports both a custom-food create (food source='user' then log) and a quick-add (foodId null, quickAdd true).
    - Every food_log insert sets a meal enum value (breakfast/lunch/dinner/snack).
    - No network/fetch call appears in any file in this plan (grep: no `fetch(` — remote fallback is 07-08).
    - After a confirm, nutritionTargetSignal is bumped so the targets screen refreshes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-typed food name/macros → food + food_log | Untrusted text/numbers persist to the local store |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-11 | Tampering (SQL injection) | food/food_log inserts | high | mitigate | Drizzle parameterized inserts only (07-03 builders); no raw sql with interpolated user text |
| T-07-12 | Tampering / data integrity | manual macro entry | medium | mitigate | logFood clamps non-finite/negative macros to 0 (never NaN); confirm sheet is the review gate |
| T-07-SC | Tampering | npm installs | high | accept | No installs in this plan (uses only in-repo modules) |
</threat_model>

<verification>
- `pnpm --filter @apsis/mobile test` green (logFood tests); `pnpm --filter @apsis/mobile exec tsc --noEmit` clean.
- Manual (phase UAT): repeat-log a favorite in ≤3 taps; create a custom food; quick-add macros; totals update on the nutrition tab.
</verification>

<success_criteria>
Offline manual logging (search/recents/favorites/custom/quick-add) writes denormalized, meal+date
tagged food_log rows through a shared confirm sheet (NUTR-03/04/05/06/07).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (created by this plan)

- Screens: `apps/mobile/app/(tabs)/nutrition/search.tsx`, `apps/mobile/app/(tabs)/nutrition/log.tsx`
- Component: `apps/mobile/components/FoodConfirmSheet.tsx` (reused by 07-08 barcode + 07-09 OCR)
- Functions: `buildFoodLogRow`, `buildQuickAddRow` (`apps/mobile/lib/logFood.ts`)
- Test file: `apps/mobile/lib/__tests__/logFood.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/07-nutrition-tracking/07-06-SUMMARY.md` when done
</output>