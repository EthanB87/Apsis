---
phase: 07-nutrition-tracking
plan: 10
type: execute
wave: 6
depends_on: [07-03, 07-06]
files_modified:
  - packages/db/src/nutritionQueries.ts
  - packages/db/src/__tests__/nutrition-queries.test.ts
  - apps/mobile/app/(tabs)/nutrition/recipes.tsx
  - apps/mobile/app/(tabs)/nutrition/recipe-edit.tsx
autonomous: true
requirements: [NUTR-13, NUTR-14]
must_haves:
  truths:
    - "A user can combine foods into a saved recipe with a servings count; per-serving macros compute automatically from the ingredients."
    - "A user can log one recipe serving as a single food_log entry (denormalized macros frozen at log time)."
  artifacts:
    - "packages/db/src/nutritionQueries.ts: computeRecipeServingMacros + recipe CRUD builders"
    - "apps/mobile/app/(tabs)/nutrition/recipes.tsx (list) + recipe-edit.tsx (create/edit)"
  key_links:
    - "recipe-edit: pick ingredients (searchLocalFoods from 07-06 flow) → recipe + recipe_ingredient rows → computeRecipeServingMacros → per-serving display"
    - "log one serving → buildFoodLogRow-equivalent using per-serving macros → food_log (frozen at log time)"
    - "recipe delete cascades to recipe_ingredient (schema onDelete cascade from 07-01)"
---

<objective>
Add custom recipes/meals: combine ingredient foods into a saved recipe with a servings count, compute
per-serving macros by aggregation, and let the user log one serving as a food_log entry. This is the
last build-order item (step 5) — the cut-first candidate under timeline pressure.

Purpose: NUTR-13 (recipe aggregation) + NUTR-14 (log one serving).
Output: recipe query builders + tests, recipes list + edit screens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-nutrition-tracking/07-RESEARCH.md
@.planning/phases/07-nutrition-tracking/07-PATTERNS.md
@DESIGN-SYSTEM.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recipe query builders + per-serving aggregation + tests</name>
  <files>packages/db/src/nutritionQueries.ts, packages/db/src/__tests__/nutrition-queries.test.ts</files>
  <read_first>
    - packages/db/src/nutritionQueries.ts (existing builders from 07-03 — APPEND recipe builders in the same file; reuse QueryableDB + parameterized style)
    - packages/db/src/schema.ts (recipe / recipeIngredient / food shapes from 07-01)
    - .planning/phases/07-nutrition-tracking/07-RESEARCH.md (Code Examples: computeRecipeServingMacros aggregation sketch)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (nutritionQueries.ts parameterized builder pattern; ingredient inner-join)
  </read_first>
  <action>
    Append to packages/db/src/nutritionQueries.ts (do NOT create a new file): `createRecipe`,
    `addRecipeIngredient`, `listRecipes`, and `computeRecipeServingMacros(db, recipeId)` which
    inner-joins recipe_ingredient→food, sums per-100g × (qtyGrams/100) across ingredients, and divides
    by recipe.servings (guard servings <= 0 → treat as 1, never divide-by-zero) to return per-serving
    {kcal,p,c,f}. Use drizzle's parameterized builders exclusively (T-1-01). Extend
    nutrition-queries.test.ts: a 2-ingredient recipe with 2 servings computes the correct per-serving
    macros; deleting the recipe cascades its recipe_ingredient rows (asserts the 07-01 FK cascade); a
    servings=0 recipe does not divide-by-zero.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/db test -- nutrition-queries && grep -cE "export function (createRecipe|addRecipeIngredient|listRecipes|computeRecipeServingMacros)" packages/db/src/nutritionQueries.ts</automated>
  </verify>
  <done>Recipe builders + per-serving aggregation appended and tested; parameterized; divide-by-zero guarded; cascade verified.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/db test -- nutrition-queries` passes green including the recipe cases.
    - `grep -cE "export function (createRecipe|addRecipeIngredient|listRecipes|computeRecipeServingMacros)" packages/db/src/nutritionQueries.ts` returns 4.
    - A test asserting a 2-ingredient / 2-serving recipe computes correct per-serving macros passes.
    - A test asserting servings=0 does not divide-by-zero (treated as 1) passes.
    - The recipe delete-cascade test asserts recipe_ingredient rows are removed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Recipes list + create/edit screens + log-one-serving</name>
  <files>apps/mobile/app/(tabs)/nutrition/recipes.tsx, apps/mobile/app/(tabs)/nutrition/recipe-edit.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/nutrition/search.tsx (ingredient picking reuses the local food search pattern from 07-06)
    - apps/mobile/components/FoodConfirmSheet.tsx (log-one-serving reuses the shared confirm/log path)
    - apps/mobile/lib/logFood.ts (buildFoodLogRow — freeze per-serving macros at log time)
    - packages/db/src/nutritionQueries.ts (Task 1 recipe builders + computeRecipeServingMacros)
    - apps/mobile/lib/nutritionTargetSignal.ts (bump after logging a serving)
    - DESIGN-SYSTEM.md (void/volt, one-volt-per-screen)
  </read_first>
  <action>
    Create nutrition/recipes.tsx (a list of saved recipes via listRecipes, showing per-serving macros
    from computeRecipeServingMacros, with a "log 1 serving" action) and nutrition/recipe-edit.tsx
    (create/edit: name + servings + add ingredients by reusing the local food-search picker from
    07-06, writing recipe + recipe_ingredient rows). Logging one serving builds a food_log row from
    the per-serving macros via the shared logFood/FoodConfirmSheet path (frozen at log time, meal +
    today's localDate) and bumps nutritionTargetSignal. Style per DESIGN-SYSTEM. These screens
    auto-route under the nutrition Stack created in 07-05 (no _layout edit needed).
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile exec tsc --noEmit 2>&1 | head -5 && grep -c "computeRecipeServingMacros\|listRecipes" apps/mobile/app/(tabs)/nutrition/recipes.tsx && grep -c "createRecipe\|addRecipeIngredient" apps/mobile/app/(tabs)/nutrition/recipe-edit.tsx</automated>
  </verify>
  <done>User can create/edit a recipe from ingredients and log one serving as a denormalized food_log entry.</done>
  <acceptance_criteria>
    - recipes.tsx lists saved recipes with per-serving macros and offers a log-one-serving action.
    - recipe-edit.tsx creates a recipe + its ingredients using the local food-search picker.
    - Logging one serving writes a food_log row with frozen macros, a meal, and today's localDate, then bumps nutritionTargetSignal.
    - `apps/mobile` typechecks clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-composed recipe → recipe/recipe_ingredient/food_log | User input persists to the local store |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-23 | Tampering (SQL injection) | recipe builders | high | mitigate | Drizzle parameterized builders only (T-1-01); no interpolated sql |
| T-07-24 | Tampering / data integrity | per-serving aggregation | medium | mitigate | servings<=0 guarded (treated as 1, no divide-by-zero); logged serving frozen at log time |
| T-07-SC | Tampering | npm installs | high | accept | No installs in this plan |
</threat_model>

<verification>
- `pnpm --filter @apsis/db test` + `pnpm --filter @apsis/mobile test` green; root `pnpm typecheck` clean.
- Manual (phase UAT): build a recipe, see per-serving macros, log one serving → totals update.
</verification>

<success_criteria>
Custom recipes compute per-serving macros and log one serving as a denormalized food_log entry —
NUTR-13/14.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (created by this plan)

- Functions: `createRecipe`, `addRecipeIngredient`, `listRecipes`, `computeRecipeServingMacros` (`packages/db/src/nutritionQueries.ts`)
- Screens: `apps/mobile/app/(tabs)/nutrition/recipes.tsx`, `apps/mobile/app/(tabs)/nutrition/recipe-edit.tsx`
</artifacts_produced>

<output>
Create `.planning/phases/07-nutrition-tracking/07-10-SUMMARY.md` when done
</output>