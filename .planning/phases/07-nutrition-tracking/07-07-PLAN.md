---
phase: 07-nutrition-tracking
plan: 07
type: execute
wave: 4
depends_on: [07-05, 07-06]
files_modified:
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/lib/nutritionCameraAuth.ts
  - pnpm-lock.yaml
autonomous: false
requirements: [NUTR-08, NUTR-11]
user_setup:
  - service: usda-fdc
    why: "USDA FoodData Central generic-food search needs a free API key; DEMO_KEY (30/hr) is dev-only and not viable for production."
    env_vars:
      - name: EXPO_PUBLIC_USDA_FDC_API_KEY
        source: "https://fdc.nal.usda.gov/api-key-signup (free self-serve; store as an EAS secret)"
  - service: eas-dev-build
    why: "expo-camera + expo-text-extractor are native modules — on-device testing of barcode/OCR requires a fresh EAS dev build (same gate as Phase 04 Skia and Phase 05 HealthKit)."
    dashboard_config:
      - task: "Run an EAS dev build after the native installs land + pnpm-lock is synced (--frozen-lockfile); the current dev client cannot load the new native modules"
        location: "eas build --profile development --platform ios"
must_haves:
  truths:
    - "expo-camera and expo-text-extractor are installed at SDK-56-resolved versions (via expo install, not hand-pinned)."
    - "iOS declares an NSCameraUsageDescription so the camera permission prompt is legal and non-crashing."
    - "A never-throwing camera-permission wrapper exists (requests permission, returns a boolean, logs on failure)."
    - "pnpm-lock.yaml is in sync so the subsequent EAS dev build's --frozen-lockfile install succeeds."
  artifacts:
    - "expo-camera + expo-text-extractor in apps/mobile/package.json + synced pnpm-lock.yaml"
    - "app.json camera plugin/permission (NSCameraUsageDescription)"
    - "apps/mobile/lib/nutritionCameraAuth.ts (permission wrapper)"
  key_links:
    - "package legitimacy checkpoint (expo-camera [SUS], expo-text-extractor [ASSUMED]) → install → app.json permission → nutritionCameraAuth wrapper (consumed by 07-08 scan + 07-09 label-scan)"
    - "pnpm-lock sync → EAS dev build (user_setup) → native modules loadable on device"
---

<objective>
Install and wire the two native modules the camera-dependent features need (barcode scan + label
OCR): expo-camera (both barcode detection and photo capture) and expo-text-extractor (Apple Vision
on-device OCR), gate their install behind a package-legitimacy human check, declare the iOS camera
permission, and provide a never-throwing permission wrapper — budgeting the EAS dev-build cycle.

Purpose: The native prerequisite for NUTR-08 (barcode) and NUTR-11 (OCR); handled as its own plan so
the build cycle is isolated (Pitfall 1).
Output: installed deps, camera permission, permission wrapper, synced lockfile, build note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-nutrition-tracking/07-RESEARCH.md
@.planning/phases/07-nutrition-tracking/07-PATTERNS.md
@apps/mobile/AGENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (expo-camera [SUS], expo-text-extractor [ASSUMED])</name>
  <what-built>
    Nothing yet — this gate precedes the native installs. 07-RESEARCH's Package Legitimacy Audit
    flags expo-camera as [SUS] (recency false-positive on a 1.4M-downloads first-party Expo module)
    and expo-text-extractor as [ASSUMED] (discovered via WebSearch, registry lookup OK, MIT, real
    repo, ~5k downloads/week — but package-name provenance is unconfirmed).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/expo-camera — confirm it is the official Expo monorepo
       package (repo github.com/expo/expo), high download count, active.
    2. Open https://www.npmjs.com/package/expo-text-extractor — confirm MIT license, repo
       github.com/pchalupa/expo-text-extractor with real source, no postinstall script, and that it
       wraps Apple Vision VNRecognizeTextRequest as NUTRITION.md §4 requires.
    3. Confirm these are the intended packages (no typosquat) before install.
  </how-to-verify>
  <action>
    Halt before any install. Present both packages' npm pages to the user for legitimacy
    confirmation (expo-camera flagged [SUS] recency false-positive; expo-text-extractor [ASSUMED]
    package-name provenance). Do not run `expo install` until the user approves or names a
    replacement. This gate is not auto-approvable (T-07-SC).
  </action>
  <resume-signal>Type "approved" to proceed to install, or name a replacement package.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install native modules + camera permission + permission wrapper + lock sync</name>
  <files>apps/mobile/package.json, apps/mobile/app.json, apps/mobile/lib/nutritionCameraAuth.ts, pnpm-lock.yaml</files>
  <read_first>
    - apps/mobile/app.json (existing plugins array + ios.infoPlist shape — copy the healthkit plugin tuple form; add camera usage description here)
    - apps/mobile/lib/healthkitAuth.ts (never-throw permission wrapper convention: try / requestAuthorization / catch → console.error '[Apsis] ...' → return false)
    - apps/mobile/AGENTS.md (verify expo-camera API against the v56.0.0 docs before coding)
    - .planning/phases/07-nutrition-tracking/07-RESEARCH.md (Standard Stack install commands; version-verification note — do NOT hand-pin 57.x; Pitfall 1 lockfile+build gate)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (nutritionCameraAuth.ts section — healthkitAuth analog)
  </read_first>
  <action>
    From apps/mobile run `npx expo install expo-camera` and `npx expo install expo-text-extractor`
    (accept whatever SDK-56-line versions Expo's resolver selects — do NOT hand-pin 57.x tags, per
    the Phase 03/04 precedent). Add the camera permission to app.json: an NSCameraUsageDescription
    string under ios.infoPlist (and the expo-camera config plugin with its permission prompt copy if
    the plugin form is used), matching the existing plugin-tuple array shape. Keep
    ios.infoPlist.ITSAppUsesNonExemptEncryption unchanged. Create
    apps/mobile/lib/nutritionCameraAuth.ts exporting `requestCameraPermission(): Promise<boolean>`
    wrapping expo-camera's permission request in the healthkitAuth try/catch/console.error('[Apsis]
    ...')/return-false-never-throw shape. Sync pnpm-lock.yaml (so EAS --frozen-lockfile succeeds).
    Do NOT install expo-barcode-scanner (deprecated) or react-native-vision-camera.
  </action>
  <verify>
    <automated>grep -c "expo-camera\|expo-text-extractor" apps/mobile/package.json && grep -c "NSCameraUsageDescription" apps/mobile/app.json && grep -c "requestCameraPermission" apps/mobile/lib/nutritionCameraAuth.ts</automated>
  </verify>
  <done>Both native modules installed at SDK-56 versions; camera permission declared; never-throw permission wrapper added; lockfile synced.</done>
  <acceptance_criteria>
    - `expo-camera` and `expo-text-extractor` both appear in apps/mobile/package.json dependencies.
    - app.json ios.infoPlist contains an NSCameraUsageDescription string.
    - `expo-barcode-scanner` and `react-native-vision-camera` are NOT in package.json.
    - nutritionCameraAuth.ts's requestCameraPermission returns false on error (never throws) with an `[Apsis]`-prefixed console.error.
    - pnpm-lock.yaml is in sync (no `--frozen-lockfile` drift).
    - app.json ITSAppUsesNonExemptEncryption is unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → app binary | New native dependencies enter the build; supply-chain risk |
| device camera → app | Camera capture is a new hardware permission surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-SC | Tampering | expo-camera / expo-text-extractor installs | high | mitigate | Blocking human legitimacy checkpoint before install (Task 1); [SUS]/[ASSUMED] verified on npmjs.com; no postinstall scripts |
| T-07-13 | Repudiation / privacy | camera permission | medium | mitigate | NSCameraUsageDescription discloses purpose; permission requested via a never-throw wrapper; capture used only for barcode/label (07-08/09) |
| T-07-14 | Denial of Service | stale dev client | medium | mitigate | pnpm-lock sync + explicit EAS dev-build user_setup item (Pitfall 1) |
</threat_model>

<verification>
- `grep` gates above pass; `pnpm --filter @apsis/mobile exec tsc --noEmit` clean.
- EAS dev build completes with the new native modules (user_setup / on-device gate).
</verification>

<success_criteria>
expo-camera + expo-text-extractor installed (legitimacy-verified), camera permission declared, a
never-throw permission wrapper in place, lockfile synced for the EAS dev build.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (created by this plan)

- Dependencies: `expo-camera`, `expo-text-extractor` (apps/mobile/package.json)
- Config: NSCameraUsageDescription in app.json
- Function: `requestCameraPermission` (`apps/mobile/lib/nutritionCameraAuth.ts`)
</artifacts_produced>

<output>
Create `.planning/phases/07-nutrition-tracking/07-07-SUMMARY.md` when done
</output>
