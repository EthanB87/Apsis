---
phase: 07-nutrition-tracking
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - packages/db/src/nutritionQueries.ts
  - packages/db/src/nutritionTarget.ts
  - packages/db/src/index.ts
  - packages/db/src/__tests__/nutrition-queries.test.ts
autonomous: true
requirements: [NUTR-02, NUTR-04, NUTR-07, NUTR-17]
must_haves:
  truths:
    - "The local food cache is queryable: search-by-name, recents, and favorites all resolve from the local food/food_log tables with no network."
    - "A day's logged food totals (kcal/p/c/f) come from a single SUM over food_log filtered by localDate — no join, no rollup table."
    - "The day's session types for a date are queryable and fold through classifyDayType to a DayType."
    - "computeNutritionTargetRow is pure (no DB access, no wall clock) and wraps the @apsis/engine target functions into an upsert-ready row."
    - "Every query builder uses drizzle's parameterized API exclusively — no raw sql with interpolated search/barcode text (T-1-01)."
  artifacts:
    - "packages/db/src/nutritionQueries.ts: searchLocalFoods, recentFoods, favoriteFoods, dayTotals, sessionTypesForDate"
    - "packages/db/src/nutritionTarget.ts: computeNutritionTargetRow (pure)"
    - "packages/db/src/__tests__/nutrition-queries.test.ts"
  key_links:
    - "sessionTypesForDate rows → classifyDayType (@apsis/engine) → computeNutritionTargetRow → nutrition_target upsert"
    - "nutritionTarget.ts imports dailyMacroTarget/trainingKcalFromHss/classifyDayType from @apsis/engine (no re-implementation)"
    - "new builders re-exported from packages/db/src/index.ts barrel"
---

<objective>
Build the local-first data-access layer for nutrition: parameterized query builders for the food
cache (search/recents/favorites), a joinless day-totals aggregate, the day-type session query, and
a pure `computeNutritionTargetRow` that folds the engine's target functions into an upsert-ready
row — mirroring the queries.ts / loadDaily.ts conventions exactly.

Purpose: NUTR-02 (offline cache), NUTR-04 (recents/favorites), NUTR-07 (meal/date), NUTR-17
(day-type derivation) at the DB layer — the offline substrate every logging screen reads.
Output: nutritionQueries.ts, nutritionTarget.ts, db tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-nutrition-tracking/07-RESEARCH.md
@.planning/phases/07-nutrition-tracking/07-PATTERNS.md
@packages/db/src/queries.ts
@packages/db/src/loadDaily.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: nutritionQueries.ts parameterized builders + tests</name>
  <files>packages/db/src/nutritionQueries.ts, packages/db/src/index.ts, packages/db/src/__tests__/nutrition-queries.test.ts</files>
  <read_first>
    - packages/db/src/queries.ts (QueryableDB export, activeWorkoutFilter, previousSessionSet + recentExerciseIds builder shapes, T-1-01 module doc comment)
    - packages/db/src/schema.ts (the new food/foodLog/workout tables + column names from 07-01)
    - packages/db/src/index.ts (barrel — add the new exports)
    - packages/db/src/__tests__ (existing db test harness/setup to mirror)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (nutritionQueries.ts section — exact builder analogs + line refs; sessionTypesForDate sketch)
    - .planning/phases/07-nutrition-tracking/07-RESEARCH.md (Pattern 1/2/3; Pitfall 6 debounce note is UI-side, not here)
  </read_first>
  <action>
    Create packages/db/src/nutritionQueries.ts. Import and reuse the existing `QueryableDB` type
    from queries.ts (do NOT redeclare it). Add a module doc comment restating the T-1-01
    parameterized-only security contract (only static column/aggregate refs inside sql``). Implement:
    (a) `searchLocalFoods(db, query)` — case-insensitive name/brand LIKE match against `food`,
        parameterized via drizzle's `like`/`sql` with a bound parameter (never string-interpolated),
        ordered by name, limited;
    (b) `recentFoods(db, limit)` — distinct recently-logged foods joined from food_log→food ordered
        by max(food_log.createdAt), mirroring recentExerciseIds;
    (c) `favoriteFoods(db, limit)` — most-frequently-logged foods (count over food_log grouped by
        foodId), the "≤3-tap repeat" source (NUTR-04);
    (d) `dayTotals(db, localDate)` — `SUM(kcal/p/c/f)` over food_log WHERE localDate = ? (no join,
        denormalized per Pattern 2);
    (e) `sessionTypesForDate(db, localDate)` — select workout.type WHERE localDate = ? AND finished,
        applying activeWorkoutFilter (Pattern 3 sketch).
    Re-export all five from packages/db/src/index.ts. Then create
    packages/db/src/__tests__/nutrition-queries.test.ts (mirroring the existing db test harness)
    seeding a few food + food_log + workout rows and asserting: search matches by substring,
    recents ordering, favorites frequency ordering, dayTotals sums correctly for a date and returns
    zeros for an empty date, and sessionTypesForDate returns only finished non-deleted sessions.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/db test -- nutrition-queries && grep -c "sql`" packages/db/src/nutritionQueries.ts</automated>
  </verify>
  <done>5 parameterized builders exported + tested; day totals are joinless; no interpolated user text in any sql template.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/db test -- nutrition-queries` passes green.
    - `grep -cE "export function (searchLocalFoods|recentFoods|favoriteFoods|dayTotals|sessionTypesForDate)" packages/db/src/nutritionQueries.ts` returns 5.
    - No `sql` template literal in nutritionQueries.ts interpolates a function parameter (only static column/aggregate references appear inside `sql\`\``) — verified by review + the injection test below.
    - A test seeding a food named with a `%`/`'` character and searching for it returns the row without error (parameterization proof).
    - dayTotals over a date with no food_log rows returns zeroed totals (not null/NaN).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: nutritionTarget.ts pure row builder + tests</name>
  <files>packages/db/src/nutritionTarget.ts, packages/db/src/index.ts, packages/db/src/__tests__/nutrition-queries.test.ts</files>
  <read_first>
    - packages/db/src/loadDaily.ts (computeLoadDailyUpsertRows: pure, no DB access, no wall clock — today/localDate always passed in by caller; the module doc comment stating this)
    - packages/engine/src/nutrition.ts (dailyMacroTarget/classifyDayType/trainingKcalFromHss signatures from 07-02)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (nutritionTarget.ts section — mirror computeLoadDailyUpsertRows shape, single-day, no gap-fill)
  </read_first>
  <action>
    Create packages/db/src/nutritionTarget.ts exporting a `NutritionTargetUpsertRow` type and a pure
    `computeNutritionTargetRow(profile: NutritionProfile, sessionTypes, dayHss, localDate)` that:
    folds sessionTypes through `classifyDayType`, derives sessionKcal via `trainingKcalFromHss(dayHss)`,
    calls `dailyMacroTarget(profile, dayType, sessionKcal)`, and returns a row shaped for the
    nutrition_target upsert (id, localDate, dayType, kcal, proteinG, carbG, fatG, source:'auto').
    This module performs NO DB access and NEVER reads the wall clock — localDate is always
    caller-supplied (copy loadDaily.ts's doc-comment discipline verbatim in intent). Re-export from
    the barrel. Extend nutrition-queries.test.ts with cases: a rest day (empty sessionTypes,
    dayHss 0) yields dayType 'rest' and a rest-day target; a double day (2 session types) yields
    dayType 'double' and additive sessionKcal reflected in kcal.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/db test -- nutrition-queries && grep -c "Date.now\|\.select(\|\.insert(" packages/db/src/nutritionTarget.ts</automated>
  </verify>
  <done>computeNutritionTargetRow is pure (no DB, no Date.now), folds engine functions correctly, tested for rest + double days.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/db test -- nutrition-queries` passes green including the two new target-row cases.
    - `grep -c "Date.now" packages/db/src/nutritionTarget.ts` returns 0.
    - `grep -cE "\.select\(|\.insert\(|\.update\(|\.delete\(" packages/db/src/nutritionTarget.ts` returns 0 (no DB access in this pure module).
    - A test asserting an empty-sessionTypes input produces `dayType: 'rest'` passes.
    - `computeNutritionTargetRow` is re-exported from `packages/db/src/index.ts`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user search/barcode/OCR text → food query | Untrusted strings reach the local SQLite query layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering (SQL injection) | nutritionQueries.ts | high | mitigate | Drizzle parameterized builders only; `%`/`'` search test proves no interpolation (T-1-01 lineage) |
| T-07-06 | Tampering / data integrity | computeNutritionTargetRow | medium | mitigate | Pure fold over engine functions that already defend inputs; no DB writes in the pure module |
| T-07-SC | Tampering | npm installs | high | accept | No installs in this plan |
</threat_model>

<verification>
- `pnpm --filter @apsis/db test` full suite green.
- Root `pnpm typecheck` clean.
</verification>

<success_criteria>
Local-first nutrition data layer (search/recents/favorites/day-totals/day-type) + pure target-row
builder exist and are tested; no raw SQL interpolation.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (created by this plan)

- Functions: `searchLocalFoods`, `recentFoods`, `favoriteFoods`, `dayTotals`, `sessionTypesForDate` (`packages/db/src/nutritionQueries.ts`)
- Function/type: `computeNutritionTargetRow`, `NutritionTargetUpsertRow` (`packages/db/src/nutritionTarget.ts`)
- Test file: `packages/db/src/__tests__/nutrition-queries.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/07-nutrition-tracking/07-03-SUMMARY.md` when done
</output>
