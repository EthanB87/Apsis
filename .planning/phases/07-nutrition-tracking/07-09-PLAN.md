---
phase: 07-nutrition-tracking
plan: 09
type: execute
wave: 5
depends_on: [07-07]
files_modified:
  - apps/mobile/lib/labelOcrParse.ts
  - apps/mobile/lib/__tests__/labelOcrParse.test.ts
  - apps/mobile/app/(tabs)/nutrition/label-scan.tsx
autonomous: true
requirements: [NUTR-11, NUTR-12, NUTR-09]
must_haves:
  truths:
    - "A user can photograph a nutrition label; on-device OCR (Apple Vision via expo-text-extractor) extracts kcal/P/C/F/serving into a pre-filled custom food."
    - "OCR output is treated as untrusted: every parsed numeric is bounds-checked (no negative, no absurd values) before pre-filling."
    - "The user always confirms/edits the parsed values before anything is saved (confirm sheet); nothing is written directly from OCR."
    - "Every confirmed label scan saves as a custom food (source='user')."
  artifacts:
    - "apps/mobile/lib/labelOcrParse.ts (pure regex parser + bounds-check, no native import)"
    - "apps/mobile/lib/__tests__/labelOcrParse.test.ts (sample label fixtures)"
    - "apps/mobile/app/(tabs)/nutrition/label-scan.tsx (capture → OCR → parse → confirm → custom food)"
  key_links:
    - "label-scan CameraView.takePictureAsync → expo-text-extractor.extractTextFromImage(uri) → labelOcrParse(string[]) → FoodConfirmSheet (pre-filled) → food (source='user') → food_log"
    - "labelOcrParse receives string[] as a plain parameter (never imports expo-text-extractor) so it is vitest-testable (runEntryLogic purity discipline)"
---

<objective>
Add on-device nutrition-label OCR: capture a label photo, extract text via Apple Vision
(expo-text-extractor), parse kcal/P/C/F/serving with a pure bounds-checked regex parser, and pre-fill
the shared confirm/edit sheet so the user verifies before a custom food is saved.

Purpose: NUTR-11 (label OCR into a custom food), NUTR-12 (save as custom food), NUTR-09 (confirm/edit)
— free/private/offline OCR, no paid API (NUTRITION.md §4).
Output: pure OCR parser + tests + label-scan screen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-nutrition-tracking/07-RESEARCH.md
@.planning/phases/07-nutrition-tracking/07-PATTERNS.md
@apps/mobile/AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure labelOcrParse (regex + bounds-check) + fixture tests</name>
  <files>apps/mobile/lib/labelOcrParse.ts, apps/mobile/lib/__tests__/labelOcrParse.test.ts</files>
  <read_first>
    - apps/mobile/lib/runEntryLogic.ts (pure, native-import-free module for vitest; never-divide-by-zero / bounds guard style)
    - .planning/phases/07-nutrition-tracking/07-RESEARCH.md (Label OCR Flow; Pitfall 9 untrusted OCR bounds-check)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (labelOcrParse.ts section — runEntryLogic analog + bounds-check pattern; receives string[], never calls the native module)
  </read_first>
  <behavior>
    - labelOcrParse(lines: string[]): extracts kcal, protein_g, carb_g, fat_g, and serving (name + grams) via regex over recognized text lines; returns a partial pre-fill object.
    - Bounds-check: negative → dropped; absurd (e.g. >50,000 kcal/100g, macro > a sane ceiling) → dropped; non-numeric → undefined. Never throws.
    - A field it cannot confidently extract is left undefined (so the confirm sheet shows a blank the user fills), not silently zero.
  </behavior>
  <action>
    Create apps/mobile/lib/labelOcrParse.ts (zero @apsis/db / expo native imports — receives an
    already-extracted string[] as a plain parameter, never calls expo-text-extractor itself) exporting
    `labelOcrParse(lines: string[])` that regex-extracts kcal/protein/carbs/fat/serving from the
    recognized lines and returns a partial macro pre-fill. Apply the runEntryLogic bounds-check
    discipline to every extracted numeric (Pitfall 9): drop negatives, drop absurd values above sane
    ceilings, leave un-extractable fields undefined; never throw. Write labelOcrParse.test.ts with a
    few representative label-text fixtures (US "Nutrition Facts" style) asserting correct extraction,
    an absurd-value fixture that gets dropped, and a garbage/empty fixture that yields an all-undefined
    result without throwing.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile test -- labelOcrParse</automated>
  </verify>
  <done>Pure OCR parser extracts macros with bounds-checking, leaves unknowns undefined, never throws; tested against fixtures.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/mobile test -- labelOcrParse` passes green.
    - A test asserting a representative label fixture extracts the expected kcal/P/C/F passes.
    - A test asserting an absurd value (e.g. 90000 kcal) is dropped (field undefined) passes.
    - `grep -c "expo-text-extractor\|@apsis/db" apps/mobile/lib/labelOcrParse.ts` returns 0 (pure, vitest-runnable).
    - An empty/garbage input returns an object with no throw.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: label-scan screen (capture → OCR → parse → confirm → custom food)</name>
  <files>apps/mobile/app/(tabs)/nutrition/label-scan.tsx</files>
  <read_first>
    - apps/mobile/app/onboarding/healthkit.tsx (permission-guard + guard-flag + try/catch/finally + loading-state shape)
    - apps/mobile/lib/nutritionCameraAuth.ts (requestCameraPermission from 07-07)
    - apps/mobile/lib/labelOcrParse.ts (Task 1 parser)
    - apps/mobile/components/FoodConfirmSheet.tsx (shared confirm/edit sheet from 07-06 — pre-fill with parsed values)
    - .planning/phases/07-nutrition-tracking/07-RESEARCH.md (Label OCR Flow diagram; Code Examples takePictureAsync + extractTextFromImage)
    - apps/mobile/AGENTS.md (verify expo-camera takePictureAsync + expo-text-extractor API against v56 docs)
    - DESIGN-SYSTEM.md
  </read_first>
  <action>
    Create nutrition/label-scan.tsx: gate on requestCameraPermission (healthkit.tsx guard shape),
    mount CameraView, and on capture call takePictureAsync({ quality }) → pass the uri to
    expo-text-extractor's extractTextFromImage → feed the returned string[] to labelOcrParse → open
    the shared FoodConfirmSheet PRE-FILLED with the parsed values so the user confirms/edits before
    saving (NUTR-09 — nothing is written directly from OCR). On confirm, insert a food with
    source='user' (provenance: OCR) and log it via the existing confirm-sheet path (NUTR-12). Handle
    the OCR-returns-nothing case gracefully (fall through to blank manual entry). Style per
    DESIGN-SYSTEM.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile exec tsc --noEmit 2>&1 | head -5 && grep -c "extractTextFromImage\|labelOcrParse\|takePictureAsync\|requestCameraPermission" apps/mobile/app/(tabs)/nutrition/label-scan.tsx</automated>
  </verify>
  <done>Label-scan captures a photo, OCRs on-device, parses, pre-fills the confirm sheet, and saves a source='user' custom food only after confirmation.</done>
  <acceptance_criteria>
    - label-scan.tsx mounts CameraView only after requestCameraPermission resolves true.
    - The capture path calls takePictureAsync → extractTextFromImage → labelOcrParse → FoodConfirmSheet (pre-filled).
    - No food is written before the user confirms in FoodConfirmSheet (NUTR-09).
    - A confirmed scan inserts a food with source='user'.
    - `apps/mobile` typechecks clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OCR-recognized text → food row | Arbitrary recognized numbers are untrusted input |
| device camera → app | Label photo capture surface (on-device only, no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-20 | Tampering / data integrity | OCR-parsed macros | high | mitigate | Bounds-check every parsed numeric (drop negative/absurd); mandatory confirm/edit sheet before persist — nothing written directly from OCR (Pitfall 9, NUTR-09) |
| T-07-21 | Information disclosure | OCR is on-device | low | accept | Apple Vision runs fully on-device; no image or text leaves the phone (no network in this flow) |
| T-07-22 | Denial of Service | OCR returns nothing | medium | mitigate | Graceful fall-through to blank manual entry; never a dead end |
</threat_model>

<verification>
- `pnpm --filter @apsis/mobile test` green (labelOcrParse tests); `pnpm --filter @apsis/mobile exec tsc --noEmit` clean.
- Manual (phase UAT, on-device dev build): photograph a real label → parsed values pre-fill → confirm → custom food saved.
</verification>

<success_criteria>
On-device label OCR fills a bounds-checked, user-confirmed custom food (source='user') — NUTR-11/12/09.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (created by this plan)

- Function: `labelOcrParse` (`apps/mobile/lib/labelOcrParse.ts`)
- Screen: `apps/mobile/app/(tabs)/nutrition/label-scan.tsx`
- Test file: `apps/mobile/lib/__tests__/labelOcrParse.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/07-nutrition-tracking/07-09-SUMMARY.md` when done
</output>
