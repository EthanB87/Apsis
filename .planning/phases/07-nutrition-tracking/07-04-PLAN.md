---
phase: 07-nutrition-tracking
plan: 04
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - apps/mobile/lib/nutritionProfile.ts
  - apps/mobile/hooks/useNutritionProfile.ts
  - apps/mobile/app/nutrition-setup/index.tsx
  - apps/mobile/app/nutrition-setup/_layout.tsx
autonomous: true
requirements: [NUTR-15, NUTR-20]
must_haves:
  truths:
    - "An already-onboarded user whose height_cm/birth_year/goal_mode are NULL is prompted to complete a short Nutrition Setup before any target is shown (mirrors ONB-03)."
    - "The setup screen collects height, birth year, and goal mode and writes them via a single batched UPDATE (staged-draft pattern from Settings)."
    - "A NutritionProfile (sex, bodyweightKg, heightCm, age derived from birthYear, goalMode) is assembled from the profile row for the engine — with a clear 'incomplete' state when any field is missing."
    - "dailyMacroTarget is never called with NULL height/age (no NaN targets)."
  artifacts:
    - "apps/mobile/lib/nutritionProfile.ts: buildNutritionProfile + isNutritionProfileComplete (pure)"
    - "apps/mobile/hooks/useNutritionProfile.ts: loads the profile row and returns {profile|null, complete}"
    - "apps/mobile/app/nutrition-setup/ screen collecting height_cm/birth_year/goal_mode"
  key_links:
    - "buildNutritionProfile ← user_profile row (heightCm/birthYear/goalMode from 07-01) → NutritionProfile (@apsis/shared)"
    - "nutrition-setup Save → single batched UPDATE on user_profile (useProfile update-in-place hook pattern) → useProfileVersion.bump()"
    - "isNutritionProfileComplete=false → nutrition targets gate shows the setup prompt (the NUTR-20 existing-user gate)"
---

<objective>
Close the critical schema-gap the adaptive model depends on: collect the three new profile fields
(height, birth year, goal mode) from already-onboarded users via a small Nutrition Setup prompt, and
assemble a typed NutritionProfile for the engine — gated so targets never render off NULL fields.

Purpose: NUTR-15 (fields exist + are collectable) and NUTR-20 (existing-user gate) — Phase 07 ships
as an update to users onboarded in Phases 03-06 whose new fields are all NULL (Pitfalls 2/3).
Output: pure profile assembler + gate hook + staged-draft setup screen.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-nutrition-tracking/07-RESEARCH.md
@.planning/phases/07-nutrition-tracking/07-PATTERNS.md
@DESIGN-SYSTEM.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure NutritionProfile assembler + completeness gate + hook</name>
  <files>apps/mobile/lib/nutritionProfile.ts, apps/mobile/lib/__tests__/nutritionProfile.test.ts, apps/mobile/hooks/useNutritionProfile.ts</files>
  <read_first>
    - apps/mobile/lib/runEntryLogic.ts (pure, native-import-free lib module convention for vitest-testability)
    - apps/mobile/lib/profileVersion.ts (useProfileVersion signal to depend on for re-check on write)
    - apps/mobile/hooks (an existing hook that reads the user_profile row — mirror its DB access shape)
    - packages/shared/src/index.ts (NutritionProfile type from 07-02)
    - .planning/phases/07-nutrition-tracking/07-RESEARCH.md (Pitfall 2/3; NutritionProfile shape; age derived from birthYear)
  </read_first>
  <behavior>
    - buildNutritionProfile(row, currentYear): returns a NutritionProfile only when sex, bodyweightKg, heightCm, birthYear, goalMode are all non-null; age = currentYear - birthYear; otherwise returns null.
    - isNutritionProfileComplete(row): true iff all five fields are present.
    - Pure: currentYear is passed in (no Date.now inside the lib module).
  </behavior>
  <action>
    Create apps/mobile/lib/nutritionProfile.ts (zero @apsis/db and zero Expo native imports, like
    runEntryLogic.ts) exporting `isNutritionProfileComplete(row)` and `buildNutritionProfile(row,
    currentYear)`. buildNutritionProfile derives `age = currentYear - birthYear` and returns a fully
    typed NutritionProfile, or null if any required field is missing (never coerces NULL to 0).
    Write apps/mobile/lib/__tests__/nutritionProfile.test.ts asserting: a complete row builds a
    correct profile with derived age; a row missing heightCm returns null; a row missing goalMode
    returns null. Then create apps/mobile/hooks/useNutritionProfile.ts that reads the singleton
    user_profile row (mirroring the existing profile hook), depends on useProfileVersion so it
    re-reads after a write, computes currentYear at the hook layer (not in the pure lib), and returns
    `{ profile: NutritionProfile | null, complete: boolean }`.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile test -- nutritionProfile</automated>
  </verify>
  <done>Pure assembler + completeness check tested; hook returns typed profile + complete flag; no NULL→0 coercion.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/mobile test -- nutritionProfile` passes green.
    - A test asserting a row with `heightCm: null` yields `buildNutritionProfile(...) === null` passes.
    - `grep -c "Date.now" apps/mobile/lib/nutritionProfile.ts` returns 0 (currentYear passed in).
    - `grep -c "@apsis/db\|expo-" apps/mobile/lib/nutritionProfile.ts` returns 0 (stays vitest-runnable).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Nutrition Setup staged-draft screen (height / birth year / goal mode)</name>
  <files>apps/mobile/app/nutrition-setup/index.tsx, apps/mobile/app/nutrition-setup/_layout.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/settings/index.tsx (staged-draft → single batched UPDATE profile-editor pattern; ProfileReview values/onEditField/onSubmit contract)
    - apps/mobile/hooks (the update-in-place profile hook, e.g. useProfile — parameterized UPDATE on user_profile only)
    - apps/mobile/lib/profileVersion.ts (bump on save)
    - DESIGN-SYSTEM.md (void/volt palette, one-volt-per-screen, tap-to-type entry conventions)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (Nutrition-setup profile-fields prompt section — staged-draft analog + line refs)
  </read_first>
  <action>
    Create apps/mobile/app/nutrition-setup/_layout.tsx (a Stack matching the settings/log group
    convention) and index.tsx: a single-screen staged-draft editor collecting heightCm (numeric,
    respecting the display-units toggle for input then storing metric cm), birthYear (integer,
    sane-bounds validated), and goalMode (segmented control cut/maintain/bulk — active fill follows
    the one-volt-per-screen rule: use bone if the Save CTA is the volt-filled primary). On Save,
    commit all three via one batched UPDATE on user_profile (reuse the existing update-in-place
    profile hook; never touch workout/load_daily) and call useProfileVersion.bump() so the gate
    re-checks. Follow the void/volt DESIGN-SYSTEM styling and the tap-to-type ledger entry pattern.
    Do not add a new onboarding wizard step — this is a standalone prompt reached from the nutrition
    tab when the profile is incomplete (wired in 07-05).
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile exec tsc --noEmit 2>&1 | head -5 && grep -cE "height_cm|heightCm|birthYear|goalMode|goal_mode" apps/mobile/app/nutrition-setup/index.tsx</automated>
  </verify>
  <done>Setup screen collects the 3 fields via staged-draft + single batched UPDATE; bumps profile version; matches DESIGN-SYSTEM.</done>
  <acceptance_criteria>
    - The screen writes heightCm, birthYear, and goalMode in a single UPDATE (one hook call), not three navigations.
    - Save calls `useProfileVersion` bump so the gate re-evaluates without an app relaunch.
    - goalMode is captured via a segmented control offering exactly cut/maintain/bulk.
    - `apps/mobile` typechecks clean.
    - The screen never writes to workout or load_daily (grep: no such table refs in the file).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → user_profile | Numeric height/birth-year input persists to the profile the engine reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Tampering / data integrity | nutrition-setup inputs | medium | mitigate | Bounds-validate birthYear/height before UPDATE; pure assembler returns null (gate) rather than coercing NULL→0 (Pitfall 3) |
| T-07-08 | Information disclosure | profile fields in crash reports | low | mitigate | New fields are non-health-identifying; existing sentrySanitize allowlist already blocks arbitrary extra/contexts (NUTR-22 lineage) |
| T-07-SC | Tampering | npm installs | high | accept | No installs in this plan |
</threat_model>

<verification>
- `pnpm --filter @apsis/mobile test` green (nutritionProfile tests).
- `pnpm --filter @apsis/mobile exec tsc --noEmit` clean.
- Manual (phase UAT): opening nutrition with NULL fields routes to setup; completing it clears the gate.
</verification>

<success_criteria>
Existing users can complete the 3 new profile fields via a gated staged-draft prompt; a typed
NutritionProfile is assembled without NULL→0 coercion (NUTR-15/NUTR-20).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (created by this plan)

- Functions: `buildNutritionProfile`, `isNutritionProfileComplete` (`apps/mobile/lib/nutritionProfile.ts`)
- Hook: `useNutritionProfile` (`apps/mobile/hooks/useNutritionProfile.ts`)
- Screens: `apps/mobile/app/nutrition-setup/index.tsx`, `_layout.tsx`
- Test file: `apps/mobile/lib/__tests__/nutritionProfile.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/07-nutrition-tracking/07-04-SUMMARY.md` when done
</output>
