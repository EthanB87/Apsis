---
phase: 07-nutrition-tracking
plan: 05
type: execute
wave: 3
depends_on: [07-03, 07-04]
files_modified:
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/nutrition/_layout.tsx
  - apps/mobile/app/(tabs)/nutrition/index.tsx
  - apps/mobile/lib/recomputeNutritionTarget.ts
  - apps/mobile/lib/nutritionTargetSignal.ts
  - apps/mobile/lib/finishWorkout.ts
  - apps/mobile/lib/runEntry.ts
autonomous: true
requirements: [NUTR-19, NUTR-16, NUTR-17, NUTR-22]
must_haves:
  truths:
    - "A 5th 'Nutrition' tab exists and opens a screen showing today's targets vs. logged totals (kcal + P/C/F progress)."
    - "If the nutrition profile is incomplete, the screen shows the Nutrition Setup prompt instead of nonsense targets (NUTR-20 gate consumed here)."
    - "A nutrition_target row for today is created on a workout finish AND lazily on view (rest-day fallback: sessionKcal=0, dayType='rest' when no row exists yet)."
    - "Nutrition kcal/macro values are never attached to a crash/analytics event (NUTR-22)."
  artifacts:
    - "5th Tabs.Screen 'nutrition' + nutrition/_layout.tsx Stack + nutrition/index.tsx targets screen"
    - "apps/mobile/lib/recomputeNutritionTarget.ts (event-driven upsert wrapper)"
    - "apps/mobile/lib/nutritionTargetSignal.ts (zustand version counter)"
  key_links:
    - "finishWorkout / runEntry → recomputeNutritionTarget(db, todayLocalDate()) → nutrition_target upsert → nutritionTargetSignal.bump()"
    - "nutrition/index.tsx → useNutritionProfile.complete gate → (incomplete) route to nutrition-setup / (complete) dayTotals + nutrition_target render"
    - "nutrition/index.tsx lazy-compute-on-view fallback (Pitfall 5) → recomputeNutritionTarget on focus when today's row is absent"
---

<objective>
Ship the sellable increment's home: a 5th Nutrition tab whose screen shows today's adaptive targets
versus logged totals, wired so targets recompute both on workout-finish and lazily on view (the
rest-day fallback), and gated behind a complete nutrition profile.

Purpose: NUTR-19 (targets vs totals display) + NUTR-16/17 wiring (recompute-on-write) + the Pitfall-5
rest-day lazy-compute gap + NUTR-22 (no nutrition values in crash reports).
Output: nutrition tab + targets screen + recompute wrapper + signal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-nutrition-tracking/07-RESEARCH.md
@.planning/phases/07-nutrition-tracking/07-PATTERNS.md
@DESIGN-SYSTEM.md
@apps/mobile/lib/recomputeLoadDaily.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: recomputeNutritionTarget wrapper + signal + finish/run wiring</name>
  <files>apps/mobile/lib/recomputeNutritionTarget.ts, apps/mobile/lib/nutritionTargetSignal.ts, apps/mobile/lib/finishWorkout.ts, apps/mobile/lib/runEntry.ts</files>
  <read_first>
    - apps/mobile/lib/recomputeLoadDaily.ts (read → pure-fold → upsert-loop; log-then-rethrow [Apsis]-prefixed; todayLocalDate() usage)
    - apps/mobile/lib/profileVersion.ts (zustand version-counter shape to mirror for nutritionTargetSignal)
    - apps/mobile/lib/finishWorkout.ts + apps/mobile/lib/runEntry.ts (existing recomputeLoadDaily call sites — add the nutrition recompute right after)
    - packages/db/src/nutritionTarget.ts + nutritionQueries.ts (computeNutritionTargetRow, sessionTypesForDate from 07-03)
    - packages/db/src/queries.ts (last28DaysTrend / load_daily read for today's dayHss)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (recomputeNutritionTarget.ts section — exact analog + the lazy-trigger divergence note)
  </read_first>
  <action>
    Create apps/mobile/lib/recomputeNutritionTarget.ts exporting `recomputeNutritionTarget(database,
    localDate)`: read sessionTypesForDate(localDate) and today's load_daily.dayHss, load the profile
    row and build a NutritionProfile; if the profile is incomplete, return early WITHOUT writing a
    target (the gate owns that state — never write a NaN target); otherwise fold through
    computeNutritionTargetRow and onConflictDoUpdate into nutrition_target keyed on localDate. Use
    the log-then-rethrow `[Apsis]`-prefixed Error-only convention from recomputeLoadDaily.ts. Create
    apps/mobile/lib/nutritionTargetSignal.ts (a zustand version counter mirroring profileVersion.ts)
    and bump it after a successful upsert. In finishWorkout.ts and runEntry.ts, call
    recomputeNutritionTarget(db, todayLocalDate()) immediately AFTER the existing recomputeLoadDaily
    call (so dayHss is fresh), preserving all existing behavior and the existing HK write-back paths.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile exec tsc --noEmit 2>&1 | head -5 && grep -c "recomputeNutritionTarget" apps/mobile/lib/finishWorkout.ts apps/mobile/lib/runEntry.ts</automated>
  </verify>
  <done>Recompute wrapper upserts today's target after each session finish, bumps the signal, and no-ops safely when the profile is incomplete.</done>
  <acceptance_criteria>
    - `recomputeNutritionTarget` is called in both finishWorkout.ts and runEntry.ts after recomputeLoadDaily.
    - When the profile is incomplete, recomputeNutritionTarget returns without inserting a nutrition_target row (no NaN target).
    - The upsert uses onConflictDoUpdate keyed on localDate (idempotent per day).
    - Errors are logged `console.error('[Apsis] ...', err)` and rethrown (matches recomputeLoadDaily).
    - `apps/mobile` typechecks clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 5th Nutrition tab + targets-vs-totals screen (gated + lazy fallback)</name>
  <files>apps/mobile/app/(tabs)/_layout.tsx, apps/mobile/app/(tabs)/nutrition/_layout.tsx, apps/mobile/app/(tabs)/nutrition/index.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/_layout.tsx (the 4 existing Tabs.Screen entries + headerShown conventions — add a 5th following the settings entry shape)
    - apps/mobile/app/(tabs)/index.tsx (TODAY screen — useFocusEffect for store-writing screens, ring/stat-tile composition, DESIGN-SYSTEM styling)
    - apps/mobile/hooks/useNutritionProfile.ts (complete gate from 07-04)
    - apps/mobile/lib/nutritionTargetSignal.ts + recomputeNutritionTarget.ts (Task 1)
    - packages/db/src/nutritionQueries.ts (dayTotals from 07-03)
    - .planning/phases/07-nutrition-tracking/07-PATTERNS.md (nutrition tab registration + nutrition/index.tsx sections; Pitfall 5 lazy-compute-on-view note)
    - DESIGN-SYSTEM.md (void/volt, one-volt-per-screen)
  </read_first>
  <action>
    Add a 5th `Tabs.Screen name="nutrition"` to (tabs)/_layout.tsx following the settings entry shape
    (title, headerShown per one-volt convention, an ios/android/web SF Symbol trio such as
    fork.knife). Create nutrition/_layout.tsx as a Stack (matching the log/history group convention)
    so child screens (search/scan/label-scan/recipes, added by later plans) auto-route. Create
    nutrition/index.tsx: on focus (useFocusEffect, NOT plain useEffect — Pitfall-5 oscillation
    lesson from 03-10), read useNutritionProfile; if incomplete, render a Nutrition Setup call-to-
    action that routes to /nutrition-setup; if complete, read today's nutrition_target and if absent
    lazily call recomputeNutritionTarget(db, todayLocalDate()) once (rest-day fallback), then render
    today's targets vs dayTotals as kcal + P/C/F progress, subscribing to nutritionTargetSignal so a
    logging write elsewhere refreshes it. Style per DESIGN-SYSTEM (void/volt). Do NOT attach any
    kcal/macro value to a logging/analytics breadcrumb (NUTR-22).
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile exec tsc --noEmit 2>&1 | head -5 && grep -c "name=\"nutrition\"" apps/mobile/app/(tabs)/_layout.tsx && grep -c "useFocusEffect" apps/mobile/app/(tabs)/nutrition/index.tsx</automated>
  </verify>
  <done>5th tab renders targets vs totals, gates on incomplete profile, and lazily computes a rest-day target on view; uses useFocusEffect.</done>
  <acceptance_criteria>
    - (tabs)/_layout.tsx registers a 5th `Tabs.Screen name="nutrition"`.
    - nutrition/index.tsx uses `useFocusEffect` (not plain useEffect) for its store-touching focus logic.
    - When useNutritionProfile.complete is false, the screen routes/links to /nutrition-setup rather than rendering targets.
    - When complete and no today's row exists, recomputeNutritionTarget runs once on focus (lazy rest-day fallback).
    - `apps/mobile` typechecks clean; no kcal/macro value is passed to a Sentry/breadcrumb call in this file.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logged nutrition values → crash/analytics SDK | Nutrition kcal/macros could leak into Sentry extra/breadcrumbs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Information disclosure | nutrition value logging | high | mitigate | No kcal/macro/barcode value attached to Sentry extra/contexts/breadcrumbs; existing sentrySanitize allowlist resets extra/breadcrumbs (NUTR-22/REL-03 lineage) |
| T-07-10 | Denial of Service | lazy recompute on focus | medium | mitigate | useFocusEffect + idempotent onConflictDoUpdate (Pitfall-5 fix); no re-entrant oscillation (03-10 lesson) |
| T-07-SC | Tampering | npm installs | high | accept | No installs in this plan |
</threat_model>

<verification>
- `pnpm --filter @apsis/mobile test` green; `pnpm --filter @apsis/mobile exec tsc --noEmit` clean.
- Manual (phase UAT): tab appears; incomplete profile → setup; complete profile → targets vs totals; a rest day still shows a target.
</verification>

<success_criteria>
Nutrition tab shows adaptive targets vs logged totals, gated + lazily-computed, with targets
recomputing on every session finish and no nutrition values reaching crash reports.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (created by this plan)

- Screens: `apps/mobile/app/(tabs)/nutrition/_layout.tsx`, `apps/mobile/app/(tabs)/nutrition/index.tsx`
- Tab entry: 5th `Tabs.Screen name="nutrition"` in `(tabs)/_layout.tsx`
- Functions: `recomputeNutritionTarget` (`apps/mobile/lib/recomputeNutritionTarget.ts`)
- Store: `nutritionTargetSignal` (`apps/mobile/lib/nutritionTargetSignal.ts`)
</artifacts_produced>

<output>
Create `.planning/phases/07-nutrition-tracking/07-05-SUMMARY.md` when done
</output>
