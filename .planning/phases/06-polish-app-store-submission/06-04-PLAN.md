---
phase: 06-polish-app-store-submission
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs-site/privacy/index.html
  - docs-site/support/index.html
  - docs-site/CNAME
autonomous: false
requirements: [REL-02]
user_setup:
  - service: static-hosting (apsistraining.com)
    why: "The privacy policy + support pages must be live over HTTPS on the user-owned domain; deploying to apsistraining.com requires DNS/host control only the user has (D-04, Claude's discretion on mechanics)."
    dashboard_config:
      - task: "Publish docs-site/ to apsistraining.com over HTTPS (GitHub Pages with custom domain + CNAME is the recommended default) so /privacy and /support resolve"
        location: "GitHub repo → Settings → Pages (or chosen static host) + domain DNS provider"

must_haves:
  truths:
    - "https://apsistraining.com/privacy returns 200 and accurately states: fully offline-first; HealthKit data never leaves the device; the only off-device data is anonymous Sentry crash diagnostics (no PII, no health values)."
    - "https://apsistraining.com/support returns 200 and offers a real working contact method (monitored mailto:)."
    - "Both pages are brand-continuous (void #0B0C0E background, bone text, volt links only) plain semantic HTML — no JS, no forms."
    - "The user has reviewed the privacy policy copy before it went live (D-05)."
  artifacts:
    - "docs-site/privacy/index.html"
    - "docs-site/support/index.html"
    - "docs-site/CNAME (apsistraining.com)"
  key_links:
    - "privacy policy content ← D-03 Sentry allowlist posture (only crash diagnostics leave the device) — must stay consistent with plan 06-02"
    - "/privacy + /support live URLs → entered into App Store Connect in plan 06-07 (Privacy Policy URL + mandatory Support URL, Pitfall 5)"
---

<objective>
Produce and publish the two off-device web pages App Store Connect requires: a tailored privacy
policy at apsistraining.com/privacy (D-04/D-05) and a support page at apsistraining.com/support
(RESEARCH Pitfall 5 — a mandatory ASC field not in the original decision set). Both are plain,
brand-dark static HTML matching the UI-SPEC static-page contract; the privacy policy states Apsis's
real posture (offline-first, HealthKit on-device, only Sentry crash diagnostics leave the device).

Purpose: REL-02 requires a live HTTPS privacy policy, and submission is blocked without a Support URL.
Both are independent of the app binary, so they can be reviewed and go live early with DNS slack.
Output: Two reviewed, HTTPS-live pages + a CNAME for the custom domain.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-app-store-submission/06-RESEARCH.md
@.planning/phases/06-polish-app-store-submission/06-UI-SPEC.md
@apps/mobile/lib/healthkitAuth.ts
</context>

## Artifacts this phase produces (plan 06-04)

- `docs-site/privacy/index.html` — Privacy Policy page (H1 "Privacy Policy", "Last updated {date}" label, body sections).
- `docs-site/support/index.html` — Support page (H1 "Support", short paragraph + mailto: contact).
- `docs-site/CNAME` — contains `apsistraining.com` for GitHub Pages custom-domain routing.

<tasks>

<task type="auto">
  <name>Task 1: Draft the privacy policy page (D-05)</name>
  <files>docs-site/privacy/index.html</files>
  <read_first>
    - .planning/phases/06-polish-app-store-submission/06-UI-SPEC.md (Store & Static-Asset Specs §3; Typography + Color tables for static pages; Copywriting Contract rows for the privacy page)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (REL-02 — Apple "collect" = transmitted off-device; on-device HealthKit is exempt; D-06 posture)
    - apps/mobile/lib/healthkitAuth.ts (exact HealthKit read/write scopes the policy describes: reads workouts/HR/bodyweight, writes workouts only — all on-device)
    - .planning/phases/05-healthkit-integration/05-SECURITY.md (confirms no health values leave the device)
  </read_first>
  <action>
    Create docs-site/privacy/index.html as a single-column plain semantic HTML page (h1/h2/p/a
    only, no JS, no forms) using inline or a single embedded style block matching the UI-SPEC static-
    page contract exactly: background #0B0C0E (void), body text #F2F1EC (bone) 16px/1.6, secondary
    text #8A9098 (ash), links #C6F23D (volt) only, max content width 640px centered, page horizontal
    margin 24px, section headings 20px/600 with 48px top margin, H1 "Privacy Policy" 28px/700, and a
    "Last updated {shipping date}" label (13px, ash) beneath the H1 with a volt hairline underline
    under the title. Content must accurately and plainly state Apsis's real posture: fully offline-
    first; all logging and HealthKit data (workouts, heart rate, bodyweight, distances, durations,
    HSS) stays on-device and is never transmitted; HealthKit access is optional/skippable; the ONLY
    data that ever leaves the device is anonymous Sentry crash diagnostics containing no personal or
    health information (matching the D-03 allowlist: error type/message, stack frames, device/OS
    model, app version); no accounts, no tracking, no third-party ad SDKs. Do not import Archivo/
    JetBrains Mono web fonts — use the system font stack. Tone: plain and direct, no legalese padding.
  </action>
  <verify>
    <automated>test -f docs-site/privacy/index.html && grep -qi "Privacy Policy" docs-site/privacy/index.html && grep -qi "crash diagnostics" docs-site/privacy/index.html && grep -q "#0B0C0E" docs-site/privacy/index.html</automated>
  </verify>
  <done>Privacy page exists, brand-dark, and states the offline-first / on-device / Sentry-only posture.</done>
  <acceptance_criteria>
    - docs-site/privacy/index.html exists with an H1 "Privacy Policy" and a "Last updated" label.
    - The page explicitly states HealthKit data stays on-device and never leaves; the only off-device data is anonymous crash diagnostics.
    - Colors used are limited to void/bone/ash/volt per the UI-SPEC static-page Color table; volt used only for links + the title hairline.
    - No <script> tag and no <form> element present.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Draft the support page + CNAME (Pitfall 5)</name>
  <files>docs-site/support/index.html, docs-site/CNAME</files>
  <read_first>
    - .planning/phases/06-polish-app-store-submission/06-UI-SPEC.md (Copywriting Contract — Support page rows; static-page layout)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Pitfall 5 — mandatory ASC Support URL; recommended GitHub Pages + CNAME hosting default)
    - docs-site/privacy/index.html (from Task 1 — reuse the identical layout/style)
  </read_first>
  <action>
    Create docs-site/support/index.html reusing the exact same single-column brand-dark layout and
    style as the privacy page: H1 "Support" (28px/700), a short paragraph offering a real contact
    method as a mailto: link styled in volt (e.g. "Questions or something not working? Email
    {support address} and we'll get back to you."). A full FAQ/help center is out of scope — this
    only needs to satisfy ASC's mandatory Support URL with a genuine working contact path. Also
    create docs-site/CNAME containing the single line `apsistraining.com` so a GitHub Pages custom-
    domain deploy routes the domain (hosting mechanics are Claude's discretion per D-04; GitHub Pages
    is the recommended default). Confirm the mailto address is one the user actually monitors (flag
    it in the checkpoint if a placeholder is used).
  </action>
  <verify>
    <automated>test -f docs-site/support/index.html && grep -qi "Support" docs-site/support/index.html && grep -q "mailto:" docs-site/support/index.html && test -f docs-site/CNAME && grep -q "apsistraining.com" docs-site/CNAME</automated>
  </verify>
  <done>Support page exists with a working mailto: contact; CNAME set to apsistraining.com.</done>
  <acceptance_criteria>
    - docs-site/support/index.html exists with an H1 "Support" and a mailto: link styled as a volt-colored anchor.
    - docs-site/CNAME contains exactly `apsistraining.com`.
    - The support page reuses the same void/bone/ash/volt layout as the privacy page (no new colors).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review privacy copy + deploy both pages to apsistraining.com over HTTPS</name>
  <action>Blocking human checkpoint — the human performs the steps in how-to-verify; no autonomous code change. Review the privacy policy for accuracy against shipped behavior (D-05), confirm the support mailto: is monitored, then publish docs-site/ to apsistraining.com over HTTPS and confirm /privacy and /support return 200.</action>
  <what-built>
    Both static pages are drafted (docs-site/privacy + docs-site/support) with a CNAME for
    apsistraining.com. Claude cannot deploy to the user-owned domain (no DNS/host access) and the
    privacy policy needs the user's review before it is public (D-05).
  </what-built>
  <how-to-verify>
    1. Read docs-site/privacy/index.html end to end. Confirm every claim is true for Apsis as shipped
       (offline-first; HealthKit on-device only; only Sentry crash diagnostics leave the device with
       no PII/health values). Correct any wording that overstates or understates before publishing.
    2. Confirm the support mailto: address is a real inbox you monitor.
    3. Publish docs-site/ to apsistraining.com over HTTPS (GitHub Pages custom domain + the CNAME, or
       your chosen static host).
    4. Run `curl -I https://apsistraining.com/privacy` and `curl -I https://apsistraining.com/support`
       — both must return HTTP 200 over HTTPS.
    Expected: both URLs live over HTTPS returning 200; privacy copy reviewed and accurate.
  </how-to-verify>
  <resume-signal>Reply "approved" once both URLs return 200 over HTTPS, or describe copy/hosting issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static host → public internet | Anyone can read the privacy/support pages (intended — public legal pages) |
| privacy claims → actual app behavior | An inaccurate policy is a compliance/repudiation risk with Apple and users |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Repudiation | Privacy policy over/under-declaring data practices | medium | mitigate | Policy is drafted directly from the verified on-device posture (05-SECURITY + D-03 allowlist) and human-reviewed for accuracy before going live (D-05 checkpoint). |
| T-06-10 | Information Disclosure | Static pages leaking anything beyond public legal text | low | accept | Pages are plain public HTML with no JS/forms/analytics and no secrets — nothing sensitive is served. |
</threat_model>

<verification>
- `curl -I https://apsistraining.com/privacy` → 200; `curl -I https://apsistraining.com/support` → 200 (both HTTPS).
- Privacy page reviewed by the user for accuracy against shipped behavior.
- Support page exposes a real monitored mailto: contact.
</verification>

<success_criteria>
A reviewed, accurate, brand-continuous privacy policy and a support page are both live over HTTPS on
apsistraining.com, satisfying REL-02 and unblocking the mandatory ASC Support URL field.
</success_criteria>

<output>
Create `.planning/phases/06-polish-app-store-submission/06-04-SUMMARY.md` when done (record the live URLs).
</output>
