---
phase: 06-polish-app-store-submission
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/seed.ts
  - packages/db/src/__tests__/seed.test.ts
autonomous: true
requirements: [REL-01]

must_haves:
  truths:
    - "The seeded exercise catalog contains ~150 curated movements (>= 150) with no duplicate ids."
    - "ab-wheel and hanging-leg-raise have non-null curated bwFactors and score honest (non-zero) HSS at 0 entered external load (D-14)."
    - "Every new bodyweight-moved entry follows push-up-style semantics (bwFactor in (0,1], entryMode 'reps'); every new timed movement is entryMode 'timed'/bwFactor null; every new pure-cardio row is entryMode null/bwFactor null."
    - "The change requires no migration — it rides the existing idempotent per-id upsert and backfills on update."
  artifacts:
    - "packages/db/src/seed.ts STARTER_EXERCISES expanded 43 -> ~150"
    - "packages/db/src/__tests__/seed.test.ts thresholds + fixture maps updated"
  key_links:
    - "seed.ts bwFactor column -> apps/mobile/lib/effectiveLoad.ts computeEffectiveLoad (bodyweight contribution) -> @apsis/engine strength scoring"
    - "seedExercises() onConflictDoUpdate(bw_factor, entry_mode) -> upgraded installs backfill new curated values with zero migration"
---

<objective>
Fold the user-raised catalog work into the release: expand `STARTER_EXERCISES` in
`packages/db/src/seed.ts` from 43 to ~150 curated movements (D-13) and fix the two `bwFactor: null`
core movements that currently score ~0 HSS (D-14: ab-wheel, hanging-leg-raise). This is a data-only
change — the existing idempotent per-id upsert backfills existing installs with zero migration.
It must land before the July 15 beta build so day-one users get a full library.

Purpose: An honest HSS depends on every logged movement having curated bwFactor/entryMode; a 43-entry
library is thin for HYROX/tactical athletes and two core movements are silently scoring zero.
Output: ~150-entry catalog + extended test fixtures asserting the new entries and the D-14 fix.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-app-store-submission/06-RESEARCH.md
@.planning/phases/06-polish-app-store-submission/06-PATTERNS.md
@packages/db/src/seed.ts
@packages/db/src/__tests__/seed.test.ts
@apps/mobile/lib/effectiveLoad.ts
@packages/engine/src/bodyweight.ts
</context>

## Artifacts this phase produces (plan 06-03)

- ~107 new entries in `STARTER_EXERCISES` (kebab-case stable ids; barbell/DB/KB lifts and variations, machines, cable work, core) — each with curated `bodyPart`, `bwFactor`, `entryMode`.
- Non-null `bwFactor` for `ab-wheel` and `hanging-leg-raise` (D-14 fix).
- Updated test fixtures in `seed.test.ts`: raised `>= 150` count assertion; extended `BODYWEIGHT_FACTORS`, `TIMED_IDS`, `ENDURANCE_IDS` maps.

**No schema/migration task required (schema-gate):** D-13/D-14 is an additive seed-data change to the
`STARTER_EXERCISES` array only — no Drizzle schema shape change (`packages/db/src/schema.ts` untouched).
The existing `seedExercises()` `onConflictDoUpdate` syncing `bw_factor`/`entry_mode` handles both new
rows and backfill of upgraded installs. No `drizzle-kit generate` / bundled migration is needed.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend seed tests for ~150 entries + D-14 fix (RED)</name>
  <files>packages/db/src/__tests__/seed.test.ts</files>
  <read_first>
    - packages/db/src/__tests__/seed.test.ts (line 15 `>= 40` count; BODYWEIGHT_FACTORS lines 46-60; TIMED_IDS line 62; ENDURANCE_IDS line 64; the "every bodyweight movement" and "every timed movement" and "every endurance-type" assertions)
    - packages/db/src/seed.ts (current 43 entries; ab-wheel line 81, hanging-leg-raise line 82 both bwFactor null)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Code Examples — "Existing test to extend, not create")
    - .planning/phases/06-polish-app-store-submission/06-PATTERNS.md (seed.test.ts section)
  </read_first>
  <behavior>
    - The count assertion requires STARTER_EXERCISES.length >= 150.
    - BODYWEIGHT_FACTORS includes 'ab-wheel' and 'hanging-leg-raise' with their new non-null curated values, so the existing "every bodyweight movement has its bwFactor and entryMode reps" test now asserts the D-14 fix.
    - BODYWEIGHT_FACTORS, TIMED_IDS, and ENDURANCE_IDS also include every new D-13 bodyweight/timed/endurance entry so the invariants cover the full catalog.
    - The existing uniqueness, non-empty id/name, and valid-type tests continue to pass over the larger array.
  </behavior>
  <action>
    Update packages/db/src/__tests__/seed.test.ts FIRST: raise the count assertion from
    toBeGreaterThanOrEqual(40) to toBeGreaterThanOrEqual(150); add 'ab-wheel' and
    'hanging-leg-raise' to the BODYWEIGHT_FACTORS record with their new curated non-null values
    (the D-14 fix is then asserted by the existing bodyweight-movement test — do not write a new
    test for it); and add every new D-13 bodyweight-moved id to BODYWEIGHT_FACTORS, every new timed
    id to TIMED_IDS, and every new pure-cardio id to ENDURANCE_IDS so the existing invariant tests
    cover them. Run the suite and confirm it now fails (RED) because seed.ts has not yet been
    expanded. Keep the values in the fixture maps consistent with the values you will write into
    seed.ts in Task 2.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/db test -- seed</automated>
  </verify>
  <done>Test file asserts >= 150, includes ab-wheel/hanging-leg-raise non-null bwFactors, and fails against the current 43-entry array (RED).</done>
  <acceptance_criteria>
    - seed.test.ts count assertion reads toBeGreaterThanOrEqual(150).
    - BODYWEIGHT_FACTORS contains 'ab-wheel' and 'hanging-leg-raise' mapped to non-null numbers in (0,1].
    - Running `pnpm --filter @apsis/db test -- seed` FAILS before Task 2 (RED state confirmed).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Expand STARTER_EXERCISES to ~150 + D-14 bwFactor fix (GREEN)</name>
  <files>packages/db/src/seed.ts</files>
  <read_first>
    - packages/db/src/seed.ts (entry shape + section grouping; upsert mechanism lines 103-118 — unchanged)
    - packages/db/src/__tests__/seed.test.ts (the fixtures from Task 1 that must now pass)
    - apps/mobile/lib/effectiveLoad.ts (computeEffectiveLoad: bwFactor null => load unchanged; non-null => profileBodyweightKg*bwFactor + loadField — the semantics new entries must respect)
    - packages/engine/src/bodyweight.ts (rep-max-table e1RM used only when bwFactor != null — confirms bodyweight-moved entries need a non-null bwFactor to score)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Don't Hand-Roll: free-exercise-db for NAMES/categorization only; bwFactor manual curation per D-13/D-14)
  </read_first>
  <action>
    Expand STARTER_EXERCISES from 43 to ~150 curated movements: major barbell/DB/KB lifts and common
    variations, machines, cable work, and core — grouped under the existing section comments. For each
    entry follow the embedded rules exactly: id is kebab-case and permanently stable (FK target); an
    externally-loaded movement uses bwFactor null + entryMode 'reps' (or 'timed' for carries/holds);
    a bodyweight-moved movement uses a curated bwFactor in (0,1] + entryMode 'reps' (push-up-style
    semantics per D-14 — the load field is added external load only, computeEffectiveLoad adds the
    bodyweight contribution); a pure-cardio movement uses bwFactor null + entryMode null. Fix D-14:
    change ab-wheel and hanging-leg-raise from bwFactor null to curated non-null values (core
    anti-flexion movements; use literature/biomechanics-informed estimates — Claude's discretion per
    D-14, values must match the Task 1 fixtures). Cross-reference free-exercise-db for names/taxonomy
    only; do NOT copy any bwFactor from it (no public dataset carries Apsis's HSS bodyweight-load
    semantics). Do not touch seedExercises() — the existing onConflictDoUpdate already backfills.
    Ensure no duplicate ids.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/db test</automated>
  </verify>
  <done>STARTER_EXERCISES has >= 150 unique entries; ab-wheel/hanging-leg-raise non-null; all seed tests green.</done>
  <acceptance_criteria>
    - STARTER_EXERCISES.length >= 150 with all-unique ids.
    - ab-wheel and hanging-leg-raise have non-null bwFactor matching the Task 1 fixtures; both entryMode 'reps'.
    - `pnpm --filter @apsis/db test` is fully green (GREEN state).
    - packages/db/src/schema.ts is unmodified (no migration introduced).
    - seedExercises() upsert body is unchanged from its current form.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed array (developer-authored) → SQLite exercise table | Static curated catalog data is upserted on every launch |
| exercise.bwFactor → effectiveLoad → engine | A wrong/null bwFactor changes the HSS a user sees (honesty, not security) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | seedExercises upsert / static column refs | low | mitigate | Reuses the established parameterized drizzle upsert (T-1-01) with static `excluded.*` column refs only — no user input, no raw SQL interpolation; data-only additive change. |
| T-06-08 | Repudiation | Incorrect bwFactor producing dishonest HSS | low | accept | bwFactor curation is explicit manual biomechanics judgment (D-13/D-14 discretion); values are literature-informed estimates, asserted by fixed test fixtures. Not a security exposure in an offline single-user app. |
</threat_model>

<verification>
- `pnpm --filter @apsis/db test` green with STARTER_EXERCISES.length >= 150.
- ab-wheel/hanging-leg-raise assert non-null bwFactor via the existing bodyweight-movement test.
- No new file under packages/db/drizzle (no migration introduced).
</verification>

<success_criteria>
The seeded catalog is ~150 honest, non-duplicate movements with curated bwFactor/entryMode, the two
D-14 zero-scoring core movements are fixed, and the whole change ships migration-free via the existing
idempotent upsert.
</success_criteria>

<output>
Create `.planning/phases/06-polish-app-store-submission/06-03-SUMMARY.md` when done.
</output>
