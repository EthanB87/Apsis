---
phase: 06-polish-app-store-submission
plan: 07
type: execute
wave: 3
depends_on: [06-04, 06-05, 06-06]
files_modified:
  - apps/mobile/eas.json
autonomous: false
requirements: [REL-01, REL-02, REL-04]
user_setup:
  - service: app-store-connect
    why: "Screenshot capture (physical iOS device — no simulator on Windows), metadata entry, privacy nutrition label, and the review submission all happen in Apple's systems with the user's authenticated session."
    dashboard_config:
      - task: "Capture the 4 locked screenshots on a 6.9\" device, upload assets + enter all listing metadata, set the privacy nutrition label (Diagnostics → Crash Data only), attach privacy + support URLs, enter review notes, and submit for App Review by ~July 25"
        location: "App Store Connect → Apps → Apsis (+ eas submit from apps/mobile)"

must_haves:
  truths:
    - "The App Store listing has the 4 locked 6.9\" screenshots per D-09 (device-framed on brand-dark void backgrounds, short mono captions, locked screen order, 6.9\" iPhone set only — no iPad), the approved metadata, category Health & Fitness, price Free/no-IAP, the live privacy policy URL, and the mandatory support URL."
    - "The App Store Connect privacy nutrition label declares ONLY Diagnostics → Crash Data (not linked to identity, no tracking) and does NOT declare Health & Fitness as collected — because HealthKit data never leaves the device (D-06, verified)."
    - "The binary is submitted to App Store Review by ~July 25, 2026, leaving a 1–3 day buffer before July 28; release is manual after approval (D-15)."
  artifacts:
    - "apps/mobile/eas.json submit.production.ios filled with the ascAppId + auth from 06-01"
    - "A build submitted to App Store Review (in ASC, not in repo)"
  key_links:
    - "ascAppId (06-01 ASC record) → eas.json submit profile → eas submit"
    - "live /privacy + /support URLs (06-04) → ASC Privacy Policy URL + Support URL fields"
    - "app-store-listing.md strings (06-05) → ASC metadata fields entered verbatim"
    - "D-06 label posture ← 05-SECURITY on-device confirmation + 06-02 Sentry-only off-device data"
---

<objective>
Complete App Store Connect and submit for review by ~July 25 (REL-01/REL-02/REL-04). Fill the eas.json
submit profile with the ascAppId from the 06-01 ASC record, capture and upload the 4 locked
screenshots, enter the approved metadata, attach the live privacy + support URLs, set the privacy
nutrition label to the D-06-verified posture (Diagnostics → Crash Data only; Health & Fitness NOT
declared as collected), enter the D-16 review notes, and submit — with manual release after approval
(D-15).

Purpose: This is the phase's terminal deliverable — a privacy-compliant binary in App Review with a
buffer before the July 28 deadline.
Output: A submitted build + a completed, accurate App Store listing and privacy label.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-app-store-submission/06-CONTEXT.md
@.planning/phases/06-polish-app-store-submission/06-UI-SPEC.md
@.planning/phases/06-polish-app-store-submission/06-RESEARCH.md
@apps/mobile/store/app-store-listing.md
@apps/mobile/eas.json
</context>

## Artifacts this phase produces (plan 06-07)

- `apps/mobile/eas.json` — `submit.production.ios` filled (`ascAppId` + interactive Apple ID auth, or API-key fields per Open Question 3).
- App Store screenshots (4, 6.9" class) — uploaded to ASC (not in repo).
- A build submitted to App Store Review + a completed privacy nutrition label (in ASC, not in repo).

<tasks>

<task type="auto">
  <name>Task 1: Fill eas.json submit profile with the ascAppId (D-17/Pattern 3)</name>
  <files>apps/mobile/eas.json</files>
  <read_first>
    - apps/mobile/eas.json (submit.production {} stub from 06-05; build.production.ios autoIncrement)
    - .planning/phases/06-polish-app-store-submission/06-01-SUMMARY.md (the captured ascAppId from the ASC record)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Pattern 3 submit fields; Open Question 3 — interactive Apple ID auth is the recommended default for this one-shot manual submission)
    - .gitignore (confirm *.p8/*.p12/credentials.json already excluded — never commit auth keys)
  </read_first>
  <action>
    Edit apps/mobile/eas.json submit.production.ios to reference the ASC record: set ascAppId to the
    numeric Apple ID captured in 06-01. Default to interactive Apple ID auth for this single manual
    submission (Open Question 3) — i.e. provide ascAppId and let eas prompt for the Apple ID at submit
    time; only add ascApiKeyPath/ascApiKeyIssuerId/ascApiKeyId if the user prefers API-key auth. NEVER
    commit a .p8/.p12/credentials.json (already gitignored — do not add any secret file to the repo).
    Keep the file valid JSON and leave build/development/preview profiles untouched.
  </action>
  <verify>
    <automated>node -e "const e=require('./apps/mobile/eas.json'); if(!e.submit.production.ios||!e.submit.production.ios.ascAppId) throw new Error('ascAppId not set');" && ! git ls-files --error-unmatch '*.p8' 2>/dev/null</automated>
  </verify>
  <done>submit.production.ios carries the real ascAppId; no auth secret is tracked in git.</done>
  <acceptance_criteria>
    - apps/mobile/eas.json submit.production.ios.ascAppId is set to the numeric ASC App ID from 06-01.
    - eas.json parses as valid JSON.
    - No *.p8/*.p12/credentials.json file is tracked by git.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture screenshots + enter ASC metadata and URLs</name>
  <action>Blocking human checkpoint — the human performs the steps in how-to-verify; no autonomous code change. Capture the 4 locked screenshots per D-09 (device-framed captures on void backgrounds, mono captions, locked screen order, 6.9" iPhone set only) on a physical device, upload them, and enter the approved metadata, category, price, review notes, and the live privacy + support URLs into App Store Connect.</action>
  <what-built>
    The listing copy (06-05), live privacy/support URLs (06-04), a TestFlight-validated build (06-06),
    and the submit profile (Task 1) are all ready. Screenshot capture requires a physical 6.9" iOS
    device (no iOS simulator on this Windows host), and metadata/label entry happens in ASC — both are
    the user's to execute against the locked specs.
  </what-built>
  <how-to-verify>
    1. On a 6.9" iPhone running the TestFlight build with realistic (not lorem/"Test User") data,
       capture the 4 screenshots in the locked order with the locked mono-uppercase captions on a
       void #0B0C0E background: (1) TODAY hero (HSS ring + readiness light) "ONE NUMBER FOR ALL
       TRAINING STRESS"; (2) lift logger ledger "LOG A LIFT AT STRONG SPEED"; (3) run form + History
       "RUN, LIFT, ONE LEDGER"; (4) Apple Health integration (connect step or Settings APPLE HEALTH
       chip) "YOUR HEALTH DATA, ALREADY THERE". Spot-check the exact 6.9" pixel dimensions against
       Apple's live screenshot-specifications page before export. Upload the set (6.9" only — ASC
       scales down).
    2. Enter all metadata from apps/mobile/store/app-store-listing.md verbatim: name, subtitle,
       description, keywords, promotional text; set Primary Category Health & Fitness; price Free /
       no IAP; attach Privacy Policy URL https://apsistraining.com/privacy and Support URL
       https://apsistraining.com/support.
    3. Enter the App Review notes (D-16) and answer export compliance using
       ITSAppUsesNonExemptEncryption:false already in app.json (D-18 — no per-upload questionnaire).
    Expected: screenshots uploaded; all metadata + URLs entered; review notes + export compliance set.
  </how-to-verify>
  <resume-signal>Reply "listing ready" once screenshots + metadata + URLs + review notes are entered, or list what is blocked.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Set privacy nutrition label + submit for App Review (D-06/D-15)</name>
  <action>Blocking human checkpoint — the human performs the steps in how-to-verify; no autonomous code change. Set the privacy nutrition label to the D-06 posture (Diagnostics → Crash Data only; Health & Fitness NOT collected), submit the build for App Review by ~July 25, and select Manual release (D-15).</action>
  <what-built>
    The listing is populated. The remaining steps are the privacy nutrition label (the REL-02
    compliance crux) and the actual review submission — both must reflect the verified on-device
    posture and hit the ~July 25 target.
  </what-built>
  <how-to-verify>
    1. In App Store Connect → App Privacy, set the privacy nutrition label to the D-06 verified posture:
       declare ONLY Diagnostics → Crash Data, marked NOT linked to the user's identity and NOT used for
       tracking. Do NOT declare Health & Fitness as "collected" — HealthKit data never leaves the device,
       and Apple's own "collect" definition (transmitted off-device) means on-device-only data is exempt
       (RESEARCH REL-02, verified). Self-certify accuracy.
    2. Submit the build for App Store Review — run `eas submit --platform ios --profile production` from
       apps/mobile (interactive Apple ID auth), or submit the build directly in ASC. Target submission by
       ~July 25, 2026 (1–3 day buffer before July 28).
    3. Set release to Manual (D-15) — after approval you press Release; no phased release.
    Expected: privacy label = Diagnostics/Crash Data only; build submitted to App Review by ~July 25;
    manual release selected.
  </how-to-verify>
  <resume-signal>Reply "submitted" with the submission date + review status, or describe any validation/rejection.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submit auth (Apple ID / ASC API key) → Apple | Submission credentials cross to Apple; must never enter the repo |
| self-reported privacy label → Apple/users | An inaccurate label is a compliance risk Apple can audit post-launch |
| listing/screenshots → public App Store | Public-facing marketing must match the shipped app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Information Disclosure | Submission credential leak (.p8/Apple ID) | high | mitigate | .gitignore already excludes *.p8/*.p12/credentials.json (verified); Task 1 verify asserts no auth key is git-tracked; interactive Apple ID auth avoids storing a key at all. |
| T-06-15 | Repudiation | Privacy nutrition label over/under-declaring | medium | mitigate | Label set to the D-06 verified posture (Diagnostics/Crash Data only; Health & Fitness not collected) grounded in Apple's "collect"=off-device definition + 05-SECURITY on-device confirmation; user self-certifies. |
| T-06-11 | Repudiation | Screenshots/metadata misrepresenting the app | low | mitigate | Screenshots use realistic real-feature data per the locked shot list; metadata is the user-approved 06-05 copy entered verbatim. |
</threat_model>

<verification>
- eas.json submit.production.ios.ascAppId set; no auth secret tracked in git.
- 4 screenshots (6.9", correct order/captions) + approved metadata + both live URLs entered in ASC.
- Privacy label = Diagnostics/Crash Data only; Health & Fitness NOT declared collected.
- Build submitted to App Store Review by ~July 25; manual release selected.
</verification>

<success_criteria>
A privacy-compliant Apsis binary — accurate nutrition label, complete listing, live privacy + support
URLs — is submitted to App Store Review by ~July 25, 2026 with a buffer before July 28 and manual
release configured.
</success_criteria>

<output>
Create `.planning/phases/06-polish-app-store-submission/06-07-SUMMARY.md` when done (record submission date + review status).
</output>
