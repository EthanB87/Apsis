---
phase: 06-polish-app-store-submission
plan: 07
type: execute
wave: 3
depends_on: [06-04, 06-06, 06-08]
files_modified:
  - apps/mobile/eas.json
autonomous: false
requirements: [REL-01, REL-02, REL-04]

estimate:
  tokens: 30000
  raw_tokens: 24000
  tasks: 5
  confidence: low

user_setup:
  - service: app-store-connect
    why: "Screenshot capture (physical iOS device — no simulator on Windows), metadata entry, privacy nutrition label, and the review submission all happen in Apple's systems with the user's authenticated session."
    dashboard_config:
      - task: "Capture the 4 locked screenshots on a 6.9\" device, upload assets + enter all listing metadata from the 06-08 re-approved copy, set the privacy nutrition label (Diagnostics -> Crash Data), attach privacy + support URLs, enter review notes, and submit for App Review ASAP"
        location: "App Store Connect -> Apps -> Apsis (+ eas submit from apps/mobile)"

must_haves:
  truths:
    - "The App Store listing uses the 06-08 re-approved copy (nutrition + share card disclosed, offline-first framing, NEVER '100% offline'), the 4 locked 6.9\" screenshots per D-09 (device-framed on void backgrounds, locked order, 6.9\" iPhone set only — no iPad), category Health & Fitness, price Free/no-IAP, the live privacy policy URL, and the mandatory support URL."
    - "The App Store Connect privacy nutrition label reflects the ACTUAL 2026-08-03 posture: it declares Diagnostics -> Crash Data (not linked to identity, no tracking); it does NOT declare Health & Fitness as collected (HealthKit stays on-device); it does NOT declare photos as collected (share-card photos are composed on-device and only shared via the user-initiated iOS share sheet); and the OFF/USDA food-lookup queries (barcode/search text only, no identity, no tracking) are represented per current Apple guidance."
    - "eas.json submit.production.ios.ascAppId is the numeric ASC App ID from the 06-01 record (6792933794) and no auth secret (.p8/.p12/credentials.json) is tracked in git."
    - "The binary is submitted to App Store Review ASAP (within days of 2026-08-03); release is manual after approval (D-15)."
    - statement: "Re-entering metadata or re-uploading screenshots overwrites in place in App Store Connect — it never creates a duplicate app record or a second listing."
      verification: backstop
    - statement: "Every metadata string entered into ASC respects Apple's character-count limits (name <=30, subtitle <=30, keywords <=100 incl. commas), matching the counts recorded in app-store-listing.md — regardless of byte vs. character counting."
      verification: backstop
    - statement: "The privacy nutrition label with no health/nutrition/photo collection is the correct empty/default state — only Diagnostics -> Crash Data is declared."
      verification: backstop
    - statement: "Re-submitting after a rejection uses a fresh autoIncremented build; manual release (D-15) prevents an accidental double-release; an interrupted eas submit can be retried against the same build without producing two review submissions."
      verification: backstop
    - statement: "The privacy label's treatment of OFF/USDA food-lookup queries (barcode/search text sent to third parties, not linked to identity, no tracking) is verified against current Apple App Privacy guidance before self-certifying."
      verification: backstop
  prohibitions:
    - statement: "No submission credential (Apple ID password, ASC API key .p8/.p12, credentials.json, EAS token) MUST be committed to the repo."
      status: flagged-unverified
      verification: automated
    - statement: "The privacy nutrition label MUST NOT under-declare a real off-device flow (Sentry crash data, OFF/USDA queries) nor over-declare on-device-only data (HealthKit values, share-card photos) as collected."
      status: flagged-unverified
      verification: human
    - statement: "The listing MUST NOT be submitted with the pre-06-08 copy (no '100% offline', no missing nutrition/share-card disclosure)."
      status: flagged-unverified
      verification: human
  artifacts:
    - "apps/mobile/eas.json submit.production.ios with the verified ascAppId (6792933794)"
    - "A build submitted to App Store Review (in ASC, not in repo) + a completed privacy nutrition label"
  key_links:
    - "ascAppId (06-01 ASC record, already in eas.json as 6792933794) -> eas.json submit profile -> eas submit"
    - "live /privacy + /support URLs (06-04, corrected by 06-08) -> ASC Privacy Policy URL + Support URL fields"
    - "06-08 re-approved app-store-listing.md strings -> ASC metadata fields entered verbatim"
    - "TestFlight-validated build (06-06) -> the binary submitted here"
    - "privacy nutrition label posture <- must agree with the 06-08 privacy policy's disclosed off-device flow set"
---

<objective>
Complete App Store Connect and submit for review ASAP (REL-01/REL-02/REL-04). Verify the eas.json
submit profile carries the ascAppId from the 06-01 ASC record (already present: 6792933794), capture and
upload the 4 locked screenshots, enter the 06-08 RE-APPROVED metadata (nutrition + share card disclosed,
offline-first, never "100% offline"), attach the live privacy + support URLs (06-04, corrected by 06-08),
set the privacy nutrition label to the actual 2026-08-03 posture (Diagnostics -> Crash Data; Health &
Fitness NOT collected; photos NOT collected; OFF/USDA food-lookup queries represented per Apple guidance),
enter the D-16 review notes, and submit — with manual release after approval (D-15).

Tracer note: submission logistics are inherently sequential ops with no vertical-slice to prove; natural
ordering is used (the 06-06 build already integrated every prior slice).

Purpose: This is the phase's terminal deliverable — a privacy-compliant binary in App Review.
Output: A submitted build + a completed, accurate App Store listing and privacy label.
Deadline framing: ASAP — within days of 2026-08-03 (the original ~July 25/28 target has passed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-app-store-submission/06-CONTEXT.md
@.planning/phases/06-polish-app-store-submission/06-UI-SPEC.md
@.planning/phases/06-polish-app-store-submission/06-RESEARCH.md
@apps/mobile/store/app-store-listing.md
@apps/mobile/eas.json
</context>

## Artifacts this phase produces (plan 06-07)

- `apps/mobile/eas.json` — `submit.production.ios.ascAppId` verified as `6792933794` (from the 06-01 ASC record); no auth secret added to the repo.
- App Store screenshots (4, 6.9" class) — uploaded to ASC (not in repo).
- A build submitted to App Store Review + a completed privacy nutrition label reflecting the 2026-08-03 posture (in ASC, not in repo).
- (No new repo symbols/config keys beyond the ascAppId verification.)

<tasks>

<task type="auto">
  <name>Task 1: Verify eas.json submit profile ascAppId + no tracked auth secret (Pattern 3)</name>
  <files>apps/mobile/eas.json</files>
  <read_first>
    - apps/mobile/eas.json (submit.production.ios.ascAppId is already 6792933794; build.production.ios autoIncrement + cli.appVersionSource remote)
    - .planning/phases/06-polish-app-store-submission/06-01-PLAN.md (the ASC record + captured ascAppId source; confirm 6792933794 matches the com.apsis.app record)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Pattern 3 submit fields; Open Question 3 — interactive Apple ID auth is the recommended default for this one-shot manual submission)
    - .gitignore (confirm *.p8/*.p12/credentials.json already excluded — never commit auth keys)
  </read_first>
  <action>
    Confirm apps/mobile/eas.json submit.production.ios.ascAppId equals the numeric ASC App ID for the
    com.apsis.app record from 06-01 (currently 6792933794). If it is present and correct, leave it — do
    NOT churn the file. Default to interactive Apple ID auth for this single manual submission (Open
    Question 3): provide ascAppId only and let eas prompt for the Apple ID at submit time; only add
    ascApiKeyPath/ascApiKeyIssuerId/ascApiKeyId if the user explicitly prefers API-key auth. NEVER add a
    .p8/.p12/credentials.json to the repo (already gitignored). Keep the file valid JSON and leave the
    build/development/preview profiles untouched. If (and only if) the ascAppId is missing or wrong,
    set it to the numeric ASC App ID recorded in 06-01.
  </action>
  <verify>
    <automated>node -e "const e=require('./apps/mobile/eas.json'); if(!e.submit.production.ios||!e.submit.production.ios.ascAppId) throw new Error('ascAppId not set');" && ! git ls-files --error-unmatch '*.p8' 2>/dev/null && ! git ls-files --error-unmatch 'apps/mobile/credentials.json' 2>/dev/null</automated>
  </verify>
  <done>submit.production.ios carries the real ascAppId (6792933794); no auth secret is tracked in git; file is valid JSON.</done>
  <acceptance_criteria>
    - apps/mobile/eas.json submit.production.ios.ascAppId is a numeric ASC App ID (6792933794) matching the 06-01 com.apsis.app record.
    - eas.json parses as valid JSON and build/development/preview profiles are unchanged.
    - No *.p8/*.p12/credentials.json file is tracked by git.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture screenshots + enter ASC metadata and URLs (06-08 re-approved copy)</name>
  <action>Blocking human checkpoint — the human performs the steps in how-to-verify; no autonomous code change. Capture the 4 locked screenshots per D-09 on a physical device, upload them, and enter the 06-08 re-approved metadata, category, price, review notes, and the live privacy + support URLs into App Store Connect.</action>
  <precondition>The 06-08 re-approved app-store-listing.md is final, and the privacy + support pages are live over HTTPS on apsistraining.com (06-04 published).</precondition>
  <read_first>
    - apps/mobile/store/app-store-listing.md (the 06-08 re-approved copy — enter verbatim; includes nutrition + share-card disclosures, offline-first framing)
    - .planning/phases/06-polish-app-store-submission/06-UI-SPEC.md (D-09 shot list, captions, void background, 6.9" pixel dims)
    - .planning/phases/06-polish-app-store-submission/06-CONTEXT.md (D-10 free/no-IAP, D-11 category, D-16 review notes, D-18 export compliance)
  </read_first>
  <what-built>
    The 06-08 re-approved listing copy, live privacy/support URLs (06-04), a TestFlight-validated build
    (06-06), and the verified submit profile (Task 1) are all ready. Screenshot capture requires a
    physical 6.9" iOS device (no iOS simulator on this Windows host), and metadata/label entry happens
    in ASC — both are the user's to execute against the locked specs.
  </what-built>
  <instructions>
    1. On a 6.9" iPhone running the TestFlight build with realistic (not lorem/"Test User") data,
       capture the 4 screenshots in the locked order with the locked mono-uppercase captions on a void
       #0B0C0E background: (1) TODAY hero (HSS ring + readiness light) "ONE NUMBER FOR ALL TRAINING
       STRESS"; (2) lift logger ledger "LOG A LIFT AT STRONG SPEED"; (3) run form + History "RUN, LIFT,
       ONE LEDGER"; (4) Apple Health integration "YOUR HEALTH DATA, ALREADY THERE". Spot-check the exact
       6.9" pixel dimensions against Apple's live screenshot-specifications page before export. Upload the
       set (6.9" only — ASC scales down).
    2. Enter all metadata from the 06-08 re-approved apps/mobile/store/app-store-listing.md VERBATIM:
       name, subtitle, description (with the share-card + nutrition copy), keywords, promotional text;
       set Primary Category Health & Fitness; price Free / no IAP; attach Privacy Policy URL
       https://apsistraining.com/privacy and Support URL https://apsistraining.com/support.
    3. Enter the App Review notes from app-store-listing.md (offline, no account, HealthKit optional,
       camera/photo usage disclosure, OFF/USDA food-lookup disclosure, Sentry-only diagnostics) and
       answer export compliance using ITSAppUsesNonExemptEncryption:false already in app.json (D-18).
  </instructions>
  <verification>
    App Store Connect shows the 4 uploaded 6.9" screenshots in the locked order, every metadata field
    matches the 06-08 re-approved app-store-listing.md verbatim (nutrition + share card present, no
    "100% offline"), both live HTTPS URLs are attached, and the review notes + export-compliance answer
    are set.
  </verification>
  <resume-signal>Reply "listing ready" once screenshots + metadata + URLs + review notes are entered, or list what is blocked.</resume-signal>
  <acceptance_criteria>
    - The 4 locked 6.9" screenshots (correct order + captions) are uploaded; no iPad set.
    - Every metadata field matches the 06-08 re-approved app-store-listing.md verbatim (nutrition + share card present; no "100% offline").
    - Privacy Policy URL and Support URL both point to the live HTTPS apsistraining.com pages.
    - App Review notes disclose photo-library use, OFF/USDA food lookups, and Sentry-only diagnostics; export compliance answered false.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Set privacy nutrition label to the 2026-08-03 posture (D-06)</name>
  <action>Blocking human checkpoint — the human performs the steps in instructions; no autonomous code change. Set the privacy nutrition label to the actual 2026-08-03 posture (Diagnostics -> Crash Data; Health & Fitness NOT collected; photos NOT collected; OFF/USDA queries represented per Apple guidance). This label is still editable until submission, so it is reversible here.</action>
  <precondition>Task 2 is complete (listing populated in App Store Connect).</precondition>
  <read_first>
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (REL-02 "collect" = transmitted off-device; on-device HealthKit exempt)
    - docs-site/privacy/index.html (06-08 corrected — the label must describe the SAME off-device flow set: Sentry, OFF/USDA lookups, on-device photos)
    - .planning/phases/06-polish-app-store-submission/06-CONTEXT.md (D-06 label posture, D-18 export compliance)
  </read_first>
  <instructions>
    1. In App Store Connect -> App Privacy, set the privacy nutrition label to the actual 2026-08-03 posture:
       declare Diagnostics -> Crash Data (NOT linked to identity, NOT used for tracking). Do NOT declare
       Health & Fitness as collected (HealthKit data never leaves the device). Do NOT declare Photos as
       collected (share-card photos are composed on-device and only shared via the user-initiated iOS share
       sheet).
    2. For the OFF/USDA food lookups, decide per CURRENT Apple App Privacy guidance whether the
       barcode/search-text queries require declaring a data type (e.g. Search History / Other Data — NOT
       linked to identity, NOT used for tracking); if in doubt, match what the 06-08 privacy policy discloses
       and err toward accurate disclosure. The label MUST describe the same off-device flow set as the
       published privacy policy.
    3. Self-certify accuracy. Do not submit yet — that is gated by Task 4.
  </instructions>
  <verification>
    The privacy nutrition label declares Diagnostics -> Crash Data only; Health & Fitness and Photos are
    NOT declared collected; the OFF/USDA food-lookup treatment is decided against current Apple guidance
    and agrees with the published privacy policy's disclosed off-device flow set.
  </verification>
  <resume-signal>Reply "label set" once the privacy nutrition label matches the 2026-08-03 posture, or describe any uncertainty.</resume-signal>
  <acceptance_criteria>
    - The privacy nutrition label declares Diagnostics -> Crash Data and does NOT declare Health & Fitness or Photos as collected.
    - The OFF/USDA food-lookup treatment is decided against current Apple guidance and agrees with the published privacy policy's disclosed flow set.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: One-way-door gate — approve the irreversible App Store submission</name>
  <decision>Submit this build + listing + privacy label to App Store Review now? Submission enters Apple's public review queue against this exact build and listing and cannot be silently undone.</decision>
  <context>
    Submitting to App Store Review is a one-way door. Before approving, all of these must be true: the
    06-06 D-12 beta gate is clear (no crash/data-loss/HealthKit/nutrition blocker); the 06-08 re-approved
    listing copy (nutrition + share card, no "100% offline") is entered verbatim (Task 2); the privacy
    nutrition label matches the 2026-08-03 posture and agrees with the published privacy policy (Task 3);
    the privacy + support URLs are live over HTTPS (06-04). If any is not true, hold.
  </context>
  <options>
    <option id="submit">
      <name>Submit now</name>
      <pros>Enters review ASAP (within days of 2026-08-03); everything is verified and consistent.</pros>
      <cons>Irreversible entry into Apple's review queue against this build/listing.</cons>
    </option>
    <option id="hold">
      <name>Hold</name>
      <pros>Time to fix an inaccurate label/listing, an unresolved beta blocker, or an unpublished URL.</pros>
      <cons>Delays the submission.</cons>
    </option>
  </options>
  <resume-signal>Reply "submit" to approve the irreversible submission, or "hold" with what must be fixed first.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Submit for App Review + set manual release (D-15)</name>
  <action>Blocking human checkpoint — the human performs the steps in instructions; no autonomous code change. Submit the 06-06 build for App Store Review and select Manual release. Proceed ONLY after Task 4 approved "submit".</action>
  <precondition>Task 4 returned "submit" (the one-way-door gate approved the irreversible submission).</precondition>
  <reversibility rating="one-way">Submitting to App Store Review enters Apple's public review queue against this build and listing and cannot be silently undone — gated by the preceding Task 4 checkpoint:decision.</reversibility>
  <read_first>
    - apps/mobile/eas.json (submit.production.ios.ascAppId 6792933794, verified in Task 1)
    - .planning/phases/06-polish-app-store-submission/06-CONTEXT.md (D-15 manual release; D-16 review notes)
  </read_first>
  <instructions>
    1. Submit the 06-06 build for App Store Review — run `eas submit --platform ios --profile production`
       from apps/mobile (interactive Apple ID auth), or submit the build directly in App Store Connect.
       Submit ASAP (within days of 2026-08-03).
    2. Set release to Manual (D-15) — after approval you press Release; no phased release.
  </instructions>
  <verification>
    App Store Connect shows the build submitted to App Store Review with Manual release selected, and no
    submission credential (.p8/.p12/credentials.json/Apple ID password) was added to the repo.
  </verification>
  <resume-signal>Reply "submitted" with the submission date + review status, or describe any validation/rejection.</resume-signal>
  <acceptance_criteria>
    - The build is submitted to App Store Review with Manual release selected (D-15).
    - Submission occurs ASAP within days of 2026-08-03 (no reference to the passed July target).
    - No submission credential is tracked in git.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submit auth (Apple ID / ASC API key) -> Apple | Submission credentials cross to Apple; must never enter the repo |
| self-reported privacy label -> Apple/users | An inaccurate label is a compliance risk Apple can audit post-launch |
| listing/screenshots -> public App Store | Public-facing marketing must match the shipped app |
| privacy label <-> published privacy policy | The two must describe the same off-device flow set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Information Disclosure | Submission credential leak (.p8/Apple ID) | high | mitigate | .gitignore already excludes *.p8/*.p12/credentials.json (verified); Task 1 verify asserts no auth key is git-tracked; interactive Apple ID auth avoids storing a key at all. |
| T-06-15 | Repudiation | Privacy nutrition label over/under-declaring (nutrition egress or photos) | high | mitigate | Label set to the verified 2026-08-03 posture and required to agree with the 06-08 privacy policy; OFF/USDA queries assessed against current Apple guidance; user self-certifies; prohibition + human gate catch mismatch. |
| T-06-11 | Repudiation | Screenshots/metadata misrepresenting the app | low | mitigate | Screenshots use realistic real-feature data per the locked shot list; metadata is the 06-08 re-approved copy entered verbatim. |
| T-06-20 | Repudiation | Submitting the stale pre-06-08 listing copy ("100% offline", no nutrition/share-card) | medium | mitigate | Task 2 requires entering the 06-08 re-approved copy verbatim; acceptance criteria assert no "100% offline" and nutrition + share card present. |
</threat_model>

<verification>
- eas.json submit.production.ios.ascAppId set (6792933794); no auth secret tracked in git.
- 4 screenshots (6.9", correct order/captions) + 06-08 re-approved metadata + both live URLs entered in ASC.
- Privacy label = Diagnostics/Crash Data; Health & Fitness NOT collected; Photos NOT collected; OFF/USDA queries handled per Apple guidance and consistent with the privacy policy.
- Build submitted to App Store Review ASAP; manual release selected.
</verification>

<success_criteria>
A privacy-compliant Apsis binary — accurate nutrition label (spanning nutrition egress + photo posture),
complete 06-08 re-approved listing, live privacy + support URLs — is submitted to App Store Review within
days of 2026-08-03 with manual release configured.
</success_criteria>

<output>
Create `.planning/phases/06-polish-app-store-submission/06-07-SUMMARY.md` when done (record submission date + review status).
</output>
