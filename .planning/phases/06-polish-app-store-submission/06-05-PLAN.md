---
phase: 06-polish-app-store-submission
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/store/app-store-listing.md
  - apps/mobile/eas.json
autonomous: false
requirements: [REL-01, REL-04]

must_haves:
  truths:
    - "A single reviewed reference document holds every App Store listing string: name (<=30 chars), subtitle (<=30 chars), description (athlete-direct voice), keywords, promotional text, and the App Review notes (D-16)."
    - "eas.json build.production.ios auto-increments buildNumber while the marketing version stays 1.0.0 (D-17)."
    - "The user has approved the final listing strings, including a subtitle that fits the 30-character limit (D-07 resolved gap)."
  artifacts:
    - "apps/mobile/store/app-store-listing.md (locked listing copy + review notes)"
    - "apps/mobile/eas.json build.production.ios autoIncrement + appVersionSource"
  key_links:
    - "app-store-listing.md strings -> entered verbatim into App Store Connect in plan 06-07"
    - "eas.json autoIncrement -> the production build in plan 06-06 gets a fresh buildNumber without hand-editing"
---

<objective>
Lock the App Store listing copy and the build-versioning config so the later build and submission
waves have zero ambiguity. Draft every listing string (name, subtitle, description, keywords, App
Review notes) per D-07/D-08/D-16 and the UI-SPEC copywriting contract (resolving the over-length
subtitle), get user approval, and configure eas.json production build to auto-increment buildNumber
while the marketing version stays 1.0.0 (D-17).

Purpose: REL-01 needs complete, approved store metadata; REL-04's build needs deterministic
versioning. Doing the copy + config now (not at submission time) removes deadline-day churn.
Output: A reviewed listing-copy reference doc + eas.json versioning config.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-app-store-submission/06-CONTEXT.md
@.planning/phases/06-polish-app-store-submission/06-UI-SPEC.md
@.planning/phases/06-polish-app-store-submission/06-RESEARCH.md
@apps/mobile/eas.json
@apps/mobile/app.json
</context>

## Artifacts this phase produces (plan 06-05)

- `apps/mobile/store/app-store-listing.md` — reference doc with all listing strings + App Review notes (source of truth the human pastes into ASC in 06-07).
- `apps/mobile/eas.json` — `build.production.ios.autoIncrement: true` (+ `cli.appVersionSource` set as Expo requires when autoIncrement is used), `submit.production` left as a stub to be filled with ascAppId in plan 06-07.

<tasks>

<task type="auto">
  <name>Task 1: Draft the App Store listing copy + review notes (D-07/D-08/D-16)</name>
  <files>apps/mobile/store/app-store-listing.md</files>
  <read_first>
    - .planning/phases/06-polish-app-store-submission/06-UI-SPEC.md (Copywriting Contract — the exact resolved strings, including the 25-char subtitle "One score for lift + run")
    - .planning/phases/06-polish-app-store-submission/06-CONTEXT.md (D-07 name/subtitle, D-08 voice, D-16 review notes, D-10 free/no-IAP, D-11 category)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (REL-01 metadata; State of the Art — 6.9" single set)
    - apps/mobile/app.json (marketing version 1.0.0; bundle com.apsis.app)
  </read_first>
  <action>
    Create apps/mobile/store/app-store-listing.md capturing every App Store Connect text field as a
    labeled, ready-to-paste block: App Name "Apsis: Hybrid Training Log" (29 chars — confirm <=30);
    Subtitle "One score for lift + run" (25 chars — the UI-SPEC-resolved replacement for the
    over-length "One score for lifting + running"; confirm <=30); Description opening exactly "You
    lift. You run. One number tells you what it cost." followed by athlete-direct, second-person body
    copy written for HYROX/tactical athletes (terse, mono-caption energy per D-08); a Keywords field
    (comma-separated, within voice — Claude's discretion on exact terms, e.g. hybrid/HYROX/tactical/
    training load/readiness/lifting/running); Promotional text (optional, short); Primary Category
    Health & Fitness (D-11); Price Free, no IAP (D-10); and the App Review Notes verbatim from the
    UI-SPEC D-16 block (offline, no account/login, HealthKit optional/skippable + where to reconnect,
    all health data on-device except anonymous Sentry crash diagnostics). Add a top note that these
    strings are entered into ASC in plan 06-07 and require user approval (Task 3). Include the
    character count next to name and subtitle. Do NOT invent monetization or features not shipped.
  </action>
  <verify>
    <automated>test -f apps/mobile/store/app-store-listing.md && grep -q "You lift. You run." apps/mobile/store/app-store-listing.md && grep -qi "Health & Fitness" apps/mobile/store/app-store-listing.md && grep -qi "on-device" apps/mobile/store/app-store-listing.md</automated>
  </verify>
  <done>Listing doc holds name/subtitle (both <=30 chars), description, keywords, category, price, and the D-16 review notes.</done>
  <acceptance_criteria>
    - The App Name string is <= 30 characters; the Subtitle string is <= 30 characters (both counts shown in the doc).
    - The description opening line is exactly "You lift. You run. One number tells you what it cost."
    - The doc contains the D-16 App Review notes stating no account/login, HealthKit optional, all health data on-device except anonymous crash diagnostics.
    - Category is Health & Fitness and price is Free/no-IAP.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Configure eas.json build versioning (D-17)</name>
  <files>apps/mobile/eas.json</files>
  <read_first>
    - apps/mobile/eas.json (current: cli.version ">= 20.0.0"; build.production.ios.buildConfiguration "Release"; submit.production {})
    - apps/mobile/app.json (version 1.0.0 — marketing version must stay fixed here)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Pattern 3; D-17 autoIncrement)
    - .planning/phases/06-polish-app-store-submission/06-PATTERNS.md (eas.json section)
  </read_first>
  <action>
    Edit apps/mobile/eas.json to add "autoIncrement": true to build.production.ios (keeping the
    existing "buildConfiguration": "Release"), and set the cli.appVersionSource field as Expo
    requires when autoIncrement is used, so that the iOS buildNumber auto-increments on each
    production build while the marketing version stays 1.0.0 in app.json (D-17 — beta and review
    builds differ only by build number). Leave submit.production as an empty object for now — the
    ascAppId submit-profile fields are filled in plan 06-07 once the ASC record exists. Do not change
    the development/preview build profiles. Confirm the file remains valid JSON.
  </action>
  <verify>
    <automated>node -e "const e=require('./apps/mobile/eas.json'); if(e.build.production.ios.autoIncrement!==true) throw new Error('autoIncrement not set'); if(e.build.production.ios.buildConfiguration!=='Release') throw new Error('Release lost');"</automated>
  </verify>
  <done>eas.json production build auto-increments buildNumber; marketing version stays 1.0.0; valid JSON.</done>
  <acceptance_criteria>
    - apps/mobile/eas.json build.production.ios has autoIncrement: true AND retains buildConfiguration: "Release".
    - cli.appVersionSource is set (Expo requires it alongside autoIncrement).
    - apps/mobile/app.json version is still "1.0.0" (marketing version unchanged).
    - eas.json parses as valid JSON.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve final App Store listing strings</name>
  <action>Blocking human checkpoint — the human performs the steps in how-to-verify; no autonomous code change. Review and approve (or edit) the listing strings in apps/mobile/store/app-store-listing.md, confirming name and subtitle both fit the 30-character limits (D-07).</action>
  <what-built>
    All App Store listing strings are drafted in apps/mobile/store/app-store-listing.md and the build
    versioning is configured. Per D-07 the final name/subtitle/description strings are the user's to
    approve before they are entered into App Store Connect.
  </what-built>
  <how-to-verify>
    1. Read apps/mobile/store/app-store-listing.md. Confirm the App Name and Subtitle both fit their
       30-character limits and read the way you want in App Store search results.
    2. Confirm the description voice and keywords are acceptable, and the App Review notes are accurate.
    3. Edit any string you want changed directly in the doc (it is the source of truth for 06-07).
    Expected: listing copy approved (or edited to your final wording); subtitle confirmed <= 30 chars.
  </how-to-verify>
  <resume-signal>Reply "approved" or provide the exact final strings to use.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| listing copy → App Store Connect | Marketing claims must match the shipped app (Apple review + user trust) |
| eas build config → binary version identity | buildNumber/version identity feeds submission and TestFlight |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Repudiation | Store copy claiming capabilities the app lacks | low | mitigate | Copy is drafted only from shipped features + the verified on-device posture, and user-approved before ASC entry. |
| T-06-12 | Tampering | Wrong/duplicate build version identity | low | mitigate | autoIncrement guarantees unique buildNumbers; marketing version pinned in app.json — no manual version drift between beta and review builds. |
</threat_model>

<verification>
- app-store-listing.md contains all fields with name/subtitle both <= 30 chars.
- eas.json build.production.ios.autoIncrement === true and buildConfiguration === "Release".
- User approval recorded for the final listing strings.
</verification>

<success_criteria>
Every App Store listing string is drafted, fits Apple's field limits, and is user-approved, and the
production build is configured to auto-increment buildNumber against a fixed 1.0.0 marketing version.
</success_criteria>

<output>
Create `.planning/phases/06-polish-app-store-submission/06-05-SUMMARY.md` when done.
</output>
