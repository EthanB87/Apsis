---
phase: 06-polish-app-store-submission
plan: 06
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03, 06-05]
files_modified:
  - pnpm-lock.yaml
autonomous: false
requirements: [REL-04]
user_setup:
  - service: expo-eas
    why: "iOS builds are EAS-cloud-only on this Windows host; producing and distributing the TestFlight build requires the user's authenticated EAS + Apple session (interactive)."
    dashboard_config:
      - task: "Run the production EAS build and upload to TestFlight; create the external test group and submit the first build for Beta App Review; share the public TestFlight link (D-12)"
        location: "eas build (from apps/mobile) + App Store Connect → TestFlight"

must_haves:
  truths:
    - "A production EAS build carrying the Sentry native module + the ~150-entry catalog is produced from apps/mobile against bundle com.apsis.app with an auto-incremented buildNumber."
    - "The build passes Beta App Review and is installable by external testers via a public TestFlight link (D-12)."
    - "The external-beta gate is evaluated: any crash, data-loss, or HealthKit failure blocks App Store submission; small UX friction fixes are taken if quick; feature requests bank for v1.1."
  artifacts:
    - "A TestFlight external build + public tester link"
    - "pnpm-lock.yaml in sync (EAS --frozen-lockfile precondition)"
  key_links:
    - "Sentry native install (06-02) → requires this FRESH EAS build before crash reporting works or TestFlight distributes (Pitfall 3)"
    - "Beta App Review queue (Pitfall 4, no SLA) → must start early in the July 14–15 window to protect the July 15–22 external beta"
    - "beta gate outcome → gates whether plan 06-07 submission proceeds"
---

<objective>
Converge the three code/config tracks into ONE fresh EAS production build (mandatory because Sentry
is a new native module — Pitfall 3) and get it into external TestFlight beta before the July 15
window, accounting for the no-SLA Beta App Review queue (Pitfall 4). Then run the D-12 beta gate:
crashes/data-loss/HealthKit failures block submission; quick UX fixes are taken; feature requests
bank for v1.1.

Purpose: REL-04 requires a real EAS build, and the beta is the last safety net before App Review.
Bundling Sentry + catalog into this single build means every later build is metadata-only, carrying
zero new native-linking risk into the July 23–25 submission window.
Output: A TestFlight external build, a public tester link, and a recorded beta-gate decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-app-store-submission/06-CONTEXT.md
@.planning/phases/06-polish-app-store-submission/06-RESEARCH.md
@apps/mobile/eas.json
@apps/mobile/app.json
@apps/mobile/package.json
</context>

## Artifacts this phase produces (plan 06-06)

- A production EAS iOS build (cloud artifact, not in repo) with Sentry source maps uploaded.
- A TestFlight external test group + public tester link (in App Store Connect, not in repo).
- `pnpm-lock.yaml` synced so EAS's `--frozen-lockfile` install succeeds.

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-build verification gate</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    - apps/mobile/scripts/check-icon-alpha.mjs (from 06-01 — the icon regression guard)
    - apps/mobile/eas.json (autoIncrement + Release from 06-05)
    - apps/mobile/app.json (single config, com.apsis.app, Sentry + HealthKit plugins)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Pitfall 1/2/3 — icon alpha, single bundle id, frozen-lockfile)
    - STATE.md Blockers/Concerns (Phase 04 note: native-dep changes require pnpm-lock sync + fresh EAS build)
  </read_first>
  <action>
    Run the full automated pre-build checklist from the repo so a wasted cloud build is avoided:
    (a) `node apps/mobile/scripts/check-icon-alpha.mjs` exits 0 (icon still has no alpha); (b) confirm
    no root-level app.json/eas.json exists and no file declares com.apsistraining.apsis (single bundle
    id com.apsis.app); (c) verify pnpm-lock.yaml is in sync by running a frozen-lockfile install check
    (`pnpm install --frozen-lockfile` from repo root — must succeed, since EAS installs this way);
    (d) run the full workspace test suite `pnpm -r test` green (Sentry sanitizer + ~150-entry catalog
    included). Fix a lockfile drift by running a normal `pnpm install` and committing the updated
    pnpm-lock.yaml. This task modifies only pnpm-lock.yaml (if drifted); it does not trigger the cloud
    build (that is the human checkpoint).
  </action>
  <verify>
    <automated>node apps/mobile/scripts/check-icon-alpha.mjs && test ! -f app.json && test ! -f eas.json && pnpm install --frozen-lockfile && pnpm -r test</automated>
  </verify>
  <done>Icon clean, single bundle id, lockfile frozen-install-clean, all workspace tests green — ready to build.</done>
  <acceptance_criteria>
    - `node apps/mobile/scripts/check-icon-alpha.mjs` exits 0.
    - `pnpm install --frozen-lockfile` succeeds from repo root.
    - `pnpm -r test` is fully green.
    - grep across the repo finds no "com.apsistraining.apsis".
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run production EAS build + upload to external TestFlight</name>
  <action>Blocking human checkpoint — the human performs the steps in how-to-verify; no autonomous code change. From apps/mobile, run the production EAS build, confirm Sentry source maps upload, and submit the build to an external TestFlight group for Beta App Review.</action>
  <what-built>
    All code/config/assets for the build are landed and the automated pre-build gate is green. The
    iOS production build itself is EAS-cloud-only on this Windows host and needs the user's
    authenticated EAS/Apple session; source-map upload needs SENTRY_AUTH_TOKEN as an EAS secret.
  </what-built>
  <how-to-verify>
    1. From apps/mobile, ensure the EAS secret SENTRY_AUTH_TOKEN is set (eas secret) and
       EXPO_PUBLIC_SENTRY_DSN is available to the build; never commit either.
    2. Run `eas build --platform ios --profile production` FROM apps/mobile (never the repo root).
       Confirm the build succeeds and that Sentry source maps upload during the build.
    3. Confirm the resulting build shows bundle id com.apsis.app and an auto-incremented buildNumber
       (marketing version 1.0.0) in App Store Connect.
    4. In App Store Connect → TestFlight, add the build to an External test group, submit it for Beta
       App Review as early in the July 14–15 window as possible (the beta-review queue has no SLA,
       Pitfall 4), and once approved enable a public link.
    Expected: a production build in ASC + a build submitted to external Beta App Review.
  </how-to-verify>
  <resume-signal>Reply "built" with the buildNumber + TestFlight status, or describe the build/upload error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-12 external beta gate + on-device Sentry scrub confirmation</name>
  <action>Blocking human checkpoint — the human performs the steps in how-to-verify; no autonomous code change. Run the D-12 beta gate over July 15–22 (crashes/data-loss/HealthKit failures block; quick UX fixes taken; feature requests banked) and confirm a live Sentry event carries only allowlisted fields.</action>
  <what-built>
    The external TestFlight build is live for testers. This is the D-12 beta gate — the last check
    before App Store submission.
  </what-built>
  <how-to-verify>
    1. Share the public TestFlight link with hybrid-athlete testers (or, per the D-12 fallback, run an
       internal self-test if external recruiting stalled by ~July 15).
    2. Over the July 15–22 window, collect feedback. Apply the D-12 gate: any crash, data-loss, or
       HealthKit failure is a BLOCKER and must be fixed (a fix means a new metadata-only build — no new
       native deps); small UX friction fixes (copy, confusing flows) are taken if quick; feature
       requests are banked for v1.1, not acted on now.
    3. Sanity-check that crash reporting works: force/observe a test error and confirm a Sentry event
       arrives carrying ONLY allowlisted fields (no HR/HSS/bodyweight/distance/duration, no breadcrumbs
       with health values) — the live confirmation of REL-03 / plan 06-02.
    Expected: no open blocker-class issues; crash reporting confirmed scrubbing correctly.
  </how-to-verify>
  <resume-signal>Reply "beta clear" if no blockers remain (submission may proceed), or list blockers to fix first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → EAS cloud build | Source + lockfile cross into the cloud build (frozen-lockfile install) |
| EAS build CWD → bundle id resolution | Building from the wrong directory could resolve a mismatched bundle id |
| build → TestFlight external testers | Pre-release binary distributed to outside users |
| running app → Sentry ingest | First real (device) confirmation the allowlist holds end to end |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13 | Tampering | Stale binary missing the Sentry native module | medium | mitigate | Fresh production EAS build after the native install (Pitfall 3) + frozen-lockfile pre-build gate; device beta confirms crash reporting actually fires. |
| T-06-14 | Information Disclosure | Live crash event leaking health values on-device | high | mitigate | Beta gate step 3 forces a real Sentry event and verifies only allowlisted fields arrive — device-level confirmation of the 06-02 allowlist (T-06-01). |
| T-06-05 | Repudiation | Build produced under the wrong bundle id | low | mitigate | Pre-build gate asserts single com.apsis.app config; build is run from apps/mobile only; ASC build shows com.apsis.app. |
</threat_model>

<verification>
- Automated pre-build gate green (icon, single bundle id, frozen lockfile, `pnpm -r test`).
- Production EAS build succeeds from apps/mobile with an auto-incremented buildNumber under com.apsis.app.
- Build reaches external TestFlight; D-12 beta gate returns no blocker-class issues.
- A live Sentry event is confirmed to carry only allowlisted fields (REL-03 device confirmation).
</verification>

<success_criteria>
A fresh production build carrying Sentry + the expanded catalog is live in external TestFlight, the
D-12 beta gate is clear, and crash reporting is confirmed on-device to scrub health-derived values —
clearing the path to App Store submission.
</success_criteria>

<output>
Create `.planning/phases/06-polish-app-store-submission/06-06-SUMMARY.md` when done (record buildNumber + beta outcome).
</output>
