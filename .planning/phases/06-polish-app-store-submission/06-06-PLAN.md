---
phase: 06-polish-app-store-submission
plan: 06
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03, 06-05]
files_modified:
  - pnpm-lock.yaml
autonomous: false
requirements: [REL-04]

estimate:
  tokens: 30000
  raw_tokens: 24000
  tasks: 3
  confidence: low

user_setup:
  - service: sentry
    why: "Source-map upload + crash ingest need the real Sentry org/project slugs, EXPO_PUBLIC_SENTRY_DSN, and SENTRY_AUTH_TOKEN (EAS secret) before the production build; per 06-USER-SETUP.md these gate 06-06."
    env_vars:
      - name: EXPO_PUBLIC_SENTRY_DSN
        source: "Sentry -> Settings -> Projects -> (project) -> Client Keys (DSN)"
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry -> Settings -> Auth Tokens (scope project:releases) — EAS secret ONLY, never committed"
    dashboard_config:
      - task: "Replace the placeholder @sentry/react-native/expo plugin org/project slugs in app.json with the real Sentry slugs; set EXPO_PUBLIC_SENTRY_DSN and the SENTRY_AUTH_TOKEN EAS secret"
        location: "Sentry dashboard + apps/mobile/app.json + eas secret:create (from apps/mobile)"
  - service: expo-eas
    why: "iOS builds are EAS-cloud-only on this Windows host; producing and distributing the TestFlight build requires the user's authenticated EAS + Apple session (interactive), plus an active Apple Developer membership + ASC record from 06-01."
    dashboard_config:
      - task: "Run the production EAS build from apps/mobile, confirm Sentry source-map upload, upload to TestFlight, create the external test group, submit for Beta App Review, share the public link (D-12)"
        location: "eas build (from apps/mobile) + App Store Connect -> TestFlight"

must_haves:
  truths:
    - "A single production EAS build is produced from apps/mobile against bundle com.apsis.app with an auto-incremented buildNumber (marketing version 1.0.0) that includes EVERY native module shipped through Phases 1-8: op-sqlite, @kingstinct/react-native-healthkit, @shopify/react-native-skia, react-native-reanimated/worklets, react-native-svg, expo-camera, expo-image-picker, expo-file-system, expo-sharing, and @sentry/react-native."
    - "The build carries NO descoped native module — expo-text-extractor (label OCR, descoped 2026-07-20) is absent from package.json and the lockfile, so it fully leaves the binary on this build."
    - "The build passes Beta App Review and is installable by external testers via a public TestFlight link (D-12); the D-12 gate is evaluated (crash/data-loss/HealthKit-or-nutrition failure blocks submission; quick UX fixes taken; feature requests bank for v1.1)."
    - "On-device, a real Sentry event carries ONLY allowlisted fields — it contains NO HealthKit-derived values (HR/HSS/bodyweight/distance/duration) AND NO nutrition values (foods, calories, macros, daily targets), and no breadcrumbs with those values (REL-03 device confirmation extended to nutrition per NUTR-22)."
    - statement: "Re-running the production build yields a new unique buildNumber via eas.json autoIncrement (cli.appVersionSource remote); a re-run never collides with or overwrites a prior TestFlight build."
      verification: backstop
    - statement: "An error event with no attached extra/context still produces a well-formed allowlisted event — empty/null attachments neither crash the sanitizer nor leak a field."
      verification: backstop
    - statement: "If a build is interrupted or a second build starts in parallel, EAS assigns distinct buildNumbers and no partial/unsigned binary reaches TestFlight."
      verification: backstop
    - statement: "The allowlist event-field reconstruction and breadcrumb clearing are deterministic regardless of input field order."
      verification: backstop
  prohibitions:
    - statement: "SENTRY_AUTH_TOKEN, any Apple ID password, or any EAS token MUST NOT be committed to the repo or placed in a tracked .env file; SENTRY_AUTH_TOKEN exists only as an EAS secret."
      status: flagged-unverified
      verification: automated
    - statement: "The production build MUST NOT ship any label-OCR / expo-text-extractor native code (descoped 2026-07-20)."
      status: flagged-unverified
      verification: automated
    - statement: "The build MUST NOT be run from the repo root — only from apps/mobile — so a stray/wrong bundle id can never be resolved."
      status: flagged-unverified
      verification: human
  artifacts:
    - "A TestFlight external build + public tester link (in ASC, not in repo)"
    - "pnpm-lock.yaml in sync (EAS --frozen-lockfile precondition)"
  key_links:
    - "Sentry + Phase 7 (camera) + Phase 8 (image-picker/file-system/sharing) native installs -> require THIS single fresh EAS build before crash reporting works or those features run on-device (Pitfall 3)"
    - "pnpm-lock.yaml sync -> EAS installs with --frozen-lockfile; a stale lockfile blocks the build (Phase 04 lesson)"
    - "Sentry env/slugs (06-USER-SETUP.md) -> gate the source-map upload + crash ingest"
    - "beta gate outcome -> gates whether plan 06-07 submission proceeds"
---

<objective>
Converge ALL shipped code — Phases 1-8, including Phase 7 nutrition and Phase 8 share card — into ONE
fresh EAS production build. A fresh build is mandatory because multiple native modules changed since
any existing binary: @sentry/react-native (06-02), expo-camera (Phase 7), and expo-image-picker /
expo-file-system / expo-sharing (Phase 8), while expo-text-extractor was REMOVED (label OCR descoped).
Get that build into external TestFlight, accounting for the no-SLA Beta App Review queue, then run the
D-12 beta gate and confirm on-device that Sentry scrubs BOTH health AND nutrition values.

Tracer note: this build IS the end-to-end integration point for every prior phase's vertical slice —
a separate tracer slice is not applicable to build/submit logistics; natural ordering is used.

Purpose: REL-04 requires a real EAS build, and the beta is the last safety net before App Review.
Bundling every native change into this single build means every later build is metadata-only.
Output: A TestFlight external build, a public tester link, and a recorded beta-gate decision.
Deadline framing: ASAP — within days of 2026-08-03 (the original ~July 25/28 target has passed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-app-store-submission/06-CONTEXT.md
@.planning/phases/06-polish-app-store-submission/06-RESEARCH.md
@.planning/phases/06-polish-app-store-submission/06-USER-SETUP.md
@.planning/phases/08-share-card-social-overlay/08-01-SUMMARY.md
@apps/mobile/eas.json
@apps/mobile/app.json
@apps/mobile/package.json
</context>

## Artifacts this phase produces (plan 06-06)

- A production EAS iOS build (cloud artifact, not in repo) with Sentry source maps uploaded, carrying all Phase 1-8 native modules.
- A TestFlight external test group + public tester link (in App Store Connect, not in repo).
- `pnpm-lock.yaml` synced so EAS's `--frozen-lockfile` install succeeds.
- (No new repo symbols/config keys — this plan only reconciles the lockfile and drives the cloud build.)

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-build verification gate (Phases 1-8 reconciled)</name>
  <files>pnpm-lock.yaml</files>
  <read_first>
    - apps/mobile/scripts/check-icon-alpha.mjs (from 06-01 — the icon regression guard)
    - apps/mobile/eas.json (autoIncrement + cli.appVersionSource remote + Release from 06-05; ascAppId already 6792933794)
    - apps/mobile/app.json (single config, com.apsis.app; Sentry + HealthKit + camera + image-picker + sharing plugins)
    - apps/mobile/package.json (native-dep surface: confirm expo-image-picker/expo-file-system/expo-sharing/expo-camera/@sentry/react-native present; confirm expo-text-extractor ABSENT)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Pitfall 1/2/3 — icon alpha, single bundle id, frozen-lockfile)
    - .planning/phases/08-share-card-social-overlay/08-01-SUMMARY.md (three Phase 8 native deps that MUST be in this build + lockfile-synced)
    - STATE.md Blockers/Concerns (Phase 04 lesson: native-dep changes require pnpm-lock sync + fresh EAS build; last EAS pnpm-detection fix 87b5a3d unverified)
  </read_first>
  <action>
    Run the full automated pre-build checklist from the repo so a wasted cloud build is avoided:
    (a) `node apps/mobile/scripts/check-icon-alpha.mjs` exits 0 (icon still has no alpha channel);
    (b) confirm no root-level app.json/eas.json exists and no file declares com.apsistraining.apsis
    (single bundle id com.apsis.app); (c) confirm the Phase 7/8 native deps are all present in
    apps/mobile/package.json (expo-camera, expo-image-picker, expo-file-system, expo-sharing,
    @sentry/react-native) and that expo-text-extractor is ABSENT from both package.json and
    pnpm-lock.yaml (label OCR descoped — must not re-appear); (d) verify pnpm-lock.yaml is in sync by
    running `pnpm install --frozen-lockfile` from the repo root (must succeed, since EAS installs this
    way); (e) run the full workspace test suite `pnpm -r test` green (Sentry sanitizer + ~150-entry
    catalog + nutrition + share-card tests all included). If the frozen-lockfile install fails, run a
    normal `pnpm install` and commit the updated pnpm-lock.yaml — that lockfile reconciliation is the
    ONLY file this task changes. This task does NOT trigger the cloud build (that is Task 2, the human
    checkpoint).
  </action>
  <verify>
    <automated>node apps/mobile/scripts/check-icon-alpha.mjs && test ! -f app.json && test ! -f eas.json && ! grep -rq "com.apsistraining.apsis" apps/mobile/app.json && ! grep -q "expo-text-extractor" apps/mobile/package.json pnpm-lock.yaml && pnpm install --frozen-lockfile && pnpm -r test</automated>
  </verify>
  <done>Icon clean, single bundle id, Phase 7/8 deps present, expo-text-extractor gone, lockfile frozen-install-clean, all workspace tests green — ready to build.</done>
  <acceptance_criteria>
    - `node apps/mobile/scripts/check-icon-alpha.mjs` exits 0.
    - `pnpm install --frozen-lockfile` succeeds from repo root.
    - `pnpm -r test` is fully green.
    - grep finds no "com.apsistraining.apsis" and no "expo-text-extractor" in package.json or pnpm-lock.yaml.
    - expo-image-picker, expo-file-system, expo-sharing, expo-camera, and @sentry/react-native are all present in apps/mobile/package.json.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run production EAS build + upload to external TestFlight</name>
  <action>Blocking human checkpoint — the human performs the steps in how-to-verify; no autonomous code change. Ensure the Sentry env/slug preconditions are met, then run the production EAS build from apps/mobile, confirm Sentry source-map upload, and submit the build to an external TestFlight group for Beta App Review.</action>
  <precondition>EXPO_PUBLIC_SENTRY_DSN + SENTRY_AUTH_TOKEN (EAS secret) are set and the @sentry/react-native/expo plugin org/project slugs in app.json are the real Sentry slugs (per 06-USER-SETUP.md); an active Apple Developer membership + the ASC record for com.apsis.app from 06-01 exist.</precondition>
  <reversibility rating="costly">A cloud build consumes an EAS build slot + Beta App Review queue time; wasted if the pre-build gate or env is wrong, so Task 1 + this precondition must pass first.</reversibility>
  <read_first>
    - .planning/phases/06-polish-app-store-submission/06-USER-SETUP.md (Sentry DSN/token + slug setup that gates this build)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Pitfall 3 fresh-build requirement; Pitfall 4 no-SLA Beta App Review)
    - apps/mobile/eas.json (production profile; run from apps/mobile only)
  </read_first>
  <what-built>
    All code/config/assets for Phases 1-8 are landed and Task 1's automated pre-build gate is green.
    The iOS production build itself is EAS-cloud-only on this Windows host and needs the user's
    authenticated EAS/Apple session; source-map upload needs SENTRY_AUTH_TOKEN as an EAS secret and the
    real Sentry slugs in app.json.
  </what-built>
  <instructions>
    1. From apps/mobile, confirm the EAS secret SENTRY_AUTH_TOKEN is set (`eas secret:list`),
       EXPO_PUBLIC_SENTRY_DSN is available to the build, and app.json's @sentry/react-native/expo plugin
       carries the REAL org/project slugs (not the "apsis"/"apsis-mobile" placeholders). Never commit
       the token.
    2. Run `eas build --platform ios --profile production` FROM apps/mobile (never the repo root).
       Confirm the build succeeds and that Sentry source maps upload during the build.
    3. Confirm the resulting build shows bundle id com.apsis.app and an auto-incremented buildNumber
       (marketing version 1.0.0) in App Store Connect, and that it launches with the share card and
       nutrition features present (Phase 7/8 native modules linked).
    4. In App Store Connect -> TestFlight, add the build to an External test group, submit it for Beta
       App Review ASAP (the beta-review queue has no SLA, Pitfall 4), and once approved enable a public link.
  </instructions>
  <verification>
    A production build appears in App Store Connect under com.apsis.app with an auto-incremented
    buildNumber and marketing version 1.0.0, the EAS build log shows Sentry source-map upload, and the
    build is submitted to an External TestFlight group for Beta App Review. No auth token or Apple
    credential was committed to the repo.
  </verification>
  <resume-signal>Reply "built" with the buildNumber + TestFlight status, or describe the build/upload error.</resume-signal>
  <acceptance_criteria>
    - `eas build --platform ios --profile production` completed from apps/mobile with Sentry source-map upload logs.
    - ASC shows the build under com.apsis.app with an auto-incremented buildNumber and marketing version 1.0.0.
    - The build is submitted to an External TestFlight group for Beta App Review.
    - No auth token or Apple credential was committed to the repo during this step.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-12 external beta gate + on-device Sentry scrub confirmation (health + nutrition)</name>
  <action>Blocking human checkpoint — the human performs the steps in how-to-verify; no autonomous code change. Run the D-12 beta gate and confirm a live Sentry event carries only allowlisted fields — with the scrub check now covering nutrition values as well as HealthKit values (NUTR-22 / REL-03).</action>
  <read_first>
    - .planning/phases/06-polish-app-store-submission/06-CONTEXT.md (D-12 beta gate rules; D-03 allowlist)
    - .planning/phases/06-polish-app-store-submission/06-02-SUMMARY.md (the shipped Sentry allowlist sanitizer behavior being confirmed on-device)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Pitfall 6 breadcrumb re-capture)
  </read_first>
  <what-built>
    The external TestFlight build is live for testers. This is the D-12 beta gate — the last check
    before App Store submission — now spanning nutrition and share-card flows added since the original plan.
  </what-built>
  <how-to-verify>
    1. Share the public TestFlight link with hybrid-athlete testers (or, per the D-12 fallback, run an
       internal self-test if external recruiting stalls). Exercise lifting, running, HealthKit, nutrition
       (barcode/search/manual + adaptive targets), and the share card (void + photo) on-device.
    2. Apply the D-12 gate: any crash, data-loss, or HealthKit/nutrition failure is a BLOCKER and must be
       fixed (a fix is a new metadata-only build — no new native deps); small UX friction fixes (copy,
       confusing flows) are taken if quick; feature requests are banked for v1.1, not acted on now.
    3. Confirm crash reporting scrubs correctly: force/observe a test error and confirm the Sentry event
       carries ONLY allowlisted fields (error type/message, stack, device/OS model, app version) — and
       specifically NO HealthKit-derived values (HR/HSS/bodyweight/distance/duration) AND NO nutrition
       values (foods, calories, macros, daily targets), and no breadcrumbs containing any of those. This
       is the live REL-03 confirmation extended to nutrition (NUTR-22).
    Expected: no open blocker-class issues; crash reporting confirmed scrubbing both health and nutrition values.
  </how-to-verify>
  <resume-signal>Reply "beta clear" if no blockers remain and the scrub is confirmed (submission may proceed), or list blockers to fix first.</resume-signal>
  <acceptance_criteria>
    - The D-12 gate is applied and no blocker-class (crash/data-loss/HealthKit/nutrition) issue remains open.
    - A real on-device Sentry event is confirmed to contain only allowlisted fields.
    - The confirmed event contains no HealthKit-derived values AND no nutrition values, in neither the event body nor breadcrumbs.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> EAS cloud build | Source + lockfile cross into the cloud build (frozen-lockfile install) |
| EAS build CWD -> bundle id resolution | Building from the wrong directory could resolve a mismatched bundle id |
| build -> TestFlight external testers | Pre-release binary distributed to outside users |
| running app -> Sentry ingest | First real (device) confirmation the allowlist holds for health AND nutrition |
| SENTRY_AUTH_TOKEN -> EAS build | Auth secret crosses to EAS; must never enter the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-13 | Tampering | Stale binary missing a native module (Sentry / image-picker / camera) | high | mitigate | ONE fresh production EAS build after all native installs (Pitfall 3) + frozen-lockfile pre-build gate that asserts Phase 7/8 deps present and expo-text-extractor absent; device beta confirms features + crash reporting actually work. |
| T-06-14 | Information Disclosure | Live crash event leaking health OR nutrition values on-device | high | mitigate | Beta gate Task 3 forces a real Sentry event and verifies only allowlisted fields arrive — device-level confirmation of the 06-02 allowlist, extended to nutrition (NUTR-22). |
| T-06-19 | Information Disclosure | SENTRY_AUTH_TOKEN committed to the repo | high | mitigate | Token lives only as an EAS secret; never in .env or git; Task 2 verify confirms no credential was committed. |
| T-06-05 | Repudiation | Build produced under the wrong bundle id | low | mitigate | Pre-build gate asserts single com.apsis.app config; build run from apps/mobile only; ASC build shows com.apsis.app. |
</threat_model>

<verification>
- Automated pre-build gate green (icon, single bundle id, Phase 7/8 deps present, no expo-text-extractor, frozen lockfile, `pnpm -r test`).
- Production EAS build succeeds from apps/mobile with an auto-incremented buildNumber under com.apsis.app and Sentry source-map upload.
- Build reaches external TestFlight; D-12 beta gate returns no blocker-class issues across health + nutrition + share card.
- A live Sentry event carries only allowlisted fields — no health AND no nutrition values (REL-03 / NUTR-22 device confirmation).
</verification>

<success_criteria>
A single fresh production build carrying every Phase 1-8 native module (and no descoped module) is live
in external TestFlight, the D-12 beta gate is clear, and crash reporting is confirmed on-device to scrub
both health-derived and nutrition values — clearing the path to App Store submission.
</success_criteria>

<output>
Create `.planning/phases/06-polish-app-store-submission/06-06-SUMMARY.md` when done (record buildNumber + beta outcome).
</output>
