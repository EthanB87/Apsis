---
phase: 06-polish-app-store-submission
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/lib/sentrySanitize.ts
  - apps/mobile/lib/__tests__/sentrySanitize.test.ts
  - apps/mobile/app/_layout.tsx
  - apps/mobile/app.json
  - apps/mobile/metro.config.js
  - apps/mobile/package.json
autonomous: true
requirements: [REL-03]
user_setup:
  - service: sentry
    why: "Crash events need a Sentry project (DSN) and source-map upload needs an auth token; both are Claude's-discretion setup per CONTEXT.md but the account/project itself is human-created."
    env_vars:
      - name: EXPO_PUBLIC_SENTRY_DSN
        source: "Sentry → Settings → Projects → (project) → Client Keys (DSN)"
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry → Settings → Auth Tokens (store as an EAS secret, never commit)"
    dashboard_config:
      - task: "Create a Sentry project (React Native platform); note the organization + project slugs for the app.json plugin block"
        location: "sentry.io → Projects → Create Project"

must_haves:
  truths:
    - "A crash or unhandled JS error is reported to Sentry with only allowlisted fields (error type/message, stack frames, device/OS model, app version)."
    - "Any app-attached field (heart rate, HSS, bodyweight, distance, duration) placed on extra/contexts/breadcrumbs is stripped before send — provable by unit test without a device."
    - "Breadcrumbs are always emptied on outgoing events regardless of input (defense in depth vs Console re-capture, Pitfall 6)."
    - "sendDefaultPii is false and no performance tracing is enabled (D-02)."
  artifacts:
    - "apps/mobile/lib/sentrySanitize.ts exporting sanitizeSentryEvent"
    - "apps/mobile/lib/__tests__/sentrySanitize.test.ts"
    - "@sentry/react-native dependency + expo config plugin registration + metro getSentryExpoConfig wrap"
  key_links:
    - "sanitizeSentryEvent → wired as Sentry.init beforeSend in _layout.tsx (allowlist actually runs)"
    - "metro.config.js sql sourceExt push → preserved through the getSentryExpoConfig wrap (drizzle migration bundling must not break)"
    - "Console integration filtered out of Sentry defaults → console.* is not turned into breadcrumbs (T-05-01 lineage)"
---

<objective>
Ship real crash reporting (D-01) that cannot leak HealthKit-derived values (REL-03/D-03). Extract
the scrubbing into a pure, vitest-testable allowlist function (`sanitizeSentryEvent`), wire it as
`Sentry.init`'s `beforeSend`, install `@sentry/react-native`, register its Expo config plugin, and
wrap Metro — preserving the existing `.sql` sourceExt push that drizzle migration bundling depends on.

Purpose: REL-03 is verified as an allowlist (only known-safe fields forwarded), not a denylist —
new code physically cannot attach health values to an outgoing event.
Output: Testable sanitizer, Sentry init/wrap in the root layout, config-plugin + metro wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-app-store-submission/06-RESEARCH.md
@.planning/phases/06-polish-app-store-submission/06-PATTERNS.md
@.planning/phases/05-healthkit-integration/05-SECURITY.md
@apps/mobile/app/_layout.tsx
@apps/mobile/lib/healthkitAuth.ts
@apps/mobile/metro.config.js
@apps/mobile/app.json
@apps/mobile/vitest.config.mts
</context>

## Artifacts this phase produces (plan 06-02)

- Function `sanitizeSentryEvent(event: ErrorEvent): ErrorEvent` in `apps/mobile/lib/sentrySanitize.ts` — pure, native-import-free (type-only import of `ErrorEvent`), explicit allowlist reconstruction.
- Test file `apps/mobile/lib/__tests__/sentrySanitize.test.ts`.
- New dependency `@sentry/react-native` (8.18.0 line) in `apps/mobile/package.json`.
- `app.json` plugins entry `["@sentry/react-native/expo", { organization, project, url }]`.
- `metro.config.js` wrapped via `getSentryExpoConfig(__dirname)` (retaining `config.resolver.sourceExts.push('sql')`).
- `Sentry.init(...)` module-scope call + `export default Sentry.wrap(RootLayout)` in `_layout.tsx`.
- Env vars `EXPO_PUBLIC_SENTRY_DSN`, `SENTRY_AUTH_TOKEN`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure Sentry allowlist sanitizer + tests (RED→GREEN)</name>
  <files>apps/mobile/lib/sentrySanitize.ts, apps/mobile/lib/__tests__/sentrySanitize.test.ts</files>
  <read_first>
    - apps/mobile/lib/healthkitMapping.ts (analog: pure, native-import-free sanitize functions)
    - apps/mobile/lib/__tests__/healthkitMapping.test.ts (analog test header + per-field assertion granularity)
    - apps/mobile/vitest.config.mts (include glob: lib/**/__tests__/*.test.ts — put the test there)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Pattern 1)
    - .planning/phases/06-polish-app-store-submission/06-PATTERNS.md (sentrySanitize.ts section)
  </read_first>
  <behavior>
    - `exception` is preserved verbatim (error type/message/stack frames survive).
    - `contexts` is reduced to exactly { device, app } — any other contexts key is dropped.
    - `user` is always undefined on the output (no identity ever attached).
    - `extra` is always undefined on the output — a poisoned input like extra { heartRate, hss } does not survive.
    - `breadcrumbs` is always [] on the output regardless of input breadcrumbs.
    - The module imports only the `ErrorEvent` TYPE from @sentry/react-native — no runtime/native import (stays vitest-runnable under node).
  </behavior>
  <action>
    Create apps/mobile/lib/__tests__/sentrySanitize.test.ts FIRST with one describe block for
    sanitizeSentryEvent (D-03) and one `it` per allowlist field plus one adversarial `it` per
    dropped field (poisoned extra/contexts/breadcrumbs cannot leak). Confirm it fails (RED). Then
    create apps/mobile/lib/sentrySanitize.ts exporting `sanitizeSentryEvent(event: ErrorEvent):
    ErrorEvent` that returns an explicit allowlist reconstruction: keep event.exception; set
    contexts to only device and app subfields; set user undefined, extra undefined, breadcrumbs [].
    Never spread event.extra/event.contexts/event.breadcrumbs verbatim (that is how a future field
    leaks). Import ErrorEvent as a type-only import so no native module loads. Add a module doc
    comment stating it encodes D-03 and that health-derived values (HR/HSS/bodyweight/distance/
    duration) must NEVER be forwarded — the T-05-01 no-raw-health-values convention pushed to the
    SDK-event layer.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/mobile test -- sentrySanitize</automated>
  </verify>
  <done>sanitizeSentryEvent passes all allowlist + adversarial tests; no native import in the module.</done>
  <acceptance_criteria>
    - `pnpm --filter @apsis/mobile test -- sentrySanitize` passes green.
    - A test asserting `sanitizeSentryEvent({ extra: { heartRate: 172, hss: 88.4 }, exception: {}, contexts: {} }).extra` is undefined passes.
    - A test asserting output.breadcrumbs deep-equals [] for a non-empty breadcrumbs input passes.
    - A test asserting output.contexts has no keys other than device/app passes.
    - `sentrySanitize.ts` contains no runtime import from '@sentry/react-native' (only `import type`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install @sentry/react-native + config plugin + metro wrap</name>
  <files>apps/mobile/package.json, apps/mobile/app.json, apps/mobile/metro.config.js, pnpm-lock.yaml</files>
  <read_first>
    - apps/mobile/package.json (current deps; confirm sentry not present)
    - apps/mobile/app.json (plugins array — copy the @kingstinct/react-native-healthkit tuple shape)
    - apps/mobile/metro.config.js (getDefaultConfig + config.resolver.sourceExts.push('sql') — must be preserved)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Package Legitimacy Audit — @sentry/react-native verdict OK/Approved; Pattern 2)
  </read_first>
  <action>
    Install @sentry/react-native (8.18.0 line) from apps/mobile via `npx expo install
    @sentry/react-native` (per RESEARCH — Package Legitimacy Audit verdict OK/Approved, no
    postinstall). Add the plugin tuple ["@sentry/react-native/expo", { "organization":
    "<org-slug>", "project": "<project-slug>", "url": "https://sentry.io/" }] to the app.json
    plugins array in the same array-of-tuples shape as the existing HealthKit plugin entry; org/
    project slugs come from the Sentry project (user_setup). Do NOT install the deprecated
    sentry-expo package. Rewrite metro.config.js to build its base via getSentryExpoConfig(__dirname)
    from '@sentry/react-native/metro' INSTEAD of getDefaultConfig, and KEEP the existing
    config.resolver.sourceExts.push('sql') line intact afterward (drizzle migration bundling breaks
    the boot sequence without it). Confirm ios.infoPlist.ITSAppUsesNonExemptEncryption stays false
    (D-18). Sync pnpm-lock.yaml so EAS's --frozen-lockfile install will succeed later.
  </action>
  <verify>
    <automated>grep -q "@sentry/react-native" apps/mobile/package.json && grep -q "getSentryExpoConfig" apps/mobile/metro.config.js && grep -q "sourceExts.push('sql')" apps/mobile/metro.config.js && grep -q "@sentry/react-native/expo" apps/mobile/app.json</automated>
  </verify>
  <done>Sentry installed; expo plugin registered; metro wrapped via getSentryExpoConfig with sql sourceExt preserved.</done>
  <acceptance_criteria>
    - `@sentry/react-native` appears in apps/mobile/package.json dependencies and pnpm-lock.yaml is in sync.
    - metro.config.js sources its base config from getSentryExpoConfig(__dirname) AND still contains `config.resolver.sourceExts.push('sql')`.
    - app.json plugins array contains a "@sentry/react-native/expo" tuple.
    - `sentry-expo` is NOT installed anywhere.
    - app.json ios.infoPlist.ITSAppUsesNonExemptEncryption is still false.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Sentry.init + Sentry.wrap in root layout (D-02/D-03)</name>
  <files>apps/mobile/app/_layout.tsx</files>
  <read_first>
    - apps/mobile/app/_layout.tsx (module-scope SplashScreen.preventAutoHideAsync() at line 48; default export RootLayout; console.error convention)
    - apps/mobile/lib/sentrySanitize.ts (from Task 1 — beforeSend source)
    - .planning/phases/06-polish-app-store-submission/06-RESEARCH.md (Pattern 1 init block + Pitfall 6)
    - .planning/phases/06-polish-app-store-submission/06-PATTERNS.md (_layout.tsx section)
  </read_first>
  <action>
    Add a module-scope Sentry.init(...) call to _layout.tsx alongside the existing
    SplashScreen.preventAutoHideAsync() (runs once per launch, before the component). Configure:
    dsn from process.env.EXPO_PUBLIC_SENTRY_DSN; sendDefaultPii false (D-03); tracesSampleRate 0
    (D-02, no performance tracing); attachScreenshot false; attachViewHierarchy false; beforeSend
    set to sanitizeSentryEvent imported from '../lib/sentrySanitize'; and an integrations callback
    that filters the default 'Console' integration out (Pitfall 6 — do not turn console.* into
    breadcrumbs). Convert the current `export default function RootLayout()` into a named function
    declaration `function RootLayout()` and add `export default Sentry.wrap(RootLayout);` at the
    bottom. Preserve every existing boot-sequence effect, the error/loading/resume gates, and the
    console.error('[Apsis] ...', err) discipline unchanged.
  </action>
  <verify>
    <automated>grep -q "Sentry.init" apps/mobile/app/_layout.tsx && grep -q "beforeSend" apps/mobile/app/_layout.tsx && grep -q "sanitizeSentryEvent" apps/mobile/app/_layout.tsx && grep -q "Sentry.wrap(RootLayout)" apps/mobile/app/_layout.tsx && pnpm --filter @apsis/mobile test</automated>
  </verify>
  <done>Sentry initialized with the allowlist beforeSend + pruned integrations; RootLayout wrapped; mobile tests green.</done>
  <acceptance_criteria>
    - _layout.tsx calls Sentry.init at module scope with beforeSend: sanitizeSentryEvent, sendDefaultPii: false, tracesSampleRate: 0.
    - The Sentry.init integrations callback removes the Console integration (breadcrumb-from-console defense).
    - The file's default export is `Sentry.wrap(RootLayout)`.
    - All existing boot gates (error/loading/resume/profile) remain present and unmodified in behavior.
    - `pnpm --filter @apsis/mobile test` is green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app runtime → Sentry ingest (external SaaS) | Crash/error events cross the process boundary to Sentry's servers over HTTPS |
| npm registry → native build | @sentry/react-native ships native code + a config plugin |
| console.* / app state → Sentry breadcrumbs | Default integrations can re-capture console output as breadcrumbs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Sentry event/breadcrumb payload carrying health-derived values | high | mitigate | Allowlist `beforeSend` (sanitizeSentryEvent) reconstructs only known-safe fields + always empties breadcrumbs; `sendDefaultPii: false`; Console integration filtered out (Pitfall 6). Verified by unit test, not device-only. Extends Phase 5 T-05-01 to the SDK layer. |
| T-06-SC | Tampering | npm install of @sentry/react-native (native module) | high | mitigate | RESEARCH Package Legitimacy Audit completed: verdict OK/Approved (7-yr package, 2.7M wk downloads, official getsentry repo, no postinstall) — not [ASSUMED]/[SUS], so no blocking legitimacy checkpoint required; version pinned in package.json. |
| T-06-06 | Information Disclosure | Performance/replay telemetry over-collection | medium | mitigate | tracesSampleRate 0, attachScreenshot/attachViewHierarchy false, breadcrumbs pruned (D-02) — scope limited to native crashes + unhandled JS errors. |
</threat_model>

<verification>
- `pnpm --filter @apsis/mobile test -- sentrySanitize` green; full `pnpm --filter @apsis/mobile test` green.
- metro.config.js retains the `.sql` sourceExt push after the getSentryExpoConfig wrap.
- _layout.tsx wires beforeSend to sanitizeSentryEvent and exports Sentry.wrap(RootLayout).
</verification>

<success_criteria>
Crash reporting is installed and initialized with a structurally-enforced allowlist that unit tests
prove strips every health-derived field, breadcrumbs are always emptied, and the drizzle sql-bundling
metro config survives the Sentry wrap.
</success_criteria>

<output>
Create `.planning/phases/06-polish-app-store-submission/06-02-SUMMARY.md` when done.
</output>
