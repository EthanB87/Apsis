---
phase: 02-hss-engine
plan: 06
type: execute
wave: 4
depends_on: [02-02, 02-03, 02-04, 02-05]
files_modified:
  - packages/engine/src/index.ts
  - packages/engine/src/config.ts
  - packages/engine/src/__tests__/calibration.test.ts
  - packages/engine/README.md
autonomous: true
requirements: [ENG-07]
must_haves:
  truths:
    - "The engine's public API (strength/endurance/session/daily/trend + config + ENGINE_VERSION) is importable from the @apsis/engine barrel."
    - "A calibration test asserts the canonical hard 5x5 squat and 60-min threshold run land within a 0.8..1.25 ratio (D-13), with kStrength tuned to pass it."
    - "packages/engine has zero runtime dependencies (only devDeps + the first-party @apsis/shared workspace link) and never reads the wall clock."
    - "The engine suite has ≥20 passing vitest tests via `pnpm --filter @apsis/engine test`, and `pnpm typecheck` is green."
    - "packages/engine/README.md documents every formula and constant (BUILD.md §4.4)."
  artifacts:
    - packages/engine/src/index.ts
    - packages/engine/src/__tests__/calibration.test.ts
    - packages/engine/README.md
  key_links:
    - "index.ts re-exports every public symbol so Phase 3/4 import from '@apsis/engine'"
    - "the calibration test drives the final kStrength value in config.ts"
---

<objective>
Assemble and prove the engine (ENG-07): wire the public barrel, delete the Phase-1
placeholder test, tune `kStrength` against the calibration test that anchors the "one honest
number" thesis (a hard 5x5 squat within ±25% of a 60-min threshold run — D-13), document
every formula/constant in a README (BUILD.md §4.4), and verify purity + the ≥20-test bar.

Purpose: The calibration test is a first-class deliverable — it is what makes the single HSS
number honest across modalities. This plan closes §4.4's acceptance checklist.
Output: A curated `@apsis/engine` public API, a passing calibration golden, a tuned
`DEFAULT_CONFIG.kStrength`, `packages/engine/README.md`, and a green ≥20-test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-hss-engine/02-CONTEXT.md
@.planning/phases/02-hss-engine/02-PATTERNS.md
@BUILD.md
@packages/engine/src/config.ts
@packages/engine/src/strength.ts
@packages/engine/src/endurance.ts
@packages/engine/src/session.ts
@packages/engine/src/daily.ts
@packages/engine/src/trend.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the public barrel, remove the placeholder test, confirm purity</name>
  <files>packages/engine/src/index.ts, packages/engine/src/__tests__/placeholder.test.ts</files>
  <read_first>
    - packages/engine/src/index.ts (current: module header + ENGINE_VERSION only — this becomes the barrel)
    - packages/db/src/index.ts (barrel re-export convention to mirror: grouped `export * from './x'` with comments)
    - packages/engine/src/config.ts, strength.ts, endurance.ts, session.ts, daily.ts, trend.ts (the symbols to re-export)
    - packages/engine/package.json (confirm no runtime `dependencies` beyond the @apsis/shared workspace link)
    - .planning/phases/02-hss-engine/02-PATTERNS.md (barrel convention; keep ENGINE_VERSION)
  </read_first>
  <action>
    Convert packages/engine/src/index.ts into a barrel while keeping the exported ENGINE_VERSION
    constant. Rewrite its module JSDoc header so it names the package and restates the purity
    contract using the phrase "time is always passed in as a parameter; the engine never reads the
    wall clock" — do NOT keep or reintroduce any comment that spells out the current-time API token
    (so the purity grep in the verify step stays clean). Add grouped re-exports:
    `export * from './config'`, `export * from './strength'`, `export * from './endurance'`,
    `export * from './session'`, `export * from './daily'`, `export * from './trend'`, plus keep
    `export const ENGINE_VERSION`. If two modules would collide on a name, prefer named re-exports of
    the BUILD.md §4.1 public surface plus the detailed variants and helpers.

    Delete packages/engine/src/__tests__/placeholder.test.ts (real coverage from plans 02-01..02-05
    replaces it).
  </action>
  <verify>
    <automated>test ! -f packages/engine/src/__tests__/placeholder.test.ts && grep -Eq "export \* from './trend'" packages/engine/src/index.ts && test $(grep -rIn --include='*.ts' -e 'Date\.now' -e 'performance\.now' -e 'new Date(' packages/engine/src | wc -l) -eq 0 && test $(node -e "const p=require('./packages/engine/package.json'); process.stdout.write(String(Object.keys(p.dependencies||{}).filter(d=>d!=='@apsis/shared').length))") -eq 0 && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/engine/src/__tests__/placeholder.test.ts no longer exists
    - packages/engine/src/index.ts re-exports config, strength, endurance, session, daily, trend and still exports ENGINE_VERSION
    - Zero matches for wall-clock time APIs anywhere under packages/engine/src (grep count is 0, comments included)
    - packages/engine/package.json has no runtime dependency other than the @apsis/shared workspace link
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>The @apsis/engine public barrel exports the full API, the placeholder is gone, and purity (no wall-clock, zero third-party runtime deps) is proven.</done>
</task>

<task type="auto">
  <name>Task 2: Add the calibration golden and tune kStrength to pass it (D-13/D-14)</name>
  <files>packages/engine/src/__tests__/calibration.test.ts, packages/engine/src/config.ts</files>
  <read_first>
    - .planning/phases/02-hss-engine/02-CONTEXT.md D-13 (±25% ratio 0.8..1.25), D-14 (HSS scale anchor + tune kStrength), specifics (calibration is a first-class deliverable)
    - BUILD.md §4.3 (tune kStrength/kEndurance so a 60-min threshold run and a hard 5x5 squat are comparable)
    - packages/engine/src/config.ts (current kStrength 2.0 — this task changes it)
    - packages/engine/src/session.ts, strength.ts, endurance.ts (functions the test drives)
    - packages/engine/src/__tests__/placeholder.test.ts is deleted; use vitest globals convention from any sibling test (e.g. strength.test.ts)
  </read_first>
  <action>
    Create packages/engine/src/__tests__/calibration.test.ts importing sessionHSS (and/or
    strengthStress, enduranceStress) from '../index' (the barrel is wired in Task 1; vitest globals,
    no import of describe/it/expect). Define two canonical sessions with DEFAULT_CONFIG:
    - Threshold run: enduranceSegments [{ durationS: 3600, intensityFactor: 1.0 }] → runHSS (expected ≈100).
    - Hard 5x5 squat: five identical working sets { loadKg: 140, e1rmKg: 180, reps: 5, rpe: 9, isLowerBody: true, isWarmup: false } → liftHSS.
    Assert: runHSS toBeCloseTo 100 (±1); and the ratio liftHSS / runHSS is >= 0.8 AND <= 1.25 (D-13).

    Then tune `DEFAULT_CONFIG.kStrength` in packages/engine/src/config.ts to the value that makes
    the 5x5 squat land inside that ratio (with the fixed kEndurance 1.6667, the canonical squat's
    summed set stress is 22.75, so kStrength must be ~4.0–4.4 to reach ~91–100; pick the value that
    puts the ratio near 1.0 and keeps hard sessions inside BUILD.md's ~30–120 readable range).
    Update the kStrength JSDoc in config.ts to record the calibrated value and that it was derived
    from this test. Do not change kEndurance.
  </action>
  <verify>
    <automated>test -f packages/engine/src/__tests__/calibration.test.ts && ! grep -Eq "kStrength:\s*2\.0\b" packages/engine/src/config.ts && pnpm --filter @apsis/engine test</automated>
  </verify>
  <acceptance_criteria>
    - calibration.test.ts asserts runHSS toBeCloseTo 100 and the lift/run ratio is within [0.8, 1.25]
    - DEFAULT_CONFIG.kStrength is no longer the 2.0 starting value (it was tuned to pass calibration)
    - `pnpm --filter @apsis/engine test` exits 0 with calibration.test.ts passing
    - The canonical hard 5x5 squat HSS falls within BUILD.md's ~30–120 readable range
  </acceptance_criteria>
  <done>The calibration golden passes with a tuned kStrength — the cross-modality HSS number is honest within ±25% (D-13).</done>
</task>

<task type="auto">
  <name>Task 3: Write the engine README and verify the ≥20-test / typecheck bar</name>
  <files>packages/engine/README.md</files>
  <read_first>
    - BUILD.md §4.2 (formulas to document), §4.3 (constants), §4.4 (README requirement + acceptance checklist)
    - packages/engine/src/config.ts (final constant values incl. the tuned kStrength to document)
    - .planning/phases/02-hss-engine/02-CONTEXT.md (approved deviations D-01/D-03/D-05/D-07 to note in the README)
    - all packages/engine/src/*.ts (public API surface to summarize)
  </read_first>
  <action>
    Create packages/engine/README.md documenting: the public API (strengthStress,
    enduranceStress, sessionHSS, dailyHSS, computeLoadTrend, computeLoadTrendSeries, readinessBand,
    plus the detailed variants and helpers estimateE1RM/ifFromPace/ifFromHR/resolveIF); each formula
    (endurance ES = durationMin * IF^2 * kEndurance; strength per-set stress and SS; session ES+SS;
    daily sum + doublePenalty; EWMA ATL/CTL and TSB = CTL − ATL; readiness band via TSB/CTL ratio with
    the calibrating gate); and every DEFAULT_CONFIG constant with its final value and meaning (note the
    calibrated kStrength and the D-14 kEndurance derivation). Record the approved deviations from
    BUILD.md §4.1 (D-01 calibrating band, D-03 readinessBand opts param, D-05 detailed variants, D-07
    series function). Note the engine is pure TS with zero runtime dependencies and that time is always
    passed in.
  </action>
  <verify>
    <automated>test -f packages/engine/README.md && grep -Eqi "kEndurance|readiness|calibrat" packages/engine/README.md && pnpm typecheck && pnpm --filter @apsis/engine test && test $(pnpm --filter @apsis/engine exec vitest run --reporter=json 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{try{const j=JSON.parse(s.slice(s.indexOf('{')));process.stdout.write(String(j.numTotalTests||0))}catch{process.stdout.write('0')}})") -ge 20</automated>
  </verify>
  <acceptance_criteria>
    - packages/engine/README.md exists and documents each formula and each DEFAULT_CONFIG constant (BUILD.md §4.4)
    - README records the approved BUILD.md deviations (D-01/D-03/D-05/D-07)
    - `pnpm typecheck` exits 0 and `pnpm --filter @apsis/engine test` exits 0
    - The engine reports ≥20 total passing vitest tests
  </acceptance_criteria>
  <done>Engine README documents every formula/constant; the suite is green with ≥20 tests and typecheck passes — BUILD.md §4.4 acceptance fully checked.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)
New symbols / artifacts introduced by 02-06 (exclude from source-drift verification):
- New files: `packages/engine/src/__tests__/calibration.test.ts`, `packages/engine/README.md`
- Modified: `packages/engine/src/index.ts` (now a public barrel re-exporting config/strength/endurance/session/daily/trend + ENGINE_VERSION), `packages/engine/src/config.ts` (kStrength tuned to the calibrated value)
- Removed: `packages/engine/src/__tests__/placeholder.test.ts`
- No new exported functions/types (this plan assembles and verifies symbols created in 02-01..02-05)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 3/4 consumers → @apsis/engine public API | Downstream code imports the barrel; the surface must expose only pure, no-throw functions |

Note: pure-TS, no I/O/network/persistence/auth. The integration risk is contract-shape and
purity regression, not a runtime attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | engine purity (wall clock / runtime deps) | high | mitigate | Verify step greps for wall-clock time APIs (0 matches) and asserts zero third-party runtime dependencies — the engine stays deterministic and reusable (ENG-07). |
| T-02-06-Cal | Tampering | calibration honesty | medium | mitigate | The calibration golden fails the build if the cross-modality HSS ratio drifts outside 0.8..1.25, preventing a dishonest "single number" from shipping (D-13). |
| T-02-SC | Tampering | dependency install | low | accept | No third-party packages installed this phase; the only dependency is the first-party @apsis/shared workspace link. Verified by the zero-runtime-dependency assertion. |
</threat_model>

<verification>
- placeholder.test.ts removed; barrel re-exports the full public API.
- Zero wall-clock time-API references and zero third-party runtime deps under packages/engine.
- Calibration golden passes (lift/run ratio in [0.8,1.25]; run ≈100).
- `pnpm typecheck` exits 0; `pnpm --filter @apsis/engine test` exits 0 with ≥20 tests.
- packages/engine/README.md documents every formula and constant.
</verification>

<success_criteria>
ENG-07 satisfied and BUILD.md §4.4 fully checked: exact public API via the barrel, ≥20 green
vitest tests including the calibration golden, zero runtime deps, no wall-clock reads, tsc
clean, and a README documenting every formula and constant.
</success_criteria>

<output>
Create `.planning/phases/02-hss-engine/02-06-SUMMARY.md` when done.
</output>
