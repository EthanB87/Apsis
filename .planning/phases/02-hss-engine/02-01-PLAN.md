---
phase: 02-hss-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/index.ts
  - packages/engine/src/config.ts
  - packages/engine/src/clamp.ts
  - packages/engine/src/__tests__/clamp.test.ts
  - packages/engine/package.json
autonomous: true
requirements: [ENG-07]
must_haves:
  truths:
    - "Every engine input/output type (StrengthSet, EnduranceSegment, EngineConfig, SessionInput, detail/result objects, LoadTrendPoint) is importable from @apsis/shared."
    - "DEFAULT_CONFIG exposes all tuning constants (k values, legMultiplier, doublePenalty, atlDays, ctlDays) plus band thresholds and the calibrating gate constants."
    - "clampRange returns a clamped value plus an optional warning string and never throws."
    - "@apsis/shared resolves inside the engine's vitest run."
  artifacts:
    - packages/shared/src/index.ts
    - packages/engine/src/config.ts
    - packages/engine/src/clamp.ts
    - packages/engine/src/__tests__/clamp.test.ts
  key_links:
    - "engine modules import types from '@apsis/shared' via the workspace dependency + tsconfig path alias"
    - "config.ts imports the EngineConfig interface from @apsis/shared (interface in shared, value in engine)"
---

<objective>
Build the pure-TS foundation every other Phase 02 plan depends on: the shared engine
input/output type surface, the versioned `EngineConfig` + `DEFAULT_CONFIG` constants
(all formula/tuning knobs in one tunable object), and the clamp-and-warn helper that
guarantees the engine never throws on bad input (D-15). Wire `@apsis/shared` into the
engine package so downstream compute modules can import types under vitest.

Purpose: These are the substrate for ENG-01..ENG-06. Types live in `@apsis/shared` per
BUILD.md §4.1; the tunable constants live in one versioned object so they can be re-fit
without code changes (PROJECT.md "engine logs raw components so constants can be re-fit").
Output: Extended `@apsis/shared`, `packages/engine/src/config.ts`, `packages/engine/src/clamp.ts`,
a clamp unit test, and a working `@apsis/shared` import from inside the engine.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hss-engine/02-CONTEXT.md
@.planning/phases/02-hss-engine/02-PATTERNS.md
@BUILD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend @apsis/shared with engine I/O and config types</name>
  <files>packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/index.ts (current types: Sex, Units, ReadinessBand incl. 'calibrating', ActivityType — extend in place, do not restructure)
    - BUILD.md §4.1 lines ~112-160 (authoritative StrengthSet, EnduranceSegment, EngineConfig field lists)
    - .planning/phases/02-hss-engine/02-CONTEXT.md D-05, D-06 (breakdown/result object shapes, version stamping)
    - .planning/phases/02-hss-engine/02-PATTERNS.md (one-line-JSDoc-per-type convention, keep file dependency-free)
  </read_first>
  <action>
    Append (do not reorder existing types) the following exported interfaces/types, each
    with a one-line JSDoc above it, matching the existing style. Keep the file pure types,
    zero runtime, zero imports.

    Input types (copy field names/types exactly from BUILD.md §4.1):
    - `StrengthSet` { loadKg: number; reps: number; rpe: number; e1rmKg: number; isLowerBody: boolean; isWarmup: boolean }
    - `EnduranceSegment` { durationS: number; intensityFactor: number }
    - `SessionInput` { strengthSets?: StrengthSet[]; enduranceSegments?: EnduranceSegment[] }

    Config type (interface lives in shared so result objects can reference it; the value
    DEFAULT_CONFIG lives in the engine — resolves BUILD.md's "types live in packages/shared"
    without a circular dependency):
    - `EngineConfig` with the six BUILD.md §4.1 fields (kStrength, kEndurance, legMultiplier,
      doublePenalty, atlDays, ctlDays: all number) PLUS the CONTEXT-approved extensions:
      calibratingMinHistoryDays: number (D-02), calibratingCtlFloor: number (D-02),
      bandRedRatio: number (D-04), bandAmberRatio: number (D-04).

    Result/detail types (D-05 breakdown objects; D-06 version stamp):
    - `StrengthStressDetail` { ss: number; perSetStress: number[]; warnings: string[] }
    - `EnduranceStressDetail` { es: number; warnings: string[] }
    - `SessionHSSResult` { hss: number; ss: number; es: number; perSetStress: number[]; warnings: string[]; engineVersion: string; config: EngineConfig }
    - `LoadTrendPoint` { atl: number; ctl: number; tsb: number; band: ReadinessBand } (reuse existing ReadinessBand)

    Do not add any import/require statement to this file.
  </action>
  <verify>
    <automated>grep -Eq 'export (interface|type) (StrengthSet|EnduranceSegment|EngineConfig|SessionInput|SessionHSSResult|LoadTrendPoint)' packages/shared/src/index.ts && ! grep -Eq "^\s*(import|require)\b" packages/shared/src/index.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/index.ts contains `export interface StrengthSet {` with fields loadKg, reps, rpe, e1rmKg, isLowerBody, isWarmup
    - packages/shared/src/index.ts contains `export interface EngineConfig {` with kStrength, kEndurance, legMultiplier, doublePenalty, atlDays, ctlDays, calibratingMinHistoryDays, calibratingCtlFloor, bandRedRatio, bandAmberRatio
    - packages/shared/src/index.ts contains `export interface SessionHSSResult {` with an engineVersion: string field and a config: EngineConfig field
    - packages/shared/src/index.ts contains `export interface LoadTrendPoint {` with a band: ReadinessBand field
    - The file has zero import/require statements
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>All engine I/O + config + result types are exported from @apsis/shared, dependency-free, and typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create engine config.ts (DEFAULT_CONFIG + constants) and wire @apsis/shared into the engine</name>
  <files>packages/engine/src/config.ts, packages/engine/package.json</files>
  <read_first>
    - packages/engine/src/index.ts (Phase-1 skeleton: module JSDoc header style, ENGINE_VERSION export)
    - packages/engine/package.json (current: devDeps only; add the workspace dependency here)
    - packages/db/src/seed.ts top-of-file (the `as const` exported-const-table convention to mirror)
    - packages/engine/tsconfig.json (the "@apsis/shared" path alias + project reference already present)
    - BUILD.md §4.3 (suggested starting constants) and §4.2 (formulas the constants feed)
    - .planning/phases/02-hss-engine/02-CONTEXT.md D-02, D-04, D-14 (CTL floor ~10, history 14 days, band ratios, kEndurance anchor)
  </read_first>
  <action>
    Add `"dependencies": { "@apsis/shared": "workspace:*" }` to packages/engine/package.json
    (keep existing name/version/private/main/types/scripts/devDependencies untouched), then run
    `pnpm install` from the repo root so the workspace symlink is created and vitest/vite can
    resolve the `@apsis/shared` bare specifier at runtime.

    Create packages/engine/src/config.ts with a module JSDoc header (name the file's
    responsibility + note constants are tunable without code changes). Import the EngineConfig
    type from '@apsis/shared' (type-only import). Export a `DEFAULT_CONFIG` object typed as
    EngineConfig, declared `as const`-style with these concrete starting values:
    - kStrength: 2.0 (starting guess; plan 02-06 tunes this to pass the calibration test — do NOT
      change it here)
    - kEndurance: 1.6667 (D-14 anchor: 60 min at IF 1.0 → 100 HSS, since ES = 60 * 1^2 * k)
    - legMultiplier: 1.3
    - doublePenalty: 1.1
    - atlDays: 7
    - ctlDays: 28
    - calibratingMinHistoryDays: 14 (D-02)
    - calibratingCtlFloor: 10 (D-02)
    - bandRedRatio: -0.30 (D-04: TSB/CTL below this → red)
    - bandAmberRatio: -0.10 (D-04: TSB/CTL in [bandRedRatio, this) → amber; at/above → green)

    Also export a `mergeConfig(cfg?: Partial<EngineConfig>): EngineConfig` helper that returns
    `{ ...DEFAULT_CONFIG, ...cfg }` so every compute function can accept a partial override merged
    over defaults (CONTEXT "Config override merging"). Document each constant's meaning and the
    D-14 kEndurance derivation in JSDoc. Do not import anything except the type from '@apsis/shared'.
  </action>
  <verify>
    <automated>grep -q "@apsis/shared" packages/engine/package.json && grep -Eq "export const DEFAULT_CONFIG" packages/engine/src/config.ts && grep -Eq "kEndurance:\s*1\.6" packages/engine/src/config.ts && grep -Eq "export function mergeConfig" packages/engine/src/config.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - packages/engine/package.json contains a dependencies block with `"@apsis/shared": "workspace:*"`
    - node_modules/@apsis/shared exists (pnpm install created the workspace link)
    - packages/engine/src/config.ts exports DEFAULT_CONFIG with all ten fields set to the values above (kStrength 2.0, kEndurance ≈1.6667, legMultiplier 1.3, doublePenalty 1.1, atlDays 7, ctlDays 28, calibratingMinHistoryDays 14, calibratingCtlFloor 10, bandRedRatio -0.30, bandAmberRatio -0.10)
    - packages/engine/src/config.ts exports `mergeConfig` returning DEFAULT_CONFIG merged with a partial override
    - config.ts imports only the EngineConfig type from '@apsis/shared'
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>DEFAULT_CONFIG + mergeConfig exist with documented starting constants; the engine resolves @apsis/shared as a workspace dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create clamp-and-warn helper with unit tests (D-15)</name>
  <files>packages/engine/src/clamp.ts, packages/engine/src/__tests__/clamp.test.ts</files>
  <read_first>
    - packages/engine/src/__tests__/placeholder.test.ts (vitest 4 shape: globals describe/it/expect used WITHOUT import; relative import path convention)
    - .planning/phases/02-hss-engine/02-PATTERNS.md (the recommended ClampResult<T>/clampRange shape and rationale)
    - .planning/phases/02-hss-engine/02-CONTEXT.md D-15 (clamp + warnings, never throw; physiological ranges)
  </read_first>
  <behavior>
    - clampRange(8, 1, 10, 'rpe') returns { value: 8 } with no warning (in range)
    - clampRange(12, 1, 10, 'rpe') returns { value: 10 } and a warning string mentioning 'rpe' and 12
    - clampRange(0, 1, 10, 'rpe') returns { value: 1 } and a warning string mentioning 'rpe'
    - clampRange with a NaN input returns a finite clamped value and a warning (never NaN, never throw)
    - clampRange never throws for any numeric input including Infinity/-Infinity
  </behavior>
  <action>
    Create packages/engine/src/clamp.ts with a module JSDoc header. Export an interface
    `ClampResult` { value: number; warning?: string } and a pure function
    `clampRange(value: number, min: number, max: number, label: string): ClampResult`.
    Behavior: coerce non-finite input (NaN/Infinity) to a clamped, finite value with a warning
    (treat NaN as below-min); if value < min return { value: min, warning: `${label} ${value} below min ${min}, clamped` };
    if value > max return { value: max, warning: `${label} ${value} above max ${max}, clamped` };
    otherwise return { value }. Never throw. This helper is imported by every compute module to
    collect warnings into detailed results' warnings[] arrays.

    Create packages/engine/src/__tests__/clamp.test.ts importing clampRange from '../clamp',
    using vitest globals (no import of describe/it/expect, matching placeholder.test.ts). Cover
    every case in the behavior block above.
  </action>
  <verify>
    <automated>pnpm --filter @apsis/engine test</automated>
  </verify>
  <acceptance_criteria>
    - packages/engine/src/clamp.ts contains `export function clampRange(` and `export interface ClampResult`
    - clamp.test.ts imports from '../clamp' and uses vitest globals without importing them
    - `pnpm --filter @apsis/engine test` exits 0 with clamp.test.ts cases passing
    - No call to clampRange in any test causes a thrown exception
  </acceptance_criteria>
  <done>clampRange is a never-throwing clamp+warn primitive with passing behavioral tests.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)
New symbols introduced by 02-01 (exclude from source-drift verification):
- `@apsis/shared`: types `StrengthSet`, `EnduranceSegment`, `SessionInput`, `EngineConfig`, `StrengthStressDetail`, `EnduranceStressDetail`, `SessionHSSResult`, `LoadTrendPoint`
- `packages/engine/src/config.ts`: `DEFAULT_CONFIG` (const), `mergeConfig` (function)
- `packages/engine/src/clamp.ts`: `ClampResult` (interface), `clampRange` (function)
- New files: `packages/engine/src/config.ts`, `packages/engine/src/clamp.ts`, `packages/engine/src/__tests__/clamp.test.ts`
- `packages/engine/package.json`: new `@apsis/shared` workspace dependency
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (Phase 3/4 app / DB) → engine function args | User-logged numeric values (load, reps, RPE, pace, HR) cross into pure functions as untrusted input |

Note: `packages/engine` is a pure-TS offline compute library — no network, no persistence,
no auth, no filesystem. Most STRIDE categories (Spoofing, Repudiation, Info Disclosure,
Elevation) are not applicable. The only real surface is input-validation robustness.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | clampRange / all inputs | medium | mitigate | Non-finite (NaN/Infinity) and out-of-range values are clamped into physiological ranges with warnings; never throw (D-15). Verified by clamp.test.ts. |
| T-02-01-DoS | Denial of Service | compute inputs | low | accept | Inputs are bounded by real workout size (sets per session, 28-day trend window); no unbounded recursion or allocation. |
| T-02-SC | Tampering | dependency install | low | accept | No third-party registry packages installed this phase. The only dependency added is the first-party workspace link `@apsis/shared` (workspace:*) — no external supply-chain surface, so no legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm typecheck` exits 0 (shared + engine project references compile).
- `pnpm --filter @apsis/engine test` exits 0.
- `@apsis/shared` importable from engine source (config.ts type-only import compiles).
</verification>

<success_criteria>
All engine I/O + config types exported from @apsis/shared; DEFAULT_CONFIG + mergeConfig +
clampRange implemented and documented; clamp tests pass; @apsis/shared wired into the engine.
</success_criteria>

<output>
Create `.planning/phases/02-hss-engine/02-01-SUMMARY.md` when done.
</output>
