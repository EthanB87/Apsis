{
  "name": "apsis",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "typecheck": "tsc --build tsconfig.json",
    "eas": "pnpm --filter @apsis/mobile exec eas",
    "build:ios": "pnpm --filter @apsis/mobile exec eas build --profile production --platform ios",
    "submit:ios": "pnpm --filter @apsis/mobile exec eas submit --profile production --platform ios"
  },
  "devDependencies": {
    "@rolldown/binding-win32-x64-msvc": "1.1.3",
    "typescript": "6.0.3"
  }
}
