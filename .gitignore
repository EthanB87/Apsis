# GSD planning artifacts ARE tracked (roadmap, state, phase context/plans).
# Only the research web-fetch cache is transient.
.planning/research/.cache/

# Dependencies
node_modules/

# Dependencies
node_modules/

# TypeScript build artifacts (composite project emit)
dist/
*.tsbuildinfo

# Expo generated native projects (generated by prebuild — never commit)
ios/
android/
apps/mobile/ios/
apps/mobile/android/

# EAS / signing credentials
*.p8
*.p12
*.mobileprovision
credentials.json
google-services.json
GoogleService-Info.plist

# Expo local state
.expo/
expo-env.d.ts

# GSD research cache (regenerated locally; not source)
.planning/research/.cache/

# Stray planning dir under the app package (local scratch)
apps/mobile/.planning/

# OS
.DS_Store
Thumbs.db
