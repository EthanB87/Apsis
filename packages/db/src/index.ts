/**
 * @apsis/db — database layer placeholder
 * Schema, migrations, client, and seed are wired in Phase 1 Plans 02–03.
 * This file exists so tsc --build resolves the package reference.
 */

// Exports added in later plans when op-sqlite is installed
